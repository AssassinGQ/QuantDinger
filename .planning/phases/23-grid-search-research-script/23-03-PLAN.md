---
phase: 23-grid-search-research-script
plan: 03
type: execute
wave: 1
depends_on: [23-00, 23-01, 23-02]
files_modified: [scripts/cross_sectional/grid_search.py, backend_api_python/tests/test_grid_search_script.py]
autonomous: true
requirements: [SCRIPT-02, SCRIPT-03]
user_setup: []

must_haves:
  truths:
    - "Checkpoint file tracks completed backtests"
    - "Script resumes from checkpoint, skipping completed items"
    - "JSONL output appends results incrementally"
    - "CSV export aggregates all results for analysis"
    - "HTML report shows top results and factor statistics"
  artifacts:
    - path: "scripts/cross_sectional/grid_search.py"
      provides: "Checkpoint + output functions"
      contains: "def load_checkpoint"
      contains: "def append_result_jsonl"
      contains: "def export_results_csv"
      contains: "def generate_html_report"
    - path: "scripts/cross_sectional/results/"
      provides: "Output directory structure"
      contains: "all_results.jsonl"
  key_links:
    - from: "scripts/cross_sectional/grid_search.py"
      to: "checkpoint.json"
      via: "json.load/save"
      pattern: "checkpoint.json"
    - from: "scripts/cross_sectional/grid_search.py"
      to: "all_results.jsonl"
      via: "json.dumps append"
      pattern: "all_results.jsonl"
---

<objective>
Implement checkpoint resume functionality (SCRIPT-03) and multi-format output generation (SCRIPT-02: JSONL, CSV, HTML). Enables fault tolerance for long-running grid searches and produces researcher-ready output files.

Purpose: Fault tolerance + output generation - critical for SCRIPT-03 checkpoint and SCRIPT-02 report formats.
Output: Functions for checkpoint management, JSONL append, CSV export, HTML report generation.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-grid-search-research-script/23-CONTEXT.md
@.planning/phases/23-grid-search-research-script/23-RESEARCH.md
@.planning/phases/23-grid-search-research-script/23-VALIDATION.md

<interfaces>
<!-- Prototype checkpoint pattern -->
From scripts/cross_sectional/nq100_cross_sectional.py lines 365-418:

```python
def grid_search(..., ckpt_file, output_dir):
    done, results = set(), []
    if os.path.exists(ckpt_file):
        with open(ckpt_file) as fh:
            ck = json.load(fh)
        done = set(ck.get("done", []))
        results = ck.get("results", [])
    
    # ... search loop ...
    
    key = f"{fn_str}|{nl}"  # Checkpoint key
    if key in done:
        continue
    
    # ... run backtest ...
    
    # Append JSONL
    with open(jsonl_path, "a") as fh:
        fh.write(json.dumps(entry, ensure_ascii=False, default=str) + "\n")
    
    done.add(key)
    
    # Save checkpoint periodically
    _save_ckpt(ckpt_file, done, results)

def _save_ckpt(path, done, results):
    with open(path, "w") as f:
        json.dump({"done": list(done), "results": results}, f, default=str)
```

<!-- Output fields per D-15 -->
From .planning/phases/23-grid-search-research-script/23-CONTEXT.md D-15:
- JSONL/CSV fields: 因子组合, 持仓数, 年化收益, Sharpe, Calmar, MaxDD, 胜率, 总交易月数, Score, 窗口标识

<!-- HTML report sections per D-16 -->
From .planning/phases/23-grid-search-research-script/23-CONTEXT.md D-16:
- 配置摘要
- TOP 20表格 (neutral_off vs neutral_on对比)
- 各持仓数最优
- 因子频率统计 (TOP 100中出现次数)
- Walk-forward各窗口OOS表现曲线
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement checkpoint load and save functions</name>
  <files>scripts/cross_sectional/grid_search.py, backend_api_python/tests/test_grid_search_script.py</files>
  <read_first>
    - scripts/cross_sectional/grid_search.py (Plan 02 output)
    - scripts/cross_sectional/nq100_cross_sectional.py (lines 365-443 for checkpoint pattern)
    - .planning/phases/23-grid-search-research-script/23-CONTEXT.md (D-17 for checkpoint format)
    - backend_api_python/tests/test_grid_search_script.py (to update test)
  </read_first>
  <behavior>
    - Test 1: load_checkpoint returns empty set for non-existent file
    - Test 2: load_checkpoint returns set of done keys from existing file
    - Test 3: save_checkpoint writes JSON with done list
    - Test 4: Key format matches "{factors}|{n_long}|{window_id}"
  </behavior>
  <test_cases>
  ### Test Case 1: Load Non-existent Checkpoint
  - **Tests:** load_checkpoint returns empty set for missing file
  - **Input:** Path to file that does not exist
  - **Expected:** Returns empty set()

  ### Test Case 2: Load Existing Checkpoint
  - **Tests:** load_checkpoint parses checkpoint.json correctly
  - **Input:** checkpoint.json with {"done": ["MOM_1M|10|w1", "VOL_20D|20|w1"]}
  - **Expected:** Returns set(["MOM_1M|10|w1", "VOL_20D|20|w1"])

  ### Test Case 3: Save Checkpoint
  - **Tests:** save_checkpoint writes valid JSON
  - **Input:** done=set(["A|10|w1"]), results=[{"factors": ["A"]}]
  - **Expected:** File contains {"done": ["A|10|w1"], "results": [{"factors": ["A"]}]}

  ### Test Case 4: Checkpoint Key Format
  - **Tests:** Key format is "{factors}|{n_long}|{window_id}"
  - **Input:** factors=['MOM_1M','VOL_20D'], n_long=15, window_id=2
  - **Expected:** Key = "MOM_1M+VOL_20D|15|w2"
  </test_cases>
  <action>
Add to `scripts/cross_sectional/grid_search.py`:

1. **Checkpoint functions:**
```python
def load_checkpoint(ckpt_path: str) -> Tuple[Set[str], List[Dict[str, Any]]]:
    """
    Load checkpoint file for resume support.
    
    Per D-17: checkpoint.json records completed backtests.
    Key format: "{factor_combo_str}|{n_long}|{window_id}"
    
    Args:
        ckpt_path: Path to checkpoint.json
        
    Returns:
        Tuple of (done_keys set, cached_results list)
        Returns (empty set, empty list) if file doesn't exist
    """
    if not os.path.exists(ckpt_path):
        return set(), []
    
    try:
        with open(ckpt_path, 'r') as f:
            data = json.load(f)
        done_keys = set(data.get('done', []))
        cached_results = data.get('results', [])
        return done_keys, cached_results
    except (json.JSONDecodeError, IOError):
        # Corrupted checkpoint - start fresh
        print(f"Warning: Corrupted checkpoint at {ckpt_path}, starting fresh")
        return set(), []


def save_checkpoint(ckpt_path: str, done_keys: Set[str], results: List[Dict[str, Any]]) -> None:
    """
    Save checkpoint file for resume support.
    
    Per D-17: checkpoint.json for fault tolerance.
    
    Args:
        ckpt_path: Path to checkpoint.json
        done_keys: Set of completed backtest keys
        results: List of all results so far
    """
    with open(ckpt_path, 'w') as f:
        json.dump({
            'done': list(done_keys),
            'results': results,
        }, f, default=str)


def make_checkpoint_key(factors: List[str], n_long: int, window_id: Optional[int] = None) -> str:
    """
    Generate checkpoint key for a backtest configuration.
    
    Key format per D-17: "{factor_combo_str}|{n_long}|{window_id}"
    
    Args:
        factors: List of factor names
        n_long: Number of long positions
        window_id: Walk-forward window ID (None for simple mode)
        
    Returns:
        Unique string key for checkpoint
    """
    factor_str = '+'.join(factors)
    window_suffix = f"|w{window_id}" if window_id is not None else ""
    return f"{factor_str}|{n_long}{window_suffix}"
```

2. **Add Set import:**
```python
from typing import Set, Tuple, List, Dict, Any, Optional
```

3. **Update test file** to replace placeholder `test_checkpoint_resume()`:
```python
import tempfile
import os

def test_checkpoint_load_missing():
    """Test load_checkpoint returns empty for missing file."""
    from scripts.cross_sectional.grid_search import load_checkpoint
    
    done, results = load_checkpoint("/nonexistent/checkpoint.json")
    assert done == set()
    assert results == []

def test_checkpoint_load_existing():
    """Test load_checkpoint parses existing file."""
    from scripts.cross_sectional.grid_search import load_checkpoint
    
    with tempfile.NamedTemporaryFile(mode='w', suffix='.json', delete=False) as f:
        json.dump({"done": ["MOM_1M|10|w1", "VOL_20D|20|w1"]}, f)
        ckpt_path = f.name
    
    try:
        done, results = load_checkpoint(ckpt_path)
        assert "MOM_1M|10|w1" in done
        assert "VOL_20D|20|w1" in done
        assert len(done) == 2
    finally:
        os.unlink(ckpt_path)

def test_checkpoint_save():
    """Test save_checkpoint writes valid JSON."""
    from scripts.cross_sectional.grid_search import save_checkpoint, load_checkpoint
    
    with tempfile.NamedTemporaryFile(mode='w', suffix='.json', delete=False) as f:
        ckpt_path = f.name
    
    try:
        save_checkpoint(ckpt_path, {"A|10|w1"}, [{"factors": ["A"], "n_long": 10}])
        
        # Verify by loading
        done, results = load_checkpoint(ckpt_path)
        assert "A|10|w1" in done
        assert len(results) == 1
    finally:
        os.unlink(ckpt_path)

def test_checkpoint_key_format():
    """Test checkpoint key format."""
    from scripts.cross_sectional.grid_search import make_checkpoint_key
    
    key1 = make_checkpoint_key(['MOM_1M', 'VOL_20D'], 15, window_id=2)
    assert key1 == "MOM_1M+VOL_20D|15|w2"
    
    key2 = make_checkpoint_key(['SHARPE_60D'], 20, window_id=None)
    assert key2 == "SHARPE_60D|20"
```
  </action>
  <verify>
<automated>
1. Run checkpoint tests: `cd backend_api_python && pytest tests/test_grid_search_script.py -k checkpoint -v` — exits 0, 4 tests pass
2. Run full suite: `cd backend_api_python && pytest tests/ -q` — exits 0
</automated>
  </verify>
  <done>
load_checkpoint() returns done keys set; save_checkpoint() writes JSON; make_checkpoint_key() generates unique keys; 4 checkpoint tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement JSONL append and CSV export functions</name>
  <files>scripts/cross_sectional/grid_search.py, backend_api_python/tests/test_grid_search_script.py</files>
  <read_first>
    - scripts/cross_sectional/grid_search.py (Task 1 output)
    - scripts/cross_sectional/nq100_cross_sectional.py (lines 416-418 for JSONL pattern)
    - .planning/phases/23-grid-search-research-script/23-CONTEXT.md (D-14, D-15 for output formats)
    - backend_api_python/tests/test_grid_search_script.py (to update test)
  </read_first>
  <behavior>
    - Test 1: append_result_jsonl writes valid JSONL line
    - Test 2: JSONL file accumulates multiple results
    - Test 3: export_results_csv produces valid CSV with correct columns
    - Test 4: CSV columns match D-15 specification
  </behavior>
  <test_cases>
  ### Test Case 1: JSONL Append Valid Line
  - **Tests:** append_result_jsonl writes valid JSON line
  - **Input:** result dict with factors, n_long, score
  - **Expected:** JSONL file contains valid JSON on one line

  ### Test Case 2: JSONL Accumulates
  - **Tests:** Multiple append calls produce multiple lines
  - **Input:** 3 sequential append calls
  - **Expected:** JSONL file has 3 lines, each parseable

  ### Test Case 3: CSV Export Columns
  - **Tests:** export_results_csv has correct columns per D-15
  - **Input:** List of results with neutral_off summaries
  - **Expected:** CSV has columns: factors, n_long, annualReturn, sharpeRatio, calmarRatio, maxDrawdown, winRate, totalMonths, score

  ### Test Case 4: CSV Valid Parse
  - **Tests:** CSV can be parsed by pandas
  - **Input:** export_results_csv output
  - **Expected:** pandas.read_csv succeeds with correct row count
  </test_cases>
  <action>
Add to `scripts/cross_sectional/grid_search.py`:

1. **Add pandas import:**
```python
import pandas as pd
```

2. **append_result_jsonl function:**
```python
def append_result_jsonl(jsonl_path: str, result: Dict[str, Any]) -> None:
    """
    Append a result to JSONL file incrementally.
    
    Per D-14, D-17: JSONL for incremental output supporting checkpoint.
    
    Args:
        jsonl_path: Path to all_results.jsonl
        result: Result dict to append
    """
    # Ensure parent directory exists
    os.makedirs(os.path.dirname(jsonl_path), exist_ok=True)
    
    with open(jsonl_path, 'a') as f:
        f.write(json.dumps(result, ensure_ascii=False, default=str) + '\n')
```

3. **export_results_csv function:**
```python
def export_results_csv(results: List[Dict[str, Any]], csv_path: str, neutral_mode: str = 'off') -> None:
    """
    Export results to CSV for analysis.
    
    Per D-14, D-15: CSV export with standard fields.
    
    Args:
        results: List of result dicts
        csv_path: Path to output CSV
        neutral_mode: 'off' or 'on' for which summary to use
    """
    # Build DataFrame with D-15 specified fields
    rows = []
    for r in results:
        summary_key = f'neutral_{neutral_mode}_summary'
        summary = r.get(summary_key) or {}
        
        row = {
            'factors': '+'.join(r.get('factors', [])),
            'n_long': r.get('n_long', 0),
            'annualReturn': summary.get('annualReturn', 0) or 0,
            'sharpeRatio': summary.get('sharpeRatio', 0) or 0,
            'calmarRatio': summary.get('calmarRatio', 0) or 0,
            'maxDrawdown': summary.get('maxDrawdown', 0) or 0,
            'winRate': summary.get('winRate', 0) or 0,
            'totalMonths': summary.get('totalMonths', 0) or 0,
            'score': r.get(f'neutral_{neutral_mode}_score', -9999),
            'window_id': r.get('window_id', None),
        }
        rows.append(row)
    
    df = pd.DataFrame(rows)
    
    # Ensure parent directory exists
    os.makedirs(os.path.dirname(csv_path), exist_ok=True)
    
    df.to_csv(csv_path, index=False)
```

4. **Update test file** to replace placeholder tests:
```python
def test_jsonl_append():
    """Test append_result_jsonl writes valid JSONL."""
    from scripts.cross_sectional.grid_search import append_result_jsonl
    import tempfile
    
    with tempfile.NamedTemporaryFile(mode='w', suffix='.jsonl', delete=False) as f:
        jsonl_path = f.name
    
    try:
        result = {'factors': ['MOM_1M'], 'n_long': 10, 'score': 5.5}
        append_result_jsonl(jsonl_path, result)
        
        # Read and verify
        with open(jsonl_path) as f:
            line = f.readline()
            parsed = json.loads(line)
            assert parsed['factors'] == ['MOM_1M']
            assert parsed['score'] == 5.5
    finally:
        os.unlink(jsonl_path)

def test_jsonl_accumulates():
    """Test multiple append calls produce multiple lines."""
    from scripts.cross_sectional.grid_search import append_result_jsonl
    import tempfile
    
    with tempfile.NamedTemporaryFile(mode='w', suffix='.jsonl', delete=False) as f:
        jsonl_path = f.name
    
    try:
        for i in range(3):
            append_result_jsonl(jsonl_path, {'idx': i, 'score': i * 10})
        
        with open(jsonl_path) as f:
            lines = f.readlines()
            assert len(lines) == 3
            for line in lines:
                json.loads(line)  # Verify each parseable
    finally:
        os.unlink(jsonl_path)

def test_csv_export_columns():
    """Test CSV export has correct columns per D-15."""
    from scripts.cross_sectional.grid_search import export_results_csv
    import tempfile
    
    results = [
        {
            'factors': ['MOM_1M'],
            'n_long': 10,
            'neutral_off_summary': {'annualReturn': 15, 'sharpeRatio': 1.5},
            'neutral_off_score': 10.0,
        }
    ]
    
    with tempfile.NamedTemporaryFile(mode='w', suffix='.csv', delete=False) as f:
        csv_path = f.name
    
    try:
        export_results_csv(results, csv_path, neutral_mode='off')
        
        df = pd.read_csv(csv_path)
        expected_cols = ['factors', 'n_long', 'annualReturn', 'sharpeRatio',
                        'calmarRatio', 'maxDrawdown', 'winRate', 'totalMonths', 'score', 'window_id']
        for col in expected_cols:
            assert col in df.columns
    finally:
        os.unlink(csv_path)

def test_csv_valid_parse():
    """Test CSV can be parsed with correct row count."""
    from scripts.cross_sectional.grid_search import export_results_csv
    import tempfile
    
    results = [
        {'factors': ['A'], 'n_long': 10, 'neutral_off_summary': {}, 'neutral_off_score': 1},
        {'factors': ['B'], 'n_long': 20, 'neutral_off_summary': {}, 'neutral_off_score': 2},
    ]
    
    with tempfile.NamedTemporaryFile(mode='w', suffix='.csv', delete=False) as f:
        csv_path = f.name
    
    try:
        export_results_csv(results, csv_path)
        
        df = pd.read_csv(csv_path)
        assert len(df) == 2
    finally:
        os.unlink(csv_path)
```
  </action>
  <verify>
<automated>
1. Run output tests: `cd backend_api_python && pytest tests/test_grid_search_script.py -k "jsonl or csv" -v` — exits 0, 4 tests pass
2. Run full suite: `cd backend_api_python && pytest tests/ -q` — exits 0
</automated>
  </verify>
  <done>
append_result_jsonl() appends valid JSON lines; export_results_csv() produces D-15 columns; tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement HTML report generation</name>
  <files>scripts/cross_sectional/grid_search.py, backend_api_python/tests/test_grid_search_script.py</files>
  <read_first>
    - scripts/cross_sectional/grid_search.py (Tasks 1-2 output)
    - scripts/cross_sectional/nq100_cross_sectional.py (lines 449-506 for report pattern)
    - .planning/phases/23-grid-search-research-script/23-CONTEXT.md (D-16 for HTML sections)
    - .planning/phases/23-grid-search-research-script/23-RESEARCH.md (Pattern 5 for HTML template)
    - backend_api_python/tests/test_grid_search_script.py (to update test)
  </read_first>
  <behavior>
    - Test 1: HTML report contains config summary section
    - Test 2: HTML report contains TOP 20 table
    - Test 3: HTML report contains factor frequency table
    - Test 4: HTML file is valid and parseable
  </behavior>
  <test_cases>
  ### Test Case 1: HTML Config Summary
  - **Tests:** HTML report contains configuration summary
  - **Input:** Config with 12 factors, combo_min=1, combo_max=4
  - **Expected:** HTML contains "Factors: 12 available" and "Combo sizes: 1 to 4"

  ### Test Case 2: HTML TOP 20 Table
  - **Tests:** HTML contains TOP 20 results table
  - **Input:** 25 results sorted by score
  - **Expected:** HTML contains table with 20 rows

  ### Test Case 3: HTML Factor Frequency
  - **Tests:** HTML contains factor frequency statistics
  - **Input:** Results with factors appearing multiple times
  - **Expected:** HTML contains factor frequency table

  ### Test Case 4: HTML Valid Parse
  - **Tests:** HTML is valid and contains expected tags
  - **Input:** Generate report
  - **Expected:** HTML contains <!DOCTYPE html>, <html>, <body> tags
  </test_cases>
  <action>
Add to `scripts/cross_sectional/grid_search.py`:

1. **generate_html_report function (per D-16):**
```python
def generate_html_report(
    results: List[Dict[str, Any]],
    config: Dict[str, Any],
    output_path: str,
    neutral_mode: str = 'off',
) -> None:
    """
    Generate HTML visualization report.
    
    Per D-16: Report includes config summary, TOP 20, factor frequency, holdings best.
    
    Args:
        results: List of result dicts (sorted by score)
        config: Config dict for summary display
        output_path: Path to output HTML file
        neutral_mode: 'off' or 'on' for which results to display
    """
    from datetime import datetime
    
    # Sort by score
    score_key = f'neutral_{neutral_mode}_score'
    sorted_results = sorted(results, key=lambda x: x.get(score_key, -9999), reverse=True)
    
    top20 = sorted_results[:20]
    
    # Factor frequency in TOP 100 per D-16
    factor_stats = {}
    for r in sorted_results[:100]:
        for f in r.get('factors', []):
            factor_stats[f] = factor_stats.get(f, 0) + 1
    
    # Build HTML using f-string (Claude's Discretion: no Jinja2)
    html = f'''<!DOCTYPE html>
<html>
<head>
<title>NQ100 Grid Search Report</title>
<style>
body {{ font-family: Arial, sans-serif; margin: 20px; }}
h1, h2 {{ color: #333; }}
table {{ border-collapse: collapse; width: 100%; }}
th, td {{ border: 1px solid #ddd; padding: 8px; text-align: left; }}
th {{ background-color: #f4f4f4; }}
tr:nth-child(even) {{ background-color: #f9f9f9; }}
.positive {{ color: green; }}
.negative {{ color: red; }}
</style>
</head>
<body>
<h1>NQ100 Cross-Sectional Grid Search Report</h1>

<h2>Configuration</h2>
<ul>
<li>Factors: {len(config['search_space']['factors'])} available</li>
<li>Combo sizes: {config['search_space']['combo_min']} to {config['search_space']['combo_max']}</li>
<li>Holdings: {config['search_space']['n_long_options']}</li>
<li>Walk-forward: {config.get('walk_forward', {}).get('enabled', False)}</li>
</ul>

<h2>Top 20 Results (Neutral {neutral_mode.upper()})</h2>
<table>
<tr><th>#</th><th>Factors</th><th>Holdings</th><th>Ann Return</th><th>Sharpe</th><th>MaxDD</th><th>Score</th></tr>
'''
    
    for i, r in enumerate(top20):
        summary_key = f'neutral_{neutral_mode}_summary'
        summary = r.get(summary_key) or {}
        ann = summary.get('annualReturn', 0) or 0
        shr = summary.get('sharpeRatio', 0) or 0
        dd = summary.get('maxDrawdown', 0) or 0
        sc = r.get(score_key, 0) or 0
        factors_str = ', '.join(r.get('factors', []))
        
        ann_class = 'positive' if ann >= 0 else 'negative'
        
        html += f'''<tr>
<td>{i+1}</td>
<td>{factors_str}</td>
<td>{r.get('n_long', 0)}</td>
<td class="{ann_class}">{ann:+.1f}%</td>
<td>{shr:.2f}</td>
<td class="negative">{dd:.1f}%</td>
<td>{sc:.1f}</td>
</tr>'''
    
    html += f'''</table>

<h2>Factor Frequency (Top 100)</h2>
<table>
<tr><th>Factor</th><th>Appearances</th><th>Percentage</th></tr>
'''
    
    for f, count in sorted(factor_stats.items(), key=lambda x: -x[1]):
        pct = count / min(100, len(sorted_results)) * 100
        html += f'''<tr><td>{f}</td><td>{count}</td><td>{pct:.0f}%</td></tr>'''
    
    html += '''</table>

<h2>Best by Holdings</h2>
<table>
<tr><th>Holdings</th><th>Best Factors</th><th>Score</th></tr>
'''
    
    # Best by n_long
    n_long_options = config['search_space']['n_long_options']
    for nl in n_long_options:
        nl_results = [r for r in sorted_results if r.get('n_long') == nl]
        if nl_results:
            best = nl_results[0]
            factors_str = ', '.join(best.get('factors', []))
            sc = best.get(score_key, 0)
            html += f'''<tr><td>{nl}</td><td>{factors_str}</td><td>{sc:.1f}</td></tr>'''
    
    html += f'''</table>

<p>Generated: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}</p>
<p>Total results: {len(results)}</p>
</body>
</html>'''
    
    # Ensure parent directory exists
    os.makedirs(os.path.dirname(output_path), exist_ok=True)
    
    with open(output_path, 'w') as f:
        f.write(html)
```

2. **Update test file** to replace placeholder tests:
```python
def test_html_config_summary():
    """Test HTML contains config summary."""
    from scripts.cross_sectional.grid_search import generate_html_report
    import tempfile
    
    config = {
        'search_space': {
            'factors': ['A', 'B', 'C'],
            'combo_min': 1,
            'combo_max': 2,
            'n_long_options': [10, 20],
        },
        'walk_forward': {'enabled': False},
    }
    results = [{'factors': ['A'], 'n_long': 10, 'neutral_off_score': 5, 'neutral_off_summary': {}}]
    
    with tempfile.NamedTemporaryFile(mode='w', suffix='.html', delete=False) as f:
        html_path = f.name
    
    try:
        generate_html_report(results, config, html_path, neutral_mode='off')
        
        with open(html_path) as f:
            html = f.read()
            assert "Factors: 3 available" in html
            assert "Combo sizes: 1 to 2" in html
    finally:
        os.unlink(html_path)

def test_html_top20_table():
    """Test HTML contains TOP 20 table."""
    from scripts.cross_sectional.grid_search import generate_html_report
    import tempfile
    
    config = {'search_space': {'factors': ['A'], 'combo_min': 1, 'combo_max': 1, 'n_long_options': [10]}}
    results = [
        {'factors': ['A'], 'n_long': 10, 'neutral_off_score': i, 'neutral_off_summary': {'annualReturn': i}}
        for i in range(25)
    ]
    
    with tempfile.NamedTemporaryFile(mode='w', suffix='.html', delete=False) as f:
        html_path = f.name
    
    try:
        generate_html_report(results, config, html_path)
        
        with open(html_path) as f:
            html = f.read()
            assert "Top 20 Results" in html
            # Check table rows (should have 20, not 25)
            assert "<tr>" in html  # Table rows exist
    finally:
        os.unlink(html_path)

def test_html_factor_frequency():
    """Test HTML contains factor frequency table."""
    from scripts.cross_sectional.grid_search import generate_html_report
    import tempfile
    
    config = {'search_space': {'factors': ['A', 'B'], 'combo_min': 1, 'combo_max': 1, 'n_long_options': [10]}}
    results = [
        {'factors': ['A'], 'n_long': 10, 'neutral_off_score': 10, 'neutral_off_summary': {}},
        {'factors': ['A'], 'n_long': 10, 'neutral_off_score': 9, 'neutral_off_summary': {}},
        {'factors': ['B'], 'n_long': 10, 'neutral_off_score': 8, 'neutral_off_summary': {}},
    ]
    
    with tempfile.NamedTemporaryFile(mode='w', suffix='.html', delete=False) as f:
        html_path = f.name
    
    try:
        generate_html_report(results, config, html_path)
        
        with open(html_path) as f:
            html = f.read()
            assert "Factor Frequency" in html
            assert "Appearances" in html
    finally:
        os.unlink(html_path)

def test_html_valid_structure():
    """Test HTML has valid structure."""
    from scripts.cross_sectional.grid_search import generate_html_report
    import tempfile
    
    config = {'search_space': {'factors': ['A'], 'combo_min': 1, 'combo_max': 1, 'n_long_options': [10]}}
    results = [{'factors': ['A'], 'n_long': 10, 'neutral_off_score': 1, 'neutral_off_summary': {}}]
    
    with tempfile.NamedTemporaryFile(mode='w', suffix='.html', delete=False) as f:
        html_path = f.name
    
    try:
        generate_html_report(results, config, html_path)
        
        with open(html_path) as f:
            html = f.read()
            assert "<!DOCTYPE html>" in html
            assert "<html>" in html
            assert "</html>" in html
            assert "<body>" in html
    finally:
        os.unlink(html_path)
```
  </action>
  <verify>
<automated>
1. Run HTML tests: `cd backend_api_python && pytest tests/test_grid_search_script.py -k html -v` — exits 0, 4 tests pass
2. Run full suite: `cd backend_api_python && pytest tests/ -q` — exits 0
</automated>
  </verify>
  <done>
generate_html_report() produces D-16 sections; contains config, TOP 20, factor frequency, holdings best; 4 HTML tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script → Output files | Write to local filesystem, no external code execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Tampering | Output paths | accept | Fixed output directory, no path traversal risk for research script |
</threat_model>

<verification>
- [ ] load_checkpoint() / save_checkpoint() manage fault tolerance
- [ ] make_checkpoint_key() generates unique keys
- [ ] append_result_jsonl() writes incrementally
- [ ] export_results_csv() produces D-15 columns
- [ ] generate_html_report() produces D-16 sections
- [ ] All tests pass
</verification>

<success_criteria>
Checkpoint resume + multi-format output (JSONL, CSV, HTML) implemented with full test coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/23-grid-search-research-script/23-03-SUMMARY.md`
</output>