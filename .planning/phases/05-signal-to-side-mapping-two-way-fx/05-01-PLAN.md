---
phase: 05-signal-to-side-mapping-two-way-fx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/app/services/live_trading/base.py
  - backend_api_python/app/services/live_trading/ibkr_trading/client.py
  - backend_api_python/app/services/live_trading/mt5_trading/client.py
  - backend_api_python/app/services/live_trading/ef_trading/client.py
  - backend_api_python/app/services/live_trading/usmart_trading/client.py
  - backend_api_python/app/services/live_trading/runners/stateful_runner.py
  - backend_api_python/tests/test_exchange_engine.py
  - backend_api_python/tests/test_stateful_runner_execute.py
autonomous: true
requirements: [EXEC-02]

must_haves:
  truths:
    - "map_signal_to_side(..., market_category=\"Forex\") returns buy/sell per locked eight-signal table for IBKR"
    - "map_signal_to_side(\"open_short\") default market_category omits Forex path and raises ValueError containing 美股/港股不支持 short"
    - "StatefulClientRunner.execute calls map_signal_to_side(signal_type, market_category=(ctx.market_category or \"\").strip())"
    - "MT5/EF/USmart map_signal_to_side accept keyword-only market_category and ignore it (behavior unchanged)"
    - "cd backend_api_python && python -m pytest tests/ -x -q --tb=line exits 0 (REGR-01)"
  artifacts:
    - path: "backend_api_python/app/services/live_trading/base.py"
      provides: "Abstract map_signal_to_side(signal_type, *, market_category=\"\")"
      contains: "market_category: str = \"\""
    - path: "backend_api_python/app/services/live_trading/ibkr_trading/client.py"
      provides: "Forex _FOREX_SIGNAL_MAP + non-Forex 美股/港股 short rejection"
      contains: "_FOREX_SIGNAL_MAP"
    - path: "backend_api_python/app/services/live_trading/runners/stateful_runner.py"
      provides: "Passes market_category into map_signal_to_side"
      contains: "map_signal_to_side"
    - path: "backend_api_python/tests/test_exchange_engine.py"
      provides: "UC-F1–F6, UC-E1–E3 IBKR assertions"
      contains: "TestIBKRSignalMapping"
    - path: "backend_api_python/tests/test_stateful_runner_execute.py"
      provides: "UC-R1 assert_called_once_with market_category"
      contains: "StatefulClientRunner"
  key_links:
    - from: "stateful_runner.py::execute"
      to: "BaseStatefulClient.map_signal_to_side"
      via: "market_category= from OrderContext"
      pattern: "map_signal_to_side\\("
    - from: "IBKRClient.map_signal_to_side"
      to: "_FOREX_SIGNAL_MAP / _SIGNAL_MAP"
      via: "cat == \"Forex\" branch first"
      pattern: "Forex"
---

<objective>
Implement EXEC-02: extend `BaseStatefulClient.map_signal_to_side` with keyword-only `market_category`, thread `OrderContext.market_category` from `StatefulClientRunner.execute`, and implement IBKR Forex two-way mapping (eight signals, MT5-aligned) with explicit 美股/港股 short rejection for non-Forex; add unit tests for UC-F1–F6, UC-E1–E3, UC-R1 and full-suite regression REGR-01.

Purpose: Unlock Forex short-style strategy signals without equity-only gating; keep backward compatibility when `market_category` defaults to empty.

Output: Signature updates across base + four clients, IBKR `_FOREX_SIGNAL_MAP` + branch logic, runner wiring, expanded `test_exchange_engine.py`, new `test_stateful_runner_execute.py`.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-signal-to-side-mapping-two-way-fx/05-CONTEXT.md
@.planning/phases/05-signal-to-side-mapping-two-way-fx/05-RESEARCH.md
@.planning/phases/05-signal-to-side-mapping-two-way-fx/05-VALIDATION.md

<interfaces>
From `backend_api_python/app/services/live_trading/base.py` (edit abstract signature):
```python
@abstractmethod
def map_signal_to_side(self, signal_type: str, *, market_category: str = "") -> str:
    """Convert strategy signal (e.g. 'open_long') to 'buy'/'sell'."""
```

From `backend_api_python/app/services/live_trading/runners/stateful_runner.py` (edit call site ~59–62):
```python
action = client.map_signal_to_side(ctx.signal_type)
```

From `backend_api_python/app/services/live_trading/mt5_trading/client.py` — reference `_SIGNAL_MAP` eight keys (lines 64–73).

From `backend_api_python/app/services/live_trading/base.py` — `OrderContext` fields including `market_category: str`; `LiveOrderResult` for mock in UC-R1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production + tests — covers [UC-F1–F6] [UC-E1–E3] [UC-R1] [REGR-01]</name>
  <files>backend_api_python/app/services/live_trading/base.py, backend_api_python/app/services/live_trading/ibkr_trading/client.py, backend_api_python/app/services/live_trading/mt5_trading/client.py, backend_api_python/app/services/live_trading/ef_trading/client.py, backend_api_python/app/services/live_trading/usmart_trading/client.py, backend_api_python/app/services/live_trading/runners/stateful_runner.py, backend_api_python/tests/test_exchange_engine.py, backend_api_python/tests/test_stateful_runner_execute.py</files>

  <read_first>
    - backend_api_python/app/services/live_trading/base.py (lines 197–203: current `map_signal_to_side` abstract; `OrderContext`, `LiveOrderResult`)
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py (lines 113–168: `_SIGNAL_MAP`, `map_signal_to_side`)
    - backend_api_python/app/services/live_trading/mt5_trading/client.py (lines 64–87: `_SIGNAL_MAP`, `map_signal_to_side`)
    - backend_api_python/app/services/live_trading/ef_trading/client.py (lines 348–358)
    - backend_api_python/app/services/live_trading/usmart_trading/client.py (lines 124–135)
    - backend_api_python/app/services/live_trading/runners/stateful_runner.py (full `execute`)
    - backend_api_python/tests/test_exchange_engine.py (class `TestIBKRSignalMapping`)
    - .planning/phases/05-signal-to-side-mapping-two-way-fx/05-CONTEXT.md (locked table + error copy + UC-R1)
  </read_first>

  <action>
    **Part A — Production (same file edits as 05-CONTEXT / 05-RESEARCH)**

    1. **`base.py`** — Replace abstract `map_signal_to_side` with:
    `def map_signal_to_side(self, signal_type: str, *, market_category: str = "") -> str:`

    2. **`ibkr_trading/client.py`** — After `_SIGNAL_MAP`, add:
    ```python
    _FOREX_SIGNAL_MAP = {
        "open_long": "buy",
        "add_long": "buy",
        "close_long": "sell",
        "reduce_long": "sell",
        "open_short": "sell",
        "add_short": "sell",
        "close_short": "buy",
        "reduce_short": "buy",
    }
    ```
    Replace `map_signal_to_side` with signature `def map_signal_to_side(self, signal_type: str, *, market_category: str = "") -> str:` and body:
    - `sig = (signal_type or "").strip().lower()`
    - `cat = (market_category or "").strip()`
    - If `cat == "Forex"`: `side = self._FOREX_SIGNAL_MAP.get(sig)`; if `side is None`: `raise ValueError(f"Unsupported signal_type for IBKR: {signal_type}")`; return `side`.
    - Else: if `"short" in sig`: `raise ValueError(f"IBKR 美股/港股不支持 short 信号: {signal_type}")`.
    - Else: `side = self._SIGNAL_MAP.get(sig)`; if `side is None`: `raise ValueError(f"Unsupported signal_type for IBKR: {signal_type}")`; return `side`.

    3. **`mt5_trading/client.py`** — `def map_signal_to_side(self, signal_type: str, *, market_category: str = "") -> str:` — body unchanged.

    4. **`ef_trading/client.py`** — Same signature change; body unchanged.

    5. **`usmart_trading/client.py`** — Same signature change; body unchanged.

    6. **`stateful_runner.py`** — Replace `action = client.map_signal_to_side(ctx.signal_type)` with `action = client.map_signal_to_side(ctx.signal_type, market_category=(ctx.market_category or "").strip())`.

    **Part B — Tests (`test_exchange_engine.py`)**

    1. **UC-F1–F6** — Using `IBKRClient.__new__(IBKRClient)`, assert:
       - `map_signal_to_side("open_long", market_category="Forex") == "buy"`
       - `map_signal_to_side("close_long", market_category="Forex") == "sell"`
       - `map_signal_to_side("open_short", market_category="Forex") == "sell"`
       - `map_signal_to_side("close_short", market_category="Forex") == "buy"`
       - `map_signal_to_side("add_short", market_category="Forex") == "sell"`
       - `map_signal_to_side("reduce_short", market_category="Forex") == "buy"`

    2. **Forex `add_long` / `reduce_long`** — Assert `map_signal_to_side("add_long", market_category="Forex") == "buy"` and `map_signal_to_side("reduce_long", market_category="Forex") == "sell"`.

    3. **UC-E1** — `pytest.raises(ValueError, match=r"美股/港股不支持 short")` for `map_signal_to_side("open_short")` with no `market_category`.

    4. **UC-E2** — Same `match` for `map_signal_to_side("open_short", market_category="USStock")`.

    5. **UC-E3** — `assert c.map_signal_to_side("close_long") == "sell"` (no kwarg).

    6. **Update** `test_short_rejected` and `test_close_short_rejected` to use `match=r"美股/港股不支持 short"` (or equivalent regex escaping) instead of only `match="short"`.

    **Part C — New `backend_api_python/tests/test_stateful_runner_execute.py` (UC-R1)**

    - `MagicMock()` client: `engine_id="ibkr"`, `map_signal_to_side.return_value="sell"`, `place_market_order.return_value=LiveOrderResult(success=True, order_id=1)`.
    - `OrderContext(order_id=1, strategy_id=1, symbol="EURUSD", signal_type="open_short", amount=10000.0, market_type="Forex", market_category="Forex", exchange_config={"market_type": "Forex"}, payload={}, order_row={})`.
    - `StatefulClientRunner().execute(client=client, order_context=ctx)`.
    - Assert `client.map_signal_to_side.assert_called_once_with("open_short", market_category="Forex")`.
  </action>

  <verify>
    <automated>cd backend_api_python &amp;&amp; python -m pytest tests/test_exchange_engine.py::TestIBKRSignalMapping -x -q --tb=line</automated>
    <automated>cd backend_api_python &amp;&amp; python -m pytest tests/test_stateful_runner_execute.py -x -q --tb=line</automated>
    <automated>cd backend_api_python &amp;&amp; python -m pytest tests/ -x -q --tb=line</automated>
  </verify>

  <acceptance_criteria>
    - [UC-F1] `test_exchange_engine.py` contains `open_long` with `market_category="Forex"` and expected `"buy"` (grep -n `open_long` + `Forex` in TestIBKRSignalMapping section)
    - [UC-F2] Same for `close_long` → `"sell"`
    - [UC-F3] Same for `open_short` + Forex → `"sell"`
    - [UC-F4] Same for `close_short` + Forex → `"buy"`
    - [UC-F5] Same for `add_short` + Forex → `"sell"`
    - [UC-F6] Same for `reduce_short` + Forex → `"buy"`
    - [UC-E1] Test raises for `open_short` without kwarg; `match` includes `美股/港股不支持 short`
    - [UC-E2] Test raises for `open_short` with `market_category="USStock"`; same `match`
    - [UC-E3] `map_signal_to_side("close_long")` without kwarg returns `"sell"` in a test assertion
    - [UC-R1] `test_stateful_runner_execute.py` exists; `grep -n 'assert_called_once_with("open_short", market_category="Forex")' backend_api_python/tests/test_stateful_runner_execute.py` exits 0
    - [REGR-01] `cd backend_api_python &amp;&amp; python -m pytest tests/ -x -q --tb=line` exits 0
    - [REGR-01] `grep -n '_FOREX_SIGNAL_MAP' backend_api_python/app/services/live_trading/ibkr_trading/client.py` exits 0
    - [REGR-01] `grep -F 'IBKR 美股/港股不支持 short 信号' backend_api_python/app/services/live_trading/ibkr_trading/client.py` exits 0
    - [REGR-01] `grep -n 'market_category=(ctx.market_category' backend_api_python/app/services/live_trading/runners/stateful_runner.py` exits 0
  </acceptance_criteria>

  <done>
    Production and tests merged in one commit wave; all UC tags verified; full `pytest tests/` green.
  </done>
</task>

</tasks>

<verification>
1. `cd backend_api_python && python -m pytest tests/test_exchange_engine.py::TestIBKRSignalMapping tests/test_stateful_runner_execute.py -x -q --tb=line`
2. `cd backend_api_python && python -m pytest tests/ -x -q --tb=line` (REGR-01)
3. Grep checks in Task 1 acceptance_criteria for `_FOREX_SIGNAL_MAP`, Chinese error string, runner `market_category=`.
</verification>

<success_criteria>
- EXEC-02 satisfied: Forex two-way signal mapping + runner passes `market_category`.
- REGR-01: full backend test suite passes.
- No new Python dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/05-signal-to-side-mapping-two-way-fx/05-01-SUMMARY.md` per execute-plan template.
</output>
