---
phase: 15-normalize-pipeline-ordering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/app/services/live_trading/order_normalizer/__init__.py
  - backend_api_python/app/services/live_trading/order_normalizer/us_stock.py
  - backend_api_python/app/services/live_trading/order_normalizer/hk_share.py
  - backend_api_python/app/services/live_trading/order_normalizer/forex.py
  - backend_api_python/app/services/live_trading/ibkr_trading/order_normalizer/__init__.py
  - backend_api_python/app/services/live_trading/ibkr_trading/order_normalizer/us_stock.py
  - backend_api_python/app/services/live_trading/ibkr_trading/order_normalizer/hk_share.py
  - backend_api_python/app/services/live_trading/ibkr_trading/order_normalizer/forex.py
  - backend_api_python/tests/test_order_normalizer.py
autonomous: true
requirements:
  - INFRA-03
must_haves:
  truths:
    - "`MarketPreNormalizer` base class exists with abstract `pre_normalize` and `pre_check`."
    - "`get_market_pre_normalizer` returns `HSharePreNormalizer` for `HShare`, `USStockPreNormalizer` for default/None/empty."
    - "US/HShare/Forex/Crypto pre-normalizers preserve documented numeric behavior (floor, lots, passthrough)."
  artifacts:
    - path: backend_api_python/app/services/live_trading/order_normalizer/__init__.py
      provides: "MarketPreNormalizer, CryptoPreNormalizer, get_market_pre_normalizer"
      contains: "class MarketPreNormalizer"
    - path: backend_api_python/tests/test_order_normalizer.py
      provides: "Renamed test classes and factory tests"
      contains: "get_market_pre_normalizer"
  key_links:
    - from: backend_api_python/tests/test_order_normalizer.py
      to: app.services.live_trading.order_normalizer
      via: "imports"
      pattern: "from app.services.live_trading.order_normalizer import"
---

<objective>
Rename the market-layer quantity helpers to `MarketPreNormalizer` / `*PreNormalizer` with `pre_normalize()` and `pre_check()`, rename `get_normalizer()` to `get_market_pre_normalizer()`, and update unit tests (TC-15-T1, TC-15-T5-01, TC-15-T5-03). Update the temporary shim package `ibkr_trading/order_normalizer/` to re-export the **new** symbols so downstream plans can switch imports incrementally; shim removal is plan `15-04`.

Purpose: Establishes the canonical naming and behavior for INFRA-03 layer-1 (market pre-normalize before pre-check).

Output: Renamed production modules + updated `tests/test_order_normalizer.py` (keep `TestBackwardCompatImport` tests until `15-04` deletes the shim — see plan `15-04`).
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-normalize-pipeline-ordering/15-CONTEXT.md
@.planning/phases/15-normalize-pipeline-ordering/15-RESEARCH.md

## Renames (exact)

| Old | New |
|-----|-----|
| `OrderNormalizer` | `MarketPreNormalizer` |
| `USStockNormalizer` | `USStockPreNormalizer` |
| `HShareNormalizer` | `HSharePreNormalizer` |
| `ForexNormalizer` | `ForexPreNormalizer` |
| `CryptoNormalizer` | `CryptoPreNormalizer` |
| `normalize(` | `pre_normalize(` |
| `check(` | `pre_check(` |
| `get_normalizer` | `get_market_pre_normalizer` |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename MarketPreNormalizer hierarchy in production modules</name>
  <files>backend_api_python/app/services/live_trading/order_normalizer/__init__.py,backend_api_python/app/services/live_trading/order_normalizer/us_stock.py,backend_api_python/app/services/live_trading/order_normalizer/hk_share.py,backend_api_python/app/services/live_trading/order_normalizer/forex.py,backend_api_python/app/services/live_trading/ibkr_trading/order_normalizer/__init__.py,backend_api_python/app/services/live_trading/ibkr_trading/order_normalizer/us_stock.py,backend_api_python/app/services/live_trading/ibkr_trading/order_normalizer/hk_share.py,backend_api_python/app/services/live_trading/ibkr_trading/order_normalizer/forex.py</files>
  <read_first>
    - backend_api_python/app/services/live_trading/order_normalizer/__init__.py
    - backend_api_python/app/services/live_trading/order_normalizer/us_stock.py
    - backend_api_python/app/services/live_trading/order_normalizer/hk_share.py
    - backend_api_python/app/services/live_trading/order_normalizer/forex.py
    - backend_api_python/app/services/live_trading/ibkr_trading/order_normalizer/__init__.py
    - .planning/phases/15-normalize-pipeline-ordering/15-CONTEXT.md
    - .planning/phases/15-normalize-pipeline-ordering/15-RESEARCH.md
  </read_first>
  <action>
    In `order_normalizer/__init__.py`: rename `OrderNormalizer` → `MarketPreNormalizer`; rename abstract methods to `pre_normalize` and `pre_check`; rename `CryptoNormalizer` → `CryptoPreNormalizer` with methods `pre_normalize`/`pre_check`; rename factory `get_normalizer` → `get_market_pre_normalizer` with return type `MarketPreNormalizer`; update lazy imports inside the factory to `USStockPreNormalizer`, `HSharePreNormalizer`, `ForexPreNormalizer` from their modules.

    In `us_stock.py`: class `USStockPreNormalizer(MarketPreNormalizer)`; implement `pre_normalize` with same body as current `normalize` (floor to int); `pre_check` same body as current `check` (reject non-whole with message containing `whole number` where applicable).

    In `hk_share.py`: class `HSharePreNormalizer(MarketPreNormalizer)`; rename methods to `pre_normalize`/`pre_check`; keep `HK_LOT_SIZES` and `_hk_symbol_key` unchanged.

    In `forex.py`: class `ForexPreNormalizer(MarketPreNormalizer)`; rename methods to `pre_normalize`/`pre_check`.

    In `ibkr_trading/order_normalizer/__init__.py`: replace re-exports so they import `MarketPreNormalizer`, `get_market_pre_normalizer`, `USStockPreNormalizer`, `HSharePreNormalizer`, `ForexPreNormalizer` from the canonical modules (no old names). Update `us_stock.py`/`hk_share.py`/`forex.py` shim files to re-export the new `*PreNormalizer` symbols from the same relative canonical paths as today.

    Do not delete the shim directory in this task (plan `15-04`).
  </action>
  <acceptance_criteria>
    - `grep -n "class MarketPreNormalizer" backend_api_python/app/services/live_trading/order_normalizer/__init__.py` returns at least one match.
    - `grep -n "def get_market_pre_normalizer" backend_api_python/app/services/live_trading/order_normalizer/__init__.py` returns at least one match.
    - `grep -c "OrderNormalizer" backend_api_python/app/services/live_trading/order_normalizer/` (excluding `.pyc`) is `0` for renamed concept — old class name must not remain in canonical package.
    - **TC-15-T1-01**: Import `from app.services.live_trading.order_normalizer import MarketPreNormalizer` succeeds; `MarketPreNormalizer` is an ABC with abstract `pre_normalize` and `pre_check`.
    - **TC-15-T1-02**: `USStockPreNormalizer().pre_normalize(7.8, "AAPL") == 7` (int or numeric equal to 7).
    - **TC-15-T1-03**: `USStockPreNormalizer().pre_check(3.5, "AAPL")` returns `(False, msg)` and `msg` contains substring `whole number` (case-insensitive).
    - **TC-15-T1-04**: `HSharePreNormalizer().pre_normalize(450.0, "00005") == 400`.
    - **TC-15-T1-05**: `ForexPreNormalizer().pre_normalize(1000.7, "EURUSD") == 1000.7`.
    - **TC-15-T1-06**: `CryptoPreNormalizer().pre_normalize(0.00123, "BTCUSDT") == 0.00123`.
    - **TC-15-T1-07**: `type(get_market_pre_normalizer("HShare")).__name__ == "HSharePreNormalizer"`.
    - **TC-15-T1-08**: `type(get_market_pre_normalizer("")).__name__ == "USStockPreNormalizer"`.
    - **TC-15-T1-09**: `type(get_market_pre_normalizer(None)).__name__ == "USStockPreNormalizer"`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python &amp;&amp; python3 -c "from abc import ABC; from app.services.live_trading.order_normalizer import MarketPreNormalizer, get_market_pre_normalizer, CryptoPreNormalizer; from app.services.live_trading.order_normalizer.us_stock import USStockPreNormalizer; from app.services.live_trading.order_normalizer.hk_share import HSharePreNormalizer; from app.services.live_trading.order_normalizer.forex import ForexPreNormalizer; assert issubclass(MarketPreNormalizer, ABC); n=USStockPreNormalizer(); assert n.pre_normalize(7.8,'AAPL')==7; ok,r=n.pre_check(3.5,'AAPL'); assert ok is False and 'whole number' in r.lower(); assert HSharePreNormalizer().pre_normalize(450.0,'00005')==400; assert ForexPreNormalizer().pre_normalize(1000.7,'EURUSD')==1000.7; assert CryptoPreNormalizer().pre_normalize(0.00123,'BTCUSDT')==0.00123; assert type(get_market_pre_normalizer('HShare')).__name__=='HSharePreNormalizer'; assert type(get_market_pre_normalizer('')).__name__=='USStockPreNormalizer'; assert type(get_market_pre_normalizer(None)).__name__=='USStockPreNormalizer'"</automated>
  </verify>
  <done>Production modules and shim re-exports use `*PreNormalizer` names; factory is `get_market_pre_normalizer`; TC-15-T1-01–TC-15-T1-09 behaviors hold via inline assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Rename unit tests and factory test class (TC-15-T5)</name>
  <files>backend_api_python/tests/test_order_normalizer.py</files>
  <read_first>
    - backend_api_python/tests/test_order_normalizer.py
    - backend_api_python/app/services/live_trading/order_normalizer/__init__.py
    - .planning/phases/15-normalize-pipeline-ordering/15-RESEARCH.md
  </read_first>
  <action>
    Change module docstring first line to reference `MarketPreNormalizer` hierarchy (TC-15-T5-01).

    Update imports to use `get_market_pre_normalizer`, `CryptoPreNormalizer`, `USStockPreNormalizer`, `HSharePreNormalizer`, `ForexPreNormalizer` from `app.services.live_trading.order_normalizer` and submodule imports accordingly.

    Rename test classes per RESEARCH catalog: `TestUSStockNormalizer` → `TestUSStockPreNormalizer`; rename methods to `test_pre_normalize_*` / `test_pre_check_*` where they call `pre_normalize`/`pre_check`; apply same pattern for HShare, Forex, Crypto, and `TestGetNormalizer` → `TestGetMarketPreNormalizer` with `isinstance(..., *PreNormalizer)` assertions.

    Keep `TestBackwardCompatImport` **unchanged in behavior** for this plan (still imports from `app.services.live_trading.ibkr_trading.order_normalizer`); update only symbol names if the shim no longer exports old names — the shim’s `get_market_pre_normalizer` and `pre_normalize` must satisfy the same numeric assertions as before (`old_get("HShare").pre_normalize(450, "00005") == 400`).

    Do not add TC-15-T5-02 deletion here (plan `15-04`).
  </action>
  <acceptance_criteria>
    - **TC-15-T5-01**: First line of `tests/test_order_normalizer.py` contains the substring `MarketPreNormalizer`.
    - **TC-15-T5-03**: `grep -n "TestGetMarketPreNormalizer" backend_api_python/tests/test_order_normalizer.py` matches; `grep -n "get_market_pre_normalizer" backend_api_python/tests/test_order_normalizer.py` matches; `grep -n "get_normalizer" backend_api_python/tests/test_order_normalizer.py` returns no matches **except** inside comments if any (prefer zero matches).
    - **TC-15-T1-01–TC-15-T1-09** (re-verified via suite): all behaviors in Task 1 remain true after test renames.
    - `TestBackwardCompatImport` still passes against updated shim exports (numeric assertion `400` for HSBC lot).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python &amp;&amp; head -1 tests/test_order_normalizer.py | grep -q "MarketPreNormalizer" &amp;&amp; ! grep -q "get_normalizer" tests/test_order_normalizer.py &amp;&amp; grep -q "TestGetMarketPreNormalizer" tests/test_order_normalizer.py &amp;&amp; python3 -m pytest tests/test_order_normalizer.py -v --tb=short</automated>
  </verify>
  <done>`test_order_normalizer.py` matches new API names; TC-15-T5-01 (docstring grep) and TC-15-T5-03 (factory test class grep) satisfied; full file green including backward-compat tests pending shim removal in `15-04`.</done>
</task>

</tasks>

<verification>
After both tasks: `cd backend_api_python &amp;&amp; python3 -m pytest tests/test_order_normalizer.py -v --tb=short` exits 0.
</verification>

<success_criteria>
- Canonical package exposes `MarketPreNormalizer` / `get_market_pre_normalizer` / `*PreNormalizer` only.
- Unit tests cover TC-15-T1-01–TC-15-T1-09 and TC-15-T5-01/TC-15-T5-03 with pytest exit 0.
</success_criteria>

<output>
After completion, create `.planning/phases/15-normalize-pipeline-ordering/15-01-SUMMARY.md`
</output>
