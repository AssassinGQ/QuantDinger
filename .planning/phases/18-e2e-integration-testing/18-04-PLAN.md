---
phase: 18-e2e-integration-testing
plan: 04
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - backend_api_python/tests/test_e2e_cross_market_usstock_hshare_ibkr.py
autonomous: true
requirements:
  - TRADE-05
  - TRADE-06
must_haves:
  truths:
    - "USStock market order full chain (Worker → IBKRClient.place_market_order) passes with mock STK qualify."
    - "HShare market order full chain passes with mock STK/SEHK-style qualify (symbol 700 or 00005 per existing tests)."
    - "At least one limit-order path for USStock or HShare exercises place_limit_order with mock minTick."
  artifacts:
    - path: backend_api_python/tests/test_e2e_cross_market_usstock_hshare_ibkr.py
      provides: "USStock + HShare E2E"
      contains: "USStock"
      min_lines: 100
  key_links:
    - from: "tests/test_e2e_cross_market_usstock_hshare_ibkr.py"
      to: "PendingOrderWorker._execute_live_order"
      via: "patch create_client"
      pattern: "PendingOrderWorker"
---

<objective>
Add `test_e2e_cross_market_usstock_hshare_ibkr.py` implementing **USStock** and **HShare** full-chain E2E (market + limit) with mock IBKR, per 18-CONTEXT cross-market coverage. Maps to TRADE-05/TRADE-06 as extended multi-market validation (not browser).

Purpose: Ensure normalize/qualify/TIF paths beyond Forex/Metals in integration tests.

Output: New module passing; full suite green.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-e2e-integration-testing/18-CONTEXT.md

## Concrete symbols (from existing tests — use these exact strings)

- USStock: `AAPL` (or `MSFT`) with `market_type="USStock"` — qualify stub via `_make_qualify_for_stock` in `tests/helpers/ibkr_mocks.py` with a fixed `con_id` (e.g. `265598`).
- HShare: `00005` or `0700.HK` with `market_type="HShare"` — see `test_ibkr_client.py` lines ~255–259 and ~1331 for `place_market_order` HShare patterns.

## Worker patch stack (copy from `test_forex_ibkr_e2e.py`)

Same decorators: `@patch("app.services.pending_order_worker.load_strategy_configs")`, `mark_order_sent`, `mark_order_failed`, `_notify_live_best_effort`, `create_client`, `patched_records` from helpers.
</context>

<tasks>

<task type="auto">
  <name>Task 1: USStock + HShare market-order full chain</name>
  <files>backend_api_python/tests/test_e2e_cross_market_usstock_hshare_ibkr.py</files>
  <read_first>
    - backend_api_python/tests/test_forex_ibkr_e2e.py (`test_uc_sa_e2e_forex_full_chain` structure)
    - backend_api_python/tests/helpers/ibkr_mocks.py (`_make_ibkr_client_for_e2e` with `sec_type="STK"`)
    - backend_api_python/tests/test_ibkr_client.py (HShare `0700.HK` contract lines ~255–260)
  </read_first>
  <action>
    Create `backend_api_python/tests/test_e2e_cross_market_usstock_hshare_ibkr.py` with:
    1. `test_cross_market_usstock_open_long_full_chain` — `PendingOrderWorker()._execute_live_order` with `load_strategy_configs` returning `market_category="USStock"`, `market_type="usstock"` (or exact casing used in codebase — read `load_strategy_configs` consumer), symbol `AAPL`, `signal_type="open_long"`. Mock client via `_make_ibkr_client_for_e2e("AAPL", <con_id>, "AAPL", sec_type="STK")`. Assert `placeOrder` called and `trade.contract.secType == "STK"`.
    2. `test_cross_market_hshare_open_long_full_chain` — same for HShare symbol `00005` or `0700.HK` per `_create_contract` behavior in client; `market_type="HShare"`; assert exchange `SEHK` on contract if applicable.

    Use exact key names in `order_row` dict matching `test_forex_ibkr_e2e.py` worker examples (`strategy_id`, `symbol`, `signal_type`, `amount`, etc.).
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/test_e2e_cross_market_usstock_hshare_ibkr.py -x -q --tb=short -k "cross_market_usstock or cross_market_hshare"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def test_cross_market_usstock_open_long_full_chain" backend_api_python/tests/test_e2e_cross_market_usstock_hshare_ibkr.py` returns one line
    - `grep -n "def test_cross_market_hshare_open_long_full_chain" backend_api_python/tests/test_e2e_cross_market_usstock_hshare_ibkr.py` returns one line
    - `grep -n "USStock\|HShare" backend_api_python/tests/test_e2e_cross_market_usstock_hshare_ibkr.py` returns at least 4 lines
    - `cd backend_api_python && pytest tests/test_e2e_cross_market_usstock_hshare_ibkr.py -x -q --tb=short -k "cross_market_usstock or cross_market_hshare"` exit code 0
  </acceptance_criteria>
  <done>Both market chains pass.</done>
</task>

<task type="auto">
  <name>Task 2: USStock or HShare limit-order path (TRADE-06 cross-market)</name>
  <files>backend_api_python/tests/test_e2e_cross_market_usstock_hshare_ibkr.py</files>
  <read_first>
    - backend_api_python/tests/test_forex_ibkr_e2e.py (search `test_e2e_forex_limit` or limit worker tests)
    - backend_api_python/app/services/pending_order_worker.py (grep `limit` / `Limit` / `order_type`)
  </read_first>
  <action>
    Add **`test_cross_market_usstock_limit_order_submitted`** (or HShare variant if worker wiring favors one market):
    - `signal_type` / config must route to `place_limit_order` with non-zero limit price (e.g. `trading_config.live_order` or fields Phase 17 added — read actual `PendingOrderWorker._execute_live_order` for exact keys: copy from existing limit E2E in `test_forex_ibkr_e2e.py`).
    - `min_tick` stub via `_make_ibkr_client_for_e2e(..., min_tick=0.01)` for USStock.
    - Docstring line contains substring `TRADE-06`.

    If worker does not support limit for USStock in code, document `pytest.skip` with reason citing file:line after reading source — **prefer** implementing test only if code path exists without production change.
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/test_e2e_cross_market_usstock_hshare_ibkr.py::test_cross_market_usstock_limit_order_submitted -x -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def test_cross_market_usstock_limit_order_submitted" backend_api_python/tests/test_e2e_cross_market_usstock_hshare_ibkr.py` returns one line OR file contains `pytest.skip` with `PendingOrderWorker` and reason string
    - `grep -n "TRADE-06" backend_api_python/tests/test_e2e_cross_market_usstock_hshare_ibkr.py` returns at least one line
    - `cd backend_api_python && pytest tests/test_e2e_cross_market_usstock_hshare_ibkr.py -x -q --tb=short` exit code 0
  </acceptance_criteria>
  <done>Limit cross-market test passes or documented skip with source citation.</done>
</task>

</tasks>

<verification>
`cd backend_api_python && pytest tests/test_e2e_cross_market_usstock_hshare_ibkr.py -x -q --tb=short`
</verification>

<success_criteria>
- Cross-market E2E file exists with USStock, HShare, and limit coverage per tasks.
</success_criteria>

<output>
After completion, create `.planning/phases/18-e2e-integration-testing/18-04-SUMMARY.md`
</output>
