---
phase: 18-e2e-integration-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/tests/helpers/__init__.py
  - backend_api_python/tests/helpers/ibkr_mocks.py
  - backend_api_python/tests/helpers/flask_strategy_app.py
  - backend_api_python/tests/conftest.py
  - backend_api_python/tests/test_ibkr_forex_paper_smoke.py
  - backend_api_python/tests/test_forex_ibkr_e2e.py
autonomous: true
requirements:
  - TRADE-05
  - TRADE-06
  - TEST-02
must_haves:
  truths:
    - "Smoke and E2E tests import shared IBKR/Flask helpers from tests.helpers without changing assertion outcomes."
    - "conftest exposes a reusable Flask test_client fixture for strategy_bp at url_prefix /api with g.user_id=1."
  artifacts:
    - path: backend_api_python/tests/helpers/ibkr_mocks.py
      provides: "FakeEvent, qualify stubs, wire_ib_events, fire_callbacks, make_ibkr_client_for_e2e"
      contains: "_FakeEvent"
      min_lines: 40
    - path: backend_api_python/tests/helpers/flask_strategy_app.py
      provides: "strategy test app / client factory"
      contains: "register_blueprint"
      min_lines: 15
    - path: backend_api_python/tests/conftest.py
      provides: "strategy_client or strategy_test_app fixture"
      contains: "strategy"
      min_lines: 1
  key_links:
    - from: "tests/helpers/ibkr_mocks.py"
      to: "tests/test_ibkr_client.py"
      via: "re-export or import _make_mock_ib_insync, _make_client_with_mock_ib, _make_trade_mock"
      pattern: "test_ibkr_client"
---

<objective>
Extract shared IBKR mock utilities and a single Flask `strategy_bp` test app factory into `tests/helpers/`, wire `conftest.py` fixtures, and update `test_ibkr_forex_paper_smoke.py` and `test_forex_ibkr_e2e.py` to import from helpers **without changing test behavior** (per 18-CONTEXT.md: existing E2E file logic stays the same).

Purpose: Wave 0 foundation so Phase 18 new E2E modules do not copy/paste mock setup.

Output: `tests/helpers/*.py`, extended `conftest.py`, refactored imports; `pytest tests/test_ibkr_forex_paper_smoke.py tests/test_forex_ibkr_e2e.py` green.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-e2e-integration-testing/18-CONTEXT.md
@.planning/phases/18-e2e-integration-testing/18-RESEARCH.md
@.planning/REQUIREMENTS.md

## Locked (18-CONTEXT)

- Mock helpers land under `backend_api_python/tests/helpers/` (not project-root `tests/`).
- `test_forex_ibkr_e2e.py` **unchanged in test logic** — only import paths and shared fixture wiring may change.
- Shared Flask fixture: real `Flask` app + `strategy_bp` + `url_prefix='/api'` + `g.user_id` in `before_request` (same as current `client_fixture` in `test_forex_ibkr_e2e.py` lines 78–89).
- `login_required` stub: noop decorator on `app.utils.auth.login_required` then `importlib.reload(app.routes.strategy)` before importing `strategy_bp` (same order as existing E2E file lines 34–42).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tests/helpers/ibkr_mocks.py and switch paper smoke imports</name>
  <files>backend_api_python/tests/helpers/__init__.py,backend_api_python/tests/helpers/ibkr_mocks.py,backend_api_python/tests/test_ibkr_forex_paper_smoke.py</files>
  <read_first>
    - backend_api_python/tests/test_ibkr_forex_paper_smoke.py (full file)
    - backend_api_python/tests/test_ibkr_client.py (lines 38–120 for _make_mock_ib_insync, _make_trade_mock signatures)
    - backend_api_python/tests/test_forex_ibkr_e2e.py (helpers section lines 62–169 for _make_ibkr_client_for_e2e, _make_qualify_for_stock, patched_records)
  </read_first>
  <action>
    1. Create `backend_api_python/tests/helpers/__init__.py` (may be empty or export public names).
    2. Create `backend_api_python/tests/helpers/ibkr_mocks.py` containing moved implementations **from smoke only** in this task:
       - `_FakeEvent`, `_wire_ib_events`, `_make_qualify_for_pair`, `_fill_mock_exec`, `_fire_callbacks_after_fill` copied from `test_ibkr_forex_paper_smoke.py` (preserve docstrings and behavior).
       - Import and re-export from `tests.test_ibkr_client`: `_make_mock_ib_insync`, `_make_client_with_mock_ib`, `_make_trade_mock` (same names) so consumers can `from tests.helpers.ibkr_mocks import _make_mock_ib_insync, ...`.
    3. Update `test_ibkr_forex_paper_smoke.py`: remove in-file definitions of `_FakeEvent`, `_wire_ib_events`, `_make_qualify_for_pair`, `_fire_callbacks_after_fill`, `_fill_mock_exec`; add imports from `tests.helpers.ibkr_mocks`. Keep imports from `tests.test_ibkr_client` only if still needed, or switch to `ibkr_mocks` re-exports.
    4. Do **not** modify `test_forex_ibkr_e2e.py` in this task (only smoke file + new helpers). **Do not** move `_make_qualify_for_stock`, `_make_ibkr_client_for_e2e`, or `patched_records` until Task 2.
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/test_ibkr_forex_paper_smoke.py -x -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `test -f backend_api_python/tests/helpers/ibkr_mocks.py` exits 0
    - `grep -n "class _FakeEvent" backend_api_python/tests/test_ibkr_forex_paper_smoke.py` returns no matches
    - `grep -n "from tests.helpers.ibkr_mocks import" backend_api_python/tests/test_ibkr_forex_paper_smoke.py` returns at least one line
    - `cd backend_api_python && pytest tests/test_ibkr_forex_paper_smoke.py -x -q --tb=short` exit code 0
  </acceptance_criteria>
  <done>Smoke tests pass; IBKR helpers live in ibkr_mocks.py.</done>
</task>

<task type="auto">
  <name>Task 2: flask_strategy_app factory, conftest fixture, refactor test_forex_ibkr_e2e imports</name>
  <files>backend_api_python/tests/helpers/flask_strategy_app.py,backend_api_python/tests/conftest.py,backend_api_python/tests/test_forex_ibkr_e2e.py</files>
  <read_first>
    - backend_api_python/tests/test_forex_ibkr_e2e.py (lines 1–170)
    - backend_api_python/tests/helpers/ibkr_mocks.py (after Task 1)
    - backend_api_python/tests/conftest.py (full file)
  </read_first>
  <action>
    0. Extend `backend_api_python/tests/helpers/ibkr_mocks.py` with code moved from `test_forex_ibkr_e2e.py`: `_make_qualify_for_stock`, `_make_ibkr_client_for_e2e` (identical bodies), and `@contextmanager` `patched_records` (patches `app.services.live_trading.records.ibkr_save_position` and `ibkr_save_pnl`). Add required imports (`IBKRClient`, `types` as `_t` for `SimpleNamespace` in `_mock_details`, etc.).
    1. Create `backend_api_python/tests/helpers/flask_strategy_app.py` with:
       - Function `build_strategy_blueprint_and_client()` or split into `patch_login_and_reload_strategy()` + `make_strategy_test_client()` such that the returned `flask.testing.FlaskClient` can `post("/api/strategies/create", json=...)` with status 200 for the existing `test_uc_sa_e2e_api_forex_create_returns_200` payload shape.
       - Required mechanism: set `app.utils.auth.login_required` to the same `_noop_decorator` pattern as `test_forex_ibkr_e2e.py` lines 26–40, then `importlib.reload(app.routes.strategy)`, then `from app.routes.strategy import strategy_bp`, `Flask(__name__)`, `app.register_blueprint(strategy_bp, url_prefix="/api")`, `@app.before_request` set `g.user_id = 1`, `return app.test_client()` (or yield app + client — match fixture needs).
    2. Add to `conftest.py` pytest fixture named **`strategy_client`** (or `client_fixture` alias) that yields the test client from `flask_strategy_app` factory so new tests can depend on `strategy_client` without duplicating app setup. Document in docstring: same as legacy `client_fixture` in `test_forex_ibkr_e2e.py`.
    3. Refactor `test_forex_ibkr_e2e.py`:
       - Remove local definitions now in `ibkr_mocks.py` (`_make_qualify_for_stock`, `_make_ibkr_client_for_e2e`, `patched_records`, `_mock_db` may stay if not moved — **keep `_mock_db` in e2e file** unless identical elsewhere).
       - Replace imports from `tests.test_ibkr_forex_paper_smoke` with `from tests.helpers.ibkr_mocks import (_FakeEvent, _fire_callbacks_after_fill, _make_qualify_for_pair, _wire_ib_events, ...)` as needed.
       - Replace imports from `tests.test_ibkr_client` with `tests.helpers.ibkr_mocks` re-exports where applicable.
       - Optional: replace inline `client_fixture` with `from tests.conftest import strategy_client` **only if** the fixture name is wired — otherwise keep `client_fixture` in file delegating to helper factory to avoid breaking parametrized tests.
    4. Preserve every existing test function name and decorator order; no assertion changes.
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/test_forex_ibkr_e2e.py -x -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "register_blueprint(strategy_bp, url_prefix=\"/api\")" backend_api_python/tests/helpers/flask_strategy_app.py` returns at least one line
    - `grep -n "strategy_client\|client_fixture" backend_api_python/tests/conftest.py` shows new fixture
    - `grep -n "def test_uc_sa_e2e_api_forex_create_returns_200" backend_api_python/tests/test_forex_ibkr_e2e.py` still exists
    - `cd backend_api_python && pytest tests/test_forex_ibkr_e2e.py -x -q --tb=short` exit code 0
  </acceptance_criteria>
  <done>Legacy E2E file passes; shared Flask factory and fixtures available for Phase 18 HTTP tests.</done>
</task>

</tasks>

<verification>
Run full backend suite: `cd backend_api_python && pytest -x -q --tb=short` (regression gate per 18-CONTEXT).
</verification>

<success_criteria>
- Helpers module importable; smoke + forex E2E green; no behavioral drift in moved code.
</success_criteria>

<output>
After completion, create `.planning/phases/18-e2e-integration-testing/18-01-SUMMARY.md`
</output>
