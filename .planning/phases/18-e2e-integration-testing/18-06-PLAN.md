---
phase: 18-e2e-integration-testing
plan: 06
type: execute
wave: 3
depends_on:
  - 18-05
files_modified:
  - quantdinger_vue/tests/unit/frnt-02-wizard-forex-market.spec.js
autonomous: true
requirements:
  - TEST-02
must_haves:
  truths:
    - "Jest unit tests mount or shallow-render wizard-related Vue components and assert Forex/IBKR-related props or template content."
    - "`npm run test:unit` exits 0 in quantdinger_vue."
  artifacts:
    - path: quantdinger_vue/tests/unit/frnt-02-wizard-forex-market.spec.js
      provides: "wizard Jest coverage"
      contains: "describe("
      min_lines: 25
  key_links:
    - from: "quantdinger_vue/tests/unit/frnt-02-wizard-forex-market.spec.js"
      to: "quantdinger_vue/src/views/trading-assistant/index.vue"
      via: "import + mount/shallowMount"
      pattern: "trading-assistant"
---

<objective>
Add **Vue 2 Jest** unit tests for the trading-assistant wizard (Forex + IBKR paths) per 18-CONTEXT — complements backend HTTP TEST-02; no browser.

Purpose: Guard wizard UI regressions for the same user flows covered by `test_strategy_http_e2e.py`.

Output: New `tests/unit/*.spec.js` file; `npm run test:unit` green.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-e2e-integration-testing/18-CONTEXT.md
@quantdinger_vue/jest.config.js
@quantdinger_vue/tests/unit/frnt-01-forex-ibkr-options.spec.js

## Stack (18-RESEARCH)

- Jest 27.x, `@vue/test-utils` ^1.3.x, Vue CLI 5.

## Discretion (locked scope: “几个” tests)

- Implement **at least 2** `it(...)` blocks in the new file.
- Prefer `mount` on a small child component exported from `index.vue` if modular; otherwise `shallowMount` on `index.vue` with stubs for heavy children.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add frnt-02 wizard Jest spec (Forex / IBKR visibility)</name>
  <files>quantdinger_vue/tests/unit/frnt-02-wizard-forex-market.spec.js</files>
  <read_first>
    - quantdinger_vue/tests/unit/frnt-01-forex-ibkr-options.spec.js
    - quantdinger_vue/src/views/trading-assistant/index.vue (first 200 lines — search `FOREX`, `ibkr`, `market_category`)
    - quantdinger_vue/package.json (scripts.test:unit)
  </read_first>
  <action>
    1. Create `quantdinger_vue/tests/unit/frnt-02-wizard-forex-market.spec.js`.
    2. `describe` block title must include **`TEST-02`** or **`FRNT-02`** (pick one string for grep).
    3. Add **≥2** tests:
       - Example A: `mount` or `shallowMount` `index.vue` with minimal `data` / `props` / `mocks` so component renders; assert wrapper text or HTML contains substring `ibkr-paper` OR `IBKR` (case-sensitive choice: match actual template string from `index.vue`).
       - Example B: assert `FOREX_BROKER_OPTIONS` or equivalent computed/options array includes `value: 'ibkr-paper'` (similar to frnt-01) **or** assert `market_category` / Forex label appears when `isForexMarket` is true — use patterns actually present in `index.vue` (read file before asserting).
    4. Do not add Playwright or Cypress.
    5. If `index.vue` is too heavy to mount, test a smaller SFC from the same directory (e.g. a step component) — file path must be under `quantdinger_vue/src/views/trading-assistant/` and imported in spec.
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/quantdinger_vue && npm run test:unit -- --testPathPattern=frnt-02-wizard-forex-market --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `test -f quantdinger_vue/tests/unit/frnt-02-wizard-forex-market.spec.js` exits 0
    - `grep -n "it(" quantdinger_vue/tests/unit/frnt-02-wizard-forex-market.spec.js | wc -l` outputs integer ≥ 2
    - `grep -n "FRNT-02\|TEST-02" quantdinger_vue/tests/unit/frnt-02-wizard-forex-market.spec.js` returns at least one line
    - `cd quantdinger_vue && npm run test:unit -- --testPathPattern=frnt-02-wizard-forex-market --no-coverage` exit code 0
  </acceptance_criteria>
  <done>Wizard Jest file exists with two passing tests.</done>
</task>

</tasks>

<verification>
`cd quantdinger_vue && npm run test:unit --no-coverage` (full frontend unit suite).
</verification>

<success_criteria>
- Frontend unit suite green; new spec documents TEST-02 / FRNT-02 wizard coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/18-e2e-integration-testing/18-06-SUMMARY.md`
</output>
