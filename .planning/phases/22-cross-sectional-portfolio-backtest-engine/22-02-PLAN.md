---
phase: 22-cross-sectional-portfolio-backtest-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01-PLAN.md
files_modified:
  - backend_api_python/app/routes/cross_sectional_portfolio_backtest.py
  - backend_api_python/app/routes/__init__.py
  - backend_api_python/tests/test_cross_sectional_portfolio_bt01.py
autonomous: true
requirements:
  - BT-01
user_setup: []
must_haves:
  truths:
    - "HTTP 同步 POST；成功响应包络与现有回测路由一致：`code==1` 且 `msg=='OK'` 且业务负载在 `data`（与 `backtest.py` 成功形态一致）"
    - "`symbol_list` 与 `universe`（如 NQ100）同时非空时返回 4xx + 明确 `msg`，不做优先级裁决（D-03）"
    - "成功 `data` 内含完整可复现子结构：universe digest、因子/指标配置 hash、执行策略摘要、行业中性化开关状态等（D-02），且含 `neutral_off` / `neutral_on` 两套结果引用或内嵌"
    - "错误响应不返回 Python 栈追踪字符串到客户端 JSON"
  artifacts:
    - path: "backend_api_python/app/routes/cross_sectional_portfolio_backtest.py"
      provides: "POST 截面组合回测端点 + 请求体验证 + 调用 CrossSectionalPortfolioBacktestService"
      min_lines: 80
    - path: "backend_api_python/app/routes/__init__.py"
      provides: "register_blueprint 新路由"
      contains: "cross_sectional_portfolio"
    - path: "backend_api_python/tests/test_cross_sectional_portfolio_bt01.py"
      provides: "HTTP 层 BT-01 与 D-03 用例"
      contains: "test_.*api|test_.*pool"
---

<threat_model>
| ID | 威胁 | 严重性 | 缓解 |
|----|------|--------|------|
| T-22-http | 未认证用户调用高成本回测导致 DoS | medium | 与 `backtest.py` 对齐：若现有回测端点使用 `@login_required`，本路由同样装饰；若单标的回测无认证则本 plan 记录「对齐现有」并在 `read_first` 中固定参照的具体端点装饰器 |
| T-22-inject | 超大 JSON 体或超长 `indicator_code` 拖垮 worker | medium | 对请求体大小与 `indicator_code` 字符长度设上限（常量），超限返回 400 + 固定 `msg` |
| T-22-leak | 异常处理将 `traceback` 写入 `msg` | high | 仅向客户端返回短错误码 + 固定前缀消息；详细日志写入服务端 logger |
</threat_model>

<objective>
在 **22-01-PLAN.md** 交付的服务内核之上，暴露 **同步 POST** 截面组合回测 HTTP API：请求体验证（含 **symbol_list 与 universe 互斥**）、`{code,msg,data}` 包络与单标的回测一致、**data 内可复现包**（D-02）及 **neutral_off / neutral_on** 载荷；注册 Blueprint；扩展 `test_cross_sectional_portfolio_bt01.py` 的 API 用例。
</objective>

<verification_contract>
本 plan 内**每个** `<task>`：**`<acceptance_criteria>` 全部满足**，且 `<verify>` **最后一行**为 `cd backend_api_python && python3 -m pytest tests/ -q`（**全量**用例目录）。定向 `-k` 行仅用于加速排障，不可替代全量。
</verification_contract>

<tasks>

<task type="auto">
  <name>Task 1: Blueprint + 请求体校验（互斥池 + 必填字段）</name>
  <read_first>
    - .planning/phases/22-cross-sectional-portfolio-backtest-engine/22-CONTEXT.md
    - backend_api_python/app/routes/backtest.py（成功/失败 jsonify 形态）
    - backend_api_python/app/services/cross_sectional_portfolio_backtest.py
  </read_first>
  <files>backend_api_python/app/routes/cross_sectional_portfolio_backtest.py</files>
  <action>
    新建 Blueprint（建议 `cross_sectional_portfolio_bt_bp`，`url_prefix` 与现有约定一致，例如挂在 `/api/indicator` 下子路径 **`/cross-sectional-portfolio-backtest`** 或实现时与产品命名对齐，但须在 `acceptance_criteria` 写明**最终路径字符串**以便 grep）。  
    实现 `POST` 处理函数：解析 JSON；若 `symbol_list` 与 `universe` **同时为非空列表/非空字符串**（实现时定义「非空」与单标的回测字段类型一致），返回 **HTTP 400** 与 `jsonify({'code': 0, 'msg': '<固定中英键前缀+说明>', 'data': None})`（与 `backtest.py` 400 形态对齐：`code` 0 表示失败）。  
    校验必填：`indicator_code`、`startDate`/`endDate`（或与时序回测一致的字段名——**必须从 `backtest.py` 复制同一命名**）、以及 `symbol_list` 与 `universe` 二者**恰好其一**非空。
  </action>
  <acceptance_criteria>
    - 新路由文件存在且定义 `Blueprint`
    - 存在字符串 `symbol_list` 与 `universe` 的联合校验逻辑（grep 两词在同一函数域）
    - 双非空时 HTTP 状态码为 400 且响应 JSON `code == 0`
  </acceptance_criteria>
  <test_case_specs>
    <case id="22-02-01a">
      <what>互斥违反 D-03</what>
      <input>JSON：`symbol_list`: ["AAPL"], `universe`: "NQ100"，其余字段合法占位</input>
      <expected>HTTP 400；`msg` 非空且不含 traceback；`data is None`</expected>
    </case>
    <case id="22-02-01b">
      <what>两者皆空</what>
      <input>`symbol_list`: [] 且 `universe`: "" 或缺省（与实现一致）</input>
      <expected>HTTP 400；明确提示缺少股票池来源</expected>
    </case>
    <case id="22-02-01c">
      <what>仅 symbol_list</what>
      <input>非空 `symbol_list`，`universe` 空</input>
      <expected>通过校验进入服务调用（可 mock 服务层或打 integration）HTTP 200 且 `code==1`（若数据与 DB 可用；否则使用 pytest `monkeypatch` 服务方法固定返回成功 dict）</expected>
    </case>
  </test_case_specs>
  <verify>
    - `cd backend_api_python && python3 -m pytest tests/test_cross_sectional_portfolio_bt01.py -k "pool or validation" -q`
    - `cd backend_api_python && python3 -m pytest tests/ -q`
  </verify>
</task>

<task type="auto">
  <name>Task 2: 绑定服务 + 成功 data 形态（可复现包 + 双 neutral）</name>
  <read_first>
    - backend_api_python/app/routes/cross_sectional_portfolio_backtest.py
    - backend_api_python/app/services/cross_sectional_portfolio_backtest.py
    - backend_api_python/app/routes/backtest.py（成功响应样例行）
  </read_first>
  <files>backend_api_python/app/routes/cross_sectional_portfolio_backtest.py</files>
  <action>
    在 POST 成功路径调用 `CrossSectionalPortfolioBacktestService`（或 22-01 实际类名）并返回：  
    `jsonify({'code': 1, 'msg': 'OK', 'data': payload})`。  
    `payload` 必须包含（键名实现定稿后写入测试常量，以下为语义最小集）：  
    - `repro` 或等价根对象：含 `universe_digest`、`indicator_config_hash`、`execution_profile`（Phase 21 开关序列化）、`industry_neutral_available` 等（与 D-02 对齐）；  
    - `neutral_off` 与 `neutral_on`：与 22-01 服务返回结构一致或为其子集+引用。  
    契约错误（指标缺 weights 等）映射为 **HTTP 400 或 422**（选一种并在测试中固定），`msg` 为用户可读短句，**禁止** `traceback.format_exc()` 进入 JSON。
  </action>
  <acceptance_criteria>
    - 成功路径存在 `'code': 1` 与 `'msg': 'OK'` 字面量（grep 可命中）
    - `data` 序列化前为 dict 且包含键名 `neutral_off` 与 `neutral_on`
    - `data` 内含可复现字段之一：`universe_digest` 或 `universe_snapshot_id`（grep 命中其一即可）
  </acceptance_criteria>
  <test_case_specs>
    <case id="22-02-02a">
      <what>成功响应结构</what>
      <input>monkeypatch 服务返回带双 neutral + repro 字段的 dict</input>
      <expected>HTTP 200；JSON `code==1`；`data["neutral_off"]` 与 `data["neutral_on"]` 均存在；`data` 含 digest/hash 类字段</expected>
    </case>
    <case id="22-02-02b">
      <what>服务抛契约异常时不泄漏栈</what>
      <input>monkeypatch 服务抛 `ValueError("CROSS_SECTIONAL_CONTRACT: ...")`</input>
      <expected>响应 JSON 的 `msg` 不包含子串 `Traceback` 与 `File \""`</expected>
    </case>
  </test_case_specs>
  <verify>
    - `cd backend_api_python && python3 -m pytest tests/test_cross_sectional_portfolio_bt01.py -k "api_success or leak" -q`
    - `cd backend_api_python && python3 -m pytest tests/ -q`
  </verify>
</task>

<task type="auto">
  <name>Task 3: 注册路由与 Flask app 集成烟测</name>
  <read_first>
    - backend_api_python/app/routes/__init__.py
    - backend_api_python/tests/conftest.py（若已有 `app` fixture 则复用）
  </read_first>
  <files>backend_api_python/app/routes/__init__.py, backend_api_python/tests/test_cross_sectional_portfolio_bt01.py</files>
  <action>
    在 `register_routes` 中 `import` 新 Blueprint 并 `register_blueprint`，**顺序**与同类 `/api/indicator` 路由一致（紧挨 `backtest_bp` 或 `indicator_bp` 便于维护）。  
    在 `test_cross_sectional_portfolio_bt01.py` 增加使用 Flask `test_client()` 的烟测：`POST` 新路径，断言 400（互斥体）与路由可达（非 404）。
  </action>
  <acceptance_criteria>
    - `__init__.py` 含新 blueprint 的 import 与 `register_blueprint`
    - 测试中 `client.post(...)` 对新路径返回非 404（对非法体为 400 或对 mock 成功体为 200）
  </acceptance_criteria>
  <test_case_specs>
    <case id="22-02-03a">
      <what>路由已挂载</what>
      <input>`test_client` POST 互斥 JSON 至最终 URL</input>
      <expected>状态码 ≠ 404</expected>
    </case>
    <case id="22-02-03b">
      <what>404 未注册回归</what>
      <input>随机前缀路径 `/api/indicator/nonexistent_bt_phase22_xyz`</input>
      <expected>404（作为对照，可选若项目统一 404 形态则断言）</expected>
    </case>
  </test_case_specs>
  <verify>
    - `cd backend_api_python && python3 -m pytest tests/test_cross_sectional_portfolio_bt01.py -k "route_register" -q`
    - `cd backend_api_python && python3 -m pytest tests/ -q`
  </verify>
</task>

</tasks>

## PLANNING COMPLETE
