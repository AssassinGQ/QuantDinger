---
phase: 22-cross-sectional-portfolio-backtest-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/app/strategies/cross_sectional_indicator.py
  - backend_api_python/app/services/cross_sectional_portfolio_backtest.py
  - backend_api_python/tests/test_cross_sectional_portfolio_bt01.py
autonomous: true
requirements:
  - BT-01
user_setup: []
must_haves:
  truths:
    - "截面指标在每个调仓点同时产出 scores 与 weights；缺一即契约失败，框架不做等权或 score→weight 兜底"
    - "单次引擎调用在逻辑上产生一个组合净值序列；行业中性化关/开各一套完整子结果（neutral_off / neutral_on）"
    - "执行与定价语义复用 Phase 21 路径（CrossSectionalRunner / filter_chain / resolve_shifted_execution_date），不在本服务内复制第二套 T+1"
    - "信号日 T 的成分池仅使用 effective_date <= T 的 PIT 记录"
  artifacts:
    - path: "backend_api_python/app/strategies/cross_sectional_indicator.py"
      provides: "exec_env 含 weights；执行后强校验 scores+weights；禁止用 scores 自动填充 weights"
      min_lines: 70
    - path: "backend_api_python/app/services/cross_sectional_portfolio_backtest.py"
      provides: "组合截面回测编排：面板→指标→组合模拟→neutral_off/on→可复现 digest 字段组装"
      min_lines: 200
    - path: "backend_api_python/tests/test_cross_sectional_portfolio_bt01.py"
      provides: "BT-01 契约与引擎回归（含全量 verify 门禁）"
      contains: "test_.*contract|test_.*engine|test_.*neutral|test_.*pit"
---

<threat_model>
| ID | 威胁 | 严重性 | 缓解（本 plan 任务内） |
|----|------|--------|------------------------|
| T-22-exec | 用户提交的 `indicator_code` 在缩减 builtins 的沙箱中逃逸或读写宿主 | high | 保持/对齐现有 `safe_builtins` 黑名单；不在本任务扩大危险 builtins；契约测试断言禁止 `__import__` 等符号出现在 exec_env 的 builtins 映射中（静态或运行时探针按现有 Phase 21 风格） |
| T-22-data | 恶意或畸形 `symbol_list` / 面板键导致 OOM 或极慢循环 | medium | 对 symbol 数量与面板行数设合理上限或在服务入口校验并 4xx（具体上限写入代码常量并在测试中固定） |
| T-22-log | 指标异常将完整 traceback 经 API 返回（本 plan 主要为库层，仍禁止向上抛出裸栈） | medium | 库层抛业务异常类型 + 固定前缀消息；不将 `traceback.format_exc()` 拼进对外 `msg`（HTTP 层在 plan 02 收口） |
</threat_model>

<objective>
交付 BT-01 的**后端内核**：演进 `run_cross_sectional_indicator` 满足 **scores + weights** 硬契约；新增 `CrossSectionalPortfolioBacktestService`（或等价命名）编排多标的面板、截面指标、**单组合**权益路径、**行业中性化 off/on 双套完整子结果**；复用 Phase 21 执行语义；配套 `test_cross_sectional_portfolio_bt01.py` 契约与引擎用例。
</objective>

<verification_contract>
本 plan 内**每个** `<task>` 的完成门槛 = `<acceptance_criteria>` 全部满足 **且** `<verify>` 中**最后一行必须为** `cd backend_api_python && python3 -m pytest tests/ -q`（全量测试套件，禁止仅以单文件通过替代放行）。  
若仓库惯例使用 `pytest` 而非 `python3 -m pytest`，允许等价，但**必须**仍是 `tests/` 全目录。
</verification_contract>

<tasks>

<task type="auto">
  <name>Task 1: 截面指标 weights 契约与 exec_env</name>
  <read_first>
    - .planning/phases/22-cross-sectional-portfolio-backtest-engine/22-CONTEXT.md
    - backend_api_python/app/strategies/cross_sectional_indicator.py
    - backend_api_python/app/strategies/runners/cross_sectional_runner.py
  </read_first>
  <files>backend_api_python/app/strategies/cross_sectional_indicator.py</files>
  <action>
    在 `exec_env` 初始化中增加 `"weights": {}`（与 `scores` 并列）。  
    指标代码执行完成后：若 `scores` 或 `weights` 任一缺失、非 `dict`、或在与 `symbols` 交集上「可交易标的」子集上为空（实现时选定一种明确规则并在 `<test_case_specs>` 对齐），则**不得**返回部分结果：应抛出带固定前缀的异常（建议 `ValueError` 且消息以 `CROSS_SECTIONAL_CONTRACT:` 开头）或返回 `None` 但**同一文件内**所有调用方必须能区分契约失败与数据缺失（推荐抛异常以便服务层统一处理）。  
    **禁止**：用 `scores` 推导/填充 `weights`；在缺 `rankings` 时仅用 `scores` 排序保留，但不得推断 weights。  
    成功返回的 `dict` 必须包含键：`scores`, `weights`, `rankings`（`rankings` 仍可由 scores 排序生成，与 CONTEXT D-08 一致）。
  </action>
  <acceptance_criteria>
    - `cross_sectional_indicator.py` 中 `exec_env` 字面量包含 `"weights": {}`
    - 同一文件存在对 `weights` 的契约校验逻辑（grep 可命中 `weights` 与 `CROSS_SECTIONAL_CONTRACT` 或等价错误前缀）
    - 不存在将 `scores` 赋值给 `weights` 或 `weights.setdefault` 从 scores 派生的代码路径
  </acceptance_criteria>
  <test_case_specs>
    <case id="22-01-01a">
      <what>合法指标同时写入 scores 与 weights</what>
      <input>`indicator_code` 对 `exec_env["symbols"]` 中每个 symbol 写入正 float 的 scores 与 weights；`data` 为至少 2 个 symbol 的最小 OHLCV DataFrame fixture</input>
      <expected>返回值非 None；`set(result.keys())` 包含 `scores`,`weights`,`rankings`；`len(result["weights"])` 与参与计算的 symbol 数一致</expected>
    </case>
    <case id="22-01-01b">
      <what>缺 weights 时契约失败</what>
      <input>指标代码只填充 `scores` 不碰 `weights`</input>
      <expected>契约失败路径触发（异常或明确错误返回值，与实现一致且在测试中断言）</expected>
    </case>
    <case id="22-01-01c">
      <what>缺 scores 时契约失败</what>
      <input>指标代码只填充 `weights` 不填充 `scores`</input>
      <expected>同上，契约失败</expected>
    </case>
  </test_case_specs>
  <verify>
    - `cd backend_api_python && python3 -m pytest tests/test_cross_sectional_portfolio_bt01.py -k "indicator_contract" -q`
    - `cd backend_api_python && python3 -m pytest tests/ -q`
  </verify>
</task>

<task type="auto">
  <name>Task 2: 组合截面回测服务编排（单组合路径 + PIT + Phase 21 复用）</name>
  <read_first>
    - .planning/phases/22-cross-sectional-portfolio-backtest-engine/22-CONTEXT.md
    - .planning/phases/22-cross-sectional-portfolio-backtest-engine/22-RESEARCH.md
    - backend_api_python/app/services/backtest.py（仅读 `_calculate_metrics` / `_format_result` 签名与返回字段作对齐参考）
    - backend_api_python/app/strategies/runners/cross_sectional_runner.py
    - backend_api_python/app/strategies/runners/cross_sectional_filter_chain.py
    - backend_api_python/app/factors/panel.py
    - backend_api_python/app/services/nq100_sources.py
  </read_first>
  <files>backend_api_python/app/services/cross_sectional_portfolio_backtest.py</files>
  <action>
    新建服务模块（建议类名 `CrossSectionalPortfolioBacktestService`）：公开方法例如 `run(request: dict) -> dict`，输入字段与 **22-02** HTTP 层对齐的**内部 DTO**（本任务可先定义 `TypedDict` 或 dataclass 在同一文件，plan 02 路由直接组装该结构）。  
    职责：  
    (1) 从 `symbol_list` **或** universe 解析得到当日 PIT symbol 集合：`effective_date <= signal_date`（具体查询复用 `nq100_sources` 或现有 universe DAO，禁止未来生效行）；  
    (2) 构建与 `run_cross_sectional_indicator` 兼容的 `Dict[str, pd.DataFrame]` 面板；  
    (3) 调用 `run_cross_sectional_indicator` 取得 scores/weights；  
    (4) 将权重映射为**单组合**再平衡/成交序列：必须通过 **import 并调用** `CrossSectionalRunner`（或 Phase 21 已导出的同等调度函数）应用 T+1、`next_open` 与过滤器链，**禁止**复制粘贴一套 `execution_date` 规则。  
    (5) 产出与 `BacktestService._format_result` **字段命名可比对** 的组合级摘要（至少包含权益曲线序列、年化、Sharpe、最大回撤等中与单标的回测报告交集的那一组；精确字段表在实现时 grep `_format_result` 后锁定并在测试中快照关键键名）。  
    本任务可先实现 **neutral_off** 单路径；**neutral_on** 可在 Task 3 扩展同一服务文件。
  </action>
  <acceptance_criteria>
    - 存在新文件 `backend_api_python/app/services/cross_sectional_portfolio_backtest.py`
    - 该文件 import 链中出现 `cross_sectional_runner` 或 `CrossSectionalRunner`（grep 可验证）
    - 服务方法返回 dict 中含组合权益序列键（实现定名后写入测试常量，如 `equity_curve` 或与 `_format_result` 对齐的键）
  </acceptance_criteria>
  <test_case_specs>
    <case id="22-01-02a">
      <what>两标的固定面板 + 恒定 weights 下产生单条组合权益曲线</what>
      <input>最小 `symbol_list`、日期范围覆盖 ≥5 个交易日；指标代码返回 flat scores + 归一化 weights</input>
      <expected>返回 `equity_curve`（或对齐键）长度 > 0；数值有限无 NaN 全行（允许首日为 seed）</expected>
    </case>
    <case id="22-01-02b">
      <what>PIT：某 symbol 在 T 之后 effective_date 才生效则不参与 T 日决策</what>
      <input>fixture universe 或 mock：`SYM` 在 T 无记录、在 T+k 才生效</input>
      <expected>T 日调仓成分不含 `SYM`；T+k 后可含（断言具体集合）</expected>
    </case>
    <case id="22-01-02c">
      <what>复现性：相同请求体运行两次 digest 或 equity 序列一致</what>
      <input>固定随机种子（若有）、固定指标与数据 fixture</input>
      <expected>两次 `run` 返回的 `repro_digest`（或实现选用的 hash 字段）相同，且权益序列逐点相等（容差 1e-9）</expected>
    </case>
  </test_case_specs>
  <verify>
    - `cd backend_api_python && python3 -m pytest tests/test_cross_sectional_portfolio_bt01.py -k "engine_core or pit" -q`
    - `cd backend_api_python && python3 -m pytest tests/ -q`
  </verify>
</task>

<task type="auto">
  <name>Task 3: 行业中性化双套子结果 neutral_off / neutral_on</name>
  <read_first>
    - .planning/phases/22-cross-sectional-portfolio-backtest-engine/22-CONTEXT.md（D-12、D-13）
    - backend_api_python/app/services/cross_sectional_portfolio_backtest.py
    - backend_api_python/app/factors/normalize.py（若已有截面回归/残差工具则复用；否则在本服务内实现最小「按行业回归取残差」并单测固定数值）
  </read_first>
  <files>backend_api_python/app/services/cross_sectional_portfolio_backtest.py</files>
  <action>
    在服务层增加布尔参数（如 `industry_neutralize: bool`，默认 `False`）控制是否对**因子/用于排序的 scores** 在截面上做行业中性化（实现细节：按行业分组 OLS 残差或 CONTEXT 描述的回归残差；**不得**对未来 bar 使用未来行业标签）。  
    **单次成功 `run` 的返回结构**必须同时包含两套完整子对象（键名锁定）：`neutral_off` 与 `neutral_on`，每套至少包含：`equity_curve`（或与 plan 02 对齐的序列字段）、`summary`（标准指标子 dict）、`repro`（该套独有的配置子集，如 neutral 开关状态）。  
    若请求显式关闭对比（与 CONTEXT 冲突），**不允许**：须始终双套以满足 D-13。
  </action>
  <acceptance_criteria>
    - `grep -E "neutral_off|neutral_on" backend_api_python/app/services/cross_sectional_portfolio_backtest.py` 命中至少各 2 次（定义与序列化路径）
    - 服务返回顶层 JSON 可序列化对象中包含键 `neutral_off` 与 `neutral_on`
  </acceptance_criteria>
  <test_case_specs>
    <case id="22-01-03a">
      <what>默认与开启行业中性化均返回两套完整键</what>
      <input>同一请求；`industry_neutralize` 仅影响内部是否做残差变换，但响应始终含两套</input>
      <expected>`neutral_off["summary"]` 与 `neutral_on["summary"]` 均为 dict 且键集合相等；数值允许不同</expected>
    </case>
    <case id="22-01-03b">
      <what>中性化开启时 scores 经残差后排序与关闭时不同</what>
      <input>构造两行业、分数与行业相关的 toy panel</input>
      <expected>某一调仓日 `neutral_on` 与 `neutral_off` 的目标权重向量不相等（或排序 TopN 集合不同）</expected>
    </case>
  </test_case_specs>
  <verify>
    - `cd backend_api_python && python3 -m pytest tests/test_cross_sectional_portfolio_bt01.py -k "neutral" -q`
    - `cd backend_api_python && python3 -m pytest tests/ -q`
  </verify>
</task>

<task type="auto">
  <name>Task 4: 仅做多与权重校验策略（实现定稿 + 测试锁定）</name>
  <read_first>
    - .planning/phases/22-cross-sectional-portfolio-backtest-engine/22-CONTEXT.md（D-10、D-11）
    - backend_api_python/app/services/cross_sectional_portfolio_backtest.py
  </read_first>
  <files>backend_api_python/app/services/cross_sectional_portfolio_backtest.py</files>
  <action>
    实现 **Phase 22 仅做多**（D-11）：若 `weights` 中出现任意严格负值，契约失败并给出固定错误码/消息前缀。  
    对 `weights` 归一化策略在 CONTEXT「Claude's Discretion」中二选一并文档化：**(A)** 要求调用方已归一化且 `abs(sum-1) < 1e-6` 否则报错；**(B)** 引擎自动按 sum 归一化。选定后在代码注释与单测中断言。  
    不提供等权兜底（与 D-09 一致）。
  </action>
  <acceptance_criteria>
    - 存在对负 weight 的拒绝逻辑（grep `weight` 与 `long` 或 `< 0`）
    - `22-CONTEXT.md` 的 Discretion 在代码注释中写明所选归一化策略 A 或 B
  </acceptance_criteria>
  <test_case_specs>
    <case id="22-01-04a">
      <what>负权重拒绝</what>
      <input>指标返回任一 symbol weight = -0.1</input>
      <expected>抛出或返回错误；不生成权益曲线</expected>
    </case>
    <case id="22-01-04b">
      <what>归一化策略符合注释（A 或 B）</what>
      <input>若选 A：weights 和为 0.99；若选 B：未归一化但全正</input>
      <expected>选 A 时失败；选 B 时自动归一化后成功（与实现一致）</expected>
    </case>
  </test_case_specs>
  <verify>
    - `cd backend_api_python && python3 -m pytest tests/test_cross_sectional_portfolio_bt01.py -k "long_only or weight_norm" -q`
    - `cd backend_api_python && python3 -m pytest tests/ -q`
  </verify>
</task>

</tasks>

## PLANNING COMPLETE
