---
phase: 04-hardening-and-rollout-safety
plan: 02
type: execute
wave: 1
revision: 1
revision_notes: "Phase 4 Wave 2: bounded retry for schedule snapshot step inside orchestration; tests; operator boundaries doc; optional fixture extraction."
depends_on: []
files_modified:
  - backend_api_python/app/services/data_sufficiency_service.py
  - backend_api_python/tests/test_data_sufficiency_integration.py
  - backend_api_python/tests/test_data_sufficiency_service_retry.py (new)
  - .planning/phases/04-hardening-and-rollout-safety/04-OPERATOR-BOUNDARIES.md (new)
autonomous: true
requirements:
  - N2
  - N4
must_haves:
  truths:
    - "Retries apply only around `get_ibkr_schedule_snapshot` seam inside `evaluate_ibkr_data_sufficiency_and_log`; no TTL cache of snapshots (04-CONTEXT D-04)."
    - "After retry budget exhausted, exceptions propagate to `evaluate_ibkr_open_data_sufficiency` → synthetic insufficient (existing Phase 2 contract)."
    - "`04-OPERATOR-BOUNDARIES.md` documents residual false-block scenarios and references full pytest as gate."
---

<objective>
Deliver Phase 4 Wave 2: **bounded retries** for the schedule snapshot step per 04-CONTEXT **D-03**, automated tests proving retry + exhaustion behavior, **operator-facing boundaries** documentation (04-CONTEXT **D-07**), plus optional shared **test fixtures** extraction per ROADMAP carryover.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|-----------|-------------|
| TM-RETRY-01 | Infinite retry / hot loop stalls runner | medium | Fixed max attempts + bounded backoff + unit tests |
| TM-TEST-01 | Retry tests flaky on wall-clock | low | Inject sleep function / monkeypatch `time.sleep` |
| TM-LOG-02 | Retry logs leak exception bodies | low | Log only exception **type** + truncated message per Phase 2 R-04 caps |
</threat_model>

<context>
@.planning/phases/04-hardening-and-rollout-safety/04-CONTEXT.md
@.planning/phases/04-hardening-and-rollout-safety/04-RESEARCH.md
@backend_api_python/app/services/data_sufficiency_service.py
@backend_api_python/app/services/data_sufficiency_guard.py
@backend_api_python/tests/test_data_sufficiency_integration.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bounded retry wrapper — `get_ibkr_schedule_snapshot` inside orchestration</name>
  <files>backend_api_python/app/services/data_sufficiency_service.py</files>
  <read_first>
    backend_api_python/app/services/data_sufficiency_service.py
    backend_api_python/app/services/live_trading/ibkr_trading/ibkr_schedule_provider.py
    .planning/phases/04-hardening-and-rollout-safety/04-CONTEXT.md
  </read_first>
  <action>
    1. Wrap only the **`get_ibkr_schedule_snapshot(...)`** call inside `evaluate_ibkr_data_sufficiency_and_log` with explicit **max attempts** default **3** (Claude discretion: constants `SCHEDULE_SNAPSHOT_MAX_ATTEMPTS`, optional `SCHEDULE_SNAPSHOT_RETRY_SLEEP_SECONDS` default **0** for tests; production may keep 0 for latency).
    2. On intermediate failures: emit structured **`warning`** log `event=ibkr_schedule_snapshot_retry` with keys: `attempt` (1-based), `max_attempts`, `symbol`, `exc_type` (exception class name), **`error_summary`** via existing `truncate_evaluation_error_summary` or sibling helper — **never** raw stack.
    3. On **final failure**: **re-raise** last exception unchanged so **`evaluate_ibkr_open_data_sufficiency`** continues to map to **`DATA_EVALUATION_FAILED`** per Phase 2.
    4. **Do not** add TTL snapshot cache (**04-CONTEXT D-04**).
    5. Preserve behavior when snapshot succeeds on **first** attempt (no retry logs).
  </action>
  <use_case_spec>
    UC-04-02-01 **瞬时失败恢复**：若 `get_ibkr_schedule_snapshot` 前两次抛异常、第三次成功，则整次评估成功完成且 `ibkr_data_sufficiency_check` 照常发出恰好一次。
    UC-04-02-02 **耗尽失败**：若连续 N 次（默认 3）均失败，异常向上抛出；guard 路径得到 synthetic insufficient，`synthetic_evaluation_failure` 语义与 Phase 2 一致。
    UC-04-02-03 **可观测**：每次失败尝试产生一条 `ibkr_schedule_snapshot_retry`（或与文档等价），含 attempt/max_attempts/symbol。
    UC-04-02-04 **无缓存**：不得在内存或进程中保存「上一次成功的 snapshot」以供后续 tick 使用。
  </use_case_spec>
  <use_cases>
    - `retry_then_success`: UC-04-02-01
    - `retry_exhausted_raises`: UC-04-02-02
    - `retry_logs_present`: UC-04-02-03
    - `no_ttl_cache`: UC-04-02-04
  </use_cases>
  <verify>
    <automated>python3 -m compileall -q backend_api_python/app/services/data_sufficiency_service.py</automated>
    <automated>python3 -m pytest backend_api_python/tests -q</automated>
    <note>含**全量**套件；retry 专用测试可在 Task 2 新增文件中实现，但本任务合并后仍需全绿。</note>
  </verify>
  <acceptance_criteria>
    - Service module documents retry semantics in docstring
    - No snapshot caching across calls
    - 全量 pytest 绿
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Tests — schedule retry + integration smoke</name>
  <files>backend_api_python/tests/test_data_sufficiency_service_retry.py;backend_api_python/tests/test_data_sufficiency_integration.py</files>
  <read_first>
    backend_api_python/tests/test_ibkr_schedule_provider.py
    backend_api_python/tests/test_data_sufficiency_integration.py
  </read_first>
  <action>
    1. Add `test_data_sufficiency_service_retry.py` that patches **`get_ibkr_schedule_snapshot`** (import path **`app.services.data_sufficiency_service.get_ibkr_schedule_snapshot`**) to fail **k-1** times then succeed — assert **k** invocations and successful `DataSufficiencyResult`.
    2. Test always-fail path asserts exception propagates from `evaluate_ibkr_data_sufficiency_and_log` OR guard returns synthetic — choose **direct service call** test for propagate behavior.
    3. Optionally extend integration test file with one scenario referencing retry if coverage gap.
    4. Ensure caplog/fixtures capture `ibkr_schedule_snapshot_retry` when feasible.
  </action>
  <use_case_spec>
    UC-04-02-05 **Mock 可控**：patch 调度与异常类型可复现，测试不依赖真实 IBKR。
    UC-04-02-06 **与集成测试兼容**：新测不得破坏 `test_data_sufficiency_integration.py` 既有合约。
  </use_case_spec>
  <use_cases>
    - `patchable_retry_semantics`: UC-04-02-05
    - `integration_compatible`: UC-04-02-06
  </use_cases>
  <verify>
    <automated>python3 -m pytest backend_api_python/tests -q</automated>
    <note>**全量**后端测试 mandatory。</note>
  </verify>
  <acceptance_criteria>
    - New test module exists with ≥2 cases (recover + exhaust)
    - 全量 pytest 绿
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Operator boundaries document — false blocks & rollout</name>
  <files>.planning/phases/04-hardening-and-rollout-safety/04-OPERATOR-BOUNDARIES.md</files>
  <read_first>
    .planning/phases/04-hardening-and-rollout-safety/04-CONTEXT.md
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Write `04-OPERATOR-BOUNDARIES.md` covering:
    - When **false blocks** may still occur (data gaps, schedule evaluation failures after retry, kill-switch OFF meaning).
    - How to query logs using **`event`**, **`reason_code`**, **`exchange_id`**, **`event_lane`** (from Wave 1).
    - Env **`QUANTDINGER_IBKR_SUFFICIENCY_GUARD_ENABLED`** semantics and risk disclaimer.
    - Explicit statement: **production threshold tuning** may remain future work (**04-CONTEXT D-07**).
    Keep under ~120 lines; link to `.planning/ROADMAP.md` Phase 4 exit criteria.
  </action>
  <use_case_spec>
    UC-04-02-07 **可读性**：运维可在 5 分钟内理解何时属于「已知边界」而非 P1 bug。
    UC-04-02-08 **可追溯**：文档包含与本仓库日志字段一致的英文 key 名称。
  </use_case_spec>
  <use_cases>
    - `readable_ops_summary`: UC-04-02-07
    - `log_keys_match_code`: UC-04-02-08
  </use_cases>
  <verify>
    <automated>python3 -m pytest backend_api_python/tests -q</automated>
    <note>文档任务仍以**全量 pytest** 作为回归门禁（防止并行代码变更遗漏）；纯文档不改代码时套件应与基线一致。</note>
  </verify>
  <acceptance_criteria>
    - File exists at path above
    - Mentions retry + fail-safe + env flag
    - 全量 pytest 绿（未改代码则继承基线）
  </acceptance_criteria>
</task>

<task type="manual_optional">
  <name>Task 4 (optional): Shared kline/sufficiency test fixtures extraction</name>
  <files>backend_api_python/tests/fixtures/kline_sufficiency.py (optional)</files>
  <read_first>
    .planning/ROADMAP.md
    backend_api_python/tests/test_data_sufficiency_integration.py
    backend_api_python/tests/test_ibkr_open_guard_execution.py
  </read_first>
  <action>
    If duplication burden is high after Tasks 1–3, extract minimal shared helpers for **LOWER_LEVELS** / stub patterns into `tests/fixtures/` per ROADMAP carryover. Skip entirely if churn > value — note skip reason in `04-OPERATOR-BOUNDARIES.md` or PLAN summary.
  </action>
  <use_case_spec>
    UC-04-02-09 **可选**：若抽取则须保证原测试路径仍调用同一行为（无语义变化）。
  </use_case_spec>
  <verify>
    <automated>python3 -m pytest backend_api_python/tests -q</automated>
  </verify>
</task>

</tasks>
