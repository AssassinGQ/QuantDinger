---
phase: 04-hardening-and-rollout-safety
plan: 01
type: execute
wave: 1
revision: 1
revision_notes: "Phase 4 Wave 1: structured log dimensions for aggregation, deployment rollout env flags, typed schedule metadata constants per ROADMAP carryover."
depends_on: []
files_modified:
  - backend_api_python/app/services/data_sufficiency_logging.py
  - backend_api_python/app/services/data_sufficiency_service.py
  - backend_api_python/app/services/data_sufficiency_guard.py
  - backend_api_python/app/services/signal_executor.py
  - backend_api_python/app/services/live_trading/ibkr_trading/ibkr_schedule_provider.py
  - backend_api_python/tests/test_data_sufficiency_logging.py
autonomous: true
requirements:
  - N3
  - N4
must_haves:
  truths:
    - "`ibkr_data_sufficiency_check` payloads emitted from the live execution path include `exchange_id` and `strategy_id` when the caller supplies them (04-CONTEXT D-02)."
    - "Deployment-wide rollout flag defaults to guard/enforcement ON; disabling requires explicit env configuration and is documented as incident-only."
    - "`timezone_resolution` / `schedule_failure_reason` literals used in adapter + diagnostics draw from shared typed constants (ROADMAP Phase 4 carryover)."
  key_links:
    - from: "backend_api_python/app/services/signal_executor.py"
      to: "backend_api_python/app/services/data_sufficiency_guard.py"
      via: "`evaluate_ibkr_open_data_sufficiency(..., strategy_id=..., exchange_id=...)`"
      pattern: "evaluate_ibkr_open_data_sufficiency"
    - from: "backend_api_python/app/services/data_sufficiency_guard.py"
      to: "backend_api_python/app/services/data_sufficiency_service.py"
      via: "`evaluate_ibkr_data_sufficiency_and_log` optional context kwargs"
      pattern: "evaluate_ibkr_data_sufficiency_and_log\\("
---

<objective>
Deliver Phase 4 Wave 1: **log aggregation dimensions** for sufficiency outcomes, **typed schedule metadata constants**, and **deployment-level rollout/kill-switch** wiring — without changing Phase 2/3 behavioral contracts beyond additive logging fields.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation in this plan |
|----|--------|------------|-------------------------|
| TM-LOG-01 | Structured logs accidentally carry webhook secrets | low | Only add `exchange_id` / `strategy_id` / stable codes; never log notification URLs |
| TM-ENV-01 | Misconfigured env disables guard globally | medium | Default ON; document operator risk; consider logging one startup line when disabled |
| TM-CONST-01 | Typo in string constant breaks dashboards | low | Central StrEnum / Final constants + tests |
</threat_model>

<context>
@.planning/phases/04-hardening-and-rollout-safety/04-CONTEXT.md
@.planning/phases/04-hardening-and-rollout-safety/04-RESEARCH.md
@.planning/phases/04-hardening-and-rollout-safety/04-VALIDATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@backend_api_python/app/services/data_sufficiency_logging.py
@backend_api_python/app/services/data_sufficiency_service.py
@backend_api_python/app/services/data_sufficiency_guard.py
@backend_api_python/app/services/signal_executor.py
@backend_api_python/app/services/live_trading/ibkr_trading/ibkr_schedule_provider.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sufficiency evaluation logs — aggregation keys (exchange_id, strategy_id, event lane)</name>
  <files>backend_api_python/app/services/data_sufficiency_logging.py;backend_api_python/app/services/data_sufficiency_service.py;backend_api_python/app/services/data_sufficiency_guard.py;backend_api_python/app/services/signal_executor.py;backend_api_python/tests/test_data_sufficiency_logging.py</files>
  <read_first>
    .planning/phases/04-hardening-and-rollout-safety/04-CONTEXT.md
    backend_api_python/app/services/data_sufficiency_logging.py
    backend_api_python/app/services/signal_executor.py
  </read_first>
  <action>
    1. Extend `build_ibkr_data_sufficiency_check_payload` to accept optional `exchange_id: Optional[str]` and `strategy_id: Optional[int]` and include them in the dict when **not None** (keys exactly `exchange_id`, `strategy_id`). Add **`event_lane`** (string enum-like constant in logging module) with fixed value **`sufficiency_evaluation`** so log pipelines can filter evaluation vs enforcement vs user alert without relying solely on substring match of `event` name (04-CONTEXT D-02 “event kind”).
    2. Extend `evaluate_ibkr_data_sufficiency_and_log(...)` signature with optional `exchange_id=None`, `strategy_id=None` and pass into payload builder after classification.
    3. Extend `evaluate_ibkr_open_data_sufficiency` to accept optional `exchange_id`, `strategy_id` and forward to `evaluate_ibkr_data_sufficiency_and_log`.
    4. In `SignalExecutor` at the callsite of `evaluate_ibkr_open_data_sufficiency`, pass `strategy_id` and `exchange_id=str(ecfg.get("exchange_id") or "")` (non-empty when configured) so production logs join with `ibkr_open_blocked_insufficient_data` / `ibkr_insufficient_data_alert_sent` on shared dimensions.
    5. Update/adjust tests in `test_data_sufficiency_logging.py` (and executor tests if assertions on payload keys) so **`event_lane`**, `exchange_id`, `strategy_id` expectations are covered when provided.
  </action>
  <use_case_spec>
    UC-04-01-01 **Joinable logs**：当 `strategy_ctx` 含 `exchange_config.exchange_id` 且 `strategy_id` 已知时，同一次评估链路透传的 `ibkr_data_sufficiency_check` 记录必须带相同 `exchange_id` / `strategy_id`，以便与随后可能的 `ibkr_open_blocked_insufficient_data` 按维度关联。
    UC-04-01-02 **可选字段**：纯 Phase 1 单元测试路径若未传 `exchange_id`/`strategy_id`，payload 不得伪造默认值；字段可缺失或为 null（与实现约定一致），但不得破坏现有断言。
    UC-04-01-03 **event_lane**：每条 `ibkr_data_sufficiency_check` 结构化行必须带 `event_lane=sufficiency_evaluation`（或与实现命名等价且文档化），不得与 `ibkr_open_blocked_*` / `ibkr_insufficient_data_alert_sent` 混用同一 lane 值。
  </use_case_spec>
  <use_cases>
    - `joinable_keys_when_present`: UC-04-01-01
    - `optional_context_absent`: UC-04-01-02
    - `event_lane_distinct`: UC-04-01-03
  </use_cases>
  <verify>
    <automated>python3 -m compileall -q backend_api_python/app/services/data_sufficiency_logging.py backend_api_python/app/services/data_sufficiency_service.py backend_api_python/app/services/data_sufficiency_guard.py backend_api_python/app/services/signal_executor.py</automated>
    <automated>python3 -m pytest backend_api_python/tests -q</automated>
    <note>第二条为**全量**后端测试；任何失败不得合并。若仅改测试文件路径，仍需全量套件。</note>
  </verify>
  <acceptance_criteria>
    - `grep -n event_lane backend_api_python/app/services/data_sufficiency_logging.py` 非空且默认值为 sufficiency_evaluation 语义
    - `evaluate_ibkr_open_data_sufficiency` 签名包含 `strategy_id` / `exchange_id` 可选参数或同等 kwargs
    - `signal_executor.py` 调用处传入 exchange/strategy 上下文（当可得时）
    - 全量 pytest 绿
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Deployment rollout env — IBKR sufficiency guard master switch</name>
  <files>backend_api_python/app/services/signal_executor.py;backend_api_python/app/config/sufficiency_rollout.py (new if cleaner);backend_api_python/tests/test_signal_executor.py</files>
  <read_first>
    .planning/phases/04-hardening-and-rollout-safety/04-CONTEXT.md
    backend_api_python/app/services/signal_executor.py
  </read_first>
  <action>
    1. Introduce a single helper e.g. `is_ibkr_sufficiency_guard_enabled() -> bool` reading env **`QUANTDINGER_IBKR_SUFFICIENCY_GUARD_ENABLED`** with default **`true`** when unset or empty; interpret case-insensitive `false`/`0`/`no` as disabled (exact parsing is implementation detail — document in module docstring).
    2. At the IBKR sufficiency gate branch in `SignalExecutor.execute`, if disabled: **skip** sufficiency evaluation and **do not** block opens on sufficiency grounds for this milestone (document risk — incident-only); still allow Phase 3 alert path decisions to remain unchanged when guard ran — when skipped, user must not receive false insufficient alerts for skipped evaluations (i.e. guard skip should bypass both block and insufficient user alert). Implementation: early-continue the pre-existing guard block when disabled.
    3. Log **once per process** or **once per first hit** at `warning` level when guard is disabled with structured key `ibkr_sufficiency_guard_disabled` including `strategy_id` if available (bounded).
    4. Add pytest covering enabled default + disabled bypass using `monkeypatch.setenv`.
  </action>
  <use_case_spec>
    UC-04-01-04 **默认开启**：未设置 env 时 guard 行为与 Phase 2 完成后一致（仍评估并可能阻断）。
    UC-04-01-05 **显式关闭**：设置 env 为 false 时，IBKR live open/add **不再**因 sufficiency 被阻断（仅该闸；其它风控不变）。
    UC-04-01-06 **可观测**：disabled 路径必须产生可检索的 `ibkr_sufficiency_guard_disabled`（或文档等价事件名）日志，禁止静默关闭。
  </use_case_spec>
  <use_cases>
    - `default_enabled`: UC-04-01-04
    - `env_disables_guard`: UC-04-01-05
    - `disabled_is_visible`: UC-04-01-06
  </use_cases>
  <verify>
    <automated>python3 -m pytest backend_api_python/tests -q</automated>
    <note>**全量** `backend_api_python/tests`；执行前可对新建小模块 `compileall`（可选）。</note>
  </verify>
  <acceptance_criteria>
    - 模块/函数级单测或 executor 集成测覆盖 enabled/disabled
    - 默认无 env 时行为与现状一致（全量测保护）
    - 全量 pytest 绿
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Typed constants — timezone_resolution / schedule_failure_reason</name>
  <files>backend_api_python/app/services/schedule_metadata.py (new);backend_api_python/app/services/live_trading/ibkr_trading/ibkr_schedule_provider.py;backend_api_python/tests/test_ibkr_schedule_provider.py</files>
  <read_first>
    .planning/ROADMAP.md
    backend_api_python/app/services/live_trading/ibkr_trading/ibkr_schedule_provider.py
  </read_first>
  <action>
    1. Add module e.g. `schedule_metadata.py` exporting `Final[str]` or `StrEnum` members for literals currently free-string: at minimum **`timezone_id_unresolved`**, **`empty_or_unparsable_schedule`** (match existing outputs in `ibkr_schedule_provider.py`).
    2. Replace inline string assignments in `ibkr_schedule_provider.py` to reference constants; ensure JSON/log exports remain identical strings (no behavior change).
    3. Extend tests to assert constants match prior snapshot strings (grep or parametrized equality).
    4. Optional **ROADMAP** «timezone_trusted»: if adding a boolean to `IBKRScheduleSnapshot` is too large for Wave 1, capture as **defer** comment in PLAN summary — prefer constants-only in this task.
  </action>
  <use_case_spec>
    UC-04-01-07 **无静默漂移**：`timezone_resolution="fallback_utc"` 与 `schedule_failure_reason="timezone_id_unresolved"` 的取值必须引用同一常量源，禁止两处手写不同拼写。
    UC-04-01-08 **对外兼容**：序列化到日志/diagnostics 的字符串值与 Phase 1–3 既有测试期望字节级一致（除非测试同步更新）。
  </use_case_spec>
  <use_cases>
    - `single_source_strings`: UC-04-01-07
    - `backward_compatible_values`: UC-04-01-08
  </use_cases>
  <verify>
    <automated>python3 -m pytest backend_api_python/tests -q</automated>
    <note>全量套件 mandatory。</note>
  </verify>
  <acceptance_criteria>
    - `ibkr_schedule_provider.py` imports constants module
    - No unintended string drift in diagnostics
    - 全量 pytest 绿
  </acceptance_criteria>
</task>

</tasks>
