---
phase: 08-quantity-normalization-ib-alignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/tests/test_ibkr_align_qty.py
autonomous: true
requirements:
  - EXEC-04
user_setup: []
must_haves:
  truths:
    - "_align_qty_to_contract floors to sizeIncrement when IB returns ContractDetails."
    - "On reqContractDetailsAsync failure, original quantity is returned."
    - "Second call with same non-zero conId hits _lot_size_cache (single async fetch)."
  artifacts:
    - path: backend_api_python/tests/test_ibkr_align_qty.py
      provides: "UC-A1–UC-A5 for _align_qty_to_contract"
      contains: "_align_qty_to_contract"
  key_links:
    - from: "tests/test_ibkr_align_qty.py"
      to: "IBKRClient._align_qty_to_contract"
      via: "asyncio.run(client._align_qty_to_contract(...))"
      pattern: "_align_qty_to_contract"
---

<objective>
Add isolated unit tests for `IBKRClient._align_qty_to_contract` using `AsyncMock` for `reqContractDetailsAsync`, `types.SimpleNamespace` for contract details rows, non-zero `conId` for cache (e.g. 424242), and `IBKRClient._lot_size_cache.clear()` at the start of each test.

Purpose: Lock EXEC-04 alignment semantics independently of full `place_market_order` flows.
Output: New `test_ibkr_align_qty.py` (or equivalent name) with UC-A1–UC-A5.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-quantity-normalization-ib-alignment/08-CONTEXT.md
@.planning/phases/08-quantity-normalization-ib-alignment/08-RESEARCH.md

<interfaces>
From `backend_api_python/app/services/live_trading/ibkr_trading/client.py` (representative):
```python
async def _align_qty_to_contract(self, contract, quantity: float, symbol: str) -> float:
    ...
```

Shared test arrange (per RESEARCH): `client = IBKRClient.__new__(IBKRClient)`; `client._ib = MagicMock()`; `IBKRClient._lot_size_cache.clear()` first; `contract = types.SimpleNamespace(conId=424242)`; details elements use `types.SimpleNamespace(sizeIncrement=..., minSize=...)` not bare `MagicMock`. Execute with `asyncio.run(...)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UC-A1–UC-A5 tests for _align_qty_to_contract</name>
  <read_first>
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py (method `_align_qty_to_contract`, `_lot_size_cache`)
    - backend_api_python/tests/test_ibkr_client.py (pattern: `_lot_size_cache.clear`, `SimpleNamespace`, `reqContractDetailsAsync`)
  </read_first>
  <files>backend_api_python/tests/test_ibkr_align_qty.py</files>
  <action>
    Create `backend_api_python/tests/test_ibkr_align_qty.py` with tests discoverable by pytest (e.g. class `TestAlignQtyToContract` so `-k AlignQty` matches).

    **Shared arrange for every test function:** First line of arrange: `IBKRClient._lot_size_cache.clear()`. Build `client = IBKRClient.__new__(IBKRClient)`, `client._ib = MagicMock()`, `contract = types.SimpleNamespace(conId=424242)` (non-zero for cache per UC-A5). Use `AsyncMock` for `client._ib.reqContractDetailsAsync`. Run coroutine via `asyncio.run(client._align_qty_to_contract(contract, qty, "EURUSD"))`.

    Implement these use cases:

    - **UC-A1** — Mock `reqContractDetailsAsync` return `[SimpleNamespace(sizeIncrement=25000, minSize=1)]`. Input `qty=50000`. Expected return: `50000.0` (or `50000`). Assertions: aligned equals 50000; `floor(50000/25000)*25000 == 50000`.
    - **UC-A2** — Same mock (`sizeIncrement=25000`). Input `qty=30000`. Expected: `25000.0` (`floor(30000/25000)=1` → `1*25000`).
    - **UC-A3** — Mock return `[SimpleNamespace(sizeIncrement=1, minSize=1)]`. Input `qty=20000`. Expected: `20000.0`.
    - **UC-A4** — Set `reqContractDetailsAsync` `side_effect=RuntimeError("boom")`. Input `qty=20000`. Expected: `20000.0` (passthrough on failure). Optional: assert mock called once.
    - **UC-A5 (cache)** — Use `AsyncMock` with `return_value=[SimpleNamespace(sizeIncrement=25000, minSize=1)]`. Same `contract` object for two calls: first `_align_qty_to_contract(contract, 10000, "EURUSD")` → expect `0.0` (since `floor(10000/25000)*25000 = 0`); second `_align_qty_to_contract(contract, 15000, "EURUSD")` → expect `0.0` (`floor(15000/25000)=0`). **Primary assertion:** `reqContractDetailsAsync.call_count == 1` (cache hit on second call). Align with RESEARCH table UC-A5.
  </action>
  <acceptance_criteria>
    - `test -f backend_api_python/tests/test_ibkr_align_qty.py` succeeds.
    - `grep -n "_lot_size_cache.clear()" backend_api_python/tests/test_ibkr_align_qty.py` shows clear at start of each UC test or in `setup_method`.
    - `grep -n "SimpleNamespace(conId=424242)" backend_api_python/tests/test_ibkr_align_qty.py` matches (non-zero conId for cache).
    - `grep -n "SimpleNamespace(sizeIncrement" backend_api_python/tests/test_ibkr_align_qty.py` matches (no bare MagicMock as ContractDetails row).
    - `grep -n "call_count" backend_api_python/tests/test_ibkr_align_qty.py` matches UC-A5 cache assertion.
  </acceptance_criteria>
  <verify>
    <automated>
      cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest tests/test_ibkr_align_qty.py -k "AlignQty" -x -q --tb=line
    </automated>
    <automated>
      cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest tests/ -x -q --tb=line
    </automated>
  </verify>
  <done>UC-A1–UC-A5 are implemented; targeted `-k AlignQty` and full suite REGR-01 pass.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_ibkr_align_qty.py -k AlignQty` passes.
- Full `pytest tests/` passes.
</verification>

<success_criteria>
- All alignment behaviors UC-A1–UC-A5 are covered with stated inputs/outputs and cache isolation via `_lot_size_cache.clear()`.
</success_criteria>

<output>
After completion, create `.planning/phases/08-quantity-normalization-ib-alignment/08-02-SUMMARY.md`
</output>
