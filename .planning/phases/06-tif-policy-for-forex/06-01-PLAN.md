---
phase: 06-tif-policy-for-forex
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/app/services/live_trading/ibkr_trading/client.py
  - backend_api_python/tests/test_ibkr_client.py
autonomous: true
requirements: [EXEC-03]

must_haves:
  truths:
    - "_get_tif_for_signal(..., market_type=\"Forex\") returns IOC for every Forex signal type (open/add/close/reduce × long/short)"
    - "USStock and HShare TIF rules unchanged (open DAY, USStock close IOC, HShare close DAY)"
    - "Mocked place_market_order / place_limit_order with market_type=Forex pass tif=IOC into ib_insync orders"
    - "cd backend_api_python && python -m pytest tests/ -x -q --tb=line exits 0 (REGR-01)"
  artifacts:
    - path: "backend_api_python/app/services/live_trading/ibkr_trading/client.py"
      provides: "Forex early return IOC in _get_tif_for_signal"
      contains: 'market_type == "Forex"'
    - path: "backend_api_python/tests/test_ibkr_client.py"
      provides: "UC-T1–T8, UC-E1–E3, Forex integration tif assertions"
      contains: "_get_tif_for_signal"
  key_links:
    - from: "client.py::place_market_order"
      to: "client.py::_get_tif_for_signal"
      via: "tif = self._get_tif_for_signal(signal_type, market_type)"
      pattern: "_get_tif_for_signal\\(signal_type, market_type\\)"
    - from: "client.py::place_limit_order"
      to: "client.py::_get_tif_for_signal"
      via: "same as market order"
      pattern: "_get_tif_for_signal\\(signal_type, market_type\\)"
---

<objective>
Implement EXEC-03: add a Forex-only branch to `_get_tif_for_signal` so all Forex signals use `"IOC"`; keep USStock/HShare behavior unchanged; extend `test_ibkr_client.py` with UC-T1–T8, UC-E1–E3, Forex order-path checks, and full-suite regression REGR-01.

Purpose: Align TIF policy with locked decision (Forex → IOC everywhere) and IBKR MKT/IOC expectations; prevent accidental DAY rests for FX automation.

Output: Updated `client.py` docstring + method body; new/extended tests in `test_ibkr_client.py`.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-tif-policy-for-forex/06-CONTEXT.md
@.planning/phases/06-tif-policy-for-forex/06-RESEARCH.md
@.planning/phases/06-tif-policy-for-forex/06-VALIDATION.md

<interfaces>
From `backend_api_python/app/services/live_trading/ibkr_trading/client.py` (edit in place, signature unchanged):
```python
@staticmethod
def _get_tif_for_signal(signal_type: str, market_type: str = "USStock") -> str:
```

Existing call sites (no signature change):
- `place_market_order` ~1068–1069: `tif = self._get_tif_for_signal(signal_type, market_type)`
- `place_limit_order` ~1138–1139: `tif = self._get_tif_for_signal(signal_type, market_type)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production + tests — covers [UC-T1–T8] [UC-E1–E3] [REGR-01]</name>
  <files>backend_api_python/app/services/live_trading/ibkr_trading/client.py, backend_api_python/tests/test_ibkr_client.py</files>

  <read_first>
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py (lines 131–147: `_get_tif_for_signal`; lines 1059–1096: `place_market_order` TIF; lines 1129–1166: `place_limit_order` TIF)
    - backend_api_python/tests/test_ibkr_client.py (lines 646–665: `TestTifDay`; helpers `_make_client_with_mock_ib`, `_make_mock_ib_insync`, `_make_trade_mock` at top of file)
    - .planning/phases/06-tif-policy-for-forex/06-CONTEXT.md (UC-T1–T8, UC-E1–E3 specs; Forex → IOC lock-in)
  </read_first>

  <action>
    **Part A — Production (`client.py`)**

    1. At the **start** of `_get_tif_for_signal` (immediately after the docstring / before `is_close`), add:
       `if market_type == "Forex": return "IOC"`

    2. **Update the docstring** for `_get_tif_for_signal` to state explicitly:
       - For `market_type == "Forex"`, **all** signals return `"IOC"` (no open/close distinction).
       - For non-Forex, preserve existing behavior: non-close → `"DAY"`; `HShare` close → `"DAY"`; otherwise close → `"IOC"`.

    3. Do **not** change `place_market_order` / `place_limit_order` call sites or signatures.

    **Part B — Tests (`test_ibkr_client.py`)**

    1. **UC-T1–T8** — Add a new test class (e.g. `TestTifForexPolicy`) or parametrize within it. For **each** pair below, assert `IBKRClient._get_tif_for_signal(signal, "Forex") == "IOC"`:
       - UC-T1: `"open_long"`
       - UC-T2: `"close_long"`
       - UC-T3: `"open_short"`
       - UC-T4: `"close_short"`
       - UC-T5: `"add_long"`
       - UC-T6: `"add_short"`
       - UC-T7: `"reduce_long"`
       - UC-T8: `"reduce_short"`

    2. **UC-E1** — `assert IBKRClient._get_tif_for_signal("open_long", "USStock") == "DAY"`

    3. **UC-E2** — `assert IBKRClient._get_tif_for_signal("close_long", "USStock") == "IOC"`

    4. **UC-E3** — `assert IBKRClient._get_tif_for_signal("close_long", "HShare") == "DAY"`

    5. **Integration (Forex orders carry IOC)** — Add two tests (same `@patch` pattern as `TestTifDay`):
       - Call `place_market_order("EURUSD", "buy", 10000.0, "Forex", signal_type="open_long")` on `_make_client_with_mock_ib()` with `placeOrder` returning a trade mock; read `placed_order = client._ib.placeOrder.call_args[0][1]`; `assert placed_order.tif == "IOC"`.
       - Call `place_limit_order("EURUSD", "buy", 10000.0, 1.10, "Forex", signal_type="close_long")` similarly; assert `placed_order.tif == "IOC"`.

    6. **Adjust `TestTifDay` section header comment** if needed so it is accurate (e.g. clarify these tests are **USStock** open = DAY / limit DAY — not global “all TIF”).

    7. Ensure every new test method or parametrize **id** / docstring references the UC tag in a grep-friendly way (e.g. comment `# UC-T3` or `@pytest.mark.parametrize(..., ids=[...,"uc_t3",...])`).
  </action>

  <verify>
    <automated>cd backend_api_python &amp;&amp; python -m pytest tests/test_ibkr_client.py -k "TifForex or TifDay or _get_tif" -x -q --tb=line</automated>
    <automated>cd backend_api_python &amp;&amp; python -m pytest tests/ -x -q --tb=line</automated>
  </verify>

  <acceptance_criteria>
    - [UC-T1] `test_ibkr_client.py` contains `open_long` and `"Forex"` in a `_get_tif_for_signal` assertion expecting `"IOC"` (e.g. `grep -n 'open_long' backend_api_python/tests/test_ibkr_client.py` intersects Forex TIF test block)
    - [UC-T2] Same file asserts `close_long` + `"Forex"` → `"IOC"`
    - [UC-T3] Same file asserts `open_short` + `"Forex"` → `"IOC"`
    - [UC-T4] Same file asserts `close_short` + `"Forex"` → `"IOC"`
    - [UC-T5] Same file asserts `add_long` + `"Forex"` → `"IOC"`
    - [UC-T6] Same file asserts `add_short` + `"Forex"` → `"IOC"`
    - [UC-T7] Same file asserts `reduce_long` + `"Forex"` → `"IOC"`
    - [UC-T8] Same file asserts `reduce_short` + `"Forex"` → `"IOC"`
    - [UC-E1] Same file asserts `_get_tif_for_signal("open_long", "USStock") == "DAY"`
    - [UC-E2] Same file asserts `_get_tif_for_signal("close_long", "USStock") == "IOC"`
    - [UC-E3] Same file asserts `_get_tif_for_signal("close_long", "HShare") == "DAY"`
    - [REGR-01] `cd backend_api_python &amp;&amp; python -m pytest tests/ -x -q --tb=line` exits 0
    - [REGR-01] `grep -n 'market_type == "Forex"' backend_api_python/app/services/live_trading/ibkr_trading/client.py` exits 0
    - [REGR-01] `grep -n '_get_tif_for_signal' backend_api_python/app/services/live_trading/ibkr_trading/client.py` shows unchanged call sites at place_market_order and place_limit_order
  </acceptance_criteria>

  <done>
    Forex TIF policy implemented; all UC tags and REGR-01 verifiable; USStock/HShare tests in `TestTifDay` still pass.
  </done>
</task>

</tasks>

<verification>
1. `cd backend_api_python && python -m pytest tests/test_ibkr_client.py -k "TifForex or TifDay" -x -q --tb=line`
2. `cd backend_api_python && python -m pytest tests/ -x -q --tb=line` (REGR-01)
3. Grep checks in Task 1 `acceptance_criteria` for `market_type == "Forex"` and UC assertions.
</verification>

<success_criteria>
- EXEC-03 satisfied: documented Forex branch returning IOC; equity paths unchanged.
- REGR-01: full backend test suite passes.
- No new Python dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tif-policy-for-forex/06-01-SUMMARY.md` per execute-plan template.
</output>
