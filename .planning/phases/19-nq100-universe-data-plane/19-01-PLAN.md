---
phase: 19-nq100-universe-data-plane
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/migrations/0055_qd_nq100_universe.sql
  - backend_api_python/migrations/init.sql
  - backend_api_python/app/services/universe_nq100_service.py
  - backend_api_python/tests/test_universe_pit_query.py
autonomous: true
requirements:
  - UNIV-03
user_setup: []
must_haves:
  truths:
    - "PostgreSQL has qd_nq100_membership and qd_nq100_change_events (or equivalently named qd_* tables) with provenance columns source and scraped_at"
    - "Python can return the set of NQ100 symbols valid on a calendar date using BETWEEN valid_from AND COALESCE(valid_to, infinity) semantics"
  artifacts:
    - path: "backend_api_python/migrations/0055_qd_nq100_universe.sql"
      provides: "Idempotent DDL for universe tables and indexes"
      contains: "CREATE TABLE IF NOT EXISTS qd_nq100_membership"
    - path: "backend_api_python/app/services/universe_nq100_service.py"
      provides: "PIT query function(s) using get_db_connection"
      min_lines: 40
    - path: "backend_api_python/tests/test_universe_pit_query.py"
      provides: "Automated PIT behavior tests with mocked DB"
      contains: "def test_"
  key_links:
    - from: "universe_nq100_service.get_constituents_as_of"
      to: "qd_nq100_membership"
      via: "SQL WHERE as_of BETWEEN valid_from AND COALESCE(valid_to, 'infinity'::date)"
      pattern: "valid_from|valid_to"
---

<objective>
Add PostgreSQL schema for NQ100 point-in-time membership (validity intervals + change events with provenance) and implement read-side service functions plus regression tests.

Purpose: UNIV-03 requires historical membership, not “today only”; this plan establishes the durable model and query contract before ingest and APIs.

Output: Migration `0055_qd_nq100_universe.sql`, matching `init.sql` section, `universe_nq100_service.py` with PIT reads, and `test_universe_pit_query.py`.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-nq100-universe-data-plane/19-CONTEXT.md
@.planning/phases/19-nq100-universe-data-plane/19-RESEARCH.md

**Concrete schema targets (executor must implement exactly these names unless a single rename is documented in the task action):**

- Table `qd_nq100_membership`: columns `id SERIAL PRIMARY KEY`, `symbol VARCHAR(32) NOT NULL`, `valid_from DATE NOT NULL`, `valid_to DATE` (NULL = still member), `source VARCHAR(64) NOT NULL`, `scraped_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`; index for PIT: e.g. btree on `(valid_from)` plus composite or expression suitable for `date BETWEEN valid_from AND COALESCE(valid_to, 'infinity'::date)`.
- Table `qd_nq100_change_events`: `id SERIAL PRIMARY KEY`, `symbol VARCHAR(32) NOT NULL`, `event_type VARCHAR(16) NOT NULL` with CHECK in `('add','remove')`, `effective_date DATE NOT NULL`, `source VARCHAR(64) NOT NULL`, `scraped_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`; index on `(effective_date)`, `(symbol)`.

**Service API:** Export at minimum `get_constituents_as_of(as_of_date: date) -> list[str]` (or `set[str]` if documented) using `app.utils.db.get_db_connection()` context manager pattern consistent with other services.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add migration 0055 and sync init.sql</name>
  <read_first>
    - backend_api_python/migrations/0054_add_pending_orders_remaining.sql (style, IF NOT EXISTS patterns)
    - backend_api_python/migrations/init.sql (search for `qd_` table examples and append new section near end)
    - .planning/phases/19-nq100-universe-data-plane/19-CONTEXT.md (PIT two-table decision)
  </read_first>
  <files>backend_api_python/migrations/0055_qd_nq100_universe.sql, backend_api_python/migrations/init.sql</files>
  <action>
    Create `backend_api_python/migrations/0055_qd_nq100_universe.sql` containing idempotent `CREATE TABLE IF NOT EXISTS` for `qd_nq100_membership` and `qd_nq100_change_events` exactly as specified in this plan’s context block, including `CHECK (event_type IN ('add','remove'))` on the events table and appropriate `CREATE INDEX IF NOT EXISTS` for PIT queries and event lookups.

    Append the **same** DDL (comment header: `-- NQ100 universe PIT`) to `backend_api_python/migrations/init.sql` so fresh Docker DBs get the tables without relying only on the numbered migration file.

    Do not introduce SQLAlchemy models; remain native SQL per repo convention.
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - File `backend_api_python/migrations/0055_qd_nq100_universe.sql` exists and contains literal substrings `qd_nq100_membership`, `qd_nq100_change_events`, `valid_from`, `valid_to`, `scraped_at`, `event_type`.
    - File `backend_api_python/migrations/init.sql` contains the same two table names in the appended section.
    - **Test specification (Task 1):** No new test functions are required for DDL-only work; regression gate is full suite only.
    - **Regression:** `cd backend_api_python && pytest tests/ -q` exits 0 (baseline v1.1: 1060 tests; after this task the count must remain 0 failures — if new tests land in the same commit wave, total count increases accordingly).
  </acceptance_criteria>
  <done>Migration file and init.sql contain matching NQ100 universe DDL; grep confirms table and column names.</done>
</task>

<task type="auto">
  <name>Task 2: PIT read service + unit tests</name>
  <read_first>
    - backend_api_python/migrations/0055_qd_nq100_universe.sql
    - backend_api_python/app/utils/db.py
    - backend_api_python/app/services/kline.py (limit 80 lines: get_db_connection usage pattern)
    - backend_api_python/tests/conftest.py (fixtures: make_db_ctx or connection mocks if present)
  </read_first>
  <files>backend_api_python/app/services/universe_nq100_service.py, backend_api_python/tests/test_universe_pit_query.py</files>
  <action>
    Implement `backend_api_python/app/services/universe_nq100_service.py` with:

    - `get_constituents_as_of(as_of_date)` executing SQL against `qd_nq100_membership` where `as_of_date` is within `[valid_from, COALESCE(valid_to, 'infinity'::date)]` (use explicit `date` casting consistent with PostgreSQL), returning sorted unique symbols as `list[str]` uppercase.

    - Helper for tests only (same module or test-local): **no live DB required in CI** — use `unittest.mock` to patch `app.utils.db.get_db_connection` so the cursor returns controlled rows (pattern from other tests using `MagicMock`).

    Create `backend_api_python/tests/test_universe_pit_query.py` with:

    - `test_get_constituents_as_of_includes_symbol_when_date_inside_closed_interval` — mock rows: AAPL valid 2020-01-01..2020-12-31; as_of 2020-06-01 expects `AAPL` in result.

    - `test_get_constituents_as_of_includes_open_ended_membership` — valid_to NULL; as_of 2030-01-01 still includes symbol.

    - `test_get_constituents_as_of_excludes_after_valid_to` — valid_to 2021-12-31; as_of 2022-01-01 excludes symbol.

    - `test_get_constituents_as_of_empty_when_no_rows` — no matching rows returns empty list.

    Each test must call the real `get_constituents_as_of` with patched DB, not skip the service layer.
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/test_universe_pit_query.py -q</automated>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def get_constituents_as_of" backend_api_python/app/services/universe_nq100_service.py` returns at least one match.
    - `test_universe_pit_query.py` contains exactly these four functions: `test_get_constituents_as_of_includes_symbol_when_date_inside_closed_interval`, `test_get_constituents_as_of_includes_open_ended_membership`, `test_get_constituents_as_of_excludes_after_valid_to`, `test_get_constituents_as_of_empty_when_no_rows`.
    - Running `pytest tests/test_universe_pit_query.py -q` exits 0.
    - **Regression:** `cd backend_api_python && pytest tests/ -q` exits 0 with 0 failures.
  </acceptance_criteria>
  <done>PIT query logic and four mocked-DB tests pass; full suite green.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_universe_pit_query.py -q` and `pytest tests/ -q` both pass.
- Schema files grep-verify table names.
</verification>

<success_criteria>
- UNIV-03 read path exists with automated tests proving interval semantics.
- Provenance columns exist on both tables for audit (source, scraped_at).
</success_criteria>

<output>
After completion, create `.planning/phases/19-nq100-universe-data-plane/19-01-SUMMARY.md`
</output>
