---
phase: 19-nq100-universe-data-plane
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/migrations/0056_qd_nq100_index_eiv_and_ic_raw.sql
  - backend_api_python/migrations/init.sql
  - backend_api_python/app/services/nq100_csv_ingest_service.py
  - backend_api_python/tests/test_nq100_universe_ingest.py
autonomous: true
requirements:
  - UNIV-01
  - UNIV-03
user_setup: []
must_haves:
  truths:
    - "系统可从 scripts/NDX_IC.csv 解析并入库日级成分股，支持按 symbol+date 的幂等重复导入"
    - "系统可从 scripts/NDX_EIV.csv 解析并入库每日 eod_index_value 基准序列，按 trade_date 幂等 upsert"
    - "IC CSV 字段不丢失：除结构化核心字段外，原始行保真入库（raw_payload/raw_row）用于审计追溯"
  artifacts:
    - path: "backend_api_python/migrations/0056_qd_nq100_index_eiv_and_ic_raw.sql"
      provides: "EIV 基准序列表 + IC 原始字段保真能力"
      contains: "qd_nq100_index_eiv"
    - path: "backend_api_python/app/services/nq100_csv_ingest_service.py"
      provides: "CSV 解析与幂等导入服务"
      min_lines: 120
    - path: "backend_api_python/tests/test_nq100_universe_ingest.py"
      provides: "CSV 入库映射、幂等与字段保真测试"
      contains: "test_import_"
  key_links:
    - from: "nq100_csv_ingest_service.import_ic_csv"
      to: "qd_nq100_membership / qd_nq100_ic_raw"
      via: "symbol+date 幂等 upsert + raw_payload 落库"
      pattern: "ON CONFLICT|raw_payload|component_symbol"
    - from: "nq100_csv_ingest_service.import_eiv_csv"
      to: "qd_nq100_index_eiv"
      via: "trade_date 主键 upsert eod_index_value"
      pattern: "eod_index_value|trade_date|ON CONFLICT"
---

<objective>
将用户已下载的 `scripts/NDX_IC.csv` 与 `scripts/NDX_EIV.csv` 转换并入库，形成可审计、可 PIT 查询、可重复导入不重复写入的数据平面基础。

Purpose: Phase 19 交付目标是“CSV -> DB”，不是交付 CSV 文件本身；本计划实现 IC/EIV 的字段映射、字段保真与幂等入库契约。

Output: `0056` migration、CSV 入库服务 `nq100_csv_ingest_service.py`、以及覆盖字段映射/幂等/保真的测试集。
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-nq100-universe-data-plane/19-CONTEXT.md
@.planning/phases/19-nq100-universe-data-plane/19-01-PLAN.md

**输入文件（已存在，执行时必须直接使用）：**
- `scripts/NDX_IC.csv`
- `scripts/NDX_EIV.csv`

**数据库规格（本计划锁定）：**
- 新增表 `qd_nq100_index_eiv`：
  - `trade_date DATE PRIMARY KEY`
  - `index_symbol VARCHAR(32) NOT NULL`
  - `eod_index_value NUMERIC(20,6) NOT NULL`
  - `source VARCHAR(64) NOT NULL DEFAULT 'NDX_EIV.csv'`
  - `raw_payload JSONB NOT NULL`（保真原始行）
  - `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`
  - `updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`
- 对 IC 数据采用“结构化 + 原始保真”策略：
  - 在 `qd_nq100_membership` 继续维护 PIT 所需结构化字段（至少 symbol/date/source/scraped_at）
  - 增加原始保真容器（推荐新表 `qd_nq100_ic_raw`，或在现有表新增 `raw_payload JSONB`；二选一但必须实现）
  - 目标：CSV 所有字段均可从 DB 恢复，不丢列

**幂等规则（本计划锁定）：**
- IC：按 `index_symbol + date + component_symbol` 去重写入；重复导入不新增重复行。
- EIV：按 `trade_date` upsert，重复导入仅更新同日记录，不新增新行。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 EIV/IC 保真落库 migration 并同步 init.sql</name>
  <read_first>
    - backend_api_python/migrations/0055_qd_nq100_universe.sql
    - backend_api_python/migrations/init.sql
    - .planning/phases/19-nq100-universe-data-plane/19-CONTEXT.md
  </read_first>
  <files>backend_api_python/migrations/0056_qd_nq100_index_eiv_and_ic_raw.sql, backend_api_python/migrations/init.sql, backend_api_python/tests/test_nq100_universe_ingest.py</files>
  <action>
    创建 `0056_qd_nq100_index_eiv_and_ic_raw.sql`，实现：
    - `qd_nq100_index_eiv`（字段按 context 锁定规格）
    - IC 原始保真方案（`qd_nq100_ic_raw` 或 `qd_nq100_membership.raw_payload JSONB`）
    - 对应唯一键/索引，支撑后续幂等导入。

    在 `init.sql` 追加同等 DDL，确保新环境初始化即具备表结构。

    在 `tests/test_nq100_universe_ingest.py` 增加 DDL 合规性测试（纯文本断言，无需真实 DB）：
    - `test_migration_0056_contains_qd_nq100_index_eiv_schema`
    - `test_migration_0056_contains_ic_raw_preservation_schema`

    断言点必须包含：表名、主键/唯一键、`raw_payload`、`eod_index_value`、`trade_date`。
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/test_nq100_universe_ingest.py::test_migration_0056_contains_qd_nq100_index_eiv_schema tests/test_nq100_universe_ingest.py::test_migration_0056_contains_ic_raw_preservation_schema -q</automated>
    <automated>cd backend_api_python && pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `0056_qd_nq100_index_eiv_and_ic_raw.sql` 存在并包含 `qd_nq100_index_eiv` 与 IC 原始保真设计（`qd_nq100_ic_raw` 或 `raw_payload` 列）。
    - `init.sql` 已同步上述 DDL。
    - **测试函数名与规格（Task 1）**
      - `test_migration_0056_contains_qd_nq100_index_eiv_schema`：场景=读取 migration 文本；断言=存在 `trade_date`、`eod_index_value`、`raw_payload`、主键/唯一键。
      - `test_migration_0056_contains_ic_raw_preservation_schema`：场景=读取 migration 文本；断言=IC 字段保真容器存在（表或列）且有 symbol/date 相关索引或唯一约束。
    - **回归门禁：** `cd backend_api_python && pytest tests/ -q` 必须通过。
  </acceptance_criteria>
  <done>数据库规格落地并可被自动化测试验证，满足 EIV 必需入库与 IC 字段保真前置条件。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 IC/EIV CSV 解析 + 幂等 upsert + 字段保真导入服务</name>
  <read_first>
    - backend_api_python/app/services/universe_nq100_service.py
    - backend_api_python/app/utils/db.py
    - scripts/NDX_IC.csv
    - scripts/NDX_EIV.csv
  </read_first>
  <files>backend_api_python/app/services/nq100_csv_ingest_service.py, backend_api_python/tests/test_nq100_universe_ingest.py</files>
  <action>
    新增 `backend_api_python/app/services/nq100_csv_ingest_service.py`，至少导出：
    - `import_ic_csv(csv_path: str) -> dict`
    - `import_eiv_csv(csv_path: str) -> dict`
    - `import_nq100_csv_bundle(ic_path: str, eiv_path: str) -> dict`

    实现要求：
    - 从 `NDX_IC.csv` 提取成分股日级记录并写入 PIT 结构，键为 `index_symbol + date + component_symbol`，重复导入不重复写入。
    - 从 `NDX_EIV.csv` 提取 `eod_index_value` 并写入 `qd_nq100_index_eiv`，按 `trade_date` upsert。
    - IC/EIV 每一行原始字段完整保真到 `raw_payload`（jsonb）。
    - 返回摘要中必须包含：`ic_rows_read`、`ic_rows_inserted`、`ic_rows_upserted_or_skipped`、`eiv_rows_read`、`eiv_rows_upserted`。

    扩展 `tests/test_nq100_universe_ingest.py`（使用 mock DB，不依赖真实 PostgreSQL）：
    - `test_import_ic_csv_preserves_all_source_fields_in_raw_payload`
      - 场景：构造含额外字段的 IC 行。
      - 断言：写入参数中 `raw_payload` 包含原始全部字段键值（逐键断言）。
    - `test_import_eiv_csv_writes_eod_index_value_and_trade_date`
      - 场景：导入最小 EIV 样本。
      - 断言：SQL 参数含 `trade_date` 与 `eod_index_value`，值精确匹配。
    - `test_import_bundle_is_idempotent_for_duplicate_runs`
      - 场景：同一 IC+EIV 输入执行两次。
      - 断言：第二次 `inserted` 不增长，`upserted_or_skipped` 增长，且无异常。
    - `test_import_ic_csv_symbol_date_key_prevents_duplicates`
      - 场景：CSV 内部重复 `(index_symbol,date,component_symbol)` 行。
      - 断言：最终只保留一条有效写入（通过 execute call 或返回计数断言）。
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/test_nq100_universe_ingest.py::test_import_ic_csv_preserves_all_source_fields_in_raw_payload tests/test_nq100_universe_ingest.py::test_import_eiv_csv_writes_eod_index_value_and_trade_date tests/test_nq100_universe_ingest.py::test_import_bundle_is_idempotent_for_duplicate_runs tests/test_nq100_universe_ingest.py::test_import_ic_csv_symbol_date_key_prevents_duplicates -q</automated>
    <automated>cd backend_api_python && pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `nq100_csv_ingest_service.py` 包含 `import_ic_csv`、`import_eiv_csv`、`import_nq100_csv_bundle`。
    - **测试函数名与规格（Task 2）**
      - `test_import_ic_csv_preserves_all_source_fields_in_raw_payload`：断言不丢字段（raw_payload 全字段保真）。
      - `test_import_eiv_csv_writes_eod_index_value_and_trade_date`：断言 EIV 核心字段准确落库。
      - `test_import_bundle_is_idempotent_for_duplicate_runs`：断言重复导入幂等。
      - `test_import_ic_csv_symbol_date_key_prevents_duplicates`：断言符号+日期键去重有效。
    - **回归门禁：** `cd backend_api_python && pytest tests/ -q` 必须通过。
  </acceptance_criteria>
  <done>CSV 入库主路径可执行，满足“成分股 + EIV 基准序列 + 字段保真 + 幂等”核心目标。</done>
</task>

</tasks>

<verification>
- `pytest tests/test_nq100_universe_ingest.py -q` passes.
- `pytest tests/ -q` passes.
</verification>

<success_criteria>
- UNIV-03（EIV 日级基准）与 CSV->DB 交付目标满足。
- IC 原始字段保真与导入幂等具备自动化测试证据。
</success_criteria>

<output>
After completion, create `.planning/phases/19-nq100-universe-data-plane/19-02-SUMMARY.md`
</output>
