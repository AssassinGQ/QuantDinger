---
phase: 19-nq100-universe-data-plane
plan: 03
type: execute
wave: 2
depends_on:
  - "19-01"
  - "19-02"
files_modified:
  - backend_api_python/app/services/nq100_csv_ingest_service.py
  - backend_api_python/app/services/universe_nq100_service.py
  - backend_api_python/tests/test_universe_sync_db.py
  - backend_api_python/app/tasks/nq100_universe_sync.py
  - backend_api_python/app/tasks/__init__.py
  - backend_api_python/app/routes/universe.py
  - backend_api_python/app/routes/__init__.py
  - backend_api_python/tests/test_nq100_universe_sync_plugin.py
  - backend_api_python/tests/test_universe_routes.py
  - backend_api_python/tests/test_tasks_registry.py
autonomous: true
requirements:
  - UNIV-02
  - UNIV-03
user_setup: []
must_haves:
  truths:
    - "APScheduler 每周注册并执行 NQ100 CSV 入库刷新任务，主流程依赖 IC/EIV CSV，不依赖外网"
    - "GET /api/universe/nq100?date=YYYY-MM-DD 可返回 PIT 成分股；GET /api/universe/nq100/eiv?date=YYYY-MM-DD 返回当日 eod_index_value"
    - "POST /api/universe/nq100/refresh 可手动触发 CSV 入库并返回摘要"
    - "QQQ 权重补充失败只记录日志 warning/error，不阻塞 IC/EIV 主流程成功"
  artifacts:
    - path: "backend_api_python/app/services/nq100_csv_ingest_service.py"
      provides: "QQQ best-effort 补充与主流程解耦"
      contains: "try_enrich_weights_from_qqq"
    - path: "backend_api_python/app/tasks/nq100_universe_sync.py"
      provides: "Plugin task exports JOB_ID, INTERVAL_MINUTES, ENABLED, run"
    - path: "backend_api_python/app/routes/universe.py"
      provides: "GET/POST universe endpoints + EIV query endpoint"
      contains: "universe_bp"
  key_links:
    - from: "nq100_universe_sync.run"
      to: "universe_nq100_service.sync_nq100_from_csv"
      via: "direct import and call"
    - from: "universe_bp /nq100 and /nq100/eiv"
      to: "get_constituents_as_of / get_eiv_as_of / sync"
      via: "Flask route handlers"
---

<objective>
在 19-02 CSV 入库能力之上，完成运行面（任务编排 + API）：定时刷新、手动刷新、PIT 成分查询、EIV 查询，以及 QQQ 权重 best-effort 行为。

Purpose: Phase 19 的可用性交付不是“能解析 CSV”而是“可被任务和 API 调用的数据平面”。

Output: 编排服务 `sync_nq100_from_csv`、任务插件、路由端点、以及覆盖主流程与非阻塞失败分支的测试。
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend_api_python/app/tasks/kline_sync.py
@backend_api_python/app/tasks/__init__.py
@backend_api_python/app/routes/__init__.py
@backend_api_python/tests/test_tasks_registry.py
@.planning/phases/19-nq100-universe-data-plane/19-CONTEXT.md

**数据源锁定（必须遵守）：**
- 主流程输入：`scripts/NDX_IC.csv` + `scripts/NDX_EIV.csv`
- QQQ 仅用于权重补充；失败只日志，不可 raise 到主流程失败。

**API 契约（本计划锁定）：**
- `GET /api/universe/nq100?date=YYYY-MM-DD`：返回 PIT 成分列表
- `GET /api/universe/nq100/eiv?date=YYYY-MM-DD`：返回当日 `eod_index_value`
- `POST /api/universe/nq100/refresh`：触发 CSV 入库同步并返回摘要

**任务契约（本计划锁定）：**
- `JOB_ID = "task_nq100_universe_sync"`
- `INTERVAL_MINUTES = 10080`（每周）
- `run()` 调用 `sync_nq100_from_csv(ic_csv_path, eiv_csv_path)`，路径默认指向 `scripts/NDX_IC.csv` 与 `scripts/NDX_EIV.csv`
</context>

<tasks>

<task type="auto">
  <name>Task 1: 编排 CSV 同步服务并实现 QQQ 失败非阻塞</name>
  <read_first>
    - backend_api_python/app/services/nq100_csv_ingest_service.py
    - backend_api_python/app/services/universe_nq100_service.py
    - backend_api_python/app/utils/db.py
    - scripts/qqq_holdings.csv
  </read_first>
  <files>backend_api_python/app/services/nq100_csv_ingest_service.py, backend_api_python/app/services/universe_nq100_service.py, backend_api_python/tests/test_universe_sync_db.py</files>
  <action>
    在 `universe_nq100_service.py` 增加 `sync_nq100_from_csv(ic_csv_path: str, eiv_csv_path: str) -> dict`：
    - 调用 19-02 的 `import_nq100_csv_bundle` 完成主流程入库。
    - 调用 `try_enrich_weights_from_qqq(...)` 做权重补充（best-effort）。
    - 若 QQQ 补充失败：仅记录 warning/error 日志，返回摘要字段 `qqq_weight_enrich_status="failed_non_blocking"`，但整体 `success` 保持 true。

    在 `nq100_csv_ingest_service.py` 增加或完善 `try_enrich_weights_from_qqq`：
    - 输入可选 `scripts/qqq_holdings.csv`
    - 任意解析/读取失败均捕获并日志告警，不抛出阻塞异常。

    增加/更新 `tests/test_universe_sync_db.py`：
    - `test_sync_nq100_from_csv_calls_ic_and_eiv_importers`
      - 场景：mock IC/EIV 导入函数。
      - 断言：两者都被调用，返回摘要包含导入计数字段。
    - `test_sync_nq100_from_csv_qqq_failure_logs_warning_but_returns_success`
      - 场景：mock `try_enrich_weights_from_qqq` 抛异常。
      - 断言：logger.warning/error 被调用；返回 `success=True` 且 `qqq_weight_enrich_status=="failed_non_blocking"`。
    - `test_sync_nq100_from_csv_is_idempotent_for_duplicate_runs`
      - 场景：同一 CSV 路径连续触发两次。
      - 断言：第二次返回摘要中无新增主记录（依据 mocked importer 返回值）且无异常。
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/test_universe_sync_db.py::test_sync_nq100_from_csv_calls_ic_and_eiv_importers tests/test_universe_sync_db.py::test_sync_nq100_from_csv_qqq_failure_logs_warning_but_returns_success tests/test_universe_sync_db.py::test_sync_nq100_from_csv_is_idempotent_for_duplicate_runs -q</automated>
    <automated>cd backend_api_python && pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `universe_nq100_service.py` 包含 `sync_nq100_from_csv`。
    - `nq100_csv_ingest_service.py` 包含 `try_enrich_weights_from_qqq` 且对异常进行捕获日志。
    - **测试函数名与规格（Task 1）**
      - `test_sync_nq100_from_csv_calls_ic_and_eiv_importers`：断言主流程导入调用完整。
      - `test_sync_nq100_from_csv_qqq_failure_logs_warning_but_returns_success`：断言 QQQ 失败仅日志且不阻塞。
      - `test_sync_nq100_from_csv_is_idempotent_for_duplicate_runs`：断言重复执行幂等。
    - **回归门禁：** `cd backend_api_python && pytest tests/ -q` 必须通过。
  </acceptance_criteria>
  <done>任务编排主流程可用，且明确满足“QQQ 失败不阻塞”。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 APScheduler 插件注册与调度测试</name>
  <read_first>
    - backend_api_python/app/tasks/kline_sync.py
    - backend_api_python/app/tasks/__init__.py
    - backend_api_python/tests/test_tasks_registry.py
  </read_first>
  <files>backend_api_python/app/tasks/nq100_universe_sync.py, backend_api_python/app/tasks/__init__.py, backend_api_python/tests/test_nq100_universe_sync_plugin.py, backend_api_python/tests/test_tasks_registry.py</files>
  <action>
    创建/更新 `backend_api_python/app/tasks/nq100_universe_sync.py`：
    - 常量：`JOB_ID = "task_nq100_universe_sync"`、`INTERVAL_MINUTES = 10080`、`ENABLED = True`
    - `run()` 调用 `sync_nq100_from_csv("scripts/NDX_IC.csv", "scripts/NDX_EIV.csv")`（路径可配置但默认必须指向这两份 CSV）
    - 日志包含开始/结束与摘要。

    Update `register_all_tasks()` in `app/tasks/__init__.py` to register `nq100_universe_sync` when `ENABLED`, same pattern as `kline_sync` (`run_immediately=False`).

    Add `tests/test_nq100_universe_sync_plugin.py`:
    - `test_module_exports_plugin_contract` — asserts JOB_ID, INTERVAL_MINUTES (equals 10080), ENABLED, run exist.
    - `test_run_invokes_sync_nq100_from_csv_with_expected_default_paths` — patch `app.services.universe_nq100_service.sync_nq100_from_csv`; call `run()`; assert called once with `NDX_IC.csv` 与 `NDX_EIV.csv` 路径。

    Update `tests/test_tasks_registry.py` with explicit test function:
    - `test_register_all_tasks_includes_nq100_universe_sync_when_enabled`
    - 场景：patch `app.tasks.nq100_universe_sync.ENABLED` True alongside kline_sync（或同时 patch 两者）。
    - 断言：`task_nq100_universe_sync` appears in registered job_ids from `mock_reg.call_args_list`.

    Preserve existing kline_sync tests behavior when nq100 ENABLED is False in those tests — adjust patches to set both modules’ ENABLED explicitly so tests remain deterministic.
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/test_nq100_universe_sync_plugin.py::test_module_exports_plugin_contract tests/test_nq100_universe_sync_plugin.py::test_run_invokes_sync_nq100_from_csv_with_expected_default_paths tests/test_tasks_registry.py::test_register_all_tasks_includes_nq100_universe_sync_when_enabled -q</automated>
    <automated>cd backend_api_python && pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n task_nq100_universe_sync backend_api_python/app/tasks/nq100_universe_sync.py` finds JOB_ID line.
    - `register_all_tasks` imports and registers nq100 plugin (grep `nq100_universe_sync` in `__init__.py`).
    - **测试函数名与规格（Task 2）**
      - `test_module_exports_plugin_contract`：断言插件导出契约完整。
      - `test_run_invokes_sync_nq100_from_csv_with_expected_default_paths`：断言 run 调用目标函数且路径正确。
      - `test_register_all_tasks_includes_nq100_universe_sync_when_enabled`：断言注册器包含 `task_nq100_universe_sync`。
    - **回归门禁：** `cd backend_api_python && pytest tests/ -q` 必须通过。
  </acceptance_criteria>
  <done>Plugin registered; tests prove contract and registry integration.</done>
</task>

<task type="auto">
  <name>Task 3: 完成 universe 路由（PIT + EIV + 手动刷新）及接口测试</name>
  <read_first>
    - backend_api_python/app/routes/market.py (limit 120 lines: jsonify pattern)
    - backend_api_python/app/__init__.py (how create_app registers routes — only if needed)
    - backend_api_python/app/routes/__init__.py
    - backend_api_python/tests/helpers/flask_strategy_app.py (minimal app pattern if used)
  </read_first>
  <files>backend_api_python/app/routes/universe.py, backend_api_python/app/routes/__init__.py, backend_api_python/tests/test_universe_routes.py</files>
  <action>
    Create `backend_api_python/app/routes/universe.py` defining `universe_bp = Blueprint('universe', __name__)` with:

    - `GET /nq100`: optional `date` query。`date` 存在时调用 `get_constituents_as_of(date)`；不存在时返回当前成分。响应使用 `{code,msg,data}`。
    - `GET /nq100/eiv`: 必须接受 `date` 查询参数，调用 `get_eiv_as_of(date)` 并返回 `{date,index_symbol,eod_index_value,source}`。
    - `POST /nq100/refresh`: 调用 `sync_nq100_from_csv()`，返回摘要；异常按项目约定返回 `code!=1`。

    Register blueprint in `app/routes/__init__.py`: `app.register_blueprint(universe_bp, url_prefix='/api/universe')`.

    Add `tests/test_universe_routes.py` using Flask `app.test_client()`:
    - `test_get_nq100_without_date_returns_success_envelope`
      - 场景：mock 当前成分为 `["MSFT"]`。
      - 断言：`code==1` 且 symbols 包含 `MSFT`。
    - `test_get_nq100_with_date_calls_pit`
      - 场景：`?date=2020-06-01`。
      - 断言：`get_constituents_as_of(date(2020,6,1))` 被调用一次。
    - `test_get_nq100_eiv_with_date_returns_eod_index_value`
      - 场景：mock `get_eiv_as_of` 返回 `{eod_index_value: 12345.67}`。
      - 断言：`code==1` 且 `data.eod_index_value == 12345.67`。
    - `test_post_refresh_calls_sync`
      - 场景：mock `sync_nq100_from_csv` 返回摘要。
      - 断言：POST 成功且 sync 函数被调用一次。

    Create minimal Flask app in tests file (inline) registering only `universe_bp` with prefix `/api/universe` — same as production.
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/test_universe_routes.py::test_get_nq100_without_date_returns_success_envelope tests/test_universe_routes.py::test_get_nq100_with_date_calls_pit tests/test_universe_routes.py::test_get_nq100_eiv_with_date_returns_eod_index_value tests/test_universe_routes.py::test_post_refresh_calls_sync -q</automated>
    <automated>cd backend_api_python && pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `universe.py` 包含 `/nq100`、`/nq100/eiv`、`/nq100/refresh` 三类端点。
    - `register_routes` includes `universe_bp` at prefix `/api/universe`.
    - **测试函数名与规格（Task 3）**
      - `test_get_nq100_without_date_returns_success_envelope`
      - `test_get_nq100_with_date_calls_pit`
      - `test_get_nq100_eiv_with_date_returns_eod_index_value`
      - `test_post_refresh_calls_sync`
      - 以上均需包含明确场景与断言（如 action 描述）。
    - 响应空库分支保持提示：`NQ100 成分列表为空，请先运行成分同步`（需在测试中断言）。
    - **回归门禁：** `cd backend_api_python && pytest tests/ -q` 必须通过。
  </acceptance_criteria>
  <done>Routes live and covered by Flask client tests; full suite green.</done>
</task>

</tasks>

<verification>
- All NQ100-specific test modules pass; `pytest tests/ -q` passes.
- Manual smoke (optional): `POST /api/universe/nq100/refresh` on dev stack — not required for plan completion.
</verification>

<success_criteria>
- UNIV-02：具备定时刷新与手动刷新，不依赖外部网页爬取即可完成主流程。
- UNIV-03：PIT 成分与 EIV 日值均可按 date 查询。
- QQQ 权重补充失败不阻塞主流程，行为有自动化测试证据。
</success_criteria>

<output>
After completion, create `.planning/phases/19-nq100-universe-data-plane/19-03-SUMMARY.md`
</output>
