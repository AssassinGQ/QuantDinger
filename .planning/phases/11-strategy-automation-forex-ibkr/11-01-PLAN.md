---
phase: 11-strategy-automation-forex-ibkr
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/app/services/live_trading/base.py
  - backend_api_python/app/services/live_trading/factory.py
  - backend_api_python/app/services/strategy.py
  - backend_api_python/tests/test_strategy_exchange_validation.py
autonomous: true
requirements:
  - RUNT-03
user_setup: []
must_haves:
  truths:
    - "validate_exchange_market_category(exchange_id, market_category) returns (False, err) for Forex+binance and Crypto+ibkr-paper without instantiating clients"
    - "create_strategy and update_strategy call validation before INSERT/UPDATE and raise ValueError on illegal pairs"
    - "pytest tests/test_strategy_exchange_validation.py exits 0"
  artifacts:
    - path: backend_api_python/app/services/live_trading/factory.py
      provides: validate_exchange_market_category
      contains: "def validate_exchange_market_category"
    - path: backend_api_python/app/services/live_trading/base.py
      provides: validate_market_category_static classmethod on BaseStatefulClient
      contains: "def validate_market_category_static"
    - path: backend_api_python/tests/test_strategy_exchange_validation.py
      provides: UC-SA-VAL tests
      min_lines: 80
  key_links:
    - from: backend_api_python/app/services/strategy.py
      to: app.services.live_trading.factory.validate_exchange_market_category
      via: import + call inside create_strategy / update_strategy
---

<objective>
Add API-time exchange + market_category validation: `validate_exchange_market_category` in the factory (crypto rules aligned with `pending_order_worker._EXCHANGE_MARKET_RULES`), a class-level `validate_market_category_static` on `BaseStatefulClient` that mirrors instance `validate_market_category` without connecting, and call sites in `StrategyService.create_strategy` / `update_strategy` so invalid combinations fail before DB write. Batch create inherits via `create_strategy`.

Purpose: RUNT-03 requires operators’ Forex + IBKR configs to be accepted by the runner; rejecting impossible pairs at save time prevents silent misconfiguration.

Output: factory + base changes, `test_strategy_exchange_validation.py` covering UC-SA-VAL-01–08 from `11-RESEARCH.md`.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-strategy-automation-forex-ibkr/11-CONTEXT.md
@.planning/phases/11-strategy-automation-forex-ibkr/11-RESEARCH.md
@backend_api_python/app/services/pending_order_worker.py
@backend_api_python/app/services/live_trading/factory.py
@backend_api_python/app/services/live_trading/base.py

<interfaces>
From `pending_order_worker.py` (crypto gate — factory validation MUST use the same exchange_id keys and allowed category sets):
```python
_EXCHANGE_MARKET_RULES: Dict[str, set] = {
    "binance": {"Crypto"}, "okx": {"Crypto"}, "bitget": {"Crypto"},
    "bybit": {"Crypto"}, "coinbaseexchange": {"Crypto"},
    "kraken": {"Crypto"}, "kucoin": {"Crypto"}, "gate": {"Crypto"},
    "bitfinex": {"Crypto"}, "deepcoin": {"Crypto"},
}
```

From `ibkr_trading/client.py`:
```python
engine_id = "ibkr"
supported_market_categories = frozenset({"USStock", "HShare", "Forex"})
```

From `mt5_trading/client.py`:
```python
engine_id = "mt5"
supported_market_categories = frozenset({"Forex"})
```

From `usmart_trading/client.py`:
```python
supported_market_categories = frozenset({"HKStock", "USStock", "AShare"})
```

From `ef_trading/client.py`:
```python
supported_market_categories = frozenset({"AShare", "HKStock", "Bond", "ETF"})
```

Normalize `exchange_id` with the same rule as `factory._get`: `str(v).strip().lower()` from `exchange_config` keys `exchange_id` or `exchangeId`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BaseStatefulClient.validate_market_category_static + factory.validate_exchange_market_category</name>
  <files>backend_api_python/app/services/live_trading/base.py, backend_api_python/app/services/live_trading/factory.py</files>
  <read_first>
    - backend_api_python/app/services/live_trading/base.py (full BaseStatefulClient block)
    - backend_api_python/app/services/live_trading/factory.py (entire file)
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py (class header: engine_id, supported_market_categories)
    - backend_api_python/app/services/pending_order_worker.py (lines 312–332 _EXCHANGE_MARKET_RULES)
  </read_first>
  <action>
    1. In `base.py` inside class `BaseStatefulClient`, add a **classmethod** named `validate_market_category_static(cls, market_category: str) -> Tuple[bool, str]` that duplicates the logic of `validate_market_category` (lines 148–157) using `cls.engine_id` and `cls.supported_market_categories` — no instance required. Import `Tuple` if not already in scope.

    2. In `factory.py`:
       - Define a module-level dict **mirroring exactly** the worker’s crypto rule (same keys: binance, okx, bitget, bybit, coinbaseexchange, kraken, kucoin, gate, bitfinex, deepcoin; each value is the singleton set `{"Crypto"}` as in the worker).
       - Implement `def validate_exchange_market_category(exchange_id: str, market_category: str) -> Tuple[bool, str]`:
         - `ex = (exchange_id or "").strip().lower()`
         - If `ex == "coinbase_exchange"`, set `ex = "coinbaseexchange"` (match `create_client` branch at factory lines 92–93).
         - `cat = str(market_category or "").strip()`
         - If `ex` is in the crypto dict: if `cat` not in allowed set, return `(False, f"{ex}_only_supports_crypto_got_{cat.lower()}")` (exact prefix/suffix pattern so tests can grep `only_supports_crypto`).
         - If `ex in ("ibkr-paper", "ibkr-live")`: **lazy-import** `IBKRClient` from `app.services.live_trading.ibkr_trading.client` and `return IBKRClient.validate_market_category_static(cat)`.
         - If `ex == "mt5"`: lazy-import `MT5Client` from `app.services.live_trading.mt5_trading.client`, `return MT5Client.validate_market_category_static(cat)`.
         - If `ex == "usmart"`: lazy-import `USmartClient` from `app.services.live_trading.usmart_trading.client` and return `USmartClient.validate_market_category_static(cat)`.
         - If `ex == "eastmoney"`: lazy-import `EFClient` from `app.services.live_trading.ef_trading.client` and return `EFClient.validate_market_category_static(cat)`.
         - If `ex` is empty: return `(False, "missing_exchange_id")`.
         - If `ex` is not handled by any branch above (unknown id): return `(False, f"Unsupported exchange_id: {ex}")` — same substring pattern as `create_client` raises for unknown id.

    3. Do **not** instantiate any client; do not call `connect()`.
  </action>
  <acceptance_criteria>
    - `rg -n "def validate_market_category_static" backend_api_python/app/services/live_trading/base.py` returns at least one match inside `BaseStatefulClient`.
    - `rg -n "def validate_exchange_market_category" backend_api_python/app/services/live_trading/factory.py` returns a match.
    - `rg -n "ibkr-paper" backend_api_python/app/services/live_trading/factory.py` shows validate_exchange_market_category dispatches to IBKRClient.
    - `rg -n "binance.*Crypto|_only_supports_crypto" backend_api_python/app/services/live_trading/factory.py` confirms crypto branch exists (or equivalent error string with `only_supports_crypto`).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -c "from app.services.live_trading.factory import validate_exchange_market_category as v; assert v('ibkr-paper','Forex')[0] is True; assert v('ibkr-paper','Crypto')[0] is False; assert v('binance','Forex')[0] is False"</automated>
  </verify>
  <done>validate_exchange_market_category returns True for (ibkr-paper, Forex), False for (ibkr-paper, Crypto) and (binance, Forex); unknown exchange_id returns False.</done>
</task>

<task type="auto">
  <name>Task 2: StrategyService save-time validation + test_strategy_exchange_validation.py</name>
  <files>backend_api_python/app/services/strategy.py, backend_api_python/tests/test_strategy_exchange_validation.py</files>
  <read_first>
    - backend_api_python/app/services/strategy.py (create_strategy ~543–632, update_strategy ~786–880)
    - backend_api_python/tests/test_strategy_display_group.py (DB usage pattern for StrategyService)
    - backend_api_python/app/services/live_trading/factory.py (after Task 1)
  </read_first>
  <action>
    1. In `strategy.py`, import `validate_exchange_market_category` from `app.services.live_trading.factory`.

    2. Add a private method `_validate_exchange_market_for_save(self, exchange_config: Dict[str, Any], market_category: str) -> None` that:
       - Reads `exchange_id` via `(exchange_config or {}).get('exchange_id') or (exchange_config or {}).get('exchangeId') or ''`
       - Calls `validate_exchange_market_category(exchange_id, market_category)`
       - If result is `(False, err)`: `raise ValueError(err)` — use the factory error string as the message (no wrapping).

    3. Call `_validate_exchange_market_for_save(exchange_config, market_category)` in `create_strategy` **after** `exchange_config` and `market_category` are resolved from payload (immediately before the `with get_db_connection()` INSERT block).

    4. Call `_validate_exchange_market_for_save(exchange_config, market_category)` in `update_strategy` **after** merged `exchange_config` and `market_category` are computed (immediately before the `with get_db_connection()` UPDATE block).

    5. Create `backend_api_python/tests/test_strategy_exchange_validation.py` with pytest tests (reuse DB pattern from `test_strategy_display_group.py`):
       - **UC-SA-VAL-01**: `market_category=Forex`, `exchange_config={"exchange_id":"ibkr-paper"}` → `create_strategy` succeeds (returns int &gt; 0).
       - **UC-SA-VAL-02**: same with `ibkr-live`.
       - **UC-SA-VAL-03**: `market_category=Forex`, `exchange_id=mt5` → success.
       - **UC-SA-VAL-04**: `market_category=Forex`, `exchange_id=binance` → `ValueError` (assert `only_supports_crypto` in `str(exc)` or factory message substring).
       - **UC-SA-VAL-05**: `market_category=Crypto`, `exchange_id=ibkr-paper` → `ValueError` (assert `ibkr` and `only supports` in message per IBKRClient static validation).
       - **UC-SA-VAL-06**: create valid strategy, then `update_strategy` to illegal `exchange_config={"exchange_id":"binance"}` with `market_category=Forex` → raises; optionally assert DB row unchanged by re-fetching exchange_config (if practical in test DB).
       - **UC-SA-VAL-07**: `batch_create_strategies` with symbol `Forex:EURUSD` and payload including `exchange_config={"exchange_id":"ibkr-paper"}` → at least one created id (success path).
       - **UC-SA-VAL-08**: batch with `Forex:EURUSD` but `exchange_config={"exchange_id":"binance"}` → `failed_symbols` non-empty with error containing validation failure.

    Use distinct `strategy_name` strings per test to avoid unique constraint collisions.
  </action>
  <acceptance_criteria>
    - `test -f backend_api_python/tests/test_strategy_exchange_validation.py` exits 0.
    - File contains literal substrings: `UC-SA-VAL-01`, `ibkr-paper`, `binance`, `batch_create_strategies`.
    - `rg -n "_validate_exchange_market_for_save" backend_api_python/app/services/strategy.py` shows two call sites (create + update).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest tests/test_strategy_exchange_validation.py -q --tb=short</automated>
  </verify>
  <done>All UC-SA-VAL tests pass; create/update/batch paths enforce validation.</done>
</task>

</tasks>

<verification>
Run: `cd backend_api_python && python -m pytest tests/test_strategy_exchange_validation.py -q`
Regression spot-check: `python -m pytest tests/test_strategy_display_group.py -q`
</verification>

<success_criteria>
- `validate_exchange_market_category` implemented and dispatching IBKR/MT5/uSMART/EastMoney + crypto rules.
- Strategy save paths reject invalid exchange+category with `ValueError` messages from factory/client static validation.
- `test_strategy_exchange_validation.py` green.
</success_criteria>

<output>
After completion, create `.planning/phases/11-strategy-automation-forex-ibkr/11-01-SUMMARY.md`
</output>
