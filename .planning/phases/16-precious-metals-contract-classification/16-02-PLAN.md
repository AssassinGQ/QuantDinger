---
phase: 16-precious-metals-contract-classification
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - backend_api_python/app/services/live_trading/ibkr_trading/client.py
  - backend_api_python/app/services/live_trading/order_normalizer/__init__.py
  - backend_api_python/tests/test_ibkr_client.py
autonomous: true
requirements:
  - TRADE-04
user_setup: []
must_haves:
  truths:
    - "_create_contract builds ib_insync.Contract(CMDTY/SMART) for Metals, not Forex()"
    - "_EXPECTED_SEC_TYPES['Metals']=='CMDTY' and validation rejects wrong secType"
    - "place_market_order/place_limit_order aligned<=0 for Metals mentions troy ounce, ~$3200 (XAUUSD) / ~$32 (XAGUSD), sizeIncrement=minSize=1.0"
    - "Metals uses IOC TIF and Forex-equivalent map_signal_to_side when market_category==Metals"
  artifacts:
    - path: backend_api_python/app/services/live_trading/ibkr_trading/client.py
      provides: "Metals branch + validation + messages"
      contains: '"Metals"'
    - path: backend_api_python/app/services/live_trading/order_normalizer/__init__.py
      provides: "get_market_pre_normalizer('Metals') -> ForexPreNormalizer"
  key_links:
    - from: place_market_order
      to: get_market_pre_normalizer
      via: "market_type Metals must not raise in factory"
---

<objective>
Wire IBKR client to CMDTY/SMART precious metals contracts (paper-validated), extend post-qualify validation, TIF, signal-side mapping, RTH closed-reason text, and minimum-notional error copy; add `Metals` to `get_market_pre_normalizer` (reuse `ForexPreNormalizer`). Update `test_ibkr_client.py` for metals contracts and regression tests.

Purpose: Core TRADE-04 implementation in the trading client.
Output: Passing targeted pytest on `test_ibkr_client.py` metals/TIF/RTH subsets plus full client file coherence.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend_api_python/app/services/live_trading/ibkr_trading/client.py (imports `normalize_symbol` from symbols)
@.planning/phases/16-precious-metals-contract-classification/16-RESEARCH.md

**Interfaces (current — extend, do not break):**

From `client.py`:
- `_create_contract(self, symbol, market_type)` — add `elif market_type == "Metals":` returning `ib_insync.Contract(symbol=ib_symbol, secType="CMDTY", exchange="SMART", currency=currency)` where `(ib_symbol, exchange, currency) = normalize_symbol(symbol, "Metals")`.
- `_EXPECTED_SEC_TYPES` — add `"Metals": "CMDTY"`.
- `_get_tif_for_signal` — include `"Metals"` in IOC tuple with Forex/USStock/HShare.
- `map_signal_to_side` — treat `market_category == "Metals"` like Forex (`_FOREX_SIGNAL_MAP`).
- `supported_market_categories` — add `"Metals"`.
- `_qualify_ttl_seconds` — for `"Metals"` use `int(os.environ.get("IBKR_QUALIFY_TTL_FOREX_SEC", "600"))` (same as Forex; document in comment: metals shares Forex TTL env).
- `is_market_open` — when `market_type == "Metals"` and closed, append metals-appropriate hint (not "Forex 24/5"); e.g. precious metals liquid-hours / weekend context.
- `place_market_order` / `place_limit_order` — when `aligned <= 0` and `market_type == "Metals"`, append message text that includes: **troy ounce**, **sizeIncrement=1.0**, **minSize=1.0**, and reference notionals **~$3,200 for 1 oz XAUUSD** and **~$32 for 1 oz XAGUSD** (sample prices per user spec; not live quotes).

**Paper reference values for tests/mocks:** conId XAUUSD=69067924, XAGUSD=77124483; `localSymbol`/`tradingClass` equal symbol string.
</context>

<tasks>

<task type="auto">
  <name>Task 1: order_normalizer — Metals factory branch</name>
  <files>backend_api_python/app/services/live_trading/order_normalizer/__init__.py, backend_api_python/tests/test_order_normalizer.py</files>
  <read_first>
    - backend_api_python/app/services/live_trading/order_normalizer/__init__.py
    - backend_api_python/app/services/live_trading/order_normalizer/forex.py
    - backend_api_python/tests/test_order_normalizer.py
  </read_first>
  <action>
In `get_market_pre_normalizer`, when `cat == "Metals"` return `ForexPreNormalizer()` (same pass-through as Forex per 16-RESEARCH: sizeIncrement from IB drives align).

**UC spec table:**

| UC ID | Input | Expected output | Notes |
|-------|--------|-----------------|-------|
| UC-16-T2-01 | `get_market_pre_normalizer("Metals")` | `ForexPreNormalizer` instance | `type()` or `isinstance` |
| UC-16-T2-02 | `ForexPreNormalizer().pre_check(1.0, "XAUUSD")` | `(True, "")` | Positive qty |
| UC-16-T2-03 | `ForexPreNormalizer().pre_check(0.0, "XAGUSD")` | `(False, ...)` | Rejects non-positive |
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/test_order_normalizer.py -q --tb=short -k "metals or Metals or uc_16_t2" -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"Metals"' backend_api_python/app/services/live_trading/order_normalizer/__init__.py` shows factory branch.
    - UC-16-T2-01: test passes — `pytest tests/test_order_normalizer.py -k test_uc_16_t2_01 -q`
    - UC-16-T2-02: `pytest tests/test_order_normalizer.py -k test_uc_16_t2_02 -q`
    - UC-16-T2-03: `pytest tests/test_order_normalizer.py -k test_uc_16_t2_03 -q`
  </acceptance_criteria>
  <done>Three UC tests added and green; Metals maps to ForexPreNormalizer.</done>
</task>

<task type="auto">
  <name>Task 2: client.py — Metals routing, validation, messages, TIF, map_signal, categories</name>
  <files>backend_api_python/app/services/live_trading/ibkr_trading/client.py</files>
  <read_first>
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py (full sections: _create_contract through _validate_qualified_contract, _get_tif_for_signal, map_signal_to_side, supported_market_categories, is_market_open closed branch, place_market_order/limit aligned<=0)
    - backend_api_python/app/services/live_trading/ibkr_trading/symbols.py
  </read_first>
  <action>
Implement all behaviors in `<context>` interfaces block. Use **concrete** strings:

- Post-qualify failure message for invalid contract remains pattern `Invalid {market_type} contract: {symbol}` — Metals included.
- Metals `aligned<=0` message must contain **all** of: `precious metals`, `troy ounce`, `sizeIncrement`, `1.0`, `minSize`, `XAUUSD` line with `3200`, `XAGUSD` line with `32` (USD notionals as **approximate** sample values per user).

**UC spec table:**

| UC ID | Input | Expected output | Notes |
|-------|--------|-----------------|-------|
| UC-16-T3-01 | `_create_contract("XAUUSD","Metals")` | `secType=="CMDTY"`, `exchange=="SMART"`, `symbol=="XAUUSD"`, `currency=="USD"` | CMDTY not CASH |
| UC-16-T3-02 | `_create_contract("XAGUSD","Metals")` | same pattern for silver | |
| UC-16-T3-03 | `_validate_qualified_contract` mock conId>0, secType CMDTY | `(True,"")` for Metals | |
| UC-16-T3-04 | `_validate_qualified_contract` secType STK | error contains `Expected secType=CMDTY for Metals` | |
| UC-16-T3-05 | `_get_tif_for_signal("open_long","Metals")` | `"IOC"` | |
| UC-16-T3-06 | `map_signal_to_side("open_short", market_category="Metals")` | `"sell"` | Forex map |
| UC-16-T3-07 | `validate_market_category_static("Metals")` | `(True,"")` | |
| UC-16-T3-08 | `place_market_order` Metals, align→0 message | message contains `precious metals`, `3200`, `32`, `troy`, `sizeIncrement`, `minSize`, `1.0` | Mock contract details / align |
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/test_ibkr_client.py -k "uc_16_t3 or TestCreateContractMetals or TestValidateMetals or TestMetals" -q --tb=short -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"Metals"' backend_api_python/app/services/live_trading/ibkr_trading/client.py | head -20` shows supported set, map_signal branch, _EXPECTED_SEC_TYPES, _get_tif_for_signal.
    - UC-16-T3-01 through UC-16-T3-08 each has a pytest `test_uc_16_t3_XX` in `test_ibkr_client.py` and passes.
    - `pytest tests/test_ibkr_client.py -k "test_uc_16_t3" -q` exits 0 (8 tests).
  </acceptance_criteria>
  <done>All UC-16-T3-xx green; no regression in unrelated tests in same file run.</done>
</task>

<task type="auto">
  <name>Task 3: test_ibkr_client.py — migrate XAUUSD/XAGUSD tests to Metals + TIF matrix</name>
  <files>backend_api_python/tests/test_ibkr_client.py</files>
  <read_first>
    - backend_api_python/tests/test_ibkr_client.py (TestCreateContractForex, TestPlaceMarketOrderForex.test_uc_m3_xauusd, TestTifMatrix, TestForexRTHGate.test_UC_FX_I04_xagusd_window, mock helpers)
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py
  </read_first>
  <action>
1. **Replace** `test_uc_m3_xauusd_buy_market`: call `place_market_order("XAUUSD", "buy", 10.0, "Metals", ...)`; assert contract `secType=="CMDTY"`, `symbol=="XAUUSD"`, `exchange=="SMART"`, `currency=="USD"` (not `XAU`/`IDEALPRO`).

2. **Extend `TestTifMatrix`:** set `_TIF_MATRIX_MARKETS = ("Forex", "USStock", "HShare", "Metals")` and update docstring to 8×4 matrix; parametrized tests must still assert IOC.

3. **RTH:** `test_UC_FX_I04_xagusd_window` — change `is_market_open("XAGUSD", "Forex")` to `("XAGUSD", "Metals")`; ensure mocks qualify CMDTY contract (update `_make_forex_rth_client` / contract creation if tied to Forex only).

4. Add **`TestQualifyContractMetals`**: async qualify success sets conId 69067924 for XAUUSD, 77124483 for XAGUSD; snapshot cache key `("XAUUSD","Metals")`.

**UC spec table:**

| UC ID | Input | Expected output | Notes |
|-------|--------|-----------------|-------|
| UC-16-T4-01 | `place_market_order` XAUUSD Metals | CMDTY SMART XAUUSD | Replaces old UC-M3 |
| UC-16-T4-02 | `_get_tif` × Metals × 8 signals | all IOC | Parametrize |
| UC-16-T4-03 | `is_market_open("XAGUSD","Metals")` | open_ok True (mocked window) | RTH |
| UC-16-T4-04 | `_qualify_contract_async` XAUUSD Metals | cache key `(XAUUSD, Metals)` | |
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/test_ibkr_client.py -q --tb=short -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_ibkr_client.py -k "test_uc_m3_xauusd or TestTifMatrix or UC_FX_I04 or QualifyContractMetals or uc_16_t4" -q` exits 0.
    - UC-16-T4-01: grep shows `place_market_order(..., "Metals"` in test_uc_m3 or renamed test.
    - UC-16-T4-02: `_TIF_MATRIX_MARKETS` includes Metals in file.
    - UC-16-T4-03: `test_UC_FX_I04` uses `"Metals"`.
    - UC-16-T4-04: test file contains `(\"XAUUSD\", \"Metals\")` in cache assertion or equivalent.
  </acceptance_criteria>
  <done>Full `test_ibkr_client.py` suite passes.</done>
</task>

</tasks>

<verification>
`cd backend_api_python && pytest tests/test_ibkr_client.py tests/test_order_normalizer.py -q`
</verification>

<success_criteria>
- TRADE-04: CMDTY/SMART path live in client; Metals market_category supported; error copy documents min order notionals.
- All UC tables in Tasks 1–3 verified by pytest/grep.
</success_criteria>

<output>
After completion, create `.planning/phases/16-precious-metals-contract-classification/16-02-SUMMARY.md`
</output>
