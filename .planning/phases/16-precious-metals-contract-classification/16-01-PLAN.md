---
phase: 16-precious-metals-contract-classification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/app/services/live_trading/ibkr_trading/symbols.py
  - backend_api_python/tests/test_ibkr_symbols.py
autonomous: true
requirements:
  - TRADE-04
user_setup: []
must_haves:
  truths:
    - "parse_symbol maps XAUUSD and XAGUSD to market_type Metals (not Forex)"
    - "parse_symbol maps XAUEUR to non-Forex path after removal from KNOWN_FOREX_PAIRS (USStock default)"
    - "normalize_symbol(XAUUSD, Metals) yields SMART routing inputs suitable for CMDTY Contract(symbol=full pair)"
    - "XAUUSD/XAGUSD/XAUEUR are removed from KNOWN_FOREX_PAIRS"
  artifacts:
    - path: backend_api_python/app/services/live_trading/ibkr_trading/symbols.py
      provides: "Precious-metal pair detection + Metals normalize_symbol branch"
      contains: "Metals"
    - path: backend_api_python/tests/test_ibkr_symbols.py
      provides: "UC coverage for parse_symbol/normalize_symbol metals"
  key_links:
    - from: symbols.parse_symbol
      to: "Metals vs Forex classification"
      via: "_is_precious_metal_pair before KNOWN_FOREX_PAIRS"
---

<objective>
Implement symbol-layer classification for IBKR precious metals: `parse_symbol` returns `Metals` for validated XAU*/XAG* six-letter pairs, remove obsolete Forex entries (including XAUEUR), and extend `normalize_symbol` so `market_type=="Metals"` returns `(full_pair, "SMART", quote_ccy)` for CMDTY contract construction in plan 16-02.

Purpose: TRADE-04 requires routing separate from IDEALPRO Forex; symbols are the first gate.
Output: Updated `symbols.py` and passing `test_ibkr_symbols.py` metals UCs.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-precious-metals-contract-classification/16-CONTEXT.md
@.planning/phases/16-precious-metals-contract-classification/16-RESEARCH.md

**Hardcoded research facts (executor must not reinterpret):** Paper qualify DUQ123679 — XAUUSD/XAGUSD → `secType=CMDTY`, `exchange=SMART`; `Forex("XAUUSD")` fails Error 200; XAUEUR CMDTY fails — exclude from metals routing and remove from `KNOWN_FOREX_PAIRS`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: symbols.py — precious metals detection and Metals normalize_symbol</name>
  <files>backend_api_python/app/services/live_trading/ibkr_trading/symbols.py</files>
  <read_first>
    - backend_api_python/app/services/live_trading/ibkr_trading/symbols.py
    - .planning/phases/16-precious-metals-contract-classification/16-RESEARCH.md (Paper Qualify Experiment table)
    - .planning/phases/16-precious-metals-contract-classification/16-CONTEXT.md (canonical_refs)
  </read_first>
  <action>
Implement the following **exact** behaviors.

1. **Remove from `KNOWN_FOREX_PAIRS`:** `"XAUUSD"`, `"XAGUSD"`, `"XAUEUR"` (update the inline comment: metals are CMDTY/SMART, not IDEALPRO CASH).

2. **Add helper** `_is_precious_metal_pair(forex_clean: str) -> bool`:
   - Returns `True` iff `len(forex_clean)==6`, `forex_clean.isalpha()`, and `(forex_clean.startswith("XAU") or forex_clean.startswith("XAG"))`, and `forex_clean != "XAUEUR"` (explicit exclusion per 16-RESEARCH).

3. **`parse_symbol`:** After HK and digit branches, compute `forex_clean = _clean_forex_raw(symbol)`. **Before** checking `KNOWN_FOREX_PAIRS`, if `_is_precious_metal_pair(forex_clean)` then `return (forex_clean, "Metals")`. Else if `forex_clean in KNOWN_FOREX_PAIRS` then `return (forex_clean, "Forex")`. Else existing USStock default.

4. **`normalize_symbol`:** Add branch `elif market_type == "Metals":`:
   - `pair = _clean_forex_raw(symbol)` then validate same as Forex path: 6 letters alpha; on failure raise `ValueError` mentioning invalid **precious metals** symbol (not generic Forex-only wording).
   - Return `(pair, "SMART", pair[3:6])` — i.e. ib symbol is full six-letter pair (e.g. `XAUUSD`), exchange `SMART`, currency = last 3 letters (USD for XAUUSD/XAGUSD).

5. **`format_display_symbol`:** If `exchange == "SMART"` and `len(ib_symbol)==6` and `ib_symbol.isalpha()` and `(ib_symbol.startswith("XAU") or ib_symbol.startswith("XAG"))`, return `ib_symbol` unchanged (no dot split like IDEALPRO).

**UC spec table (complete):**

| UC ID | Input | Expected output | Notes |
|-------|--------|-----------------|-------|
| UC-16-T1-01 | `parse_symbol("XAUUSD")` | `("XAUUSD", "Metals")` | Gold pair |
| UC-16-T1-02 | `parse_symbol("XAGUSD")` | `("XAGUSD", "Metals")` | Silver pair |
| UC-16-T1-03 | `parse_symbol("xau-usd")` | `("XAUUSD", "Metals")` | Separator strip |
| UC-16-T1-04 | `parse_symbol("XAUGBP")` (if 6-letter XAU*) | `("XAUGBP", "Metals")` | Pattern future-proof (not in KNOWN) |
| UC-16-T1-05 | `parse_symbol("XAUEUR")` | `("XAUEUR", "USStock")` | Excluded from metals; not in KNOWN → default |
| UC-16-T1-06 | `parse_symbol("EURUSD")` | `("EURUSD", "Forex")` | Unchanged Forex |
| UC-16-T1-07 | `normalize_symbol("XAUUSD", "Metals")` | `("XAUUSD", "SMART", "USD")` | CMDTY inputs |
| UC-16-T1-08 | `normalize_symbol("XAGUSD", "Metals")` | `("XAGUSD", "SMART", "USD")` | CMDTY inputs |
| UC-16-T1-09 | `normalize_symbol("bad", "Metals")` | raises `ValueError` | Invalid length/alpha |
| UC-16-T1-10 | `format_display_symbol("XAUUSD", "SMART")` | `"XAUUSD"` | Display |
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/test_ibkr_symbols.py -q --tb=short -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'Metals' backend_api_python/app/services/live_trading/ibkr_trading/symbols.py` shows branch in `parse_symbol` and `normalize_symbol`.
    - `grep -n XAUEUR backend_api_python/app/services/live_trading/ibkr_trading/symbols.py` — XAUEUR not in `KNOWN_FOREX_PAIRS` set literal (line must not list XAUEUR inside the set).
    - UC-16-T1-01: `pytest tests/test_ibkr_symbols.py -k "test_uc_16_t1_01" -q` passes (after Task 2 adds test).
    - UC-16-T1-02: `pytest tests/test_ibkr_symbols.py -k "test_uc_16_t1_02" -q` passes.
    - UC-16-T1-03: `pytest tests/test_ibkr_symbols.py -k "test_uc_16_t1_03" -q` passes.
    - UC-16-T1-04: `pytest tests/test_ibkr_symbols.py -k "test_uc_16_t1_04" -q` passes.
    - UC-16-T1-05: `pytest tests/test_ibkr_symbols.py -k "test_uc_16_t1_05" -q` passes.
    - UC-16-T1-06: `pytest tests/test_ibkr_symbols.py -k "test_uc_16_t1_06" -q` passes.
    - UC-16-T1-07: `pytest tests/test_ibkr_symbols.py -k "test_uc_16_t1_07" -q` passes.
    - UC-16-T1-08: `pytest tests/test_ibkr_symbols.py -k "test_uc_16_t1_08" -q` passes.
    - UC-16-T1-09: `pytest tests/test_ibkr_symbols.py -k "test_uc_16_t1_09" -q` passes.
    - UC-16-T1-10: `pytest tests/test_ibkr_symbols.py -k "test_uc_16_t1_10" -q` passes.
  </acceptance_criteria>
  <done>symbols.py implements table UCs; Task 2 tests exist and pass for every UC ID.</done>
</task>

<task type="auto">
  <name>Task 2: test_ibkr_symbols.py — metals UC tests</name>
  <files>backend_api_python/tests/test_ibkr_symbols.py</files>
  <read_first>
    - backend_api_python/tests/test_ibkr_symbols.py
    - backend_api_python/app/services/live_trading/ibkr_trading/symbols.py
  </read_first>
  <action>
1. Rename/replace `test_metals_detected_as_forex` with **`test_metals_detected_as_metals`** asserting `parse_symbol("XAUUSD")` → `("XAUUSD", "Metals")`.

2. Add class **`TestPreciousMetalsSymbolUcs`** (or equivalent) with **one test method per UC ID** using the exact ids:
   - `test_uc_16_t1_01` … `test_uc_16_t1_10` mapping to the UC table in Task 1 (import `parse_symbol`, `normalize_symbol`, `format_display_symbol`).

3. Update any `normalize_symbol(..., "Forex")` tests that used XAUUSD as Forex pair: XAUUSD must use `market_type="Metals"` where testing metals, or use a pure Forex pair for Forex normalization tests.

4. Keep `TestNormalizeSymbolForex` coverage for EURUSD-style pairs unchanged.
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/test_ibkr_symbols.py -q --tb=short -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_ibkr_symbols.py -q` exits 0.
    - `grep -E 'test_uc_16_t1_0[1-9]|test_uc_16_t1_10' backend_api_python/tests/test_ibkr_symbols.py` lists all ten test functions.
    - UC-16-T1-01 through UC-16-T1-10: each has a dedicated `test_uc_16_t1_XX` and passes under full file run.
  </acceptance_criteria>
  <done>All 10 UC tests green; obsolete "metals as Forex" assertion removed.</done>
</task>

</tasks>

<verification>
Full file: `pytest tests/test_ibkr_symbols.py -q`; spot check `grep KNOWN_FOREX_PAIRS -A30 symbols.py` for absence of XAUUSD, XAGUSD, XAUEUR.
</verification>

<success_criteria>
- TRADE-04 symbol layer: Metals distinct from Forex in `parse_symbol` / `normalize_symbol`.
- Every UC in Task 1 table has a pytest with matching name and passes.
</success_criteria>

<output>
After completion, create `.planning/phases/16-precious-metals-contract-classification/16-01-SUMMARY.md`
</output>
