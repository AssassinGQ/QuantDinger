---
phase: 16-precious-metals-contract-classification
plan: 03
type: execute
wave: 3
depends_on:
  - 16-02
files_modified:
  - backend_api_python/tests/test_exchange_engine.py
  - backend_api_python/tests/test_strategy_exchange_validation.py
  - backend_api_python/tests/test_ibkr_forex_paper_smoke.py
  - backend_api_python/tests/test_forex_ibkr_e2e.py
autonomous: true
requirements:
  - TRADE-04
user_setup: []
must_haves:
  truths:
    - "IBKRClient.supported_market_categories tests include Metals"
    - "Strategy API accepts ibkr-paper + Metals for create_strategy (mirrors Forex validation path)"
    - "Paper smoke and E2E XAGUSD paths use market_category Metals and CMDTY qualify mocks"
  artifacts:
    - path: backend_api_python/tests/test_exchange_engine.py
      provides: "Metals in supported set assertion"
    - path: backend_api_python/tests/test_forex_ibkr_e2e.py
      provides: "XAGUSD chain with Metals + CMDTY"
  key_links:
    - from: StatefulClientRunner.execute
      to: place_market_order(..., market_type=market_category)
      via: "E2E must load strategy market_category Metals"
---

<objective>
Align integration and API tests with TRADE-04: IBKR engine advertises `Metals`, strategy validation accepts `ibkr-paper`+`Metals`, and XAGUSD paper-smoke / E2E tests use `CMDTY` qualification mocks plus `market_category: "Metals"` so the live worker → runner → client path matches production routing.

Purpose: Regression gate beyond unit tests; satisfies 16-VALIDATION.md integration rows.
Output: Green `pytest` on the four listed test modules together.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend_api_python/app/services/live_trading/runners/stateful_runner.py (execute uses `market_category` as `market_type` argument to `place_market_order`)
@backend_api_python/tests/test_ibkr_forex_paper_smoke.py (`_run_open_close_cycle`, `_make_qualify_for_pair`)

**Mock constants:** XAGUSD conId `77124483`, XAUUSD `69067924` per 16-RESEARCH; qualify must set `secType="CMDTY"`, `exchange="SMART"` for metals tests.
</context>

<tasks>

<task type="auto">
  <name>Task 1: test_exchange_engine.py + test_strategy_exchange_validation.py — Metals supported</name>
  <files>backend_api_python/tests/test_exchange_engine.py, backend_api_python/tests/test_strategy_exchange_validation.py</files>
  <read_first>
    - backend_api_python/tests/test_exchange_engine.py
    - backend_api_python/tests/test_strategy_exchange_validation.py
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py (supported_market_categories)
  </read_first>
  <action>
1. **test_exchange_engine.py:** Update IBKR `supported_market_categories` assertion to include `"Metals"` (exact frozenset: `{"USStock", "HShare", "Forex", "Metals"}`).

2. **test_strategy_exchange_validation.py:** Add `_metals_payload(strategy_name, exchange_id)` with `"market_category": "Metals"`, `trading_config.symbol` = `XAUUSD`.

**UC spec table:**

| UC ID | Input | Expected output | Notes |
|-------|--------|-----------------|-------|
| UC-16-T5-01 | `IBKRClient.supported_market_categories` | contains `"Metals"` | `test_uc_16_t5_01` |
| UC-16-T5-02 | `IBKRClient.validate_market_category_static("Metals")` | `(True, "")` | `test_uc_16_t5_02` |
| UC-16-T5-03 | `create_strategy` with `ibkr-paper` + Metals + XAUUSD | `sid` int, no exception | `test_uc_16_t5_03` |
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/test_exchange_engine.py tests/test_strategy_exchange_validation.py -q --tb=short -k "uc_16_t5 or Metals or test_uc_sa_val" -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n Metals backend_api_python/tests/test_exchange_engine.py` non-empty.
    - UC-16-T5-01: `pytest tests/test_exchange_engine.py -k test_uc_16_t5_01 -q` passes.
    - UC-16-T5-02: `pytest tests/test_exchange_engine.py -k test_uc_16_t5_02 -q` passes.
    - UC-16-T5-03: `pytest tests/test_strategy_exchange_validation.py -k test_uc_16_t5_03 -q` passes.
  </acceptance_criteria>
  <done>Exchange + strategy validation tests assert Metals support end-to-end at API/engine layer.</done>
</task>

<task type="auto">
  <name>Task 2: test_ibkr_forex_paper_smoke.py — XAGUSD Metals + CMDTY qualify</name>
  <files>backend_api_python/tests/test_ibkr_forex_paper_smoke.py</files>
  <read_first>
    - backend_api_python/tests/test_ibkr_forex_paper_smoke.py
    - backend_api_python/tests/test_ibkr_client.py (_make_mock_ib_insync Contract behavior)
  </read_first>
  <action>
1. Extend `_make_qualify_for_pair` with optional keyword `sec_type: str = "CASH"`; when qualifying metals, pass `sec_type="CMDTY"` and set `c.exchange = "SMART"` if attribute exists.

2. Refactor `_run_open_close_cycle` to accept `market_type="Forex"`, `market_category="Forex"` kwargs (defaults preserve existing tests). For XAGUSD test, call with `market_type="Metals"`, `market_category="Metals"` and use `sec_type="CMDTY"` in qualify mock.

3. Update `test_forex_paper_smoke_xagusd_uc_sa_smk_03` docstring to state **Metals / CMDTY**; assert `place_calls[0].contract.secType == "CMDTY"` (or equivalent on mock contract).

**UC spec table:**

| UC ID | Input | Expected output | Notes |
|-------|--------|-----------------|-------|
| UC-16-T6-01 | `_run_open_close_cycle` XAGUSD + Metals | `placeOrder` contract CMDTY/SMART | |
| UC-16-T6-02 | EURUSD smoke unchanged | still CASH/IDEALPRO | Regression |
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/test_ibkr_forex_paper_smoke.py -q --tb=short -x</automated>
  </verify>
  <acceptance_criteria>
    - UC-16-T6-01: `pytest tests/test_ibkr_forex_paper_smoke.py -k xagusd -q` passes; grep `CMDTY` in test file.
    - UC-16-T6-02: `pytest tests/test_ibkr_forex_paper_smoke.py -k eurusd -q` passes.
  </acceptance_criteria>
  <done>Smoke module green; XAGUSD uses Metals path.</done>
</task>

<task type="auto">
  <name>Task 3: test_forex_ibkr_e2e.py — XAGUSD E2E config Metals + qualify CMDTY</name>
  <files>backend_api_python/tests/test_forex_ibkr_e2e.py</files>
  <read_first>
    - backend_api_python/tests/test_forex_ibkr_e2e.py
    - backend_api_python/tests/test_ibkr_forex_paper_smoke.py (`_make_qualify_for_pair` after Task 2)
  </read_first>
  <action>
1. In `test_uc_sa_e2e_xagusd_open_close_full_chain`, set `mock_load_cfg.return_value` **`"market_category": "Metals"`** (remove Forex for this chain).

2. Update `_make_ibkr_client_for_e2e` call for XAGUSD to use `sec_type="CMDTY"` (add parameter if Task 2 pattern).

3. After `place_calls`, assert contract fields: `secType=="CMDTY"`, `symbol=="XAGUSD"`, `exchange=="SMART"`.

**UC spec table:**

| UC ID | Input | Expected output | Notes |
|-------|--------|-----------------|-------|
| UC-16-T7-01 | E2E XAGUSD with cfg Metals | `placeOrder` sees CMDTY XAGUSD | |
| UC-16-T7-02 | EURUSD E2E (other tests in file) | unchanged CASH | Spot-check one existing test |
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/test_forex_ibkr_e2e.py -q --tb=short -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"Metals"' backend_api_python/tests/test_forex_ibkr_e2e.py` shows XAGUSD test config.
    - UC-16-T7-01: `pytest tests/test_forex_ibkr_e2e.py -k xagusd -q` passes.
    - UC-16-T7-02: `pytest tests/test_forex_ibkr_e2e.py -k eurusd -q` passes (or first EURUSD test name in file).
  </acceptance_criteria>
  <done>Full E2E module passes with metals integration.</done>
</task>

</tasks>

<verification>
`cd backend_api_python && pytest tests/test_exchange_engine.py tests/test_strategy_exchange_validation.py tests/test_ibkr_forex_paper_smoke.py tests/test_forex_ibkr_e2e.py -q`

Then full regression: `cd backend_api_python && pytest` (phase gate per ROADMAP).
</verification>

<success_criteria>
- TRADE-04 reflected in engine validation + strategy API + smoke + E2E.
- All UC IDs in Task tables have pytest or grep proof in acceptance_criteria.
</success_criteria>

<output>
After completion, create `.planning/phases/16-precious-metals-contract-classification/16-03-SUMMARY.md`
</output>
