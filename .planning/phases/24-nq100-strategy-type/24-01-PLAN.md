---
phase: 24-nq100-strategy-type
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend_api_python/app/strategies/dynamic_cross_sectional.py, backend_api_python/app/strategies/nq100_strategy.py]
autonomous: true
requirements: [STRAT-01]
user_setup: []

must_haves:
  truths:
    - "DynamicCrossSectionalStrategy exists and inherits from CrossSectionalStrategy"
    - "DynamicCrossSectionalStrategy.get_universe_list() raises NotImplementedError if not implemented by subclass"
    - "NQ100Strategy inherits from DynamicCrossSectionalStrategy"
    - "NQ100Strategy.get_universe_list() returns PIT constituents from Phase 19 API"
    - "get_data_request() uses dynamic universe instead of static symbol_list"
  artifacts:
    - path: "backend_api_python/app/strategies/dynamic_cross_sectional.py"
      provides: "Intermediate strategy layer with get_universe_list interface"
      exports: ["DynamicCrossSectionalStrategy"]
    - path: "backend_api_python/app/strategies/nq100_strategy.py"
      provides: "NQ100-specific strategy implementation"
      exports: ["NQ100Strategy"]
  key_links:
    - from: "app/strategies/nq100_strategy.py"
      to: "app/services/universe_nq100_service.py"
      via: "get_constituents_as_of()"
      pattern: "from app.services.universe_nq100_service import get_constituents_as_of"
    - from: "DynamicCrossSectionalStrategy"
      to: "CrossSectionalStrategy"
      via: "inheritance"
      pattern: "class DynamicCrossSectionalStrategy\\(CrossSectionalStrategy\\)"
---

<objective>
Implement STRAT-01: Three-layer inheritance structure for dynamic universe binding.

Purpose: Enable strategies to dynamically query universe membership instead of static symbol_list configuration.
Output: DynamicCrossSectionalStrategy intermediate layer + NQ100Strategy implementation.
</objective>

<execution_context>
@/home/workspace/ws/QuantDinger/.claude/get-shit-down/workflows/execute-plan.md
@/home/workspace/ws/QuantDinger/.claude/get-shit-down/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-nq100-strategy-type/24-CONTEXT.md
@.planning/phases/24-nq100-strategy-type/24-RESEARCH.md
@.planning/phases/24-nq100-strategy-type/24-VALIDATION.md
@.planning/phases/19-nq100-universe-data-plane/19-CONTEXT.md

<interfaces>
<!-- Key interfaces executor needs from existing codebase -->

From app/strategies/cross_sectional.py:
```python
class CrossSectionalStrategy(IStrategyLoop):
    def get_data_request(
        self,
        strategy_id: int,
        strategy: Dict[str, Any],
        current_time: float,
    ) -> DataRequest:
        """Returns data request dict with symbol_list, timeframe, trading_config."""
```

From app/services/universe_nq100_service.py:
```python
def get_constituents_as_of(as_of_date: date) -> List[str]:
    """Return sorted unique symbols that were NQ100 constituents on as_of_date."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create DynamicCrossSectionalStrategy intermediate layer</name>
  <files>backend_api_python/app/strategies/dynamic_cross_sectional.py</files>
  <read_first>
    - backend_api_python/app/strategies/cross_sectional.py (base class to inherit from)
    - backend_api_python/app/services/universe_nq100_service.py (PIT API to reference for interface signature)
  </read_first>
  <behavior>
    - Test 1: DynamicCrossSectionalStrategy inherits from CrossSectionalStrategy
    - Test 2: get_universe_list() raises NotImplementedError with message "Subclass must implement"
    - Test 3: get_data_request() calls get_universe_list() instead of reading static symbol_list
    - Test 4: trading_config with both symbol_list and universe raises ValueError for mutual exclusion (D-04)
  </behavior>
  <action>
Per D-01/D-02/D-04/D-05: Create intermediate layer providing dynamic universe interface.

1. Create `backend_api_python/app/strategies/dynamic_cross_sectional.py`:
   - Import `CrossSectionalStrategy` from `app.strategies.cross_sectional`
   - Import `date` from `datetime`, `List` from `typing`

2. Define `DynamicCrossSectionalStrategy` class:
   - Inherits from `CrossSectionalStrategy`
   - Define `get_universe_list(self, as_of_date: date) -> List[str]`:
     - Raises `NotImplementedError("Subclass must implement get_universe_list()")`
   - Override `get_data_request()`:
     - Check trading_config for mutual exclusion (D-04):
       - If both `symbol_list` AND `universe` are configured → raise `ValueError("symbol_list and universe are mutually exclusive")`
     - If `universe` field exists → call `self.get_universe_list(date.today())` for symbol_list
     - Else → use static `trading_config.get("symbol_list", [])` (fallback for non-dynamic subclasses)
     - Return same DataRequest structure as base class with dynamic symbol_list

3. Add docstrings explaining D-01 inheritance structure and D-02 interface contract.

Do NOT implement universe-specific logic (NQ100, SP100 etc.) — that goes in subclasses.
  </action>
  <verify>
### Automated Tests
| Test Case | What it tests | Input | Expected Output |
|-----------|---------------|-------|-----------------|
| test_dynamic_strategy_inherits_from_cross_sectional | Verify inheritance chain (D-01) | Check `DynamicCrossSectionalStrategy.__bases__` | First base is `CrossSectionalStrategy` |
| test_dynamic_strategy_requires_get_universe_list | Verify interface contract (D-02) | Create instance, call `get_universe_list(date(2024,1,1))` | Raises `NotImplementedError` with message containing "Subclass must implement" |
| test_dynamic_strategy_get_data_request_calls_universe_list | Verify dynamic binding (D-05) | Mock subclass with `get_universe_list` returning `["AAPL", "MSFT"]`, call `get_data_request()` with `trading_config={"universe":"NQ100"}` | `DataRequest["symbol_list"]` equals `["AAPL", "MSFT"]` |
| test_mutual_exclusion_symbol_list_and_universe | Verify D-04 mutual exclusion | Call `get_data_request()` with `trading_config={"symbol_list":["AAPL"], "universe":"NQ100"}` | Raises `ValueError` with message containing "mutually exclusive" |
| test_static_fallback_when_no_universe | Verify backward compatibility | Call `get_data_request()` with `trading_config={"symbol_list":["AAPL"]}` (no universe field) | Returns `DataRequest["symbol_list"]` = `["AAPL"]` |

### Task Specific Tests
Run: `pytest tests/test_nq100_strategy.py::TestStrategyInheritance tests/test_nq100_strategy.py::TestDynamicUniverseBinding tests/test_nq100_strategy.py::TestConfigValidation::test_mutual_exclusion -v`
Expect: All tests pass (exit code 0)

### 全量测试通过 (Full Library Suite)
Run: `pytest tests/ -v --tb=short`
Expect: All tests pass (exit code 0)
  </verify>
  <done>
- File exists: `backend_api_python/app/strategies/dynamic_cross_sectional.py`
- Class `DynamicCrossSectionalStrategy` inherits from `CrossSectionalStrategy`
- Method `get_universe_list()` raises NotImplementedError
- Method `get_data_request()` calls `get_universe_list()` when universe field exists
- Mutual exclusion validation raises ValueError when both symbol_list and universe configured
- All 5 unit tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create NQ100Strategy implementation</name>
  <files>backend_api_python/app/strategies/nq100_strategy.py</files>
  <read_first>
    - backend_api_python/app/strategies/cross_sectional.py (understand base class structure)
    - backend_api_python/app/strategies/dynamic_cross_sectional.py (intermediate layer from Task 1)
    - backend_api_python/app/services/universe_nq100_service.py (PIT API to call)
  </read_first>
  <behavior>
    - Test 1: NQ100Strategy inherits from DynamicCrossSectionalStrategy
    - Test 2: get_universe_list() calls get_constituents_as_of() from Phase 19
    - Test 3: get_universe_list() returns sorted symbol list
    - Test 4: Empty PIT result returns empty list (no error)
    - Test 5: Database error is handled gracefully (returns empty list or logs warning)
  </behavior>
  <action>
Per D-03: Implement NQ100-specific strategy that calls Phase 19 PIT API.

1. Create `backend_api_python/app/strategies/nq100_strategy.py`:
   - Import `DynamicCrossSectionalStrategy` from `app.strategies.dynamic_cross_sectional`
   - Import `get_constituents_as_of` from `app.services.universe_nq100_service`
   - Import `date` from `datetime`, `List` from `typing`

2. Define `NQ100Strategy` class:
   - Inherits from `DynamicCrossSectionalStrategy`
   - Implement `get_universe_list(self, as_of_date: date) -> List[str]`:
     - Call `get_constituents_as_of(as_of_date)` (D-03)
     - Return sorted list (PIT API already returns sorted)
     - Handle exceptions gracefully: log warning and return empty list (don't crash strategy execution)
   - Add docstring explaining D-03 PIT integration

3. Register strategy type (optional at this stage, can be done in Plan 02 when routes are modified):
   - Export `NQ100Strategy` for strategy factory import

Do NOT implement delisting policy logic — that goes in DelistingPolicyFilter (Plans 03-04).
  </action>
  <verify>
### Automated Tests
| Test Case | What it tests | Input | Expected Output |
|-----------|---------------|-------|-----------------|
| test_nq100_strategy_inherits_dynamic | Verify inheritance chain (D-01) | Check `NQ100Strategy.__bases__` | First base is `DynamicCrossSectionalStrategy` |
| test_nq100_get_universe_list_calls_pit_api | Verify PIT integration (D-03) | Mock `get_constituents_as_of(date(2024,3,15))` returning `["AAPL","MSFT","GOOGL"]`, call `strategy.get_universe_list(date(2024,3,15))` | Returns `["AAPL","GOOGL","MSFT"]` (sorted) |
| test_nq100_get_universe_list_empty_result | Handle empty PIT result | Mock `get_constituents_as_of` returning `[]` | Returns `[]` (no error raised) |
| test_nq100_get_universe_list_db_error_handling | Handle database errors gracefully | Mock `get_constituents_as_of` raising `Exception("DB connection failed")` | Returns `[]` (or logs warning, no crash) |

### Task Specific Tests
Run: `pytest tests/test_nq100_strategy.py::TestNQ100PITIntegration -v`
Expect: All tests pass (exit code 0)

### 全量测试通过 (Full Library Suite)
Run: `pytest tests/ -v --tb=short`
Expect: All tests pass (exit code 0)
  </verify>
  <done>
- File exists: `backend_api_python/app/strategies/nq100_strategy.py`
- Class `NQ100Strategy` inherits from `DynamicCrossSectionalStrategy`
- Method `get_universe_list()` calls `get_constituents_as_of()`
- Empty result and database errors handled gracefully
- All 4 unit tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Strategy config → Strategy instance | trading_config input crosses trust boundary, requires validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | DynamicCrossSectionalStrategy inheritance chain | mitigate | Unit test verifies base class inheritance, prevents invalid subclass breaking contract |
| T-24-02 | Tampering | get_universe_list returning unvalidated symbols | mitigate | PIT API `get_constituents_as_of()` returns validated constituents from database, not arbitrary input |
</threat_model>

<verification>
After all tasks complete:
1. Run `pytest tests/test_nq100_strategy.py -v` — all inheritance and PIT integration tests pass
2. Verify imports work: `python -c "from app.strategies.nq100_strategy import NQ100Strategy"`
3. Verify inheritance chain: NQ100Strategy → DynamicCrossSectionalStrategy → CrossSectionalStrategy
</verification>

<success_criteria>
- DynamicCrossSectionalStrategy exists with NotImplementedError for get_universe_list()
- NQ100Strategy implements get_universe_list() calling Phase 19 PIT API
- Mutual exclusion validation (symbol_list vs universe) implemented
- All 9 unit tests pass (5 for Task 1 + 4 for Task 2)
- No crash on empty universe or database errors
</success_criteria>

<output>
After completion, create `.planning/phases/24-nq100-strategy-type/24-01-SUMMARY.md`
</output>