---
phase: 14-tif-unification-usstock-hshare
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/app/services/live_trading/ibkr_trading/client.py
  - backend_api_python/tests/test_ibkr_client.py
autonomous: true
requirements:
  - INFRA-02
user_setup: []
must_haves:
  truths:
    - "IBKRClient._get_tif_for_signal(signal, market) returns \"IOC\" for every combination of the eight signal types with Forex, USStock, and HShare (24 combinations)."
    - "USStock and HShare market/limit order tests assert placed_order.tif == \"IOC\" for paths that previously expected DAY."
    - "Docstring for _get_tif_for_signal cites IBKR IOC exchange list including SEHK and does not claim Hong Kong stocks cannot use IOC."
  artifacts:
    - path: backend_api_python/app/services/live_trading/ibkr_trading/client.py
      provides: "_get_tif_for_signal unified IOC policy for Forex, USStock, HShare"
      contains: "_get_tif_for_signal"
    - path: backend_api_python/tests/test_ibkr_client.py
      provides: "TestTifMatrix 8×3 parametrize + updated equity TIF assertions"
      contains: "TestTifMatrix"
  key_links:
    - from: backend_api_python/app/services/live_trading/ibkr_trading/client.py
      to: "IOC"
      via: "_get_tif_for_signal return value"
      pattern: "_get_tif_for_signal"
    - from: backend_api_python/tests/test_ibkr_client.py
      to: IBKRClient._get_tif_for_signal
      via: "TestTifMatrix parametrize"
      pattern: "TestTifMatrix"
---

<objective>
Unify TIF policy: for IBKR-supported market categories `Forex`, `USStock`, and `HShare`, every signal type uses **IOC** (24 combinations). Update `_get_tif_for_signal` and exhaustive tests.

Purpose: Match Forex automation expectations for USStock and HShare; remove incorrect SEHK/IOC docstring; prevent policy drift via matrix tests (INFRA-02).
Output: Updated `client.py` and `test_ibkr_client.py`. (`.planning/ROADMAP.md` Phase 14 plans/success criteria updated during `/gsd:plan-phase`.)
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-tif-unification-usstock-hshare/14-CONTEXT.md
@.planning/phases/14-tif-unification-usstock-hshare/14-RESEARCH.md

<interfaces>
From `backend_api_python/app/services/live_trading/ibkr_trading/client.py`:

```python
@staticmethod
def _get_tif_for_signal(signal_type: str, market_type: str = "USStock") -> str:
    ...
```

`IBKRClient.supported_market_categories == frozenset({"USStock", "HShare", "Forex"})`.

**Target policy:** For `market_type` in `("Forex", "USStock", "HShare")`, return `"IOC"` for **all** eight signal types:
`open_long`, `add_long`, `close_long`, `reduce_long`, `open_short`, `add_short`, `close_short`, `reduce_short`.

For any other `market_type`: return `"DAY"`.

**Docstring URL (verbatim):** `https://www.interactivebrokers.com/en/trading/order-type-exchanges.php?ot=ioc` — SEHK listed for IOC.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IOC TIF in client.py + TestTifMatrix + update TIF tests (single atomic change)</name>
  <files>backend_api_python/app/services/live_trading/ibkr_trading/client.py, backend_api_python/tests/test_ibkr_client.py</files>
  <read_first>
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py (`_get_tif_for_signal`, `supported_market_categories`)
    - backend_api_python/tests/test_ibkr_client.py (`TestTifDay`, `TestTifForexPolicy`, `TestPlaceMarketOrderForex` `test_uc_r1`/`test_uc_r2`, line ~771+ comments)
    - .planning/phases/14-tif-unification-usstock-hshare/14-RESEARCH.md (IBKR IOC / SEHK)
  </read_first>
  <action>
    **A. `client.py` — `_get_tif_for_signal`**

    - If `market_type in ("Forex", "USStock", "HShare")`: return `"IOC"` (ignore `signal_type` for these three).
    - Else: return `"DAY"`.

    - Replace docstring completely. Must:
      - State IOC for `Forex`, `USStock`, `HShare` for all signals.
      - Include exact URL: `https://www.interactivebrokers.com/en/trading/order-type-exchanges.php?ot=ioc` and mention **Hong Kong Stock Exchange (SEHK)** or **SEHK** as listed for IOC.
      - Remove any sentence equivalent to "Hong Kong stocks do not support IOC orders".
      - Mention unknown `market_type` uses `"DAY"` until explicitly supported.

    **B. `test_ibkr_client.py`**

    1. Add `class TestTifMatrix` with `@pytest.mark.parametrize` over 24 rows `(signal_type, market_type)` with expected `"IOC"`:
       - `signal_type` ∈ `("open_long", "add_long", "close_long", "reduce_long", "open_short", "add_short", "close_short", "reduce_short")`
       - `market_type` ∈ `("Forex", "USStock", "HShare")`
       - Assert: `IBKRClient._get_tif_for_signal(signal_type, market_type) == "IOC"`
       - Use distinct `ids` per row, e.g. `forex_open_long`, `usstock_reduce_short`, `hshare_close_long`.

    2. `TestTifForexPolicy`: rename and fix:
       - `test_uc_e1_usstock_open_uses_day` → `test_uc_e1_usstock_open_uses_ioc` — assert `"IOC"`.
       - `test_uc_e3_hshare_close_uses_day` → `test_uc_e3_hshare_close_uses_ioc` — assert `"IOC"`.

    3. `TestTifDay`: rename methods and asserts:
       - `test_market_order_sets_tif_day` → `test_market_order_sets_tif_ioc`; `placed_order.tif == "IOC"`.
       - `test_limit_order_sets_tif_day` → `test_limit_order_sets_tif_ioc`; `placed_order.tif == "IOC"`.
       - Update block comment (~771–776): Phase 14 policy — USStock uses IOC for default paths (unified with Forex/HShare); remove obsolete DAY/10349-only framing if incorrect.

    4. `TestPlaceMarketOrderForex`:
       - `test_uc_r1_usstock_open_long_uses_day_tif` → `test_uc_r1_usstock_open_long_uses_ioc_tif`; assert `placed_order.tif == "IOC"`.
       - `test_uc_r2_hshare_open_long_uses_day_tif` → `test_uc_r2_hshare_open_long_uses_ioc_tif`; assert `placed_order.tif == "IOC"`.

    5. Add a defensive test to `TestTifMatrix` for unknown `market_type`:
       - `test_unknown_market_returns_day`: `IBKRClient._get_tif_for_signal("open_long", "CryptoFuture") == "DAY"` — ensures unknown markets get conservative DAY fallback.

    6. `grep` within `test_ibkr_client.py` for `placed_order.tif == "DAY"` — must be **zero** matches after edits.
  </action>
  <acceptance_criteria>
    - `grep -qF "https://www.interactivebrokers.com/en/trading/order-type-exchanges.php?ot=ioc" backend_api_python/app/services/live_trading/ibkr_trading/client.py` exits 0.
    - `grep -qF "Hong Kong stocks do not support IOC" backend_api_python/app/services/live_trading/ibkr_trading/client.py` exits 1.
    - `grep -n "class TestTifMatrix" backend_api_python/tests/test_ibkr_client.py` exits 0.
    - `grep "placed_order.tif == \"DAY\"" backend_api_python/tests/test_ibkr_client.py` exits 1 (no matches).
    - `cd backend_api_python && python -m pytest -q tests/test_ibkr_client.py::TestTifMatrix --tb=line` exits 0 with **24 passed** in output (executor confirms substring `24 passed` or `24 passed in`).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest -q tests/test_ibkr_client.py::TestTifMatrix --tb=line && python -m pytest -q tests/test_ibkr_client.py -k "Tif" --tb=line && echo "--- FULL REGRESSION ---" && python -m pytest -q --tb=line</automated>
  </verify>
  <done>Production and tests updated atomically; TestTifMatrix 24/24 green; `-k Tif` green; full suite tail shows no failures.</done>

  <test_spec>
    **Primary (Task 1):**

    | # | Test node id (pattern) | Assertion |
    |---|------------------------|-----------|
    | 1–24 | `TestTifMatrix::test_tif_matrix[forex_open_long]` … `[hshare_reduce_short]` | `_get_tif_for_signal(...) == "IOC"` |

    **Parametrize grid (24 rows, all expected `"IOC"`):**

    | market_type \\ signal | open_long | add_long | close_long | reduce_long | open_short | add_short | close_short | reduce_short |
    |-----------------------|-----------|----------|------------|-------------|------------|-----------|-------------|--------------|
    | Forex | IOC | IOC | IOC | IOC | IOC | IOC | IOC | IOC |
    | USStock | IOC | IOC | IOC | IOC | IOC | IOC | IOC | IOC |
    | HShare | IOC | IOC | IOC | IOC | IOC | IOC | IOC | IOC |

    **Unknown market_type fallback:**

    | # | Test node id (pattern) | Assertion |
    |---|------------------------|-----------|
    | 25 | `TestTifMatrix::test_unknown_market_returns_day` | `_get_tif_for_signal("open_long", "CryptoFuture") == "DAY"` |

    **Renamed / updated tests:**

    - `TestTifForexPolicy::test_uc_e1_usstock_open_uses_ioc` → `"IOC"`.
    - `TestTifForexPolicy::test_uc_e2_usstock_close_uses_ioc` → unchanged expectation `"IOC"`.
    - `TestTifForexPolicy::test_uc_e3_hshare_close_uses_ioc` → `"IOC"`.
    - `TestTifForexPolicy::test_forex_signal_returns_ioc[uc_t1..uc_t8]` → `"IOC"` each.
    - `TestTifDay::test_market_order_sets_tif_ioc` — `MarketOrder` path `tif == "IOC"`.
    - `TestTifDay::test_limit_order_sets_tif_ioc` — `LimitOrder` path `tif == "IOC"`.
    - `TestPlaceMarketOrderForex::test_uc_r1_usstock_open_long_uses_ioc_tif` — `tif == "IOC"`.
    - `TestPlaceMarketOrderForex::test_uc_r2_hshare_open_long_uses_ioc_tif` — `tif == "IOC"`.

    **Full regression (every verify):** After target tests, run `python -m pytest -q --tb=line 2>&1 | tail -5` — last lines must show **0 failed** (total ~931 tests).
  </test_spec>
</task>

</tasks>

<verification>
- `cd backend_api_python && python -m pytest -q tests/test_ibkr_client.py::TestTifMatrix --tb=line` — 24 passed + 1 unknown-market test.
- `cd backend_api_python && python -m pytest -q tests/test_ibkr_client.py -k "Tif" --tb=line` — all passed.
- Full: `cd backend_api_python && python -m pytest -q --tb=line` — zero failures (~931 tests).
- `client.py` docstring contains IBKR IOC URL; does not contain "Hong Kong stocks do not support IOC".
</verification>

<success_criteria>
- INFRA-02: matrix + USStock open IOC + HShare IOC with documented SEHK/IBKR basis.
- Full backend suite green (~931 tests).
- `.planning/ROADMAP.md` already lists `14-01-PLAN.md` and IOC success criteria (from plan-phase).
</success_criteria>

<output>
After completion, create `.planning/phases/14-tif-unification-usstock-hshare/14-01-SUMMARY.md`
</output>
