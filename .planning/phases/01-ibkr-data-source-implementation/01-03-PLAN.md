---
phase: 01-ibkr-data-source-implementation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/app/data_sources/ibkr.py
autonomous: true
requirements:
  - IBKR-03
must_haves:
  truths:
    - "get_ticker() returns dict with 'last' price"
    - "No caching per D-20 - always fetch fresh data"
    - "Synchronous blocking call (per D-12)"
  artifacts:
    - path: "backend_api_python/app/data_sources/ibkr.py"
      provides: "get_ticker() method"
      contains: "def get_ticker"
  key_links:
    - from: "get_ticker"
      to: "IBKRClient"
      via: "async request + callback pattern"
      pattern: "reqMktData"
---

<objective>
实现 get_ticker() 方法获取实时报价

Purpose: 提供实时报价获取能力（无缓存）
Output: get_ticker() method in IBKRDataSource
</objective>

<context>
@backend_api_python/app/data_sources/base.py
@/home/workspace/ws/ibkr-datafetcher/src/ibkr_datafetcher/ibkr_client.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement get_ticker()</name>
  <files>backend_api_python/app/data_sources/ibkr.py</files>
  <behavior>
    - Test 1: get_ticker(symbol='AAPL') returns dict with 'last' key
    - Test 2: Returns {'last': 0, 'symbol': symbol} on error (consistent with factory)
    - Test 3: No cache lookup - always fetches fresh per D-20
  </behavior>
  <action>
Implement get_ticker method in IBKRDataSource:
- Ensure connection is established
- Use IBKRClient's request mechanism (use _pending_requests dict per D-18 for callback)
- Request market data for symbol
- Return {'last': price, 'symbol': symbol, ...} format
- NO caching per D-20 - direct call every time
- Handle errors gracefully, return fallback {'last': 0}
  </action>
  <verify>
    <automated>python -m pytest backend_api_python/tests/test_ibkr_datasource.py::test_get_ticker -v --tb=short 2>/dev/null || echo "Test not found"</automated>
  </verify>
  <done>
get_ticker returns {'last': float, 'symbol': str, ...}
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test get_ticker no cache</name>
  <files>backend_api_python/tests/test_ibkr_datasource.py</files>
  <behavior>
    - Test: get_ticker does NOT use cache per D-20 (verifies no cache lookup)
  </behavior>
  <action>
Add test_get_ticker_no_cache that verifies:
- No cache lookup occurs in get_ticker
- Each call triggers fresh IBKR request
- Multiple calls each trigger network request
  </action>
  <verify>
    <automated>python -m pytest backend_api_python/tests/test_ibkr_datasource.py::test_get_ticker_no_cache -v --tb=short</automated>
  </verify>
  <done>
No-cache test passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Strategy → IBKRDataSource.get_ticker | Internal data fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | S | Invalid symbol | mitigate | Catch errors, return fallback |
| T-03-02 | R | Rate limit from IBKR | mitigate | Add rate limiter per D-22 |
</threat_model>

<verification>
Unit tests for get_ticker pass
</verification>

<success_criteria>
get_ticker() returns real-time price without caching
</success_criteria>

<output>
After completion, create .planning/phases/01-ibkr-data-source-implementation/01-03-SUMMARY.md
</output>