---
phase: 01-ibkr-data-source-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/app/data_sources/ibkr.py
  - backend_api_python/tests/test_ibkr_data_source.py
  - backend_api_python/app/data_sources/rate_limiter.py
  - backend_api_python/tests/test_ibkr_rate_limiter.py
autonomous: true
requirements:
  - IBKR-01
  - IBKR-02
  - IBKR-03
  - IBKR-04
  - D-21
must_haves:
  truths:
    - "Strategy can fetch K-line data via IBKRDataSource.get_kline()"
    - "Strategy can fetch real-time quotes via IBKRDataSource.get_ticker()"
    - "IBKR connection is established on first use and reused"
    - "IBKR API calls are rate-limited to prevent quota exhaustion"
  artifacts:
    - path: "backend_api_python/app/data_sources/ibkr.py"
      provides: "IBKRDataSource implementation"
      exports: ["IBKRDataSource", "get_ibkr_limiter"]
    - path: "backend_api_python/tests/test_ibkr_data_source.py"
      provides: "Unit tests for IBKRDataSource"
    - path: "backend_api_python/app/data_sources/rate_limiter.py"
      provides: "IBKR rate limiter"
      adds: "IBKR-specific rate limiting"
  key_links:
    - from: "IBKRDataSource.get_kline"
      to: "IBKRClient.get_historical_bars"
      via: "ib_insync wrapper"
    - from: "IBKRDataSource.get_ticker"
      to: "IBKRClient.get_quote"
      via: "IBKRClient internal async"
    - from: "IBKRDataSource"
      to: "BaseDataSource"
      via: "class inheritance"
---

<objective>
Implement IBKR data source class that fetches K-lines and real-time quotes, inheriting from BaseDataSource. Also add IBKR-specific rate limiting.

Purpose: Replace yfinance/Finnhub for live trading with native IBKR data source, ensuring data consistency between trading and market data.
</objective>

<execution_context>
@/home/workspace/ws/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/workspace/ws/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend_api_python/app/data_sources/base.py
@backend_api_python/app/data_sources/factory.py
@home/workspace/ws/ibkr-datafetcher/src/ibkr_datafetcher/ibkr_client.py
@.planning/phases/01-ibkr-data-source-implementation/01-CONTEXT.md
@.planning/phases/01-ibkr-data-source-implementation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IBKRDataSource class with get_kline, get_ticker, and connection management</name>
  <files>backend_api_python/app/data_sources/ibkr.py</files>
  <action>
1. Create IBKRDataSource class extending BaseDataSource in backend_api_python/app/data_sources/ibkr.py

2. Implementation must include:
   - Lazy connection initialization (D-04, D-05): _get_client() connects on first use, reuses connection
   - get_kline() implementation: Convert symbol to IBKR contract, map timeframe to IBKR format, use IBKRClient.get_historical_bars, convert to BaseDataSource format
   - get_ticker() implementation: Call IBKR quote API, return dict with 'last' price (D-12: sync blocking)
   - disconnect() method (D-06): Close IBKR connection explicitly

3. Timeframe mapping (D-10): 1m, 5m, 15m, 30m, 1H, 4H, 1D
4. Symbol format (D-11): AAPL, MSFT, GOOGL (IBKR format)
5. Configuration (D-16): Use env vars IBKR_HOST, IBKR_PORT, IBKR_CLIENT_ID

4. Create test file backend_api_python/tests/test_ibkr_data_source.py with:
   - Test class creation
   - Test get_kline method
   - Test get_ticker method  
   - Test connection management (connect/disconnect)
   - Mock IBKRClient to avoid real IBKR connection
</action>
  <verify>
<automated>grep -n "class IBKRDataSource" backend_api_python/app/data_sources/ibkr.py && grep -n "def test_" backend_api_python/tests/test_ibkr_data_source.py</automated>
  </verify>
  <done>IBKRDataSource class created with get_kline, get_ticker, disconnect methods. Tests verify functionality without real IBKR connection.</done>
</task>

<task type="auto">
  <name>Task 2: Add IBKR rate limiter to rate_limiter.py</name>
  <files>backend_api_python/app/data_sources/rate_limiter.py</files>
  <action>
1. Add IBKR-specific rate limiter to backend_api_python/app/data_sources/rate_limiter.py

2. Implementation based on ibkr-datafetcher RateLimiter pattern:
   - hist_requests_per_minute: 6 (IBKR limit for historical data)
   - news_requests_per_minute: 3
   - identical_cooldown: 15.0 seconds
   - same_contract_limit: 6 requests per 2 minutes

3. Add get_ibkr_limiter() function returning singleton instance

4. Create test file backend_api_python/tests/test_ibkr_rate_limiter.py with:
   - Test limiter initialization
   - Test rate limiting behavior
   - Test cooldown mechanism
</action>
  <verify>
<automated>grep -n "IBKR\|ibkr" backend_api_python/app/data_sources/rate_limiter.py && grep -n "def test_" backend_api_python/tests/test_ibkr_rate_limiter.py</automated>
  </verify>
  <done>IBKR rate limiter added with proper configuration. Tests verify rate limiting behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→IBKR Gateway | Untrusted network, IBKR API credentials via Gateway |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01 | Denial of Service | IBKR API rate limit | mitigate | Use IBKR rate limiter (Task 2) with 6 RPM limit |
| T-02 | Resource Exhaustion | Connection leak | mitigate | Singleton pattern + explicit disconnect() method |
| T-03 | Tampering | Invalid symbol injection | mitigate | Validate symbol format before contract creation |
</threat_model>

<verification>
Run unit tests:
- pytest backend_api_python/tests/test_ibkr_data_source.py -x
- pytest backend_api_python/tests/test_ibkr_rate_limiter.py -x
</verification>

<success_criteria>
- IBKRDataSource class extends BaseDataSource
- get_kline returns list of dicts with time, open, high, low, close, volume
- get_ticker returns dict with 'last' key
- Connection established on first use, reused on subsequent calls
- disconnect() properly closes connection
- IBKR rate limiter prevents quota exhaustion
</success_criteria>

<output>
After completion, create `.planning/phases/01-ibkr-data-source-implementation/01-01-SUMMARY.md`
</output>