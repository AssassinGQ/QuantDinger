---
phase: 01-ibkr-data-source-implementation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/app/data_sources/ibkr.py
  - backend_api_python/app/data_sources/__init__.py
  - backend_api_python/app/data_sources/factory.py
autonomous: true
requirements:
  - IBKR-01
  - IBKR-02
  - IBKR-03
  - IBKR-04
  - INT-01
  - INT-02
  - INT-03
user_setup:
  - service: ibkr_gateway
    why: "IBKR data source requires IBKR Gateway running"
    env_vars:
      - name: IBKR_HOST
        source: "IBKR Gateway configuration (default: 127.0.0.1)"
      - name: IBKR_PORT
        source: "IBKR Gateway port (default: 7497 for simulated, 7496 for paper, 4002 for live)"
      - name: IBKR_CLIENT_ID
        source: "IBKR API client ID (default: 1)"

must_haves:
  truths:
    - "exchange_id='ibkr-live' strategies use IBKRDataSource for K-line data"
    - "exchange_id='ibkr-live' strategies use IBKRDataSource for ticker quotes"
    - "DataSourceFactory defaults work with backward-compatible market parameter"
  artifacts:
    - path: "backend_api_python/app/data_sources/ibkr.py"
      provides: "IBKRDataSource class implementing BaseDataSource interface"
      contains: "class IBKRDataSource"
    - path: "backend_api_python/app/data_sources/factory.py"
      provides: "DataSourceFactory with exchange_id support"
      contains: "def get_source.*exchange_id"
    - path: "backend_api_python/tests/test_ibkr_data_source.py"
      provides: "Unit tests for IBKRDataSource"
      contains: "test_get_kline"
  key_links:
    - from: "app/services/trading_executor.py"
      to: "app/data_sources/factory.py"
      via: "DataSourceFactory.get_source(market, exchange_id=exchange_id)"
      pattern: "get_source.*exchange_id"
    - from: "app/data_sources/factory.py"
      to: "app/data_sources/ibkr.py"
      via: "return IBKRDataSource()"
      pattern: "exchange_id.*ibkr-live"
---

<objective>
**Goal:** 实现 IBKRDataSource 并与 trading_executor 集成，确保 exchange_id='ibkr-live' 的策略使用 IBKR 原生数据源

**Purpose:** 为实盘策略提供与下单同一数据源，确保数据一致性

**Output:**
- IBKRDataSource class implementing get_kline() and get_ticker()
- Modified DataSourceFactory supporting exchange_id parameter
- Unit tests for IBKRDataSource
</objective>

<execution_context>
@/home/workspace/ws/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/workspace/ws/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/workspace/ws/QuantDinger/backend_api_python/app/data_sources/base.py
@/home/workspace/ws/QuantDinger/backend_api_python/app/data_sources/factory.py
@/home/workspace/ws/ibkr-datafetcher/src/ibkr_datafetcher/ibkr_client.py
@/home/workspace/ws/ibkr-datafetcher/src/ibkr_datafetcher/rate_limiter.py
@/home/workspace/ws/ibkr-datafetcher/src/ibkr_datafetcher/types.py
@/home/workspace/ws/QuantDinger/.planning/phases/01-ibkr-data-source-implementation/01-CONTEXT.md
@/home/workspace/ws/QuantDinger/.planning/phases/01-ibkr-data-source-implementation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IBKRDataSource class (IBKR-01, IBKR-02, IBKR-03, IBKR-04)</name>
  <files>backend_api_python/app/data_sources/ibkr.py</files>
  <read_first>
    - backend_api_python/app/data_sources/base.py
    - /home/workspace/ws/ibkr-datafetcher/src/ibkr_datafetcher/ibkr_client.py
    - /home/workspace/ws/ibkr-datafetcher/src/ibkr_datafetcher/types.py
    - /home/workspace/ws/ibkr-datafetcher/src/ibkr_datafetcher/rate_limiter.py
  </read_first>
  <action>
    Create IBKRDataSource class in `backend_api_python/app/data_sources/ibkr.py` with the following:

    1. **Imports**: Use ib_insync for IB connection, os for env vars, threading for sync wrapper
    2. **__init__**: 
       - Load config from env vars: IBKR_HOST (default "127.0.0.1"), IBKR_PORT (default 7497), IBKR_CLIENT_ID (default 1)
       - Initialize internal IBKRClient reference (ib_insync.IB wrapped with threading as in ibkr-datafetcher)
       - Create RateLimiter (hist_requests_per_minute=6, identical_cooldown=15.0 per ibkr-datafetcher)
       - Set _connected = False, _pending_requests = {}, _pending_ticker_data = {}
    3. **get_kline(symbol, timeframe, limit, before_time)**:
       - Call _ensure_connection() first
       - Use RateLimiter.acquire(request_type="hist", symbol=symbol)
       - Convert timeframe using Timeframe.resolve() from ibkr-datafetcher types module
       - Create Stock contract with symbol, sec_type="STK", exchange="SMART", currency="USD"
       - Use IB reqHistoricalDataAsync to get bars
       - Return formatted klines: [{"time": ts, "open": float, "high": float, "low": float, "close": float, "volume": float}]
    4. **get_ticker(symbol)**:
       - Call _ensure_connection() first
       - Use RateLimiter.acquire(request_type="ticker", symbol=symbol)
       - Use _get_ticker_sync(contract) with threading.Event for sync wrapper
       - Return: {"last": float, "symbol": str, "change": float, "changePercent": float, "high": float, "low": float, "open": float, "previousClose": float}
    5. **_ensure_connection()**: Retry 3 times with 5s delay on failure, raise ConnectionError if all fail
    6. **disconnect()**: Clean up IB connection, set _connected = False
    
    **Important**: Per D-08, exchange_id takes priority over market_category. Per D-04, reuse single IB connection instance.
  </action>
  <verify>
    <automated>python3 -c "from app.data_sources.ibkr import IBKRDataSource; ds = IBKRDataSource(); print('IBKRDataSource imported')"</automated>
  </verify>
  <done>
    IBKRDataSource class created in ibkr.py with working get_kline(), get_ticker(), _ensure_connection(), disconnect()
  </done>
</task>

<task type="auto">
  <name>Task 2: Add exchange_id parameter to DataSourceFactory (INT-01, INT-03)</name>
  <files>backend_api_python/app/data_sources/factory.py</files>
  <read_first>
    - backend_api_python/app/data_sources/factory.py
    - backend_api_python/app/data_sources/__init__.py
  </read_first>
  <action>
    Modify DataSourceFactory in `backend_api_python/app/data_sources/factory.py`:

    1. **Add optional exchange_id parameter** to get_source() method signature:
       ```python
       def get_source(cls, market: str = None, exchange_id: str = None) -> BaseDataSource:
       ```

    2. **Add exchange_id handling** at the start of get_source():
       - If exchange_id == 'ibkr-live': return IBKRDataSource (load lazily to avoid circular import)
       - This check MUST come BEFORE market-based selection per D-08: exchange_id优先级高于 market_category

    3. **Update get_kline() and get_ticker()** convenience methods to accept exchange_id:
       ```python
       def get_kline(cls, market: str = None, exchange_id: str = None, symbol: str = ..., ...)
       def get_ticker(cls, market: str = None, exchange_id: str = None, symbol: str = ...):
       ```
       - Pass exchange_id to get_source() call

    4. **Maintain backward compatibility**: If exchange_id is None, fall back to market-based selection (D-03)
  </action>
  <verify>
    <automated>python3 -c "
from app.data_sources.factory import DataSourceFactory
import inspect
sig = inspect.signature(DataSourceFactory.get_source)
params = list(sig.parameters.keys())
assert 'exchange_id' in params, f'exchange_id not in params: {params}'
print('DataSourceFactory.get_source has exchange_id parameter')
"</automated>
  </verify>
  <done>
    DataSourceFactory.get_source() accepts exchange_id, returns IBKRDataSource for exchange_id='ibkr-live', backward compatible
  </done>
</task>

<task type="auto">
  <name>Task 3: Create unit tests for IBKRDataSource (validation)</name>
  <files>backend_api_python/tests/test_ibkr_data_source.py</files>
  <read_first>
    - backend_api_python/tests/test_price_fetcher.py
  </read_first>
  <action>
    Create test file `backend_api_python/tests/test_ibkr_data_source.py`:

    1. **Test imports**:
       ```python
       import pytest
       from unittest.mock import Mock, MagicMock, patch, PropertyMock
       from app.data_sources.ibkr import IBKRDataSource
       ```

    2. **test_ibkr_datasource_init**: Test __init__ loads env vars with defaults

    3. **test_get_kline_returns_formatted_klines**: Mock IB connection, verify get_kline returns correct format 
       - Check: [{"time": int, "open": float, "high": float, "low": float, "close": float, "volume": float}]

    4. **test_get_ticker_returns_ticker_dict**: Mock IB reqMktData, verify get_ticker returns dict with 'last' key

    5. **test_connection_not_connected_raises**: Test _ensure_connection raises ConnectionError when IB fails

    6. **test_disconnect_cleans_up**: Test disconnect sets _connected = False

    7. **Use mock IBKR Gateway** from /home/workspace/ws/ibkr-datafetcher/mock_gateway/ for integration tests if available
  </action>
  <verify>
    <automated>python3 -m pytest backend_api_python/tests/test_ibkr_data_source.py -v --collect-only 2>/dev/null | head -20</automated>
  </verify>
  <done>
    Test file created with tests for all IBKRDataSource methods
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→IBKR Gateway | HTTP/TCP connection to IBKR Gateway with authentication |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01 | Denial of Service | IBKRDataSource.get_kline | mitigate | RateLimiter enforces 6 RPM, prevents IBKR rate limits |
| T-02 | Denial of Service | IBKRDataSource.get_ticker | mitigate | RateLimiter enforces 15s cooldown per symbol |
| T-03 | Information Disclosure | IBKR Gateway connection | accept | IBKR Gateway requires authentication, internal network only |
| T-04 | Elevation of Privilege | None | N/A | Not applicable - data source only |
</threat_model>

<verification>
- [ ] IBKRDataSource class exists in app/data_sources/ibkr.py
- [ ] IBKRDataSource inherits from BaseDataSource
- [ ] get_kline() returns formatted klines: [{"time": int, "open": float, "high": float, "low": float, "close": float, "volume": float}]
- [ ] get_ticker() returns dict with 'last' key
- [ ] DataSourceFactory.get_source() accepts exchange_id parameter
- [ ] DataSourceFactory.get_source(exchange_id='ibkr-live') returns IBKRDataSource instance
- [ ] Tests run without errors (may skip if IBKR Gateway not running)
</verification>

<success_criteria>
- [ ] exchange_id='ibkr-live' strategies use IBKRDataSource for K-line data
- [ ] exchange_id='ibkr-live' strategies use IBKRDataSource for ticker quotes
- [ ] DataSourceFactory defaults work with backward-compatible market parameter
- [ ] All requirement IDs covered: IBKR-01, IBKR-02, IBKR-03, IBKR-04, INT-01, INT-02, INT-03
- [ ] Task actions reference decision IDs: D-01, D-02, D-03, D-04, D-05, D-08
</success_criteria>

<output>
After completion, create `.planning/phases/01-ibkr-data-source-implementation/{phase}-01-SUMMARY.md`
</output>