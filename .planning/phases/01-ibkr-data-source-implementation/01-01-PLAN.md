---
phase: 01-ibkr-data-source-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/app/data_sources/ibkr.py
  - backend_api_python/app/data_sources/__init__.py
autonomous: true
requirements:
  - IBKR-01
  - IBKR-04
must_haves:
  truths:
    - "IBKRDataSource class exists and can be instantiated"
    - "Connection to IBKR Gateway can be established"
    - "Connection is reusable across multiple calls"
    - "Auto-reconnect works after connection failure"
  artifacts:
    - path: "backend_api_python/app/data_sources/ibkr.py"
      provides: "IBKRDataSource class with connection management"
      contains: "class IBKRDataSource"
    - path: "backend_api_python/app/data_sources/__init__.py"
      provides: "Export IBKRDataSource"
  key_links:
    - from: "IBKRDataSource"
      to: "BaseDataSource"
      via: "inheritance"
      pattern: "class IBKRDataSource\\(BaseDataSource\\)"
    - from: "IBKRDataSource"
      to: "IBKRClient (ibkr-datafetcher)"
      via: "import and instantiate"
      pattern: "from ibkr_datafetcher"
---

<objective>
创建 IBKRDataSource 类，继承 BaseDataSource，实现连接管理。

Purpose: 为 exchange_id="ibkr-live" 提供数据源基础
Output: IBKRDataSource 类，可连接 IBKR Gateway
</objective>

<context>
@backend_api_python/app/data_sources/base.py
@/home/workspace/ws/ibkr-datafetcher/src/ibkr_datafetcher/ibkr_client.py
@/home/workspace/ws/ibkr-datafetcher/src/ibkr_datafetcher/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IBKRDataSource class</name>
  <files>backend_api_python/app/data_sources/ibkr.py</files>
  <action>
Create IBKRDataSource class inheriting from BaseDataSource with:
- Import IBKRClient and GatewayConfig from ibkr-datafetcher
- Config loading from environment variables (IBKR_HOST, IBKR_PORT, IBKR_CLIENT_ID)
- Member variable _client: Optional[IBKRClient] = None
- Member variable _pending_requests: Dict[int, Any] = {} per D-18
- connect() method that establishes connection lazily
- is_connected() method that checks connection status
- disconnect() method per D-06
- reconnect(max_retries=3) method per D-17
  </action>
  <verify>
    <automated>python -c "from app.data_sources.ibkr import IBKRDataSource; print('Import OK')"</automated>
  </verify>
  <done>
IBKRDataSource class created with connect(), is_connected(), disconnect(), reconnect() methods
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test connection management</name>
  <files>backend_api_python/tests/test_ibkr_datasource.py</files>
  <behavior>
    - Test 1: IBKRDataSource can be instantiated with default config
    - Test 2: is_connected() returns False before connection
    - Test 3: reconnect triggers after disconnect
  </behavior>
  <action>
Write test file with:
- Mock IBKRClient for testing without real IBKR Gateway
- Test instantiation
- Test connection state checks
- Test reconnect logic
  </action>
  <verify>
    <automated>python -m pytest backend_api_python/tests/test_ibkr_datasource.py -v --tb=short</automated>
  </verify>
  <done>
Tests pass: instantiation, is_connected(), reconnect behavior
  </done>
</task>

<task type="auto">
  <name>Task 3: Update data_sources __init__</name>
  <files>backend_api_python/app/data_sources/__init__.py</files>
  <action>
Add export for IBKRDataSource:
from app.data_sources.ibkr import IBKRDataSource
  </action>
  <verify>
    <automated>python -c "from app.data_sources import IBKRDataSource; print('Export OK')"</automated>
  </verify>
  <done>
IBKRDataSource can be imported from app.data_sources
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IBKR Gateway → Python | External connection via network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | S | IBKR Gateway connection | mitigate | Validate config before connect, handle ConnectionError |
| T-01-02 | D | Connection reuse | accept | Multiple calls use same connection instance |
| T-01-03 | R | Reconnect loop | mitigate | Max retries (3) prevents infinite loop |
</threat_model>

<verification>
Unit tests for connection management pass
</verification>

<success_criteria>
IBKRDataSource can connect to IBKR Gateway (mock), reconnect on failure, and disconnect properly
</success_criteria>

<output>
After completion, create .planning/phases/01-ibkr-data-source-implementation/01-01-SUMMARY.md
</output>