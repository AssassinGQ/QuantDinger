---
phase: 01-ibkr-data-source-implementation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/app/services/live_trading/ibkr_trading/client.py
  - backend_api_python/app/data_sources/ibkr.py
  - backend_api_python/tests/test_ibkr_client.py
autonomous: true
requirements:
  - INT-01
  - INT-02
  - INT-03

must_haves:
  truths:
    - "Internal IBKRClient has get_historical_bars() method with correct signature"
    - "Internal IBKRClient get_quote() returns dict format"
    - "IBKRDataSource uses internal IBKRClient via get_ibkr_client() singleton"
    - "No ibkr_datafetcher external imports in IBKRDataSource"
  artifacts:
    - path: "backend_api_python/app/services/live_trading/ibkr_trading/client.py"
      provides: "get_historical_bars() method implementation"
      min_lines: 50
      test_specs: [TC-40, TC-41, TC-42, TC-43]
    - path: "backend_api_python/app/data_sources/ibkr.py"
      provides: "IBKRDataSource using internal client"
      contains: "get_ibkr_client"
      test_specs: [TC-50, TC-51, TC-55, TC-56, TC-57, TC-58]
  key_links:
    - from: "IBKRDataSource.get_kline()"
      to: "IBKRClient.get_historical_bars()"
      via: "self._client.get_historical_bars()"
      test_specs: [TC-57]
    - from: "IBKRDataSource.get_ticker()"
      to: "IBKRClient.get_quote()"
      via: "self._client.get_quote()"
      test_specs: [TC-58]

user_setup: []
---

<objective>
Add get_historical_bars() method to internal IBKRClient and migrate IBKRDataSource to use internal client instead of external ibkr_datafetcher library.
</objective>

<context>
@/home/workspace/ws/QuantDinger/backend_api_python/app/data_sources/base.py
@/home/workspace/ws/QuantDinger/.planning/v2.0-test-cases.md
@/home/workspace/ws/QuantDinger/.planning/phases/01-ibkr-data-source-implementation/01-CONTEXT.md
@/home/workspace/ws/QuantDinger/backend_api_python/app/data_sources/ibkr.py
@/home/workspace/ws/QuantDinger/backend_api_python/app/services/live_trading/ibkr_trading/client.py

**Internal IBKRClient methods (existing in client.py):**
- `get_quote(symbol: str, market_type: str = "USStock") -> Dict[str, Any]` (line 1297)
- `_create_contract(symbol: str, market_type: str)` (line 780)
- `_qualify_contract_async(contract)` (line 785)
- `get_ibkr_client(config, mode) -> IBKRClient` singleton (line 1349)
- `IBKRConfig` class (line 48)

**Required new method:**
- `get_historical_bars(symbol, timeframe, limit, before_time=None) -> List[Dict]`

**Key locked decisions (D-28 to D-36):**
- D-28: Signature must match BaseDataSource.get_kline(): (symbol, timeframe, limit, before_time=None)
- D-29: Return List[Dict] format: [{"time": int, "open": float, "high": float, "low": float, "close": float, "volume": float}]
- D-30: get_kline exception → logger.error → return []
- D-31: get_ticker exception → logger.warning → return {last: 0, symbol: symbol}
- D-35: get_ticker_price(contract) → get_quote(symbol, market_type)
- D-36: make_contract(SymbolConfig) → _create_contract(symbol, market_type)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add get_historical_bars() method to internal IBKRClient</name>
  <files>backend_api_python/app/services/live_trading/ibkr_trading/client.py</files>
  <read_first>
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py (lines 1295-1330 for get_quote reference)
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py (lines 1345-1370 for get_ibkr_client singleton)
  </read_first>
  <behavior>
    - TC-40: get_historical_bars(symbol=AAPL, timeframe=1m, limit=100) returns List[Dict] with 6 keys per bar
    - TC-41: get_historical_bars works with different timeframes: 5m, 15m, 1H
    - TC-42: get_historical_bars respects before_time filter (returns only bars before timestamp)
    - TC-43: get_historical_bars returns [] on error (contract invalid, connection failed)
  </behavior>
  <action>
Add get_historical_bars() method to the IBKRClient class after get_quote() method (around line 1325):

```python
def get_historical_bars(
    self,
    symbol: str,
    timeframe: str,
    limit: int,
    before_time: Optional[int] = None
) -> List[Dict[str, Any]]:
    """获取历史K线数据
    
    Args:
        symbol: 股票代码 (例如: AAPL, MSFT)
        timeframe: 时间周期 (1m, 5m, 15m, 30m, 1H, 4H, 1D)
        limit: 数据条数
        before_time: 获取此时间之前的数据（Unix时间戳）
        
    Returns:
        K线数据列表，格式:
        [{"time": int, "open": float, "high": float, "low": float, "close": float, "volume": float}, ...]
    """
    import asyncio as _aio
    from datetime import datetime

    # Timeframe to IBKR duration mapping
    DURATION_MAP = {
        '1m': '1 D', '5m': '5 D', '15m': '15 D', 
        '30m': '1 M', '1H': '1 M', '4H': '1 M', '1D': '1 Y'
    }
    BAR_SIZE_MAP = {
        '1m': '1 secs', '5m': '5 secs', '15m': '15 secs',
        '30m': '30 secs', '1H': '1 min', '4H': '4 hours', '1D': '1 day'
    }

    async def _task():
        await self._ensure_connected_async()
        
        # Create contract using internal method (D-36)
        contract = self._create_contract(symbol, "USStock")
        if not await self._qualify_contract_async(contract):
            logger.warning(f"Contract qualification failed for {symbol}")
            return []
        
        # Determine duration based on timeframe
        duration_str = DURATION_MAP.get(timeframe, '1 D')
        bar_size = BAR_SIZE_MAP.get(timeframe, '1 secs')
        
        # Get historical bars from IBKR
        bars = await self._ib.reqHistoricalBarsAsync(
            contract=contract,
            endDateTime="",
            durationStr=duration_str,
            barSize=bar_size,
            whatToShow='TRADES',
            useRTH=False,
            formatDate=1,
            keepUpToDate=False,
        )
        
        # Convert to dict format per D-29
        result: List[Dict[str, Any]] = []
        for bar in bars:
            result.append({
                'time': int(bar.date.timestamp()),
                'open': float(bar.open),
                'high': float(bar.high),
                'low': float(bar.low),
                'close': float(bar.close),
                'volume': float(bar.volume),
            })
        
        # Sort by time ascending
        result.sort(key=lambda x: x['time'])
        
        # Apply before_time filter (TC-42)
        if before_time is not None:
            result = [b for b in result if b['time'] < before_time]
        
        # Apply limit
        if len(result) > limit:
            result = result[-limit:]
        
        return result
    
    try:
        return self._submit(_task(), timeout=30.0) or []
    except Exception as e:
        logger.error(f"Failed to get historical bars for {symbol}: {e}")
        return []
```

This method:
1. Uses TaskQueue pattern via _submit() like existing get_quote()
2. Uses _create_contract() per D-36 instead of external make_contract()
3. Returns List[Dict] format per D-29
4. Returns [] on error per D-30
  </action>
  <verify>
<automated>grep -n "def get_historical_bars" backend_api_python/app/services/live_trading/ibkr_trading/client.py</automated>
  </verify>
  <done>Method get_historical_bars exists in internal IBKRClient with signature (symbol, timeframe, limit, before_time=None) -> List[Dict]</done>
  <test_specs>
    - TC-40: Basic get_historical_bars returns List[Dict] with correct format
    - TC-41: Different timeframes (5m/15m/1H) work correctly
    - TC-42: before_time filtering works
    - TC-43: Error handling returns empty list
  </test_specs>
</task>

<task type="auto">
  <name>Task 2: Migrate IBKRDataSource to use internal IBKRClient</name>
  <files>backend_api_python/app/data_sources/ibkr.py</files>
  <read_first>
    - backend_api_python/app/data_sources/ibkr.py (current v1.0 implementation using ibkr_datafetcher)
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py (lines 48-55 for IBKRConfig, 1349-1370 for get_ibkr_client)
  </read_first>
  <action>
Replace IBKRDataSource to use internal IBKRClient instead of external ibkr_datafetcher:

**1. Remove external imports (lines 9-11):**
```python
# REMOVE THESE:
from ibkr_datafetcher.config import GatewayConfig
from ibkr_datafetcher.ibkr_client import IBKRClient
from ibkr_datafetcher.types import KlineBar, SymbolConfig, Timeframe, resolve_timeframe
```

**2. Add internal imports:**
```python
from app.services.live_trading.ibkr_trading.client import get_ibkr_client, IBKRConfig
```

**3. Modify __init__:**
```python
def __init__(
    self,
    mode: Optional[str] = None,  # paper/live
):
    self._mode = mode or os.environ.get("IBKR_MODE", "paper")
    self._market_type = "USStock"
    self._client: Optional[Any] = None
    self._rate_limiter = get_ibkr_limiter()
```

**4. Replace client property:**
```python
@property
def client(self):
    """Get or create internal IBKRClient instance"""
    if self._client is None:
        cfg = IBKRConfig.from_env(self._mode)
        self._client = get_ibkr_client(cfg, mode=self._mode)
    return self._client
```

**5. Replace get_kline() method:**
- Remove SymbolConfig, make_contract, resolve_timeframe
- Replace with direct internal method call:
```python
bars = self.client.get_historical_bars(symbol, timeframe, limit, before_time)
# bars already in dict format per D-34
return self.filter_and_limit(bars, limit, before_time)
```

**6. Replace get_ticker() method:**
- Remove SymbolConfig, make_contract, qualify_contract, get_ticker_price
- Replace with (per D-35):
```python
quote = self.client.get_quote(symbol, self._market_type)
return {
    "symbol": symbol,
    "last": quote.get("last", 0) if quote.get("success", False) else 0,
}
```

**7. Update connection methods:**
- is_connected() → return self.client.connected
- connect() → uses internal client (already connected via singleton)
- disconnect() → uses internal client shutdown

**Per D-30, D-31 error handling:**
- get_kline exceptions → logger.error → return []
- get_ticker exceptions → logger.warning → return {last: 0, symbol: symbol}
  </action>
  <verify>
<automated>
echo "=== Check no ibkr_datafetcher imports ===" && \
grep -n "ibkr_datafetcher" backend_api_python/app/data_sources/ibkr.py || echo "PASS: No ibkr_datafetcher imports" && \
echo "=== Check get_ibkr_client usage ===" && \
grep -n "get_ibkr_client" backend_api_python/app/data_sources/ibkr.py && \
echo "=== Check get_historical_bars call ===" && \
grep -n "get_historical_bars" backend_api_python/app/data_sources/ibkr.py && \
echo "=== Check get_quote call ===" && \
grep -n "get_quote" backend_api_python/app/data_sources/ibkr.py
</automated>
  </verify>
  <done>IBKRDataSource uses internal IBKRClient via get_ibkr_client() singleton, no external ibkr_datafetcher imports</done>
  <test_specs>
    - TC-50: Uses get_ibkr_client() for internal client
    - TC-51: No ibkr_datafetcher imports
    - TC-55: Uses _create_contract() instead of make_contract()
    - TC-56: Uses _qualify_contract_async() (in internal client)
    - TC-57: get_kline calls get_historical_bars()
    - TC-58: get_ticker uses get_quote() instead of get_ticker_price()
  </test_specs>
</task>

<task type="auto">
  <name>Task 3: Add unit tests for get_historical_bars</name>
  <files>backend_api_python/tests/test_ibkr_client.py</files>
  <read_first>
    - backend_api_python/tests/test_ibkr_client.py (existing test patterns)
  </read_first>
  <action>
Add test cases for get_historical_bars method to test_ibkr_client.py:

```python
class TestGetHistoricalBars:
    """TC-40 ~ TC-43: Tests for get_historical_bars() method"""
    
    def test_basic_historical_bars(self, mock_ib_client):
        """TC-40: get_historical_bars returns List[Dict] with correct format"""
        # Setup mock bars with proper attributes
        mock_bar = Mock()
        mock_bar.date = datetime(2024, 1, 1, 12, 0)
        mock_bar.open = 150.0
        mock_bar.high = 152.0
        mock_bar.low = 149.0
        mock_bar.close = 151.0
        mock_bar.volume = 1000000
        
        mock_ib_client.reqHistoricalBarsAsync = AsyncMock(return_value=[mock_bar])
        
        client = IBKRClient(...)
        result = client.get_historical_bars("AAPL", "1m", 100)
        
        assert isinstance(result, list)
        assert len(result) > 0
        assert "time" in result[0]
        assert "open" in result[0]
        assert "high" in result[0]
        assert "low" in result[0]
        assert "close" in result[0]
        assert "volume" in result[0]
    
    def test_different_timeframes(self, mock_ib_client):
        """TC-41: Different timeframes (5m, 15m, 1H) work correctly"""
        for timeframe in ['5m', '15m', '1H']:
            result = client.get_historical_bars("AAPL", timeframe, 100)
            # Verify duration mapping applied
    
    def test_before_time_filter(self, mock_ib_client):
        """TC-42: before_time filtering works correctly"""
        before_ts = 1704067200  # 2024-01-01
        result = client.get_historical_bars("AAPL", "1m", 100, before_time=before_ts)
        
        # All bars should have time < before_ts
        for bar in result:
            assert bar["time"] < before_ts
    
    def test_error_handling(self, mock_ib_client):
        """TC-43: Error handling returns empty list"""
        mock_ib_client.reqHistoricalBarsAsync = AsyncMock(side_effect=Exception("Test error"))
        
        result = client.get_historical_bars("INVALID", "1m", 100)
        
        assert result == []
```

Add corresponding pytest fixtures for mocking IBKR client.
  </action>
  <verify>
<automated>grep -n "def test.*historical_bars\|TC-40\|TC-41\|TC-42\|TC-43" backend_api_python/tests/test_ibkr_client.py</automated>
  </verify>
  <done>Unit tests for get_historical_bars with TC-40~TC-43 coverage</done>
  <test_specs>
    - TC-40: Basic functionality test
    - TC-41: Timeframe compatibility test
    - TC-42: before_time filter test
    - TC-43: Error handling test
  </test_specs>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IBKR Gateway → Internal Client | IBKR API connection (untrusted network) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01 | Denial of Service | get_historical_bars | mitigate | Rate limiter already in place (per D-21), timeout=30s |
| T-02 | Information Disclosure | Contract data | accept | IBKR contract data is non-sensitive market data |
</threat_model>

<verification>
[Overall phase verification]
1. grep -n "def get_historical_bars" backend_api_python/app/services/live_trading/ibkr_trading/client.py
2. grep -n "ibkr_datafetcher" backend_api_python/app/data_sources/ibkr.py || echo "PASS: No external imports"
3. grep -n "get_ibkr_client" backend_api_python/app/data_sources/ibkr.py
4. grep -n "get_quote" backend_api_python/app/data_sources/ibkr.py
</verification>

<success_criteria>
1. Internal IBKRClient has get_historical_bars() method with signature (symbol, timeframe, limit, before_time=None) -> List[Dict]
2. IBKRDataSource imports only from internal IBKRClient, no external ibkr_datafetcher imports
3. IBKRDataSource.get_kline() calls internal get_historical_bars() (TC-57)
4. IBKRDataSource.get_ticker() calls internal get_quote() (TC-58)
5. Unit tests exist for TC-40~TC-43 (get_historical_bars)
6. All imports resolved correctly - no import errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-ibkr-data-source-implementation/01-01-SUMMARY.md`
</output>
