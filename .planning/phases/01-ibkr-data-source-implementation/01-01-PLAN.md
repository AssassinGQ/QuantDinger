---
phase: 01-ibkr-data-source-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/app/data_sources/ibkr.py
  - backend_api_python/app/data_sources/rate_limiter.py
  - backend_api_python/app/data_sources/__init__.py
  - backend_api_python/tests/test_ibkr_data_source.py
autonomous: true
requirements:
  - IBKR-01
  - IBKR-02
  - IBKR-03
  - IBKR-04
  - INT-01
  - INT-03
user_setup: []
must_haves:
  truths:
    - "exchange_id='ibkr-live' automatically returns IBKRDataSource"
    - "get_kline() returns formatted kline data from IBKR"
    - "get_ticker() returns real-time quote from IBKR"
    - "Connection is lazy-initialized and reusable"
  artifacts:
    - path: "backend_api_python/app/data_sources/ibkr.py"
      provides: "IBKRDataSource implementation"
      contains: "class IBKRDataSource"
    - path: "backend_api_python/app/data_sources/rate_limiter.py"
      provides: "IBKR rate limiter"
      contains: "get_ibkr_limiter"
  key_links:
    - from: "backend_api_python/app/data_sources/ibkr.py"
      to: "base.py"
      via: "class IBKRDataSource(BaseDataSource)"
      pattern: "class IBKRDataSource.*BaseDataSource"
---

<objective>
创建 IBKRDataSource 类，实现 K线获取和实时报价功能，并扩展 DataSourceFactory 支持 exchange_id 参数。

Purpose: 为 exchange_id='ibkr-live' 的策略提供原生 IBKR 数据源
Output: IBKRDataSource 类 + 更新的 DataSourceFactory
</objective>

<execution_context>
@/home/workspace/ws/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/workspace/ws/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/workspace/ws/QuantDinger/backend_api_python/app/data_sources/base.py
@/home/workspace/ws/QuantDinger/backend_api_python/app/data_sources/factory.py
@/home/workspace/ws/ibkr-datafetcher/src/ibkr_datafetcher/ibkr_client.py
@/home/workspace/ws/ibkr-datafetcher/src/ibkr_datafetcher/rate_limiter.py
@/home/workspace/ws/QuantDinger/backend_api_python/app/services/price_fetcher.py
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From app/data_sources/base.py:
```python
class BaseDataSource(ABC):
    name: str = "base"
    
    @abstractmethod
    def get_kline(
        self,
        symbol: str,
        timeframe: str,
        limit: int,
        before_time: Optional[int] = None
    ) -> List[Dict[str, Any]]:
        pass

    def get_ticker(self, symbol: str) -> Dict[str, Any]:
        raise NotImplementedError("get_ticker is not implemented for this data source")
```

From app/data_sources/factory.py:
```python
class DataSourceFactory:
    _sources: Dict[str, BaseDataSource] = {}
    
    @classmethod
    def get_source(cls, market: str) -> BaseDataSource:
        # needs extension to accept exchange_id
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 IBKRDataSource 类 (IBKR-01)</name>
  <files>
    backend_api_python/app/data_sources/ibkr.py
  </files>
  <read_first>
    /home/workspace/ws/QuantDinger/backend_api_python/app/data_sources/base.py
    /home/workspace/ws/ibkr-datafetcher/src/ibkr_datafetcher/ibkr_client.py
  </read_first>
  <behavior>
    - IBKRDataSource 继承 BaseDataSource
    - name = "ibkr"
    - __init__ 初始化 _client = None (lazy init per D-05)
    - _get_client() 方法在首次使用时建立连接
    - disconnect() 方法供外部调用 (D-06)
    - reconnect() 方法实现自动重连 (D-17)
  </behavior>
  <action>
创建 backend_api_python/app/data_sources/ibkr.py，实现:

1. 导入所需模块:
   - from ib_insync import Stock, Forex, Future, Index
   - 导入 BaseDataSource, TIMEFRAME_SECONDS 从 base
   - 导入 IBKRClient 参考实现 (可复制简化版本或直接使用)
   - 导入 GatewayConfig 从参考实现

2. IBKRDataSource 类:
```python
class IBKRDataSource(BaseDataSource):
    name = "ibkr"
    
    def __init__(self):
        self._client = None
        self._connected = False
        self._client_lock = threading.Lock()
```

3. _get_client() 方法:
   - 使用 threading.Lock 确保线程安全
   - 首次调用时创建 IBKRClient 并连接
   - 连接参数从环境变量: IBKR_HOST, IBKR_PORT, IBKR_CLIENT_ID

4. disconnect() 方法:
   - 断开 IBKR 连接
   - 设置 _client = None

5. reconnect() 方法:
   - 调用 disconnect()
   - 重新连接

6. is_connected() 属性返回连接状态
  </action>
  <verify>
    <automated>grep -n "class IBKRDataSource" /home/workspace/ws/QuantDinger/backend_api_python/app/data_sources/ibkr.py</automated>
  </verify>
  <done>IBKRDataSource 类已创建，包含 __init__, _get_client, disconnect, reconnect 方法</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 get_kline() 方法 (IBKR-02)</name>
  <files>
    backend_api_python/app/data_sources/ibkr.py
  </files>
  <read_first>
    /home/workspace/ws/QuantDinger/backend_api_python/app/data_sources/ibkr.py
  </read_first>
  <behavior>
    - get_kline(symbol, timeframe, limit, before_time) 返回 List[Dict]
    - 格式: [{"time": int, "open": float, "high": float, "low": float, "close": float, "volume": float}]
    - 支持 timeframe: 1m, 5m, 15m, 30m, 1H, 4H, 1D (D-10)
    - 使用 IBKRClient.get_historical_bars() 获取数据
  </behavior>
  <action>
在 IBKRDataSource 中添加:

1. _make_contract() 方法:
   - 根据 symbol 创建 IBKR contract
   - 使用 Stock 类型，默认 exchange="SMART", currency="USD" (美股)
   - 支持 symbol 格式: AAPL, MSFT, GOOGL (D-11)

2. _map_timeframe() 方法:
   - 映射 QuantDinger timeframe 到 IBKR 格式
   - 1m -> "1 min", 5m -> "5 mins", 15m -> "15 mins"
   - 30m -> "30 mins", 1H -> "1 hour", 4H -> "4 hours", 1D -> "1 day"

3. get_kline() 方法实现:
   - 调用 _get_client() 确保连接
   - 调用 _make_contract() 创建合约
   - 调用 client.get_historical_bars() 获取数据
   - 使用 format_kline() 格式化每条数据
   - 按时间排序返回

4. 缓存策略 (D-19, D-23):
   - get_kline 复用 kline_fetcher 逻辑（已有数据库缓存）
   - 调用 kline_fetcher.get_kline 处理缓存
  </action>
  <verify>
    <automated>grep -n "def get_kline" /home/workspace/ws/QuantDinger/backend_api_python/app/data_sources/ibkr.py</automated>
  </verify>
  <done>get_kline() 方法返回正确格式的 K线数据</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 实现 get_ticker() 方法 (IBKR-03)</name>
  <files>
    backend_api_python/app/data_sources/ibkr.py
  </files>
  <read_first>
    /home/workspace/ws/QuantDinger/backend_api_python/app/data_sources/ibkr.py
    /home/workspace/ws/ibkr-datafetcher/src/ibkr_datafetcher/ibkr_client.py
  </read_first>
  <behavior>
    - get_ticker(symbol) 返回 Dict[str, Any]
    - 同步阻塞调用 (D-12)
    - 无缓存 (D-20)
    - 使用 IBKRClient.get_quote() 或类似方法
  </behavior>
  <action>
在 IBKRDataSource 中添加 get_ticker() 方法:

1. 使用 IBKRClient.reqMktData() 获取实时报价
2. 同步阻塞等待结果 (通过 _pending_requests + request_id 模式, D-18)
3. 返回格式: {"last": float, "symbol": str, "bid": float, "ask": float, ...}

注意: 由于 IBKRClient 没有现成的 get_quote 方法，需要:
- 使用 _pending_requests 字典存储 pending 请求
- 调用 ib.reqMktData() 
- 通过 callback 回调设置结果
- 使用 thread-safe 的方式等待结果
  </action>
  <verify>
    <automated>grep -n "def get_ticker" /home/workspace/ws/QuantDinger/backend_api_python/app/data_sources/ibkr.py</automated>
  </verify>
  <done>get_ticker() 方法返回实时报价</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: 添加 IBKR 限流器 (D-21, D-22)</name>
  <files>
    backend_api_python/app/data_sources/rate_limiter.py
  </files>
  <read_first>
    /home/workspace/ws/QuantDinger/backend_api_python/app/data_sources/rate_limiter.py
    /home/workspace/ws/ibkr-datafetcher/src/ibkr_datafetcher/rate_limiter.py
  </read_first>
  <behavior>
    - 复用 ibkr-datafetcher 的 RateLimiter 逻辑
    - hist_requests_per_minute=6 (IBKR 限制)
    - 对 get_ticker 添加限流保护
  </behavior>
  <action>
在 backend_api_python/app/data_sources/rate_limiter.py 中添加:

1. 复制 RateLimiter 类从 ibkr-datafetcher 参考实现:
   - hist_requests_per_minute: int = 6
   - news_requests_per_minute: int = 3
   - identical_cooldown: float = 15.0
   - same_contract_limit: int = 6
   - same_contract_window: float = 2.0

2. 添加 get_ibkr_limiter() 函数:
```python
_ibkr_limiter = RateLimiter(
    hist_requests_per_minute=6,
    news_requests_per_minute=3,
    identical_cooldown=15.0,
    same_contract_limit=6,
    same_contract_window=2.0
)

def get_ibkr_limiter() -> RateLimiter:
    """获取 IBKR 限流器"""
    return _ibkr_limiter
```

3. 在 __init__.py 中导出 get_ibkr_limiter
  </action>
  <verify>
    <automated>grep -n "def get_ibkr_limiter" /home/workspace/ws/QuantDinger/backend_api_python/app/data_sources/rate_limiter.py</automated>
  </verify>
  <done>IBKR 限流器已添加并导出</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: 扩展 DataSourceFactory 支持 exchange_id (INT-01, INT-03)</name>
  <files>
    backend_api_python/app/data_sources/factory.py
  </files>
  <read_first>
    /home/workspace/ws/QuantDinger/backend_api_python/app/data_sources/factory.py
  </read_first>
  <behavior>
    - get_source() 添加可选 exchange_id 参数 (D-01)
    - exchange_id='ibkr-live' 返回 IBKRDataSource (D-02, D-08)
    - 不传 exchange_id 时按 market 参数处理 (D-03 向后兼容)
  </behavior>
  <action>
修改 DataSourceFactory.get_source() 方法:

1. 添加 Optional 导入: from typing import Optional

2. 修改 get_source 签名:
```python
@classmethod
def get_source(cls, market: str, exchange_id: Optional[str] = None) -> BaseDataSource:
```

3. 实现 exchange_id 优先逻辑:
```python
# D-08: exchange_id 优先级高于 market_category
if exchange_id == 'ibkr-live':
    if 'ibkr' not in cls._sources:
        from app.data_sources.ibkr import IBKRDataSource
        cls._sources['ibkr'] = IBKRDataSource()
    return cls._sources['ibkr']

# D-03: 向后兼容 - 不传 exchange_id 时按 market 处理
if market not in cls._sources:
    cls._sources[market] = cls._create_source(market)
return cls._sources[market]
```

4. 同时更新 get_kline() 和 get_ticker() 方法添加 exchange_id 参数
  </action>
  <verify>
    <automated>grep -n "exchange_id" /home/workspace/ws/QuantDinger/backend_api_python/app/data_sources/factory.py</automated>
  </verify>
  <done>DataSourceFactory 支持 exchange_id 参数，exchange_id='ibkr-live' 返回 IBKRDataSource</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: 创建单元测试</name>
  <files>
    backend_api_python/tests/test_ibkr_data_source.py
  </files>
  <read_first>
    /home/workspace/ws/QuantDinger/backend_api_python/app/data_sources/ibkr.py
  </read_first>
  <behavior>
    - 测试 IBKRDataSource 类创建
    - 测试 get_kline 格式
    - 测试 get_ticker 返回
    - 测试连接管理
  </behavior>
  <action>
创建 backend_api_python/tests/test_ibkr_data_source.py:

```python
import pytest
from unittest.mock import Mock, patch, MagicMock

class TestIBKRDataSource:
    """IBKRDataSource 单元测试"""
    
    def test_class_creation(self):
        """测试 IBKRDataSource 类创建 (IBKR-01)"""
        from app.data_sources.ibkr import IBKRDataSource
        ds = IBKRDataSource()
        assert ds.name == "ibkr"
        assert ds._client is None
    
    def test_get_kline_signature(self):
        """测试 get_kline 方法签名 (IBKR-02)"""
        from app.data_sources.ibkr import IBKRDataSource
        import inspect
        sig = inspect.signature(IBKRDataSource.get_kline)
        assert 'symbol' in sig.parameters
        assert 'timeframe' in sig.parameters
        assert 'limit' in sig.parameters
    
    def test_get_ticker_signature(self):
        """测试 get_ticker 方法签名 (IBKR-03)"""
        from app.data_sources.ibkr import IBKRDataSource
        import inspect
        sig = inspect.signature(IBKRDataSource.get_ticker)
        assert 'symbol' in sig.parameters
    
    def test_disconnect_method_exists(self):
        """测试 disconnect 方法存在 (IBKR-04, D-06)"""
        from app.data_sources.ibkr import IBKRDataSource
        assert hasattr(IBKRDataSource, 'disconnect')
```
  </action>
  <verify>
    <automated>grep -n "class TestIBKRDataSource" /home/workspace/ws/QuantDinger/backend_api_python/tests/test_ibkr_data_source.py</automated>
  </verify>
  <done>单元测试已创建</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → IBKR Gateway | untrusted network, IBKR API credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | get_ticker | mitigate | 使用 RateLimiter (IBKR 6 RPM 限制) |
| T-01-02 | Resource Exhaustion | connection | mitigate | 单例模式复用连接, explicit disconnect |
| T-01-03 | Tampering | invalid symbol | mitigate | validate symbol format before API call |
</threat_model>

<verification>
- [ ] IBKRDataSource 类创建成功
- [ ] get_kline() 方法可调用
- [ ] get_ticker() 方法可调用  
- [ ] DataSourceFactory.get_source(exchange_id='ibkr-live') 返回 IBKRDataSource
- [ ] 单元测试通过
</verification>

<success_criteria>
1. exchange_id='ibkr-live' 自动返回 IBKRDataSource
2. get_kline() 返回正确格式 [{time, open, high, low, close, volume}]
3. get_ticker() 返回实时报价 {last, symbol, ...}
4. 连接可复用，支持 disconnect/reconnect
5. 单元测试覆盖核心功能
</success_criteria>

<output>
After completion, create `.planning/phases/01-ibkr-data-source-implementation/01-01-SUMMARY.md`
</output>
