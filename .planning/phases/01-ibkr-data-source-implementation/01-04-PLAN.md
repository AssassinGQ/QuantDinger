---
phase: 01-ibkr-data-source-implementation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/app/data_sources/rate_limiter.py
autonomous: true
requirements:
  - D-21
  - D-22
must_haves:
  truths:
    - "IBKR rate limiter exists with 6 RPM for historical data (per ibkr-datafetcher)"
    - "Rate limiter protects get_ticker from triggering IBKR limits per D-22"
    - "Rate limiter integrated with get_kline per D-23"
  artifacts:
    - path: "backend_api_python/app/data_sources/rate_limiter.py"
      provides: "get_ibkr_limiter() function"
      contains: "IBKR"
  key_links:
    - from: "IBKRDataSource.get_ticker"
      to: "get_ibkr_limiter"
      via: "import and call"
      pattern: "get_ibkr_limiter"
    - from: "IBKRDataSource.get_kline"
      to: "get_ibkr_limiter"
      via: "import and call"
      pattern: "get_ibkr_limiter"
---

<objective>
添加 IBKR 限流器，防止触发 IBKR API 限制

Purpose: 保护 IBKR API 调用，符合 D-21, D-22, D-23
Output: IBKR rate limiter function in rate_limiter.py
</objective>

<context>
@backend_api_python/app/data_sources/rate_limiter.py
@/home/workspace/ws/ibkr-datafetcher/src/ibkr_datafetcher/rate_limiter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add IBKR rate limiter</name>
  <files>backend_api_python/app/data_sources/rate_limiter.py</files>
  <action>
Add IBKR rate limiter to rate_limiter.py:
- Import and adapt the ibkr-datafetcher RateLimiter class
- Create get_ibkr_limiter() function returning singleton instance
- Configure: hist_requests_per_minute=6, news_requests_per_minute=3, identical_cooldown=15.0
- Export function at module level
  </action>
  <verify>
    <automated>python -c "from app.data_sources.rate_limiter import get_ibkr_limiter; print('OK')"</automated>
  </verify>
  <done>
get_ibkr_limiter() function exists and can be imported
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate rate limiter in IBKRDataSource</name>
  <files>backend_api_python/app/data_sources/ibkr.py</files>
  <action>
Update IBKRDataSource to use rate limiter:
- Import get_ibkr_limiter from rate_limiter
- Add _rate_limiter member variable
- Call limiter.acquire() before get_ticker requests per D-22
- Call limiter.acquire() before get_kline requests per D-23
  </action>
  <verify>
    <automated>python -c "from app.data_sources.ibkr import IBKRDataSource; print('OK')"</automated>
  </verify>
  <done>
IBKRDataSource uses rate limiter for API calls
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IBKRDataSource → IBKR API | Rate-limited external calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | R | Rate limit exceeded | mitigate | Use rate limiter, wait if needed |
</threat_model>

<verification>
Import works correctly
</verification>

<success_criteria>
IBKR rate limiter integrated with IBKRDataSource
</success_criteria>

<output>
After completion, create .planning/phases/01-ibkr-data-source-implementation/01-04-SUMMARY.md
</output>