---
phase: 01-ibkr-data-source-implementation
plan: 05
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - backend_api_python/app/data_sources/factory.py
  - backend_api_python/app/services/trading_executor.py
autonomous: true
requirements:
  - INT-01
  - INT-02
  - INT-03
must_haves:
  truths:
    - "DataSourceFactory.get_source() accepts optional exchange_id parameter per D-01"
    - "When exchange_id='ibkr-live', returns IBKRDataSource per D-02, D-03"
    - "exchange_id takes priority over market_category per D-08"
    - "trading_executor passes exchange_id to DataSourceFactory per D-14, D-15"
  artifacts:
    - path: "backend_api_python/app/data_sources/factory.py"
      provides: "get_source with exchange_id support"
      contains: "exchange_id"
    - path: "backend_api_python/app/services/trading_executor.py"
      provides: "exchange_id to DataSourceFactory"
      contains: "exchange_id"
  key_links:
    - from: "DataSourceFactory.get_source"
      to: "IBKRDataSource"
      via: "conditional return"
      pattern: "ibkr-live"
    - from: "trading_executor"
      to: "DataSourceFactory.get_source"
      via: "passes exchange_id"
      pattern: "get_source.*exchange_id"
---

<objective>
集成 IBKRDataSource 到 DataSourceFactory，支持 exchange_id 优先级

Purpose: 实现 D-01, D-02, D-03, D-08 以及 INT-01, INT-02, INT-03
Output: DataSourceFactory 支持 exchange_id 参数
</objective>

<context>
@backend_api_python/app/data_sources/factory.py
@backend_api_python/app/services/trading_executor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DataSourceFactory.get_source</name>
  <files>backend_api_python/app/data_sources/factory.py</files>
  <action>
Update DataSourceFactory.get_source:
- Add optional `exchange_id` parameter (default None)
- When exchange_id is provided and equals 'ibkr-live', return IBKRDataSource instance
- Store IBKRDataSource in _sources dict under key 'ibkr-live'
- Maintain backward compatibility: no exchange_id = use market parameter per D-03
- exchange_id takes priority over market per D-08
  </action>
  <verify>
    <automated>python -c "from app.data_sources.factory import DataSourceFactory; s = DataSourceFactory.get_source('Crypto'); print('OK')"</automated>
  </verify>
  <done>
DataSourceFactory.get_source(exchange_id='ibkr-live') returns IBKRDataSource
  </done>
</task>

<task type="auto">
  <name>Task 2: Update trading_executor to pass exchange_id</name>
  <files>backend_api_python/app/services/trading_executor.py</files>
  <action>
Update trading_executor to use exchange_id:
- Find where DataSourceFactory.get_source() is called
- Add exchange_id parameter to the call
- Pass exchange_id from strategy config to DataSourceFactory
- Ensure backward compatibility when exchange_id is not set
  </action>
  <verify>
    <automated>python -c "from app.services.trading_executor import TradingExecutor; print('OK')"</automated>
  </verify>
  <done>
trading_executor passes exchange_id to DataSourceFactory
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trading_executor → DataSourceFactory | Internal API call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | I | Missing exchange_id | accept | Default to market if exchange_id not provided |
</threat_model>

<verification>
Import and basic call work
</verification>

<success_criteria>
exchange_id='ibkr-live' strategies use IBKRDataSource via DataSourceFactory
</success_criteria>

<output>
After completion, create .planning/phases/01-ibkr-data-source-implementation/01-05-SUMMARY.md
</output>