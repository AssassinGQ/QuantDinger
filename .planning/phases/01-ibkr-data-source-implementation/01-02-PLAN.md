---
phase: 01-ibkr-data-source-implementation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend_api_python/app/data_sources/factory.py
  - backend_api_python/tests/test_ibkr_integration.py
  - backend_api_python/app/data_sources/__init__.py
autonomous: true
requirements:
  - INT-01
  - INT-02
  - INT-03
  - D-01
  - D-02
  - D-03
  - D-08
must_haves:
  truths:
    - "DataSourceFactory.get_source() accepts optional exchange_id parameter"
    - "exchange_id='ibkr-live' returns IBKRDataSource instance"
    - "Backward compatible: without exchange_id, behaves as before"
    - "trading_executor passes exchange_id to DataSourceFactory"
  artifacts:
    - path: "backend_api_python/app/data_sources/factory.py"
      provides: "DataSourceFactory with exchange_id support"
      adds: "Optional exchange_id parameter"
    - path: "backend_api_python/app/data_sources/__init__.py"
      provides: "Module exports"
      adds: "IBKRDataSource export"
  key_links:
    - from: "DataSourceFactory.get_source"
      to: "IBKRDataSource"
      via: "exchange_id=='ibkr-live' branch"
    - from: "trading_executor"
      to: "DataSourceFactory.get_source"
      via: "exchange_id parameter"
---

<objective>
Integrate IBKRDataSource with DataSourceFactory and ensure trading_executor passes exchange_id to data source selection.

Purpose: Enable automatic selection of IBKR data source when exchange_id="ibkr-live" in strategy configuration.
</objective>

<execution_context>
@/home/workspace/ws/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/workspace/ws/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend_api_python/app/data_sources/factory.py
@backend_api_python/app/data_sources/ibkr.py
@backend_api_python/app/data_sources/__init__.py
@.planning/phases/01-ibkr-data-source-implementation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend DataSourceFactory with exchange_id support</name>
  <files>backend_api_python/app/data_sources/factory.py</files>
  <action>
1. Modify DataSourceFactory.get_source() to accept optional exchange_id parameter (D-01)

2. Implementation:
   - Add Optional[str] import
   - Add exchange_id: Optional[str] = None parameter to get_source()
   - If exchange_id == 'ibkr-live': return IBKRDataSource instance (D-02)
   - Otherwise: fall back to market-based selection (D-03, backward compatible)
   - exchange_id takes priority over market (D-08)

3. Also update get_kline() and get_ticker() convenience methods to accept exchange_id parameter and pass to get_source()

4. Update get_data_source() to handle 'ibkr-live' case

5. Create test file backend_api_python/tests/test_ibkr_integration.py with:
   - Test factory returns IBKRDataSource for exchange_id='ibkr-live'
   - Test backward compatibility without exchange_id
   - Test exchange_id takes priority over market parameter
</action>
  <verify>
<automated>grep -n "exchange_id" backend_api_python/app/data_sources/factory.py && grep -n "def test_" backend_api_python/tests/test_ibkr_integration.py</automated>
  </verify>
  <done>DataSourceFactory accepts exchange_id parameter. exchange_id='ibkr-live' returns IBKRDataSource. Backward compatible when not provided.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Factory → DataSource | Factory selects data source based on exchange_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01 | Spoofing | Invalid exchange_id injection | mitigate | Validate exchange_id against known values (ibkr-live) |
</threat_model>

<verification>
Run integration tests:
- pytest backend_api_python/tests/test_ibkr_integration.py -x
</verification>

<success_criteria>
- DataSourceFactory.get_source() accepts exchange_id parameter
- exchange_id='ibkr-live' returns IBKRDataSource
- Backward compatible when exchange_id not provided
- trading_executor can use exchange_id to select data source
</success_criteria>

<output>
After completion, create `.planning/phases/01-ibkr-data-source-implementation/01-02-SUMMARY.md`
</output>