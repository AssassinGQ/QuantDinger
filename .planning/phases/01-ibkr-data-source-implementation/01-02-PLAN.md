---
phase: 01-ibkr-data-source-implementation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend_api_python/app/services/kline_fetcher.py
  - backend_api_python/app/services/price_fetcher.py
  - backend_api_python/app/services/kline.py
  - backend_api_python/tests/test_ibkr_data_source.py
  - backend_api_python/tests/test_ibkr_integration.py
autonomous: true
requirements:
  - INT-02
user_setup: []
must_haves:
  truths:
    - "kline_fetcher passes exchange_id to DataSourceFactory"
    - "price_fetcher passes exchange_id to DataSourceFactory"
    - "trading_executor integration works via service chain"
  artifacts:
    - path: "backend_api_python/app/services/kline_fetcher.py"
      provides: "K-line fetching with exchange_id support"
      contains: "exchange_id.*DataSourceFactory"
    - path: "backend_api_python/app/services/price_fetcher.py"
      provides: "Price fetching with exchange_id support"
      contains: "exchange_id.*DataSourceFactory"
    - path: "backend_api_python/tests/test_ibkr_data_source.py"
      provides: "Unit tests for IBKRDataSource"
      contains: "class.*Test.*IBKR"
    - path: "backend_api_python/tests/test_ibkr_integration.py"
      provides: "Integration tests"
      contains: "test.*exchange_id"
---

<objective>
集成 IBKRDataSource 到服务层并添加测试

更新 kline_fetcher 和 price_fetcher 以传递 exchange_id 给 DataSourceFactory，并创建单元测试和集成测试。
</objective>

<context>
@/home/workspace/ws/QuantDinger/backend_api_python/app/services/kline_fetcher.py
@/home/workspace/ws/QuantDinger/backend_api_python/app/services/price_fetcher.py
@/home/workspace/ws/QuantDinger/backend_api_python/app/services/kline.py
@/home/workspace/ws/QuantDinger/backend_api_python/app/data_sources/factory.py

## Integration chain

```python
# kline_fetcher.py - current pattern (line 335)
data = DataSourceFactory.get_kline(market, symbol, "1m", limit, before_time=before_time)

# price_fetcher.py - current pattern (line 59)
ticker = DataSourceFactory.get_ticker(market_category, symbol)

# kline.py - current pattern (line 88)
ticker = DataSourceFactory.get_ticker(market, symbol)
```

Updated patterns should include exchange_id when available:
```python
ticker = DataSourceFactory.get_ticker(market, symbol, exchange_id=exchange_id)
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update kline_fetcher to pass exchange_id</name>
  <files>backend_api_python/app/services/kline_fetcher.py</files>
  <read_first>
    - /home/workspace/ws/QuantDinger/backend_api_python/app/services/kline_fetcher.py
  </read_first>
  <action>
Update all `DataSourceFactory.get_kline()` calls in `kline_fetcher.py` to accept and pass exchange_id parameter:

1. **Find all call sites**: Use grep to locate all DataSourceFactory.get_kline() calls
   - Expected: lines 335, 338, 523, 554, 591, 639, 646, 663

2. **For each call site**, check if exchange_id is available in scope:
   - If the calling function has exchange_id parameter, pass it through
   - Otherwise, pass exchange_id=None (backward compatible)

3. **Add exchange_id parameter to key functions**:
   - `_fetch_1m_or_fallback_5m(market, symbol, limit, before_time, exchange_id=None)`
   - `get_kline(market, symbol, timeframe, limit, before_time, exchange_id=None)`
   - etc.

4. **Pass through to DataSourceFactory**:
```python
# Before (line 335):
data = DataSourceFactory.get_kline(market, symbol, "1m", limit, before_time=before_time)

# After:
data = DataSourceFactory.get_kline(market, symbol, "1m", limit, before_time=before_time, exchange_id=exchange_id)
```

Per D-15: "trading_executor 将 exchange_id 传递给 DataSourceFactory"
  </action>
  <verify>
    <automated>grep -n "DataSourceFactory.get_kline.*exchange_id" backend_api_python/app/services/kline_fetcher.py | wc -l</automated>
  </verify>
  <done>All DataSourceFactory.get_kline() calls pass exchange_id parameter</done>
</task>

<task type="auto">
  <name>Task 2: Update price_fetcher to pass exchange_id</name>
  <files>backend_api_python/app/services/price_fetcher.py</files>
  <read_first>
    - /home/workspace/ws/QuantDinger/backend_api_python/app/services/price_fetcher.py
  </read_first>
  <action>
Update `price_fetcher.py` to pass exchange_id to DataSourceFactory:

1. **Find all DataSourceFactory.get_ticker() calls**:
   - Line ~59: `ticker = DataSourceFactory.get_ticker(market_category, symbol)`

2. **Add exchange_id parameter to get_price() function**:
```python
def get_price(
    self,
    market_category: str,
    symbol: str,
    exchange_id: Optional[str] = None  # NEW
) -> Optional[float]:
```

3. **Pass to DataSourceFactory**:
```python
# Before:
ticker = DataSourceFactory.get_ticker(market_category, symbol)

# After:
ticker = DataSourceFactory.get_ticker(market_category, symbol, exchange_id=exchange_id)
```

4. **Check other methods in price_fetcher.py** that call DataSourceFactory:
   - Add exchange_id parameter and pass through
  </action>
  <verify>
    <automated>grep -n "DataSourceFactory.get_ticker.*exchange_id" backend_api_python/app/services/price_fetcher.py | wc -l</automated>
  </verify>
  <done>All DataSourceFactory.get_ticker() calls pass exchange_id parameter</done>
</task>

<task type="auto">
  <name>Task 3: Update kline service to pass exchange_id</name>
  <files>backend_api_python/app/services/kline.py</files>
  <read_first>
    - /home/workspace/ws/QuantDinger/backend_api_python/app/services/kline.py
  </read_first>
  <action>
Update `kline.py` (KlineService class) to pass exchange_id:

1. **Find DataSourceFactory calls**:
   - Line 88: `ticker = DataSourceFactory.get_ticker(market, symbol)`
   - Line 135: Additional calls

2. **Add exchange_id parameter to methods**:
   - `get_kline(market, symbol, timeframe, limit, before_time, exchange_id=None)`
   - `get_realtime_price(market, symbol, exchange_id=None)`

3. **Pass through to DataSourceFactory**:
```python
ticker = DataSourceFactory.get_ticker(market, symbol, exchange_id=exchange_id)
```
  </action>
  <verify>
    <automated>grep -n "DataSourceFactory.get_ticker" backend_api_python/app/services/kline.py</automated>
  </verify>
  <done>KlineService methods pass exchange_id to DataSourceFactory</done>
</task>

<task type="auto">
  <name>Task 4: Create IBKRDataSource unit tests</name>
  <files>backend_api_python/tests/test_ibkr_data_source.py</files>
  <read_first>
    - /home/workspace/ws/QuantDinger/backend_api_python/tests/  (check existing test patterns)
  </read_first>
  <action>
Create `backend_api_python/tests/test_ibkr_data_source.py` with unit tests:

1. **Test class creation** (IBKR-01):
```python
def test_class_creation():
    from app.data_sources.ibkr import IBKRDataSource
    source = IBKRDataSource()
    assert source.name == "ibkr"
    assert hasattr(source, 'get_kline')
    assert hasattr(source, 'get_ticker')
    assert hasattr(source, 'disconnect')
```

2. **Test get_kline signature** (IBKR-02):
```python
def test_get_kline_signature():
    source = IBKRDataSource()
    # Should accept symbol, timeframe, limit, optional before_time
    import inspect
    sig = inspect.signature(source.get_kline)
    params = list(sig.parameters.keys())
    assert 'symbol' in params
    assert 'timeframe' in params
    assert 'limit' in params
```

3. **Test get_ticker signature** (IBKR-03):
```python
def test_get_ticker_signature():
    source = IBKRDataSource()
    import inspect
    sig = inspect.signature(source.get_ticker)
    assert 'symbol' in sig.parameters
```

4. **Test disconnect method** (IBKR-04):
```python
def test_disconnect_method():
    source = IBKRDataSource()
    # disconnect() should exist and be callable
    assert callable(source.disconnect)
```

5. **Test timeframe mapping**:
```python
def test_timeframe_mapping():
    source = IBKRDataSource()
    # Test _map_timeframe() method
    assert source._map_timeframe('1m') == '1 min'
    assert source._map_timeframe('1H') == '1 hour'
```

Note: These tests verify the interface. Connection tests require IBKR Gateway running - mark integration tests separately.
  </action>
  <verify>
    <automated>grep -n "def test_" backend_api_python/tests/test_ibkr_data_source.py | wc -l</automated>
  </verify>
  <done>At least 4 test functions in test_ibkr_data_source.py</done>
</task>

<task type="auto">
  <name>Task 5: Create integration tests</name>
  <files>backend_api_python/tests/test_ibkr_integration.py</files>
  <read_first>
    - /home/workspace/ws/QuantDinger/backend_api_python/tests/  (check existing test patterns)
  </read_first>
  <action>
Create `backend_api_python/tests/test_ibkr_integration.py`:

1. **Test DataSourceFactory with exchange_id** (INT-01, INT-03):
```python
def test_factory_exchange_id_parameter():
    from app.data_sources import DataSourceFactory
    import inspect
    sig = inspect.signature(DataSourceFactory.get_source)
    params = list(sig.parameters.keys())
    assert 'exchange_id' in params

def test_factory_ibkr_live_selection():
    from app.data_sources import DataSourceFactory
    source = DataSourceFactory.get_source('Crypto', exchange_id='ibkr-live')
    from app.data_sources.ibkr import IBKRDataSource
    assert isinstance(source, IBKRDataSource)
```

2. **Test backward compatibility** (D-03):
```python
def test_factory_backward_compatible():
    from app.data_sources import DataSourceFactory
    # Without exchange_id, should return regular data source
    source = DataSourceFactory.get_source('Crypto')
    # Should not raise
```

3. **Test exchange_id in get_kline** (INT-02):
```python
def test_get_kline_exchange_id():
    from app.data_sources import DataSourceFactory
    import inspect
    sig = inspect.signature(DataSourceFactory.get_kline)
    assert 'exchange_id' in sig.parameters
```

4. **Test exchange_id in get_ticker**:
```python
def test_get_ticker_exchange_id():
    from app.data_sources import DataSourceFactory
    import inspect
    sig = inspect.signature(DataSourceFactory.get_ticker)
    assert 'exchange_id' in sig.parameters
```
  </action>
  <verify>
    <automated>grep -n "def test_" backend_api_python/tests/test_ibkr_integration.py | wc -l</automated>
  </verify>
  <done>At least 4 test functions in test_ibkr_integration.py</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration tests → DataSourceFactory | No external network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | None | Tests are read-only verification | accept | No security implications |
</threat_model>

<verification>
1. Run unit tests: `pytest backend_api_python/tests/test_ibkr_data_source.py -x`
2. Run integration tests: `pytest backend_api_python/tests/test_ibkr_integration.py -x`
3. Verify all tests pass
</verification>

<success_criteria>
- kline_fetcher passes exchange_id to DataSourceFactory.get_kline()
- price_fetcher passes exchange_id to DataSourceFactory.get_ticker()
- kline.py passes exchange_id to DataSourceFactory.get_ticker()
- Unit tests exist and pass
- Integration tests exist and pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-ibkr-data-source-implementation/01-02-SUMMARY.md`
</output>