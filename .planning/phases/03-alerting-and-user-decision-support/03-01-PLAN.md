---
phase: 03-alerting-and-user-decision-support
plan: 01
type: execute
wave: 1
revision: 1
revision_notes: "Replanned from 03-REVIEWS.md (Claude + OpenCode): sentinel test co-migrated with N3 emitters; dedup lock; empty notification_config; load_strategy_name; canonical signal_type constant; notification_config resolution in insufficient branch."
depends_on: []
files_modified:
  - backend_api_python/app/services/ibkr_insufficient_user_alert.py
  - backend_api_python/app/services/data_sufficiency_logging.py
  - backend_api_python/app/services/signal_executor.py
  - backend_api_python/tests/test_data_sufficiency_logging.py
autonomous: true
requirements:
  - R4
  - R5
  - N3
must_haves:
  truths:
    - "User insufficient alerts fire only when IBKR live open/add is actually blocked for insufficiency (same moment as `ibkr_open_blocked_insufficient_data`)."
    - "Dedup key is strategy_id + symbol + reason_code + exchange_id with default 300s cooldown; suppressed repeats do not spam user channels."
    - "Dispatch uses `SignalNotifier.notify_signal` + strategy `notification_config` (DB via `load_notification_config` when ctx incomplete)."
    - "Structured `ibkr_insufficient_data_alert_sent` is emitted when an alert passes dedup and dispatch completes per plan."
  key_links:
    - from: "backend_api_python/app/services/signal_executor.py"
      to: "backend_api_python/app/services/ibkr_insufficient_user_alert.py"
      via: "post-`emit_ibkr_open_blocked_insufficient_data` hook before `return False`"
      pattern: "dispatch_insufficient|ibkr_insufficient_user_alert"
    - from: "backend_api_python/app/services/ibkr_insufficient_user_alert.py"
      to: "backend_api_python/app/services/signal_notifier.py"
      via: "`SignalNotifier.notify_signal`"
      pattern: "notify_signal\\("
    - from: "backend_api_python/app/services/ibkr_insufficient_user_alert.py"
      to: "backend_api_python/app/services/data_sufficiency_logging.py"
      via: "`emit_ibkr_insufficient_data_alert_sent` after successful dispatch gate"
      pattern: "emit_ibkr_insufficient_data_alert_sent"
  artifacts:
    - path: "backend_api_python/app/services/ibkr_insufficient_user_alert.py"
      provides: "Cooldown registry + user payload/title builder + orchestration entrypoint"
    - path: "backend_api_python/app/services/data_sufficiency_logging.py"
      provides: "`ibkr_insufficient_data_alert_sent` payload builder + emitter"
    - path: "backend_api_python/app/services/signal_executor.py"
      provides: "Post-block hook calling orchestration"
---

<objective>
Implement Phase 3 core: deduplicated user-channel insufficient-data alerts and N3 `ibkr_insufficient_data_alert_sent`, wired from the existing IBKR open/add sufficiency block in `SignalExecutor`.
</objective>

<review_resolution source=".planning/phases/03-alerting-and-user-decision-support/03-REVIEWS.md consensus 2026-04-18">
- **R-01 Phase 2 sentinel:** `test_logging_module_does_not_define_phase3_alert_event` is updated in **this plan Task 2** (same diff as N3 helpers) so Wave 1 Task 2 no longer leaves full suite red.
- **R-02 Dedup concurrency:** `ibkr_insufficient_user_alert.py` uses a `threading.Lock` around cooldown dict mutations (OpenCode).
- **R-03 Empty notification_config:** If resolved config has no usable `channels`, skip `notify_signal`, log bounded `warning`, do not raise (OpenCode).
- **R-04 strategy_name:** Executor hook uses `strategy_ctx.get("_strategy_name") or load_strategy_name(strategy_id)` from `records.py` (Claude).
- **R-05 signal_type:** Single exported constant `IBKR_INSUFFICIENT_DATA_ALERT_SIGNAL_TYPE = "ibkr_data_insufficient_block"` in `ibkr_insufficient_user_alert.py`; executor and tests import it (Claude).
- **R-06 notification_config at block:** Insufficient branch always normalizes config: start from ctx, then `load_notification_config` when missing/empty channels; re-read `signal_executor.py` at implementation time (OpenCode HIGH).
- **R-07 direction (flat):** Code comment documents `direction` is cosmetic for flat alerts because `signal_type` drives `_signal_meta` in `SignalNotifier` (Claude LOW).
</review_resolution>

<context>
@.planning/phases/03-alerting-and-user-decision-support/03-REVIEWS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-alerting-and-user-decision-support/03-CONTEXT.md
@.planning/phases/03-alerting-and-user-decision-support/03-RESEARCH.md
@.planning/phases/03-alerting-and-user-decision-support/03-VALIDATION.md
@.planning/phases/02-open-signal-guard-in-execution-path/02-CONTEXT.md
@backend_api_python/app/services/data_sufficiency_logging.py
@backend_api_python/app/services/signal_notifier.py
@backend_api_python/app/services/live_trading/records.py
@backend_api_python/app/services/signal_executor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dedup registry + user alert orchestration module</name>
  <files>backend_api_python/app/services/ibkr_insufficient_user_alert.py</files>
  <read_first>
    .planning/phases/03-alerting-and-user-decision-support/03-CONTEXT.md
    .planning/phases/03-alerting-and-user-decision-support/03-RESEARCH.md
    backend_api_python/app/services/signal_notifier.py
    backend_api_python/app/services/live_trading/records.py
    backend_api_python/app/services/data_sufficiency_types.py
  </read_first>
  <action>
    Add `backend_api_python/app/services/ibkr_insufficient_user_alert.py` implementing:
    - Module-level constant exactly: `IBKR_INSUFFICIENT_DATA_ALERT_SIGNAL_TYPE: Final[str] = "ibkr_data_insufficient_block"` (tests MUST `from app.services.ibkr_insufficient_user_alert import IBKR_INSUFFICIENT_DATA_ALERT_SIGNAL_TYPE` for assertions).
    - A process-local cooldown store keyed by tuple `(strategy_id: int, symbol: str, reason_code: str, exchange_id: str)` with default TTL **300.0** seconds (override hook optional via function param `cooldown_seconds` defaulting to 300). Wrap **all** reads/writes to the last-sent map in `threading.Lock()` (module-level `_dedup_lock`) so concurrent `execute` calls cannot corrupt cooldown state (03-REVIEWS / OpenCode).
    - Pure function `insufficient_user_alert_dedup_key(strategy_id, symbol, reason_code, exchange_id) -> tuple[...]` returning the composite key (normalize `symbol`/`reason_code`/`exchange_id` with `strip()` and lower-case for `exchange_id` only if CONTEXT requires case-insensitivity — use exact `reason_code.value` string from `DataSufficiencyResult`).
    - `should_send_insufficient_user_alert(now_monotonic: float, key) -> bool` returning False when still inside cooldown; on True, record `now_monotonic` as last-sent for that key.
    - `build_insufficient_user_alert_extra(*, blocked_payload: dict, suff_result, strategy_ctx, current_positions, current_price: float) -> dict` that mirrors stable keys from Phase 2 blocked-open payload: at minimum include string copies of `exchange_id`, `_execution_mode` from `strategy_ctx.get("_execution_mode")` placed under extra key **`_execution_mode`** (not ambiguous `execution_mode` prose), `reason_code`, `required_bars`, `available_bars`, `effective_lookback`, `missing_window`, `schedule_status`, `effective_intent` / `effective_intent` field name matching blocked payload’s `effective_intent`, plus `position_snapshot` (list or summary dict from `current_positions` filtered to the traded symbol if feasible).
    - Title/body helpers: when **no** open position for `symbol` (flat): `severity_hint="warning"`, body MUST NOT contain the substring `平仓` nor `持有` nor English `close`+`hold` paired instruction (keep generic “open blocked” explanation per D-06). When **open** position exists: title or first line of plain body MUST contain the exact substring **`有持仓`** and MUST contain explicit user decision wording equivalent to asking user to choose **close vs hold** (e.g. Chinese: 请自行决定平仓或继续持有) per D-07/R5.
    - `dispatch_insufficient_user_alert_after_block(...)` (name may vary) accepting `SignalNotifier` instance (inject for tests), `strategy_id`, `strategy_name`, `symbol`, `notification_config`, `signal_type_for_notifier` (**must** pass `IBKR_INSUFFICIENT_DATA_ALERT_SIGNAL_TYPE`), `price`, `direction`, `extra`, calling `notify_signal` once when `should_send` is True and `notification_config` resolves to a non-empty `channels` list; otherwise log one bounded `logger.warning` (include `strategy_id`) and return without raising (03-REVIEWS R-03).
    Expose a module-level `reset_insufficient_user_alert_dedup_state()` used only from tests to clear the process-local store.
  </action>
  <use_case_spec>
    UC-03-01-01 冷却键隔离：给定 strategy A / symbol SPY / reason missing_bars / exchange ibkr-paper，与同一 strategy 下 symbol QQQ 的键不得互相抑制。
    UC-03-01-02 冷却窗抑制：给定相同四元组键，在第一次发送后的 299 秒内第二次调用 `should_send` 必须返回 False；301 秒后（测试通过注入单调时钟或 patch time.monotonic 返回值）必须返回 True。
    UC-03-01-03 空仓文案：current_positions 无对应标的持仓时，生成的中文正文不得暗示用户“选择平仓/持有”；不得出现子串「有持仓」。
    UC-03-01-04 有仓文案：存在对应方向持仓时，标题或首行含「有持仓」，且正文含明确请用户决定平仓或继续持有的语句。
  </use_case_spec>
  <use_cases>
    - `dedup_key_isolation`: UC-03-01-01
    - `cooldown_suppresses_repeat`: UC-03-01-02
    - `flat_no_close_hold_wording`: UC-03-01-03
    - `positioned_close_hold_prompt`: UC-03-01-04
  </use_cases>
  <verify>
    <automated>python3 -m compileall -q backend_api_python/app/services/ibkr_insufficient_user_alert.py</automated>
    <automated>python3 -m pytest backend_api_python/tests -q</automated>
    <note>通过全量用例：第二条 `python3 -m pytest backend_api_python/tests -q` 必须 exit code 0。首条在本任务阶段用 compileall 证明新模块可导入；`test_ibkr_insufficient_user_alert.py` 的 pytest 断言由 03-02-PLAN 负责补齐。</note>
  </verify>
  <acceptance_criteria>
    - `backend_api_python/app/services/ibkr_insufficient_user_alert.py` exists and defines `DEFAULT_COOLDOWN_SECONDS = 300` (or exact comment `300` with named constant)
    - the file contains exact substring `IBKR_INSUFFICIENT_DATA_ALERT_SIGNAL_TYPE` and assigns value `ibkr_data_insufficient_block`
    - the file imports `threading` and defines `_dedup_lock = threading.Lock()` (or equivalent name) and uses `with _dedup_lock:` (or `.acquire()`/`.release()`) around cooldown dict mutations
    - the module defines `reset_insufficient_user_alert_dedup_state` callable
    - `grep -n "有持仓" backend_api_python/app/services/ibkr_insufficient_user_alert.py` returns at least one line in title/body builder path for positioned branch
    - `grep -n "_execution_mode" backend_api_python/app/services/ibkr_insufficient_user_alert.py` shows extra dict uses underscore key for execution mode copy
    - `python3 -m compileall -q backend_api_python/app/services/ibkr_insufficient_user_alert.py` exits 0
    - `python3 -m pytest backend_api_python/tests -q` exits 0
  </acceptance_criteria>
  <done>Dedup + payload shaping for user alerts lives in one module callable from `SignalExecutor`.</done>
</task>

<task type="auto">
  <name>Task 2: N3 `ibkr_insufficient_data_alert_sent` payload + emitter + Phase 2 sentinel migration</name>
  <files>backend_api_python/app/services/data_sufficiency_logging.py, backend_api_python/tests/test_data_sufficiency_logging.py</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/03-alerting-and-user-decision-support/03-CONTEXT.md
    .planning/phases/03-alerting-and-user-decision-support/03-REVIEWS.md
    backend_api_python/app/services/data_sufficiency_logging.py
    backend_api_python/tests/test_data_sufficiency_logging.py
  </read_first>
  <action>
    Extend `backend_api_python/app/services/data_sufficiency_logging.py` with:
    - `build_ibkr_insufficient_data_alert_sent_payload(...)` returning a dict whose `"event"` key is exactly **`ibkr_insufficient_data_alert_sent`**, including: `strategy_id`, `symbol`, `exchange_id`, `_execution_mode`, `reason_code`, dedup `key` tuple fields as separate scalar keys, `channels_attempted` (list), `channels_ok` (dict channel-&gt;bool mirroring notifier results shape if available), and `signal_type` used for notifier.
    - `emit_ibkr_insufficient_data_alert_sent(payload, logger=...)` mirroring `emit_ibkr_open_blocked_insufficient_data` style (`log.info("ibkr_insufficient_data_alert_sent", extra=...)`).
    Call emitter from `ibkr_insufficient_user_alert.dispatch_*` only **after** dedup allows and `notify_signal` returns (include channel outcomes in payload).
    **Same task / same commit:** In `backend_api_python/tests/test_data_sufficiency_logging.py`, **delete or replace** `test_logging_module_does_not_define_phase3_alert_event` (it asserted `ibkr_insufficient_data_alert_sent` not in module source — 03-REVIEWS HIGH). Replace with `def test_logging_module_defines_ibkr_insufficient_data_alert_sent_helpers():` that reads the module file text and asserts **both** substrings `def build_ibkr_insufficient_data_alert_sent_payload` and `def emit_ibkr_insufficient_data_alert_sent` are present, and **still** asserts `persist_notification` not in src (Phase 2 boundary preserved).
  </action>
  <use_case_spec>
    UC-03-02-01 事件名稳定：payload 顶层 `event` 字符串必须恒等于 `ibkr_insufficient_data_alert_sent`。
    UC-03-02-02 与阻断同次：仅在用户通道告警实际进入发送路径（通过冷却闸）后记录；不得在未调用 `notify_signal` 时发射该事件。
  </use_case_spec>
  <use_cases>
    - `event_name_stable`: UC-03-02-01
    - `emit_only_after_dispatch_path`: UC-03-02-02
  </use_cases>
  <verify>
    <automated>python3 -m pytest backend_api_python/tests/test_data_sufficiency_logging.py -q</automated>
    <automated>python3 -m pytest backend_api_python/tests -q</automated>
    <note>通过全量用例：第二条全量 pytest 必须 exit code 0。首条不得依赖 `test_ibkr_insufficient_user_alert.py`（该文件在 03-02 才创建）。</note>
  </verify>
  <acceptance_criteria>
    - `grep -n "ibkr_insufficient_data_alert_sent" backend_api_python/app/services/data_sufficiency_logging.py` matches at least `build_` and `emit_` function definitions
    - `build_ibkr_insufficient_data_alert_sent_payload` includes literal key `"event"` with value `ibkr_insufficient_data_alert_sent` in implementation (grep-verifiable)
    - `test_data_sufficiency_logging.py` does **not** contain the function name `test_logging_module_does_not_define_phase3_alert_event`
    - `test_data_sufficiency_logging.py` contains `def test_logging_module_defines_ibkr_insufficient_data_alert_sent_helpers():`
    - `python3 -m pytest backend_api_python/tests -q` exits 0
  </acceptance_criteria>
  <done>N3 user-alert emission is machine-auditable and distinct from Phase 2 guard logs.</done>
</task>

<task type="auto">
  <name>Task 3: Wire `SignalExecutor` block branch to user alert orchestration</name>
  <files>backend_api_python/app/services/signal_executor.py</files>
  <read_first>
    backend_api_python/app/services/signal_executor.py
    backend_api_python/app/services/ibkr_insufficient_user_alert.py
    backend_api_python/app/services/data_sufficiency_logging.py
    backend_api_python/app/services/live_trading/records.py
    .planning/phases/03-alerting-and-user-decision-support/03-CONTEXT.md
    .planning/phases/03-alerting-and-user-decision-support/03-REVIEWS.md
  </read_first>
  <action>
    In `SignalExecutor.execute`, immediately after `emit_ibkr_open_blocked_insufficient_data(payload, logger=logger)` and before `return False`, invoke orchestration that:
    1. Resolves `notification_config` **in this branch only** (03-REVIEWS R-06): set `nc = strategy_ctx.get("_notification_config")`; if `nc` is not a `dict` or has no non-empty `channels` list (after `_as_list`-equivalent normalization), set `nc = load_notification_config(strategy_id)`. Pass `nc` to dispatch; if still no channels, dispatch layer skips per Task 1 R-03.
    2. Builds `strategy_name` from `strategy_ctx.get("_strategy_name") or load_strategy_name(strategy_id)` importing `load_strategy_name` from `app.services.live_trading.records` (same module as `load_notification_config`).
    3. Determines `direction` from existing position for `symbol` if any else `"long"` default only for notifier API — code comment MUST state: for flat alerts `direction` is **cosmetic** because `signal_type == IBKR_INSUFFICIENT_DATA_ALERT_SIGNAL_TYPE` drives `SignalNotifier` / `_signal_meta` rendering, not directional open/add copy (03-REVIEWS R-07).
    4. Passes `current_positions` from the same `execute()` scope (`exec_kwargs.get("current_positions")` or equivalent already threaded into `execute`) into orchestration for `build_insufficient_user_alert_extra` / R4 snapshot — verify at implementation time that variable is in scope; if not, load via `self.data_handler` only with a **read_first** confirmed public method (e.g. list positions for strategy) and document the exact method name in the commit.
    5. Calls into `ibkr_insufficient_user_alert` dispatch with `SignalNotifier()` default instance (allow optional constructor param on `SignalExecutor` for tests: add `signal_notifier: Optional[SignalNotifier] = None` to `__init__` defaulting to new `SignalNotifier()`). Pass `signal_type=IBKR_INSUFFICIENT_DATA_ALERT_SIGNAL_TYPE` imported from `ibkr_insufficient_user_alert` (R-05).
    6. Never raises out of alert path: wrap in try/except logging warning on unexpected errors; **must not** change `return False` outcome of insufficient guard.
  </action>
  <use_case_spec>
    UC-03-03-01 阻断不变：无论通知发送成功与否，`not suff_result.sufficient` 分支末尾对消费者仍返回 False。
    UC-03-03-02 配置回退：`strategy_ctx` 的 `_notification_config` 缺失或 channels 为空时必须调用 `load_notification_config(strategy_id)`（可通过 patch 断言调用次数）。
    UC-03-03-03 异常吞掉：若 `notify_signal` 抛异常，执行路径仍返回 False 且主流程不因告警失败崩溃（记录 warning）。
    UC-03-03-04 空渠道：`load_notification_config` 返回无 channels 时不得调用 `notify_signal`（或由 dispatch 短路）；不得抛未捕获异常。
  </use_case_spec>
  <use_cases>
    - `block_outcome_unaffected`: UC-03-03-01
    - `loads_db_notification_config`: UC-03-03-02
    - `notify_failure_swallowed`: UC-03-03-03
    - `empty_channels_skips_notify`: UC-03-03-04
  </use_cases>
  <verify>
    <automated>python3 -m pytest backend_api_python/tests/test_signal_executor.py -q</automated>
    <automated>python3 -m pytest backend_api_python/tests -q</automated>
    <note>通过全量用例：第二条全量 pytest 必须 exit code 0。与 `test_ibkr_insufficient_user_alert.py` 的联合断言留在 03-02-PLAN。</note>
  </verify>
  <acceptance_criteria>
    - `signal_executor.py` contains `emit_ibkr_open_blocked_insufficient_data` followed by a call containing substring `insufficient` or importing from `ibkr_insufficient_user_alert` before the `return False` in the same `if not suff_result.sufficient` block
    - `grep -n "load_notification_config" backend_api_python/app/services/signal_executor.py` shows import from `app.services.live_trading.records` or callsite
    - `grep -n "load_strategy_name" backend_api_python/app/services/signal_executor.py` matches at least one line
    - `grep -n "IBKR_INSUFFICIENT_DATA_ALERT_SIGNAL_TYPE" backend_api_python/app/services/signal_executor.py` matches at least one line
    - `python3 -m pytest backend_api_python/tests -q` exits 0
  </acceptance_criteria>
  <done>Blocked opens enqueue user-visible alerts without weakening the guard return path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User notification channels | Email/webhook/discord may leave the trust zone — payloads must exclude secrets |
| Log aggregator | Reads `ibkr_insufficient_data_alert_sent` — must not include webhook URLs or tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I | User alert `extra` | mitigate | Mirror only Phase 2 scalar diagnostic keys; no raw exception strings |
| T-03-02 | E | `notify_signal` failures | mitigate | try/except; never change block outcome |
| T-03-03 | I | Webhook channel | mitigate | Reuse notifier signing/headers; do not log full URLs (inherited from SignalNotifier) |
| T-03-04 | E | Dedup store | mitigate | `threading.Lock` on cooldown map mutations (multi-threaded executors) |
</threat_model>

<verification>
- Scoped (Wave 1 only — no `test_ibkr_insufficient_user_alert.py` yet): `python3 -m pytest backend_api_python/tests/test_signal_executor.py backend_api_python/tests/test_data_sufficiency_logging.py -q`
- Full suite (全量): `python3 -m pytest backend_api_python/tests -q`
</verification>

<success_criteria>
- Dedup + user dispatch module exists and is covered by tests added in `03-02-PLAN.md`.
- N3 `ibkr_insufficient_data_alert_sent` implemented and emitted on successful dispatch path.
- `SignalExecutor` invokes user alerts only from the insufficient open/add block.
</success_criteria>
