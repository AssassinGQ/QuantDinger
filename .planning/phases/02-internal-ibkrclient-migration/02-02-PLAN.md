---
phase: 01-ibkr-data-source-implementation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/app/data_sources/ibkr.py
autonomous: true
requirements:
  - IBKR-02
must_haves:
  truths:
    - "get_kline() returns list of K-line data in correct format"
    - "Supported timeframes: 1m, 5m, 15m, 30m, 1H, 4H, 1D per D-10"
    - "Symbols use IBKR format (AAPL, MSFT) per D-11"
    - "Uses caching per D-19: database 1m -> database 5m -> database kline -> network"
  artifacts:
    - path: "backend_api_python/app/data_sources/ibkr.py"
      provides: "get_kline() method"
      contains: "def get_kline"
  key_links:
    - from: "get_kline"
      to: "IBKRClient.get_historical_bars"
      via: "call"
      pattern: "get_historical_bars"
    - from: "get_kline"
      to: "kline_fetcher (existing)"
      via: "import and use for cache"
      pattern: "from app.services.kline_fetcher import"
---

<objective>
实现 get_kline() 方法获取历史K线数据

Purpose: 提供历史K线数据获取能力
Output: get_kline() method in IBKRDataSource
</objective>

<context>
@backend_api_python/app/data_sources/base.py
@/home/workspace/ws/ibkr-datafetcher/src/ibkr_datafetcher/ibkr_client.py
@backend_api_python/app/services/kline_fetcher.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement get_kline() with cache</name>
  <files>backend_api_python/app/data_sources/ibkr.py</files>
  <behavior>
    - Test 1: get_kline(symbol='AAPL', timeframe='1m', limit=100) returns list of kline dicts
    - Test 2: Each kline has keys: time, open, high, low, close, volume
    - Test 3: Returns empty list on error instead of raising
  </behavior>
  <action>
Implement get_kline method in IBKRDataSource:
- Ensure connection is established (call connect if needed)
- Use timeframe mapping (1m, 5m, 15m, 30m, 1H, 4H, 1D)
- Create IBKR contract from symbol (Stock symbol, exchange='SMART', currency='USD')
- Call IBKRClient.get_historical_bars()
- Format result using format_kline() from base class
- Apply caching strategy per D-19 (check kline_fetcher cache first)
- Handle errors gracefully, return empty list on failure
  </action>
  <verify>
    <automated>python -m pytest backend_api_python/tests/test_ibkr_datasource.py::test_get_kline -v --tb=short 2>/dev/null || echo "Test not found, will be created"</automated>
  </verify>
  <done>
get_kline returns data in format: [{"time": int, "open": float, "high": float, "low": float, "close": float, "volume": float}]
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test get_kline cache integration</name>
  <files>backend_api_python/tests/test_ibkr_datasource.py</files>
  <behavior>
    - Test: get_kline uses cache per D-19 (database 1m -> database 5m -> database kline -> network)
  </behavior>
  <action>
Add test_get_kline_cache that verifies:
- kline_fetcher is called to check cache before network request
- Cache data is returned when available
- Network call only happens on cache miss
  </action>
  <verify>
    <automated>python -m pytest backend_api_python/tests/test_ibkr_datasource.py::test_get_kline_cache -v --tb=short</automated>
  </verify>
  <done>
Cache test passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Strategy → IBKRDataSource.get_kline | Internal data fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | S | Invalid symbol | mitigate | Catch ValueError, return empty list |
| T-02-02 | R | Network timeout | mitigate | Set timeout, handle timeout error |
| T-02-03 | I | Cache corruption | accept | kline_fetcher handles cache validation |
</threat_model>

<verification>
Unit tests for get_kline pass
</verification>

<success_criteria>
get_kline() returns correct K-line format, handles errors gracefully, uses cache
</success_criteria>

<output>
After completion, create .planning/phases/01-ibkr-data-source-implementation/01-02-SUMMARY.md
</output>