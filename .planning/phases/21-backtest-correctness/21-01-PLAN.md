---
phase: 21-backtest-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/app/strategies/runners/cross_sectional_runner.py
  - backend_api_python/app/strategies/cross_sectional_signals.py
  - backend_api_python/app/strategies/cross_sectional.py
  - backend_api_python/app/services/nq100_sources.py
  - backend_api_python/scripts/cross_sectional/nq100_cross_sectional.py
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
user_setup: []
must_haves:
  truths:
    - "信号与执行严格隔离：signal_date=T，fill_date>=next_session(T)，禁止同 bar 成交"
    - "默认执行价为 next_open；invalid open 在 strict 模式不成交"
    - "不可交易语义固定：买失败留现金、卖失败继续持仓；唯一强制退出例外为 cash M&A"
    - "跨 IC effective_date 的执行必须从 T+1 顺延到 T+2"
  artifacts:
    - path: "backend_api_python/app/strategies/runners/cross_sectional_runner.py"
      provides: "execution filter chain + T+1/T+2 调度"
      min_lines: 120
    - path: "backend_api_python/app/strategies/cross_sectional_signals.py"
      provides: "signal/execution ledger 分离与意图结构"
      min_lines: 100
    - path: "backend_api_python/app/services/nq100_sources.py"
      provides: "effective_date / corporate-action 输入对齐"
      min_lines: 80
---

<objective>
实现 Phase 21 的执行语义主链路：T 日信号 -> T+1（或跨生效日 T+2）执行、严格可交易过滤、UNTRADABLE 状态机与 cash M&A 例外，确保回测行为可复现且无未来函数。
</objective>

<verification_contract>
本 plan 每个任务完成门槛 = `<acceptance_criteria>` 全部满足 且 `<verify>` 的全量回归通过。  
阻断命令必须包含：`cd backend_api_python && pytest tests/ -q`。  
禁止用单文件 pytest 结果替代全量放行。
</verification_contract>

<tasks>

<task type="auto">
  <name>Task 1: 信号-执行隔离与执行日期调度</name>
  <read_first>
    - .planning/phases/21-backtest-correctness/21-CONTEXT.md
    - .planning/phases/21-backtest-correctness/21-RESEARCH.md
    - backend_api_python/app/strategies/runners/cross_sectional_runner.py
    - backend_api_python/app/strategies/cross_sectional_signals.py
  </read_first>
  <files>backend_api_python/app/strategies/runners/cross_sectional_runner.py, backend_api_python/app/strategies/cross_sectional_signals.py</files>
  <action>
    在 runner 中引入明确的 `signal_date` 与 `execution_date` 字段，执行前断言 `execution_date > signal_date`。  
    默认执行价源固定为 `next_open`，并接入 fallback 配置（strict/ffill/close/bfill）；strict 下 invalid open 必须返回 skip。  
    落地 IC effective_date 顺延规则：若原执行日命中 effective_date，则强制再顺延一个交易日（T+2）。
  </action>
  <acceptance_criteria>
    - `cross_sectional_runner.py` 中存在显式比较 `execution_date > signal_date`
    - 存在统一执行价解析入口（如 `resolve_execution_price` 或等价函数）且支持 `strict|ffill|close|bfill`
    - 命中 effective_date 时存在执行日顺延逻辑（可 grep `effective_date` 与 `next_session` 同域出现）
  </acceptance_criteria>
  <verify>
    - `cd backend_api_python && pytest tests/test_backtest_correctness_phase21.py -k "no_lookahead or calendar_shift or fallback" -q`
    - `cd backend_api_python && pytest tests/ -q`
  </verify>
</task>

<task type="auto">
  <name>Task 2: 执行过滤链与 UNTRADABLE 状态机</name>
  <read_first>
    - .planning/phases/21-backtest-correctness/21-CONTEXT.md
    - .planning/phases/21-backtest-correctness/21-RESEARCH.md
    - backend_api_python/app/strategies/runners/cross_sectional_runner.py
    - backend_api_python/app/services/nq100_sources.py
  </read_first>
  <files>backend_api_python/app/strategies/runners/cross_sectional_runner.py, backend_api_python/app/services/nq100_sources.py, backend_api_python/app/strategies/cross_sectional.py</files>
  <action>
    在执行层加入固定顺序过滤链：公司行动/退市/代码变更/破产 -> 流动性阈值 -> next_open 有效性。  
    定义 `UNTRADABLE` 统一状态，买单失败写 `skipped_untradable_buy_cash_kept`，卖单失败写 `skipped_untradable_sell_hold_position`。  
    恢复交易当日禁止盘中强卖；仅在下一次完整调仓周期按 T->T+1 规则处理。  
    唯一例外为 `cash_mna_forced_exit`：按事件价强制平仓并入账现金。
  </action>
  <acceptance_criteria>
    - 执行日志中包含状态枚举：`skipped_untradable_buy_cash_kept`、`skipped_untradable_sell_hold_position`、`forced_cash_exit`
    - 存在长期停牌阈值与流动性阈值配置入口（env 或 config）
    - cash M&A 分支具备独立路径且不会走普通 hold 分支
  </acceptance_criteria>
  <verify>
    - `cd backend_api_python && pytest tests/test_backtest_correctness_phase21.py -k "untradable or sell_hold or cash_substitution or cash_mna" -q`
    - `cd backend_api_python && pytest tests/ -q`
  </verify>
</task>

</tasks>
