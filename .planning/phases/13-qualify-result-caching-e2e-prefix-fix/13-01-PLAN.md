---
phase: 13-qualify-result-caching-e2e-prefix-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/app/services/live_trading/ibkr_trading/client.py
  - backend_api_python/tests/test_ibkr_client.py
  - backend_api_python/app/services/live_trading/ibkr_trading/README.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - INFRA-01
user_setup: []
must_haves:
  truths:
    - "Two `place_market_order` calls with the same `symbol` and `market_type` within TTL invoke `qualifyContractsAsync` once (second call is a cache hit)."
    - "After empty qualify, exception during qualify, or `_validate_qualified_contract` failure, the cache entry for that `(symbol, market_type)` is removed."
    - "IBKR reconnect does not flush the qualify cache (`_on_connected` / `_on_disconnected` unchanged for cache)."
    - "Per-market TTL is read from env vars with default 600 seconds each."
  artifacts:
    - path: backend_api_python/app/services/live_trading/ibkr_trading/client.py
      provides: "_qualify_contract_async(contract, symbol, market_type) + in-memory TTL cache"
      contains: "_qualify_cache"
    - path: backend_api_python/tests/test_ibkr_client.py
      provides: "unit tests for cache hit and invalidation"
      contains: "qualifyContractsAsync"
    - path: backend_api_python/app/services/live_trading/ibkr_trading/README.md
      provides: "operator documentation for IBKR_QUALIFY_TTL_*_SEC"
      contains: "IBKR_QUALIFY_TTL_FOREX_SEC"
  key_links:
    - from: backend_api_python/app/services/live_trading/ibkr_trading/client.py
      to: self._ib.qualifyContractsAsync
      via: "_qualify_contract_async cache miss path"
      pattern: "qualifyContractsAsync"
---

<objective>
Implement in-memory per-`IBKRClient` caching for successful `qualifyContractsAsync` results keyed by `(symbol, market_type)` with per-market TTL from environment variables (default 600 seconds). Invalidate entries on qualify failure, exception, or post-qualify validation failure — **not** on IBKR reconnect (Phase 13 CONTEXT is authoritative over legacy ROADMAP/REQUIREMENTS wording).

Purpose: Cut redundant IB API qualify traffic while keeping behavior safe (stale cache may cause reject, not silent wrong trades).
Output: Updated `client.py`, extended `test_ibkr_client.py`, operator env documentation, reconciled `.planning/REQUIREMENTS.md` INFRA-01 and `.planning/ROADMAP.md` Phase 13 success criteria with reconnect policy.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-qualify-result-caching-e2e-prefix-fix/13-CONTEXT.md
@.planning/phases/13-qualify-result-caching-e2e-prefix-fix/13-RESEARCH.md
@.planning/ROADMAP.md

<interfaces>
From `backend_api_python/app/services/live_trading/ibkr_trading/client.py` (current — executor updates signatures):

```python
async def _qualify_contract_async(self, contract) -> bool:
    ...

# Call sites (must pass logical symbol + market_type after change):
# is_market_open ~1047, place_market_order ~1125, place_limit_order ~1203, get_quote ~1425
```

Cache key: **(symbol, market_type)** where `symbol` is the same string passed to `_create_contract` / order APIs (e.g. `EURUSD`, `AAPL`, `0700.HK`). `market_type` values: `Forex`, `USStock`, `HShare`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement qualify TTL cache and wire four call sites</name>
  <files>backend_api_python/app/services/live_trading/ibkr_trading/client.py</files>
  <read_first>
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py (full read: `__init__`, `_qualify_contract_async`, `_validate_qualified_contract`, `is_market_open`, `place_market_order`, `place_limit_order`, `get_quote`)
    - .planning/phases/13-qualify-result-caching-e2e-prefix-fix/13-CONTEXT.md (locked: key `(symbol, market_type)`, no reconnect clear, invalidate on bad qualify/validation)
  </read_first>
  <action>
    1. **Environment variables (defaults in code):** Read TTL in seconds with `int(os.environ.get(..., "600"))` for each:
       - `IBKR_QUALIFY_TTL_FOREX_SEC` → default `600`
       - `IBKR_QUALIFY_TTL_USSTOCK_SEC` → default `600`
       - `IBKR_QUALIFY_TTL_HSHARE_SEC` → default `600`
       Map `market_type` `"Forex"` → FOREX env, `"USStock"` → USSTOCK env, `"HShare"` → HSHARE env. Add a small helper `_qualify_ttl_seconds(self, market_type: str) -> int`.

    2. **Instance state in `IBKRClient.__init__`:** Add `self._qualify_cache: Dict[Tuple[str, str], Dict[str, Any]]` where each value is `{"expires_at": float}` (use `time.monotonic()` for expiry) plus a `"snapshot"` dict of contract fields to re-apply on hit. Key: `(symbol.strip().upper(), market_type)` or use symbol as passed from callers without mutation — **must match** the `symbol` argument at each call site.

    3. **Replace signature:** `async def _qualify_contract_async(self, contract, symbol: str, market_type: str) -> bool:`
       - Build cache key `key = (symbol, market_type)` (document: logical API symbol, same as order path).
       - **Cache hit:** If `key` in cache and current `time.monotonic()` is strictly less than `expires_at`, copy snapshot fields onto `contract` using `setattr` for at least: `conId`, `secType`, `localSymbol`, `exchange`, `currency`, and `tradingClass` if present in snapshot. Return `True` without calling `qualifyContractsAsync`.
       - **Cache miss / expired:** Remove stale entry. Call `await self._ib.qualifyContractsAsync(contract)`. If result is empty: call `_invalidate_qualify_cache(symbol, market_type)` (new private method), return `False`. On exception: log as today, `_invalidate_qualify_cache(symbol, market_type)`, return `False`.
       - **Success path:** If `len(...) > 0`, build snapshot from `contract` attributes listed above, store `expires_at = time.monotonic() + ttl`, return `True`.

    4. **Invalidation helper:** `def _invalidate_qualify_cache(self, symbol: str, market_type: str) -> None:` deletes `self._qualify_cache.pop((symbol, market_type), None)` — use the **same** key tuple as cache writes.

    5. **Post-qualify validation failure:** Wherever `_validate_qualified_contract(contract, market_type)` returns `(False, reason)` **after** `_qualify_contract_async` returned `True`, call `_invalidate_qualify_cache(symbol, market_type)` before returning the error to the caller. Locations: `is_market_open` (async `_task`), `place_market_order` `_do`, `place_limit_order` `_do`, `get_quote` `_task`.

    6. **Update all four call sites** to pass `symbol` and `market_type` into `_qualify_contract_async(contract, symbol, market_type)`:
       - `is_market_open`: `symbol`, `market_type` already in scope.
       - `place_market_order` / `place_limit_order` / `get_quote`: same.

    7. **Do not** add cache clearing to `_on_connected`, `_on_disconnected`, `_schedule_reconnect`, or `_reconnect_loop`.

    8. **RTH retry loop:** The inner loop may call `_qualify_contract_async` multiple times on failure; cache behavior should remain correct (failed attempts invalidate; success may populate cache for subsequent retries within TTL).
  </action>
  <acceptance_criteria>
    - `grep -n "_qualify_cache" backend_api_python/app/services/live_trading/ibkr_trading/client.py` returns at least one match.
    - `grep -n "IBKR_QUALIFY_TTL_FOREX_SEC" backend_api_python/app/services/live_trading/ibkr_trading/client.py` returns at least one match.
    - `grep -n "IBKR_QUALIFY_TTL_USSTOCK_SEC" backend_api_python/app/services/live_trading/ibkr_trading/client.py` returns at least one match.
    - `grep -n "IBKR_QUALIFY_TTL_HSHARE_SEC" backend_api_python/app/services/live_trading/ibkr_trading/client.py` returns at least one match.
    - `grep -n "async def _qualify_contract_async" backend_api_python/app/services/live_trading/ibkr_trading/client.py` shows a line containing `symbol: str, market_type: str`.
    - `grep -n "_on_connected" backend_api_python/app/services/live_trading/ibkr_trading/client.py` has **no** new line in that function body containing `_qualify_cache` or `_invalidate_qualify_cache` (reconnect must not clear cache).
    - `rg "_qualify_contract_async\\(contract\\)" backend_api_python/app/services/live_trading/ibkr_trading/client.py` returns **zero** matches (all call sites pass symbol + market_type).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m py_compile app/services/live_trading/ibkr_trading/client.py && test "$(grep -c "_qualify_cache" app/services/live_trading/ibkr_trading/client.py || true)" -ge 1 && grep -q "IBKR_QUALIFY_TTL_FOREX_SEC" app/services/live_trading/ibkr_trading/client.py && grep -q "IBKR_QUALIFY_TTL_USSTOCK_SEC" app/services/live_trading/ibkr_trading/client.py && grep -q "IBKR_QUALIFY_TTL_HSHARE_SEC" app/services/live_trading/ibkr_trading/client.py && grep "async def _qualify_contract_async" app/services/live_trading/ibkr_trading/client.py | grep -q "symbol: str, market_type: str" && ! rg -q '_qualify_contract_async\(contract\)' app/services/live_trading/ibkr_trading/client.py</automated>
  </verify>
  <note type="execute-phase">Task 1 verify is intentionally **structural only** (py_compile + greps) so it stays green before Task 2 updates `test_ibkr_client.py`. Full qualify unit tests are Task 2’s automated verify.</note>
  <done>client.py implements TTL cache, new signature is used at all four production call sites, reconnect handlers do not touch the cache; structural verify passes. `TestQualifyContractForex` / full qualify pytest green is Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests — cache hit, invalidation, update direct qualify tests</name>
  <files>backend_api_python/tests/test_ibkr_client.py</files>
  <read_first>
    - backend_api_python/tests/test_ibkr_client.py (`_make_client_with_mock_ib`, `TestQualifyContractForex`, imports)
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py (post-Task-1 `_qualify_contract_async` signature and behavior)
  </read_first>
  <action>
    1. **Update existing `TestQualifyContractForex` tests** that call `_qualify_contract_async(contract)` to pass `symbol="EURUSD"` and `market_type="Forex"` (or the appropriate symbol for the contract under test).

    2. **Add new test class** `TestQualifyContractCache` (or extend existing class) with `@patch("app.services.live_trading.ibkr_trading.client.ib_insync", _make_mock_ib_insync())`:
       - **test_qualify_cache_second_call_skips_ib:** Create client via `_make_client_with_mock_ib()`. Replace `client._ib.qualifyContractsAsync` with an `AsyncMock` that sets `conId`/`localSymbol`/`secType` on contracts like existing mocks. Call `place_market_order("EURUSD", "buy", 20000, "Forex")` twice. Assert `client._ib.qualifyContractsAsync.await_count == 1` (or call_count depending on mock type).
       - **test_qualify_cache_invalidated_on_empty_qualify:** First call succeeds (populate cache); configure second `qualifyContractsAsync` to return `[]` for a **new** client OR force miss by invalidation: simpler — call `place_market_order` with mock returning empty on first call, assert failure; then on a **fresh** client, first success then patch to empty on a theoretical second — **minimal approach:** one test that after empty qualify the internal cache has no entry: call `_qualify_contract_async` with success then mock empty and call again with **same** client — second call must invoke `qualifyContractsAsync` (cache invalidated on empty). Implement using asyncio event loop for direct `_qualify_contract_async` calls if easier.
       - **test_validate_failure_invalidates_cache:** Mock qualify success then make `_validate_qualified_contract` fail — easiest path: use contract with wrong `secType` after qualify mock sets wrong type, or patch `_validate_qualified_contract` to return `(False, "x")` once and assert subsequent order path re-qualifies — keep test deterministic.

    3. Use **Forex** symbol `EURUSD` and `market_type="Forex"` for cache tests to match production paths.

    4. If `TestQualifyContractForex` tests need `secType` on mock contracts for validation, ensure mocks set `secType` to `"CASH"` for Forex per `_EXPECTED_SEC_TYPES`.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_ibkr_client.py::TestQualifyContractForex -q` exits with code 0.
    - `python -m pytest tests/test_ibkr_client.py::TestQualifyContractCache -q` exits with code 0 (use exact class name chosen in implementation).
    - `grep -n "qualifyContractsAsync" backend_api_python/tests/test_ibkr_client.py` includes at least one line with `await_count` or `call_count` asserting single qualify for double order scenario.
    - `grep -n "_qualify_contract_async(contract" backend_api_python/tests/test_ibkr_client.py` returns **zero** matches (all calls include symbol and market_type per new signature).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest tests/test_ibkr_client.py::TestQualifyContractForex tests/test_ibkr_client.py::TestQualifyContractCache -q --tb=short 2>&1 | tail -15 && echo "--- FULL REGRESSION ---" && python -m pytest -q --tb=line 2>&1 | tail -5</automated>
  </verify>
  <done>New/updated tests prove cache hit and invalidation; all qualify unit tests pass; full regression (~928 tests) green.</done>
</task>

<task type="auto">
  <name>Task 3: Operator docs + reconcile REQUIREMENTS.md and ROADMAP Phase 13</name>
  <files>backend_api_python/app/services/live_trading/ibkr_trading/README.md,.planning/REQUIREMENTS.md,.planning/ROADMAP.md</files>
  <read_first>
    - backend_api_python/app/services/live_trading/ibkr_trading/README.md (existing structure)
    - .planning/REQUIREMENTS.md (INFRA-01 line)
    - .planning/ROADMAP.md (Phase 13 Success Criteria block)
  </read_first>
  <action>
    1. **README.md:** Add a section titled `## Contract qualification cache` containing a markdown table or bullet list with **exact** env var names: `IBKR_QUALIFY_TTL_FOREX_SEC`, `IBKR_QUALIFY_TTL_USSTOCK_SEC`, `IBKR_QUALIFY_TTL_HSHARE_SEC`, default **600**, unit seconds, and one sentence that reconnect does **not** clear the cache (TTL + failure/validation invalidation only).

    2. **REQUIREMENTS.md:** Replace the INFRA-01 bullet text so it states: `qualifyContractsAsync` results cached per symbol with configurable TTL **per market category (Forex / USStock / HShare)**; cache invalidated on qualify failure or post-qualify validation failure for that symbol; **IBKR reconnect does not clear the cache** (supersedes earlier reconnect wording).

    3. **ROADMAP.md — Phase 13 detail block:** In `### Phase 13: Qualify result caching + E2E prefix fix` → `**Success Criteria**`, replace item **2** (reconnect clears cache) with text equivalent to: *After qualify failure, qualify exception, or post-qualify validation failure for a symbol, the cached entry for that symbol/market is removed; IBKR reconnect does not flush the cache (see Phase 13 CONTEXT).* Update `**Plans**:` from `TBD` to `**Plans:** 2 plans` and add:
       - `[ ] 13-01-PLAN.md — Qualify TTL cache + docs + requirements reconcile`
       - `[ ] 13-02-PLAN.md — E2E Flask blueprint prefix /api alignment`

    3b. **ROADMAP.md — v1.1 one-liner (line ~32):** Under `### v1.1 — Tech Debt Cleanup + Limit Orders`, edit the checklist bullet for **Phase 13** so it **does not** say “invalidate on reconnect”. Replace with wording aligned with 13-CONTEXT: TTL cache for `qualifyContractsAsync` with per-market TTL and **targeted** invalidation (qualify/validation failures); **IBKR reconnect does not flush** the cache; fix E2E test API prefix drift. This reconciles the summary list with the detailed success criteria and CONTEXT.

    3c. **ROADMAP.md — Phase 13 Goal (if still misleading):** If `**Goal**` under Phase 13 still implies reconnect-driven freshness, rewrite one sentence so it matches reconnect-not-flush (e.g. emphasize TTL + targeted invalidation, not reconnect clearing).

    4. Update the Progress table row for phase 13 `Plans Complete` from `0/?` to `0/2` if not already.

    5. Ensure no duplicate conflicting plan lists elsewhere in ROADMAP for phase 13.
  </action>
  <acceptance_criteria>
    - `grep -n "IBKR_QUALIFY_TTL_FOREX_SEC" backend_api_python/app/services/live_trading/ibkr_trading/README.md` returns at least one match.
    - `grep -n "reconnect" backend_api_python/app/services/live_trading/ibkr_trading/README.md` returns at least one line stating reconnect does not clear the cache (case-insensitive grep acceptable).
    - `.planning/REQUIREMENTS.md` contains the substring `IBKR reconnect does not clear the cache` OR `reconnect does not clear` in the INFRA-01 bullet.
    - `.planning/ROADMAP.md` contains `13-02-PLAN.md` in the Phase 13 plan list.
    - `.planning/ROADMAP.md` Phase 13 success criteria **2** does not contain the exact phrase `After IBKR reconnect, cached qualify entries are cleared` (old behavior removed).
    - The v1.1 Phase 13 bullet (under `### v1.1`) does not contain the substring `invalidate on reconnect` (summary line matches reconnect-not-flush policy).
  </acceptance_criteria>
  <verify>
    <automated>grep -n "IBKR_QUALIFY_TTL" /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python/app/services/live_trading/ibkr_trading/README.md; grep -n "INFRA-01" /home/hgq/workspace/aicoder/ws4/QuantDinger/.planning/REQUIREMENTS.md | head -3</automated>
  </verify>
  <done>Operators can discover TTL env vars; REQUIREMENTS and ROADMAP match Phase 13 CONTEXT on reconnect.</done>
</task>

</tasks>

<verification>
- `cd backend_api_python && python -m pytest tests/test_ibkr_client.py -k "QualifyContract" -q`
- Full regression: `cd backend_api_python && python -m pytest -q` (expect ~928 tests green per STATE.md)
</verification>

<success_criteria>
- INFRA-01 satisfied with tests + documented env vars + reconciled planning docs
- No qualify cache flush on reconnect
</success_criteria>

<output>
After completion, create `.planning/phases/13-qualify-result-caching-e2e-prefix-fix/13-01-SUMMARY.md`
</output>
