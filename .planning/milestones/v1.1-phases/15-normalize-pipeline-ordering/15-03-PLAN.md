---
phase: 15-normalize-pipeline-ordering
plan: 03
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified:
  - backend_api_python/app/services/signal_executor.py
  - backend_api_python/tests/test_signal_executor.py
autonomous: true
requirements:
  - INFRA-03
must_haves:
  truths:
    - "`SignalExecutor` uses `get_market_pre_normalizer` and `pre_normalize` (not legacy names) on the live order path."
    - "Enqueue amount matches post-`pre_normalize` value for representative scenarios."
  artifacts:
    - path: backend_api_python/app/services/signal_executor.py
      provides: "get_market_pre_normalizer + pre_normalize"
      contains: "get_market_pre_normalizer"
  key_links:
    - from: signal_executor.execute
      to: get_market_pre_normalizer(market_category).pre_normalize
      via: "amount assignment before enqueue"
      pattern: "pre_normalize\\("
---

<objective>
Rename upstream market pre-normalization in `SignalExecutor.execute()` to use `get_market_pre_normalizer().pre_normalize()` so DB-persisted pending quantities match the IBKR client path (idempotent double call). Add optional integration test (TC-15-T3-03).

Purpose: Defense-in-depth + consistent naming for INFRA-03.

Output: Updated `signal_executor.py` and tests.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-normalize-pipeline-ordering/15-CONTEXT.md
@.planning/phases/15-normalize-pipeline-ordering/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SignalExecutor import and pre_normalize call site</name>
  <files>backend_api_python/app/services/signal_executor.py</files>
  <read_first>
    - backend_api_python/app/services/signal_executor.py
    - backend_api_python/app/services/live_trading/order_normalizer/__init__.py
    - .planning/phases/15-normalize-pipeline-ordering/15-CONTEXT.md
  </read_first>
  <action>
    Replace `from app.services.live_trading.order_normalizer import get_normalizer` with `from app.services.live_trading.order_normalizer import get_market_pre_normalizer`.

    Replace `normalizer = get_normalizer(market_category)` with `normalizer = get_market_pre_normalizer(market_category)`.

    Replace `amount = normalizer.normalize(amount, symbol)` with `amount = normalizer.pre_normalize(amount, symbol)`.

    Keep the `raw_amount` / logging block unchanged except variable names if needed for clarity (`pre_normalize` vs `normalize` in log text is optional; prefer log message `"Order quantity normalized"` unchanged).
  </action>
  <acceptance_criteria>
    - **TC-15-T3-01**: `grep -n "get_market_pre_normalizer" backend_api_python/app/services/signal_executor.py` returns ≥1 line; `grep -n "get_normalizer" backend_api_python/app/services/signal_executor.py` returns **0** lines.
    - **TC-15-T3-02**: `grep -n "pre_normalize(" backend_api_python/app/services/signal_executor.py` returns ≥1 line; `grep -n "normalizer.normalize" backend_api_python/app/services/signal_executor.py` returns **0** lines.
    - **TC-15-T3-03**: Covered in Task 2 — patch `get_market_pre_normalizer` to return a mock; assert enqueued `amount` equals mock `pre_normalize` output.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python &amp;&amp; ! rg -n "get_normalizer" app/services/signal_executor.py &amp;&amp; rg -n "get_market_pre_normalizer" app/services/signal_executor.py &amp;&amp; rg -n "pre_normalize" app/services/signal_executor.py</automated>
  </verify>
  <done>SignalExecutor references only `get_market_pre_normalizer` and `pre_normalize`.</done>
</task>

<task type="auto">
  <name>Task 2: test_signal_executor — TC-15-T3-03 integration</name>
  <files>backend_api_python/tests/test_signal_executor.py</files>
  <read_first>
    - backend_api_python/tests/test_signal_executor.py
    - backend_api_python/app/services/signal_executor.py
  </read_first>
  <action>
    Add `TestSignalExecutorMarketPreNormalize` (or extend `TestSignalExecutorExecute`) with one test:

    - Patch `app.services.signal_executor.get_market_pre_normalizer` (patch where used: **signal_executor module attribute after import** — use `patch("app.services.signal_executor.get_market_pre_normalizer")`).

    - Configure mock normalizer: `pre_normalize` returns a fixed float (e.g. `42.0`); `pre_check` not used on this path if not invoked — only `pre_normalize` is required before enqueue.

    - Drive `execute()` through a path that hits the normalizer block (mirror `test_open_long_sizing_swap` strategy_ctx shape but ensure `market_category` and exchange route through live trading / IBKR if required — **if the normalizer block is only hit for specific exchanges**, copy the minimal existing test that already executed the block from coverage; otherwise add `strategy_ctx` with `_market_type` and fields needed so execution reaches `get_market_pre_normalizer`).

    - Assert `execute_exchange_order` kwargs `amount` equals the mocked `pre_normalize` return value.

    Docstring must cite **TC-15-T3-03**.
  </action>
  <acceptance_criteria>
    - **TC-15-T3-03**: New test passes; `execute_exchange_order` receives `amount` equal to mocked `pre_normalize` output (e.g. `42.0`).
    - Test function name or docstring contains `TC-15-T3-03`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python &amp;&amp; python3 -m pytest tests/test_signal_executor.py::TestSignalExecutorMarketPreNormalize::test_tc_15_t3_03_enqueue_uses_pre_normalize -v --tb=short</automated>
  </verify>
  <done>TC-15-T3-03 automated with fixed pytest node id (executor MUST use this class/method name).</done>
</task>

</tasks>

<verification>
Executor MUST name the new test so this passes reliably:

`cd backend_api_python &amp;&amp; python3 -m pytest tests/test_signal_executor.py::TestSignalExecutorMarketPreNormalize::test_tc_15_t3_03_enqueue_uses_pre_normalize -v --tb=short`

If class/method names differ, update this line in SUMMARY to match actual node id (plan checker should catch mismatch).
</verification>

<success_criteria>
- TC-15-T3-01–TC-15-T3-03 satisfied with grep + pytest.
</success_criteria>

<output>
After completion, create `.planning/phases/15-normalize-pipeline-ordering/15-03-SUMMARY.md`
</output>
