---
phase: 15-normalize-pipeline-ordering
plan: 02
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified:
  - backend_api_python/app/services/live_trading/ibkr_trading/client.py
  - backend_api_python/tests/test_ibkr_client.py
autonomous: true
requirements:
  - INFRA-03
must_haves:
  truths:
    - "`place_market_order` and `place_limit_order` call `pre_normalize` then `pre_check` on the sync path before `_submit`."
    - "Async path uses the **pre-normalized** quantity for `_align_qty_to_contract` and `totalQuantity`."
    - "USStock fractional inputs like `7.8` floor to whole shares and can succeed when mocks return Submitted."
  artifacts:
    - path: backend_api_python/app/services/live_trading/ibkr_trading/client.py
      provides: "import from app.services.live_trading.order_normalizer; pre_normalize → pre_check → qualify → align"
      contains: "get_market_pre_normalizer"
    - path: backend_api_python/tests/test_ibkr_client.py
      provides: "TestQuantityGuard + pipeline tests for TC-15-T2/T6"
      contains: "pre_normalize"
  key_links:
    - from: place_market_order sync preamble
      to: get_market_pre_normalizer(market_type).pre_normalize
      via: "local variable qty closed over in _do()"
      pattern: "_align_qty_to_contract\\(contract, qty"
---

<objective>
Implement INFRA-03 in `IBKRClient`: `pre_normalize` → `pre_check` → `_qualify_contract_async` → `_align_qty_to_contract` → `placeOrder`, threading the **same** `qty` from pre-normalization into async align and order construction. Update `TestQuantityGuard` and add focused tests for call order and align input (TC-15-T2, TC-15-T6).

Purpose: Broker entry matches locked pipeline; fractional US quantities floor before qualify/align.

Output: Patched `client.py` and updated/new tests in `test_ibkr_client.py`.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-normalize-pipeline-ordering/15-CONTEXT.md
@.planning/phases/15-normalize-pipeline-ordering/15-RESEARCH.md

## Import (exact)

Replace:
`from app.services.live_trading.ibkr_trading.order_normalizer import get_normalizer`
with:
`from app.services.live_trading.order_normalizer import get_market_pre_normalizer`

## Sync preamble (exact pattern)

```python
n = get_market_pre_normalizer(market_type)
qty = n.pre_normalize(quantity, symbol)
ok, reason = n.pre_check(qty, symbol)
if not ok:
    return LiveOrderResult(success=False, message=reason, exchange_id=self.engine_id)
```

Inside `_do()`, use `qty` (not `quantity`) for `_align_qty_to_contract(contract, qty, symbol)` and for `totalQuantity=...` after align returns.
</context>

<tasks>

<task type="auto">
  <name>Task 1: IBKRClient place_market_order and place_limit_order pipeline</name>
  <files>backend_api_python/app/services/live_trading/ibkr_trading/client.py</files>
  <read_first>
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py
    - backend_api_python/app/services/live_trading/order_normalizer/__init__.py
    - .planning/phases/15-normalize-pipeline-ordering/15-CONTEXT.md
  </read_first>
  <action>
    In `place_market_order` (starts ~1159): remove import of `ibkr_trading.order_normalizer`. Add `from app.services.live_trading.order_normalizer import get_market_pre_normalizer`. After method signature, set `n = get_market_pre_normalizer(market_type)`, `qty = n.pre_normalize(quantity, symbol)`, `ok, reason = n.pre_check(qty, symbol)`; on failure return `LiveOrderResult(success=False, message=reason, exchange_id=self.engine_id)`. In the inner `async def _do():`, replace every `_align_qty_to_contract(contract, quantity, symbol)` with `_align_qty_to_contract(contract, qty, symbol)`. Replace quantity-based error messages that still reference outer `quantity` where they describe align-zero cases — use the **aligned** variable name returned from `_align_qty_to_contract` (keep Forex minimum-size wording if still applicable). Ensure `MarketOrder`/`LimitOrder` use the post-align quantity variable (same as today structurally, but sourced from `qty` input to align).

    Apply the **identical** preamble and `qty` threading to `place_limit_order` (~1238+).

    Resolve TIF **after** sync preamble (CONTEXT discretion: no functional requirement to reorder TIF vs pre_check).
  </action>
  <acceptance_criteria>
    - `grep -n "ibkr_trading.order_normalizer" backend_api_python/app/services/live_trading/ibkr_trading/client.py` returns **no** matches.
    - `grep -n "get_market_pre_normalizer" backend_api_python/app/services/live_trading/ibkr_trading/client.py` returns at least **2** matches (market + limit paths).
    - `grep -n "pre_normalize" backend_api_python/app/services/live_trading/ibkr_trading/client.py` returns at least **2** matches.
    - `grep -n "_align_qty_to_contract(contract, qty" backend_api_python/app/services/live_trading/ibkr_trading/client.py` OR equivalent with same `qty` name in both `_do` closures — **no** `_align_qty_to_contract(contract, quantity` remaining.
    - `python3 -m compileall -q app/services/live_trading/ibkr_trading/client.py` exits 0 (no syntax errors).
    - Behavioral TC-15-T2 and TC-15-T6 tests are proven by Task 2's pytest — see Task 2 acceptance_criteria for full behavioral outcomes.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python &amp;&amp; ! rg -n "ibkr_trading\.order_normalizer" app/services/live_trading/ibkr_trading/client.py &amp;&amp; rg -n "get_market_pre_normalizer" app/services/live_trading/ibkr_trading/client.py &amp;&amp; rg -n "_align_qty_to_contract\(contract, qty" app/services/live_trading/ibkr_trading/client.py &amp;&amp; python3 -m compileall -q app/services/live_trading/ibkr_trading/client.py</automated>
  </verify>
  <done>Client imports `get_market_pre_normalizer` from canonical package; `_do` uses `qty` for align; TC-15-T2/T6 behavioral proof deferred to Task 2 pytest.</done>
</task>

<task type="auto">
  <name>Task 2: test_ibkr_client — TestQuantityGuard + TestIBKRPreNormalizePipeline</name>
  <files>backend_api_python/tests/test_ibkr_client.py</files>
  <read_first>
    - backend_api_python/tests/test_ibkr_client.py
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py
    - .planning/phases/15-normalize-pipeline-ordering/15-RESEARCH.md
  </read_first>
  <action>
    Add a new class `TestIBKRPreNormalizePipeline` in `test_ibkr_client.py` with:

    1. **TC-15-T2-05**: Patch `MarketPreNormalizer.pre_normalize` and `MarketPreNormalizer.pre_check` on the class or patch `app.services.live_trading.ibkr_trading.client.get_market_pre_normalizer` to return a `MagicMock` with `pre_normalize`/`pre_check` that records call order; call `place_market_order` with USStock; assert call order list is `['pre_normalize','pre_check']` before `client._ib.qualifyContractsAsync` (or equivalent mock) is awaited — use existing mock patterns from the file.

    2. **TC-15-T2-06**: Patch `IBKRClient._align_qty_to_contract` with `AsyncMock(return_value=7)` (or sync if wrapper is sync — match existing `_align` mock style in file); call `place_market_order(..., 7.8, "USStock")`; assert `_align_qty_to_contract` called with quantity argument **7** (not 7.8).

    Update `TestQuantityGuard`:
    - Replace `test_market_order_rejects_fractional` with a test asserting **success** with `totalQuantity==7` for quantity `7.8` (TC-15-T6-01 / TC-15-T2-01).
    - Replace `test_limit_order_rejects_fractional` with **success** with `totalQuantity==3` for `3.5` (TC-15-T6-02 / TC-15-T2-02).
    - Keep zero/negative/HShare tests consistent with pre_normalize/pre_check semantics (TC-15-T2-03, TC-15-T2-04).

    Docstrings must mention TC IDs `TC-15-T2-*` / `TC-15-T6-*` where applicable.
  </action>
  <acceptance_criteria>
    - **TC-15-T2-01 / TC-15-T6-01**: Test proves market order `7.8` USStock → `result.success is True` and `placeOrder` `totalQuantity == 7` (fractional floors to whole, then succeeds).
    - **TC-15-T2-02 / TC-15-T6-02**: Limit order `3.5` USStock → `totalQuantity == 3`, `success is True`.
    - **TC-15-T2-03**: Negative quantity `place_market_order("AAPL","buy",-5,"USStock")` → `success is False`, `placeOrder` not called.
    - **TC-15-T2-04**: HShare `place_market_order("00005","buy",3,"HShare")` → `success is False`, `"400" in result.message`, `placeOrder` not called.
    - **TC-15-T2-05**: Recorded call order proves `pre_normalize` is invoked before `pre_check`, and both occur before `qualifyContractsAsync`.
    - **TC-15-T2-06**: `_align_qty_to_contract` receives quantity `7` (not `7.8`) when raw input was `7.8` USStock.
    - `grep -n "TestIBKRPreNormalizePipeline" backend_api_python/tests/test_ibkr_client.py` matches at least one line.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python &amp;&amp; python3 -m pytest tests/test_ibkr_client.py::TestQuantityGuard tests/test_ibkr_client.py::TestIBKRPreNormalizePipeline -v --tb=short</automated>
  </verify>
  <done>All TC-15-T2 and TC-15-T6 behaviors encoded as passing tests with explicit TC references in docstrings.</done>
</task>

</tasks>

<verification>
`cd backend_api_python &amp;&amp; python3 -m pytest tests/test_ibkr_client.py::TestQuantityGuard tests/test_ibkr_client.py::TestIBKRPreNormalizePipeline -v --tb=short` exits 0.
</verification>

<success_criteria>
- IBKRClient implements pre_normalize → pre_check → qualify → align with single `qty` closure.
- Pytest proves TC-15-T2-01–TC-15-T2-06 and TC-15-T6-01–TC-15-T6-02.
</success_criteria>

<output>
After completion, create `.planning/phases/15-normalize-pipeline-ordering/15-02-SUMMARY.md`
</output>
