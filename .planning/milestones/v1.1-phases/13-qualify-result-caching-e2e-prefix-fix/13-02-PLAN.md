---
phase: 13-qualify-result-caching-e2e-prefix-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/tests/test_forex_ibkr_e2e.py
autonomous: true
requirements:
  - TEST-01
user_setup: []
must_haves:
  truths:
    - "Flask test app registers `strategy_bp` with `url_prefix='/api'` (same as `register_routes` in `app/routes/__init__.py`)."
    - "Strategy create E2E POST uses path `/api/strategies/create` (not `/api/strategy/...`)."
  artifacts:
    - path: backend_api_python/tests/test_forex_ibkr_e2e.py
      provides: "E2E URLs aligned with production blueprint prefix"
      contains: "url_prefix='/api'"
  key_links:
    - from: backend_api_python/tests/test_forex_ibkr_e2e.py
      to: app.routes.strategy.strategy_bp
      via: "register_blueprint(strategy_bp, url_prefix='/api')"
      pattern: "register_blueprint\\(strategy_bp"
---

<objective>
Fix `test_forex_ibkr_e2e.py` so the test Flask app registers strategy routes with `url_prefix='/api'`, matching production `register_routes` (`app/routes/__init__.py` line `app.register_blueprint(strategy_bp, url_prefix='/api')`). Update all HTTP paths and docstrings that still say `/api/strategy/...`.

Purpose: Eliminate `/api/strategy/` vs `/api/` drift so E2E exercises the same URL shape as production (TEST-01).
Output: Updated E2E module only; no new E2E test cases (per Phase 13 CONTEXT).
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend_api_python/app/routes/__init__.py
@backend_api_python/app/routes/strategy.py

Production registration:

```37:37:backend_api_python/app/routes/__init__.py
    app.register_blueprint(strategy_bp, url_prefix='/api')
```

Strategy create route (blueprint-relative `/strategies/create` → full path `/api/strategies/create`):

```64:64:backend_api_python/app/routes/strategy.py
@strategy_bp.route('/strategies/create', methods=['POST'])
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register strategy blueprint at /api and fix E2E URLs</name>
  <files>backend_api_python/tests/test_forex_ibkr_e2e.py</files>
  <read_first>
    - backend_api_python/tests/test_forex_ibkr_e2e.py (entire file)
    - backend_api_python/app/routes/__init__.py (lines 1–45)
    - backend_api_python/app/routes/strategy.py (grep `strategies/create` and related routes if needed)
  </read_first>
  <action>
    1. In `client_fixture`, change `app.register_blueprint(strategy_bp, url_prefix="/api/strategy")` to `app.register_blueprint(strategy_bp, url_prefix="/api")` (use single quotes if the file uses single quotes consistently elsewhere).

    2. In `test_uc_sa_e2e_api_forex_create_returns_200`, change the POST URL from `"/api/strategy/strategies/create"` to `"/api/strategies/create"`.

    3. Update the docstring that currently says `POST /api/strategy/strategies/create` to `POST /api/strategies/create`.

    4. Run `rg '/api/strategy' backend_api_python/tests/test_forex_ibkr_e2e.py` and remove or replace **every** remaining occurrence so no `/api/strategy` substring remains in this file.

    5. Do **not** add new test functions or new E2E scenarios (Phase 13 CONTEXT defers new cases to Phase 18).
  </action>
  <acceptance_criteria>
    - `rg '/api/strategy' backend_api_python/tests/test_forex_ibkr_e2e.py` exits with no matches (exit code 1 from ripgrep = zero matches).
    - `grep -n "url_prefix=" backend_api_python/tests/test_forex_ibkr_e2e.py` shows a line containing `url_prefix="/api"` or `url_prefix='/api'`.
    - `grep -n '"/api/strategies/create"' backend_api_python/tests/test_forex_ibkr_e2e.py` returns at least one match (POST path).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest tests/test_forex_ibkr_e2e.py -q --tb=short 2>&1 | tail -20 && echo "--- FULL REGRESSION ---" && python -m pytest -q --tb=line 2>&1 | tail -5</automated>
  </verify>
  <done>E2E module posts to `/api/strategies/create`; full `test_forex_ibkr_e2e.py` passes; full regression (~928 tests) green.</done>
</task>

</tasks>

<verification>
- `cd backend_api_python && python -m pytest tests/test_forex_ibkr_e2e.py -q`
- Optional broader gate: `cd backend_api_python && python -m pytest -q` after Plan 01 + 02 both executed
</verification>

<success_criteria>
- TEST-01 satisfied: blueprint prefix and paths match production routing
- `pytest tests/test_forex_ibkr_e2e.py` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/13-qualify-result-caching-e2e-prefix-fix/13-02-SUMMARY.md`
</output>
