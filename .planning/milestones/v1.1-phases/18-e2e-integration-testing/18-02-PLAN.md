---
phase: 18-e2e-integration-testing
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - backend_api_python/tests/test_e2e_qualify_cache_ibkr.py
autonomous: true
requirements:
  - TRADE-05
  - TRADE-06
must_haves:
  truths:
    - "At least one test proves qualifyContractsAsync is not called twice for the same (symbol, market_type) within TTL (cache hit)."
    - "Tests prove reconnect/disconnect handlers do not clear _qualify_cache; invalidation on qualify failure/empty result is exercised."
    - "At least one test names TRADE-05 and exercises Metals (CMDTY) qualify → place_order → callback with mock IBKR."
  artifacts:
    - path: backend_api_python/tests/test_e2e_qualify_cache_ibkr.py
      provides: "qualify cache + TRADE-05 metals chain coverage"
      contains: "TRADE-05"
      min_lines: 120
  key_links:
    - from: "tests/test_e2e_qualify_cache_ibkr.py"
      to: "app.services.live_trading.ibkr_trading.client.IBKRClient._qualify_contract_async"
      via: "qualifyContractsAsync call counts / monotonic patch"
      pattern: "_qualify_cache"
---

<objective>
Add `test_e2e_qualify_cache_ibkr.py` implementing detailed qualify-cache E2E (hit, miss, TTL via `time.monotonic` patch, per-market TTL keys Forex vs USStock vs Metals, reconnect no flush, invalidation on exception/empty qualify) plus an explicit **TRADE-05** metals mock-IBKR chain (qualify → order → callbacks) per 18-CONTEXT.md.

Purpose: Close Phase 13–deferred qualify cache verification and satisfy TRADE-05/TRADE-06 requirement that client-level qualify behavior is integration-tested.

Output: New pytest module green in isolation and in full suite.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-e2e-integration-testing/18-CONTEXT.md
@.planning/phases/18-e2e-integration-testing/18-RESEARCH.md

## Concrete implementation references

- TTL mapping: `IBKRClient._qualify_ttl_seconds` — Forex and Metals both use env `IBKR_QUALIFY_TTL_FOREX_SEC` (default 600); USStock `IBKR_QUALIFY_TTL_USSTOCK_SEC`; HShare `IBKR_QUALIFY_TTL_HSHARE_SEC` (see `client.py` ~901–909).
- Reconnect: `_on_connected` / `_on_disconnected` must **not** clear `_qualify_cache` (18-RESEARCH cites lines ~583–589 vs cache in `_qualify_contract_async`).
- Metals chain pattern: reuse `tests.helpers.ibkr_mocks._make_ibkr_client_for_e2e` with `sec_type="CMDTY"`, symbol `XAGUSD`, `con_id=77124483`, and `PendingOrderWorker._execute_live_order` patch pattern from `test_forex_ibkr_e2e.py::test_uc_sa_e2e_xagusd_open_close_full_chain`.
- Cache hit test: wrap `ib.qualifyContractsAsync` with `unittest.mock.Mock(wraps=...)` or increment a list on each call; two `place_market_order` (or two code paths that call `_qualify_contract_async`) same symbol/market_type within TTL → second path does not increase call count.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Qualify cache E2E tests (hit, miss, TTL, invalidation, reconnect, per-market TTL)</name>
  <files>backend_api_python/tests/test_e2e_qualify_cache_ibkr.py</files>
  <read_first>
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py (`_qualify_contract_async`, `_invalidate_qualify_cache`, `_qualify_ttl_seconds`, `_on_connected`, `_on_disconnected`)
    - backend_api_python/tests/helpers/ibkr_mocks.py
    - backend_api_python/tests/test_forex_ibkr_e2e.py (`test_uc_sa_e2e_forex_full_chain` patch pattern)
  </read_first>
  <action>
    Create `backend_api_python/tests/test_e2e_qualify_cache_ibkr.py` with **separate** test functions (names must start with `test_`):
    1. `test_qualify_cache_hit_no_second_qualify_call` — same `IBKRClient` instance, same `symbol` + `market_type` (use Forex EURUSD-style), two operations that trigger `_qualify_contract_async` within TTL; assert `qualifyContractsAsync` call count increases by 1 only (total 1 across both paths). Use `patch("app.services.live_trading.ibkr_trading.client.time.monotonic", side_effect=...)` to keep time inside TTL without `sleep`.
    2. `test_qualify_cache_miss_after_ttl` — after monotonic advances by `ttl + 1` seconds (use TTL from client’s `_qualify_ttl_seconds("Forex")` or patch env `IBKR_QUALIFY_TTL_FOREX_SEC=2` in test setup), second qualify path calls `qualifyContractsAsync` again (count 2).
    3. `test_qualify_cache_invalidation_on_qualify_exception` — `qualifyContractsAsync` raises; assert cache entry removed (next call qualifies again). Use exception type consistent with client code path.
    4. `test_qualify_cache_invalidation_on_empty_qualify` — return `[]` from qualify; assert invalidation behavior per `client.py`.
    5. `test_qualify_cache_survives_ibkr_disconnect_connect` — populate cache, call `client._on_disconnected()` then `client._on_connected()`, assert `_qualify_cache` still has entry for a key you set (or re-hit uses cache without new qualify call — choose one verifiable assertion on `len(client._qualify_cache)` or call count).
    6. `test_qualify_ttl_forex_vs_usstock_distinct` — with env `IBKR_QUALIFY_TTL_FOREX_SEC=10` and `IBKR_QUALIFY_TTL_USSTOCK_SEC=99`, assert `_qualify_ttl_seconds("Forex")==10` and `_qualify_ttl_seconds("USStock")==99` on a fresh `IBKRClient` instance (or document patch `os.environ` in test before client init).

    Use real `IBKRClient` with `tests.helpers.ibkr_mocks._make_client_with_mock_ib` / `_wire_ib_events` as in existing tests. Import `pytest`, `unittest.mock.patch`, `MagicMock` as needed.
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/test_e2e_qualify_cache_ibkr.py -x -q --tb=short -k "qualify_cache"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def test_qualify_cache_hit" backend_api_python/tests/test_e2e_qualify_cache_ibkr.py` returns exactly one match line
    - `grep -n "_on_disconnected\|_on_connected" backend_api_python/tests/test_e2e_qualify_cache_ibkr.py` returns at least one line
    - `grep -n "qualifyContractsAsync" backend_api_python/tests/test_e2e_qualify_cache_ibkr.py` returns at least one line
    - `cd backend_api_python && pytest tests/test_e2e_qualify_cache_ibkr.py -x -q --tb=short -k "qualify_cache"` exit code 0
  </acceptance_criteria>
  <done>Six behaviors above are implemented as named tests; pytest subset green.</done>
</task>

<task type="auto">
  <name>Task 2: TRADE-05 metals qualify → order → callback E2E in same module</name>
  <files>backend_api_python/tests/test_e2e_qualify_cache_ibkr.py</files>
  <read_first>
    - backend_api_python/tests/test_forex_ibkr_e2e.py (`test_uc_sa_e2e_xagusd_open_close_full_chain`, lines ~275–380)
    - backend_api_python/tests/helpers/ibkr_mocks.py (`_make_ibkr_client_for_e2e` with `sec_type="CMDTY"`)
    - backend_api_python/app/services/pending_order_worker.py (`_execute_live_order` signature — grep file)
  </read_first>
  <action>
    Add test function **`test_trade05_metals_mock_ibkr_qualify_order_callback_xagusd`** to `test_e2e_qualify_cache_ibkr.py` that:
    - Docstring first line contains exact substring `TRADE-05`.
    - Uses `market_category="Metals"`, symbol `XAGUSD`, `sec_type="CMDTY"`, `con_id=77124483`, `local_symbol="XAGUSD"` matching `test_uc_sa_e2e_xagusd_open_close_full_chain`.
    - Patches: `PendingOrderWorker._notify_live_best_effort`, `records.mark_order_sent`, `records.mark_order_failed`, `load_strategy_configs`, `create_client` returning `ibkr_client` from `_make_ibkr_client_for_e2e("XAGUSD", 77124483, "XAGUSD", sec_type="CMDTY")`.
    - Calls `PendingOrderWorker()._execute_live_order(...)` once for `open_long` with non-zero amount so `placeOrder` is invoked; fires mock callbacks via `_fire_callbacks_after_fill` or equivalent so `qualifyContractsAsync` + `placeOrder` + order status path execute without exception.
    - Asserts `getattr(trade.contract, "secType", None) == "CMDTY"` OR `symbol == "XAGUSD"` on placed trade (same assertion style as existing XAGUSD test).

    Do **not** edit `test_forex_ibkr_e2e.py`.
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/test_e2e_qualify_cache_ibkr.py::test_trade05_metals_mock_ibkr_qualify_order_callback_xagusd -x -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TRADE-05" backend_api_python/tests/test_e2e_qualify_cache_ibkr.py` returns at least one line
    - `grep -n "def test_trade05_metals_mock_ibkr_qualify_order_callback_xagusd" backend_api_python/tests/test_e2e_qualify_cache_ibkr.py` returns one line
    - `grep -n "CMDTY\|77124483" backend_api_python/tests/test_e2e_qualify_cache_ibkr.py` returns at least one line
    - `cd backend_api_python && pytest tests/test_e2e_qualify_cache_ibkr.py::test_trade05_metals_mock_ibkr_qualify_order_callback_xagusd -x -q --tb=short` exit code 0
  </acceptance_criteria>
  <done>TRADE-05 metals mock chain test passes.</done>
</task>

</tasks>

<verification>
`cd backend_api_python && pytest tests/test_e2e_qualify_cache_ibkr.py -x -q --tb=short` then full `pytest`.
</verification>

<success_criteria>
- Qualify cache behaviors and TRADE-05 metals test are automated and grep-verifiable.
</success_criteria>

<output>
After completion, create `.planning/phases/18-e2e-integration-testing/18-02-SUMMARY.md`
</output>
