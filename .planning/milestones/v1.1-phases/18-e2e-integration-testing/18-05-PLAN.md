---
phase: 18-e2e-integration-testing
plan: 05
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - backend_api_python/tests/test_strategy_http_e2e.py
autonomous: true
requirements:
  - TEST-02
must_haves:
  truths:
    - "POST /api/strategies/create returns JSON code 1 with mocked create_strategy."
    - "PUT /api/strategies/update?id=<int> and DELETE /api/strategies/delete?id=<int> return success with mocked service."
    - "POST /api/strategies/batch-create returns success with mocked batch_create_strategies."
  artifacts:
    - path: backend_api_python/tests/test_strategy_http_e2e.py
      provides: "Flask test_client strategy CRUD + batch"
      contains: "/api/strategies/create"
      min_lines: 80
  key_links:
    - from: "tests/test_strategy_http_e2e.py"
      to: "app.routes.strategy.get_strategy_service"
      via: "@patch(\"app.routes.strategy.get_strategy_service\")"
      pattern: "get_strategy_service"
---

<objective>
Implement **TEST-02** using Flask `test_client()` only: integration tests for strategy **create**, **update**, **delete**, and **batch-create** against real `strategy_bp` routes with `get_strategy_service` mocked (no PostgreSQL).

Purpose: HTTP round-trip coverage for Vue wizard API contract without Playwright (18-CONTEXT).

Output: `test_strategy_http_e2e.py` passing.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-e2e-integration-testing/18-CONTEXT.md

## Exact routes (blueprint prefix `/api`)

| Method | Path | Query |
|--------|------|-------|
| POST | `/api/strategies/create` | — |
| PUT | `/api/strategies/update` | `?id=<int>` required |
| DELETE | `/api/strategies/delete` | `?id=<int>` required |
| POST | `/api/strategies/batch-create` | — |

## Success JSON shape (from `strategy.py`)

- create: `{"code": 1, "msg": "success", "data": {"id": <int>}}`
- update: `{"code": 1, "msg": "success", "data": null}` when ok
- delete: `{"code": 1, ...}` when ok
- batch-create: `{"code": 1, ...}` when `result['success']` from service

## Forex + IBKR create payload (minimal — copy fields from `test_uc_sa_e2e_api_forex_create_returns_200`)

```json
{
  "strategy_name": "HTTP-E2E-Forex-IBKR",
  "market_category": "Forex",
  "exchange_config": {"exchange_id": "ibkr-paper"},
  "indicator_config": {},
  "trading_config": {"symbol": "EURUSD", "market_type": "forex"},
  "notification_config": {}
}
```

Patch target string for tests: **`app.routes.strategy.get_strategy_service`** (function defined in same module).
</context>

<tasks>

<task type="auto">
  <name>Task 1: test_strategy_http_e2e.py — CRUD + batch with mocked StrategyService</name>
  <files>backend_api_python/tests/test_strategy_http_e2e.py</files>
  <read_first>
    - backend_api_python/app/routes/strategy.py (lines 64–112, 254–286)
    - backend_api_python/tests/helpers/flask_strategy_app.py (from 18-01)
    - backend_api_python/tests/test_forex_ibkr_e2e.py (`test_uc_sa_e2e_api_forex_create_returns_200`, `_mock_db` pattern)
  </read_first>
  <action>
    Create `backend_api_python/tests/test_strategy_http_e2e.py`:
    1. Reuse Flask app setup from `tests.helpers.flask_strategy_app` OR duplicate minimal `client` fixture using same `login_required` + reload pattern as `test_forex_ibkr_e2e.py` if helper not exported — **must** produce client with `g.user_id=1`.
    2. `@patch("app.routes.strategy.get_strategy_service")` at test method or module level; configure mock:
       - `create_strategy` → returns integer `501`
       - `update_strategy` → returns `True`
       - `delete_strategy` → returns `True`
       - `batch_create_strategies` → returns `{"success": True, "total_created": 2, "success_ids": [1, 2]}` (fields actually read in route — match `batch_create_strategies` handler in `strategy.py` lines 98–111)
    3. Tests:
       - `test_test02_post_strategies_create_forex_ibkr_returns_code_1` — POST `/api/strategies/create` with Forex+IBKR payload above; assert `response.json["code"] == 1` and `response.json["data"]["id"] == 501`.
       - `test_test02_put_strategies_update_returns_code_1` — PUT `/api/strategies/update?id=12` with JSON body `{"strategy_name": "x"}`; assert status 200 and `code==1`.
       - `test_test02_delete_strategies_returns_code_1` — DELETE `/api/strategies/delete?id=12`; assert `code==1`.
       - `test_test02_batch_create_multi_symbol_returns_code_1` — POST `/api/strategies/batch-create` with body containing `strategy_name`, `symbols` array with at least two entries (e.g. `["Forex:EURUSD", "Forex:GBPUSD"]` or format expected by `batch_create_strategies` — read `StrategyService.batch_create_strategies` signature / strategy route docstring); assert `code==1` and message or data indicates created count ≥2.

    First line of module docstring must contain **`TEST-02`**.
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/test_strategy_http_e2e.py -x -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TEST-02" backend_api_python/tests/test_strategy_http_e2e.py` returns at least one line
    - `grep -n "get_strategy_service" backend_api_python/tests/test_strategy_http_e2e.py` returns at least one line
    - `grep -n "/api/strategies/create\|/api/strategies/update\|/api/strategies/delete\|/api/strategies/batch-create" backend_api_python/tests/test_strategy_http_e2e.py` returns at least 4 lines total across file
    - `cd backend_api_python && pytest tests/test_strategy_http_e2e.py -x -q --tb=short` exit code 0
  </acceptance_criteria>
  <done>Four HTTP tests pass; TEST-02 documented in module docstring.</done>
</task>

</tasks>

<verification>
`cd backend_api_python && pytest tests/test_strategy_http_e2e.py -x -q --tb=short`
</verification>

<success_criteria>
- Strategy HTTP E2E module exercises create/update/delete/batch with mocked service.
</success_criteria>

<output>
After completion, create `.planning/phases/18-e2e-integration-testing/18-05-SUMMARY.md`
</output>
