---
phase: 18-e2e-integration-testing
plan: 03
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - backend_api_python/tests/test_e2e_limit_cancel_errors_ibkr.py
autonomous: true
requirements:
  - TRADE-06
must_haves:
  truths:
    - "Limit-order E2E covers Filled, PartiallyFilled→Filled, and Cancelled with filled<=0 vs filled>0 per client._on_order_status."
    - "Error-path tests cover qualify failure, empty/invalid qualify, and limit price <= 0 rejection at the actual code layer (worker or client — document in test docstring)."
  artifacts:
    - path: backend_api_python/tests/test_e2e_limit_cancel_errors_ibkr.py
      provides: "TRADE-06 limit + cancel + errors"
      contains: "place_limit_order"
      min_lines: 150
  key_links:
    - from: "tests/test_e2e_limit_cancel_errors_ibkr.py"
      to: "IBKRClient._on_order_status"
      via: "simulated orderStatusEvent / trade.orderStatus"
      pattern: "Cancelled"
---

<objective>
Add `test_e2e_limit_cancel_errors_ibkr.py` for **TRADE-06**: limit-order full-chain tests with mock IBKR covering normal fill, partial fill progression, cancel scenarios (DAY expiry semantics via `Cancelled` + `filled` branches per 18-CONTEXT), and error paths (qualify failure Error 200 / no security, post-qualify validation rejection, limit `price <= 0`).

Purpose: Satisfy roadmap TRADE-06 independently of qualify-cache module.

Output: New test module passing; full suite green.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-e2e-integration-testing/18-CONTEXT.md
@.planning/phases/18-e2e-integration-testing/18-RESEARCH.md

## Branch references (implement tests to hit these)

From `client.py` ~500–508 (18-RESEARCH):
- `status == "Cancelled" and filled > 0` → `_handle_fill`
- `status == "Cancelled" and filled <= 0` → `_handle_reject` with log messages

Partial fill reference tests: `test_forex_ibkr_e2e.py` search `test_e2e_limit_partial_fill` / `test_e2e_forex_limit`.

Limit `price <= 0`: grep `place_limit_order` and worker for rejection — assert HTTP or `LiveOrderResult(success=False)` depending on layer 18-RESEARCH pitfall 5.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Limit fill + partial + cancel (TRADE-06 core)</name>
  <files>backend_api_python/tests/test_e2e_limit_cancel_errors_ibkr.py</files>
  <read_first>
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py (`place_limit_order`, `_on_order_status`, `_handle_fill`, `_handle_reject`)
    - backend_api_python/tests/test_forex_ibkr_e2e.py (grep `limit` / `Partial` / `Cancelled`)
    - backend_api_python/tests/helpers/ibkr_mocks.py
  </read_first>
  <action>
    Create `backend_api_python/tests/test_e2e_limit_cancel_errors_ibkr.py` with tests:
    1. `test_trade06_limit_filled_full_mock` — `IBKRClient.place_limit_order` Forex EURUSD (or GBPJPY), positive price, minTick stub via `_make_ibkr_client_for_e2e` with `min_tick>0`; simulate terminal `Filled` through `_on_order_status` / trade mock; docstring contains `TRADE-06`.
    2. `test_trade06_limit_partial_then_filled` — two-step `PartiallyFilled` then `Filled` (mirror existing e2e partial test logic but in new file; may reuse helper patterns from `test_forex_ibkr_e2e.py` without editing that file).
    3. `test_trade06_cancelled_filled_zero_marks_reject_path` — drive trade to `Cancelled`, `filled==0`, assert `_handle_reject` path (e.g. `LiveOrderResult` failure or callback side effect — pick observable mock on `records` or client state documented in test).
    4. `test_trade06_cancelled_filled_positive_marks_fill_path` — `Cancelled` with `filled>0`, assert fill/position path consistent with `_handle_fill` (position mock patched like existing E2E).

    Use real `IBKRClient` + mock `ib` from helpers; do not require live IBKR.
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/test_e2e_limit_cancel_errors_ibkr.py -x -q --tb=short -k "trade06_limit"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TRADE-06" backend_api_python/tests/test_e2e_limit_cancel_errors_ibkr.py` returns at least one line
    - `grep -n "def test_trade06_cancelled_filled_zero" backend_api_python/tests/test_e2e_limit_cancel_errors_ibkr.py` returns one line
    - `grep -n "def test_trade06_cancelled_filled_positive" backend_api_python/tests/test_e2e_limit_cancel_errors_ibkr.py` returns one line
    - `cd backend_api_python && pytest tests/test_e2e_limit_cancel_errors_ibkr.py -x -q --tb=short -k "trade06_limit"` exit code 0
  </acceptance_criteria>
  <done>Four TRADE-06 scenarios implemented and passing.</done>
</task>

<task type="auto">
  <name>Task 2: Error-path E2E (qualify fail, invalid contract, price<=0)</name>
  <files>backend_api_python/tests/test_e2e_limit_cancel_errors_ibkr.py</files>
  <read_first>
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py (`_qualify_contract_async` exception branches, `place_limit_order` entry validation)
    - backend_api_python/app/services/pending_order_worker.py (grep `limit` / `price` / `place_limit`)
  </read_first>
  <action>
    Append tests to the same file:
    1. `test_error_qualify_raises_no_security_definition` — `qualifyContractsAsync` raises an exception with message containing `200` or `No security definition` (match whatever `client.py` logs); assert `_invalidate_qualify_cache` behavior or `LiveOrderResult.success is False` on subsequent path.
    2. `test_error_post_qualify_validation_rejects` — return qualified contract that fails post-validation (wrong `secType` for symbol — follow existing integration test patterns from `test_ibkr_client.py` or Phase 16 tests if present).
    3. `test_error_limit_price_non_positive_rejected` — call `place_limit_order` with `price=0` or `price=-1.0`, assert `success is False` **or** worker never enqueues (if rejection is upstream): whichever layer implements check, docstring must state exact file:function.

    If any scenario is impossible without production code change, skip with `pytest.skip` only after proving impossibility in test comment — prefer asserting current behavior.
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && pytest tests/test_e2e_limit_cancel_errors_ibkr.py -x -q --tb=short -k "error_"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def test_error_qualify_raises" backend_api_python/tests/test_e2e_limit_cancel_errors_ibkr.py` returns one line
    - `grep -n "def test_error_post_qualify" backend_api_python/tests/test_e2e_limit_cancel_errors_ibkr.py` returns one line
    - `grep -n "def test_error_limit_price" backend_api_python/tests/test_e2e_limit_cancel_errors_ibkr.py` returns one line
    - `cd backend_api_python && pytest tests/test_e2e_limit_cancel_errors_ibkr.py -x -q --tb=short -k "error_"` exit code 0
  </acceptance_criteria>
  <done>Three error-path tests pass with documented rejection layer.</done>
</task>

</tasks>

<verification>
`cd backend_api_python && pytest tests/test_e2e_limit_cancel_errors_ibkr.py -x -q --tb=short`
</verification>

<success_criteria>
- TRADE-06 limit/cancel/error coverage in dedicated module; grep finds test names and TRADE-06 marker.
</success_criteria>

<output>
After completion, create `.planning/phases/18-e2e-integration-testing/18-03-SUMMARY.md`
</output>
