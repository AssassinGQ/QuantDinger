---
phase: 10-fills-position-pnl-events
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/app/services/live_trading/records.py
  - backend_api_python/app/services/live_trading/ibkr_trading/client.py
  - backend_api_python/tests/test_ibkr_client.py
  - backend_api_python/tests/test_live_trading_records_ibkr.py
autonomous: true
requirements:
  - RUNT-02
user_setup: []
must_haves:
  truths:
    - "Forex position/portfolio callbacks persist symbol label EUR.USD (localSymbol), not base EUR alone"
    - "get_positions() returns secType/exchange/currency from DB for Forex rows (CASH, IDEALPRO, USD), not hardcoded STK/SMART/USD"
    - "ibkr_save_pnl(account, daily_pnl, unrealized_pnl, realized_pnl) runs without NameError when DB is mocked"
    - "Automated tests cover UC-FP7 Forex round-trip (map → pnl_single → get_positions) plus stock regression UC-FP5"
  artifacts:
    - path: "backend_api_python/app/services/live_trading/records.py"
      provides: "qd_ibkr_pnl_single columns sec_type, exchange, currency; ibkr_save_position INSERT/UPDATE; ibkr_get_positions SELECT; ibkr_save_pnl without dead clamps"
      contains: "ALTER TABLE qd_ibkr_pnl_single ADD COLUMN IF NOT EXISTS"
    - path: "backend_api_python/app/services/live_trading/ibkr_trading/client.py"
      provides: "localSymbol-or-symbol map; contract metadata into ibkr_save_position; get_positions fallbacks from row"
      contains: "localSymbol or contract.symbol"
    - path: "backend_api_python/tests/test_live_trading_records_ibkr.py"
      provides: "UC-FP6 direct call to real ibkr_save_pnl"
      contains: "ibkr_save_pnl"
    - path: "backend_api_python/tests/test_ibkr_client.py"
      provides: "UC-FP1–FP5, FP7 callback and get_positions tests"
      contains: "UC-FP"
  key_links:
    - from: "client._on_position / _on_update_portfolio"
      to: "records.ibkr_save_position"
      via: "keyword args including sec_type, exchange, currency"
      pattern: "ibkr_save_position\\("
    - from: "records.ibkr_get_positions"
      to: "client.get_positions"
      via: "row.get(\"sec_type\")"
      pattern: "sec_type|secType"
---

<objective>
Implement RUNT-02: IBKR execution/portfolio callbacks and `get_positions()` expose Forex rows with stable symbol keys (`localSymbol` / internal label), persisted contract metadata (secType, exchange, currency), and fix `ibkr_save_pnl` dead-code NameError. Add tests UC-FP1–FP7 including one Forex mock round-trip lifecycle.

Purpose: Unblock correct API/UI semantics for Forex vs equity “shares” and align symbol keys with Phase 1 display (`EUR.USD`).
Output: Migrated schema, updated `records.py` + `client.py`, new + extended pytest coverage, full suite green.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-fills-position-pnl-events/10-CONTEXT.md
@.planning/phases/10-fills-position-pnl-events/10-RESEARCH.md

**Requirement:** RUNT-02 — fills/position/PnL callbacks correct for Forex symbol key, quantity, currency context.

**Dependency order (within this plan):** Task 1 (schema + records) → Tasks 2 and 3 depend on Task 1 → Task 4 depends on Tasks 2 and 3.

<interfaces>
From `records.py` (current signatures — extend as specified in tasks):
```python
def ibkr_save_pnl(*, account: str, daily_pnl: float, unrealized_pnl: float, realized_pnl: float) -> bool: ...
def ibkr_save_position(*, account: str, con_id: int, symbol: str = "", position: float = 0.0, ...) -> bool: ...
def ibkr_get_positions(account: str) -> List[Dict[str, Any]]: ...
```

From `client.py` (touch points):
- `_on_update_portfolio(self, item)` — sets `_conid_to_symbol`, calls `ibkr_save_position`
- `_on_position(self, position)` — same
- `_on_pnl_single(self, entry)` — `symbol = self._conid_to_symbol.get(entry.conId, "")`
- `get_positions(self) -> List[Dict[str, Any]]` — builds dicts with `secType`/`exchange`/`currency` today hardcoded at lines ~1316–1318
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration, ibkr_save_position + ibkr_get_positions extension, ibkr_save_pnl bugfix</name>
  <read_first>
    - backend_api_python/app/services/live_trading/records.py (functions `_ensure_tables`, `ibkr_save_pnl`, `ibkr_save_position`, `ibkr_get_positions`)
    - backend_api_python/tests/conftest.py (`make_db_ctx`)
  </read_first>
  <files>
    backend_api_python/app/services/live_trading/records.py
    backend_api_python/tests/test_live_trading_records_ibkr.py
  </files>
  <action>
  **Goal:** Add `sec_type`, `exchange`, `currency` to `qd_ibkr_pnl_single`; wire INSERT/UPDATE/SELECT; remove three dead lines in `ibkr_save_pnl` that reference undefined `position`, `avg_cost`, `value` (keep clamps only for `daily_pnl`, `unrealized_pnl`, `realized_pnl`).

  **Schema (in `_ensure_tables`, after successful `CREATE TABLE` for `qd_ibkr_pnl_single`, same `with get_db_connection()` block):**
  - `ALTER TABLE qd_ibkr_pnl_single ADD COLUMN IF NOT EXISTS sec_type VARCHAR(20) DEFAULT ''`
  - `ALTER TABLE qd_ibkr_pnl_single ADD COLUMN IF NOT EXISTS exchange VARCHAR(50) DEFAULT ''`
  - `ALTER TABLE qd_ibkr_pnl_single ADD COLUMN IF NOT EXISTS currency VARCHAR(10) DEFAULT ''`
  - Commit after ALTERs if not already committed in the same transaction pattern as existing code.

  **`ibkr_save_position`:** Add keyword parameters `sec_type: str = ""`, `exchange: str = ""`, `currency: str = ""`. Extend `INSERT` column list and `VALUES` to include the three columns. Extend `ON CONFLICT DO UPDATE SET` so empty `EXCLUDED` strings do not wipe prior metadata (mirror the `symbol = COALESCE(NULLIF(EXCLUDED.symbol, ''), ...)` pattern for each new column).

  **`ibkr_get_positions`:** Extend `SELECT` to return `sec_type`, `exchange`, `currency` as dict keys (lowercase snake_case to match existing `row.get("...")` usage in `client.get_positions`).

  **UC specifications:**

  | UC ID | Description | Input | Expected output |
  |-------|-------------|-------|-----------------|
  | UC-FP6 | Real `ibkr_save_pnl` executes without exception | `patch("app.services.live_trading.records.get_db_connection")` returning `make_db_ctx()`; call `records.ibkr_save_pnl(account="DU123", daily_pnl=1.0, unrealized_pnl=2.0, realized_pnl=3.0)` | Returns `True`; **no** `NameError` / `UnboundLocalError`; cursor `execute` invoked with `INSERT INTO qd_ibkr_pnl` (or equivalent) |
  | UC-SCHEMA | New columns exist after ensure | After `_ensure_tables()`, SQL or mock shows ALTER / INSERT accepts `sec_type` | `ibkr_save_position(..., sec_type="CASH", exchange="IDEALPRO", currency="USD")` succeeds with mocked DB (assert `execute` tuple includes those three strings) |

  **New file:** `backend_api_python/tests/test_live_trading_records_ibkr.py` — implement UC-FP6 (and minimal UC-SCHEMA assertion via mock `execute` call args if no live Postgres in CI).
  </action>
  <acceptance_criteria>
    - `grep -n "position = max" backend_api_python/app/services/live_trading/records.py` returns no matches inside `ibkr_save_pnl`
    - `grep -n "ADD COLUMN IF NOT EXISTS sec_type" backend_api_python/app/services/live_trading/records.py` returns at least one line
    - `grep -n "sec_type" backend_api_python/app/services/live_trading/records.py` matches both `ibkr_save_position` INSERT and `ibkr_get_positions` SELECT
    - `test_live_trading_records_ibkr.py` contains a test function that calls real `ibkr_save_pnl` without patching the function body
  </acceptance_criteria>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest tests/test_live_trading_records_ibkr.py -x -q --tb=line</automated>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest tests/ -x -q --tb=line</automated>
  </verify>
  <done>
  UC-FP6 passes; schema ALTER and extended `ibkr_save_position` / `ibkr_get_positions` are in `records.py`; full test suite passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: _on_position and _on_update_portfolio — localSymbol label + contract metadata</name>
  <read_first>
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py (`_on_update_portfolio`, `_on_position`, imports)
    - backend_api_python/app/services/live_trading/records.py (`ibkr_save_position` signature after Task 1)
  </read_first>
  <files>
    backend_api_python/app/services/live_trading/ibkr_trading/client.py
  </files>
  <action>
  **Depends on:** Task 1 complete (`ibkr_save_position` accepts `sec_type`, `exchange`, `currency`).

  **Concrete changes:**
  - Define a single local helper or inline: `label = (getattr(contract, "localSymbol", None) or getattr(contract, "symbol", None) or "").strip()` (or equivalent) for `item.contract` / `position.contract`.
  - Replace `self._conid_to_symbol[...] = item.contract.symbol` and `_on_position` map assignment so the stored value is **`contract.localSymbol or contract.symbol or ""`** (must match CONTEXT).
  - Pass to every `ibkr_save_position` call from these two callbacks:
    - `symbol=label` (same string as map)
    - `sec_type=str(contract.secType or "")`
    - `exchange=str(contract.exchange or "")`
    - `currency=str(contract.currency or "")`
  - Update log lines that print `symbol` for Forex readability to use `label` where appropriate (optional; do not change unrelated log format).

  **UC specifications:**

  | UC ID | Description | Input | Expected output |
  |-------|-------------|-------|-----------------|
  | UC-FP1 | Position event uses EUR.USD | Mock `Position` with `contract.conId=4242`, `symbol="EUR"`, `localSymbol="EUR.USD"`, `secType="CASH"`, `exchange="IDEALPRO"`, `currency="USD"`, `position=10000`, `account="DU1"` | `_conid_to_symbol[4242] == "EUR.USD"`; `ibkr_save_position` called with `symbol="EUR.USD"`, `sec_type="CASH"`, `exchange="IDEALPRO"`, `currency="USD"` |
  | UC-FP2 | updatePortfolio matches | Mock portfolio `item` with same contract fields | Map and `ibkr_save_position` args match UC-FP1 rules |

  **Tests:** Extend `test_ibkr_client.py` (or phase-10 section): patch `records.ibkr_save_position`, invoke `_on_position` / `_on_update_portfolio` with Forex-like contract mocks; assert call kwargs.
  </action>
  <acceptance_criteria>
    - `grep -n "_conid_to_symbol\[.*\] = .*contract.symbol" backend_api_python/app/services/live_trading/ibkr_trading/client.py` returns no matches (no raw `= ...contract.symbol` assignment without localSymbol)
    - `grep -n "localSymbol" backend_api_python/app/services/live_trading/ibkr_trading/client.py` matches `_on_position` and `_on_update_portfolio` paths
    - `grep -n "sec_type=" backend_api_python/app/services/live_trading/ibkr_trading/client.py` matches both callbacks’ `ibkr_save_position` calls
  </acceptance_criteria>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest tests/test_ibkr_client.py -k "FP1 or FP2 or position or portfolio or fills_position" -x -q --tb=line</automated>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest tests/ -x -q --tb=line</automated>
  </verify>
  <done>
  UC-FP1 and UC-FP2 assertions pass; callbacks pass metadata into `ibkr_save_position`; full suite passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: get_positions() reads DB metadata with equity fallbacks</name>
  <read_first>
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py (`get_positions`, ~1289–1328)
    - backend_api_python/app/services/live_trading/records.py (`ibkr_get_positions` column names)
  </read_first>
  <files>
    backend_api_python/app/services/live_trading/ibkr_trading/client.py
  </files>
  <action>
  **Depends on:** Task 1 (`ibkr_get_positions` returns `sec_type`, `exchange`, `currency`).

  **Replace hardcoded:**
  - `"secType": "STK"` → `(row.get("sec_type") or "").strip() or "STK"`
  - `"exchange": "SMART"` → `(row.get("exchange") or "").strip() or "SMART"`
  - `"currency": "USD"` → `(row.get("currency") or "").strip() or "USD"`

  Use exact key names returned by `ibkr_get_positions` (snake_case from DB); map to camelCase API keys `secType`, `exchange`, `currency` in the output dict (unchanged public shape).

  **UC specifications:**

  | UC ID | Description | Input | Expected output |
  |-------|-------------|-------|-----------------|
  | UC-FP4 | Forex row metadata | `@patch` `records.ibkr_get_positions` returns one dict: `symbol="EUR.USD"`, `position=10000`, `sec_type="CASH"`, `exchange="IDEALPRO"`, `currency="USD"`, plus required numeric fields | `get_positions()[0]["secType"]=="CASH"`, `["exchange"]=="IDEALPRO"`, `["currency"]=="USD"`, `["quantity"]==10000` |
  | UC-FP5 | Stock regression | Mock row `sec_type="STK"`, `exchange="SMART"`, `currency="USD"`, `symbol="AAPL"`, non-zero `position` | `secType`/`exchange`/`currency` unchanged from prior semantics; not `CASH` |

  **Tests:** Add or update tests in `test_ibkr_client.py` that patch `ibkr_get_positions` (existing pattern ~1786+); name tests with `UC-FP4` / `UC-FP5` in docstring or test id.
  </action>
  <acceptance_criteria>
    - `grep -n '"secType": "STK"' backend_api_python/app/services/live_trading/ibkr_trading/client.py` returns no matches inside `get_positions`
    - `grep -n "sec_type" backend_api_python/app/services/live_trading/ibkr_trading/client.py` matches `get_positions` body
    - Tests exist asserting FP4 Forex dict and FP5 STK row
  </acceptance_criteria>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest tests/test_ibkr_client.py -k "get_positions or FP4 or FP5" -x -q --tb=line</automated>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest tests/ -x -q --tb=line</automated>
  </verify>
  <done>
  UC-FP4 and UC-FP5 pass; legacy empty columns still fall back to STK/SMART/USD; full suite passes.
  </done>
</task>

<task type="auto">
  <name>Task 4: Integration tests — pnlSingle map usage + Forex round-trip + stock regression</name>
  <read_first>
    - backend_api_python/tests/test_ibkr_client.py (existing `_on_pnl_single`, `get_positions`, mock client helpers)
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py (`_on_pnl_single`, `_fire_submit` behavior)
  </read_first>
  <files>
    backend_api_python/tests/test_ibkr_client.py
  </files>
  <action>
  **Depends on:** Tasks 2 and 3.

  **Implement:**

  | UC ID | Description | Input | Expected output |
  |-------|-------------|-------|-----------------|
  | UC-FP3 | pnlSingle uses `_conid_to_symbol` | Pre-fill `_conid_to_symbol[conId]="EUR.USD"` (or call `_on_position` first); mock `PnLSingle`-like entry with same `conId`, `account`, numeric fields; patch `ibkr_save_position` | Last `ibkr_save_position` call includes `symbol="EUR.USD"` |
  | UC-FP7 | Round-trip lifecycle | With `@patch` on `records.ibkr_save_position` / `ibkr_get_positions` as needed: simulate `_on_position` (or `_on_update_portfolio`) then `_on_pnl_single`, then `get_positions` with DB row reflecting saved metadata | Single test: symbol and `secType`/`exchange`/`currency` consistent end-to-end for Forex mocks (`CASH` / `IDEALPRO` / quote `USD`) |
  | REGR-STK | Stock unchanged | Existing or new assertion: mock STK row still yields `secType` STK when DB stores STK | No accidental `CASH` for AAPL-style row |

  Use `_make_mock_ib_insync` / `_make_client_with_mock_ib` or existing fixtures; keep `_always_rth` if required by module fixtures.

  **Stock regression:** Explicit assertion that `test_ibkr_order_callback.py` behaviors remain covered — run full suite (below); add narrow test only if a gap exists after Tasks 1–3.

  **Naming:** Prefer test class `TestForexPositionPnLEvents` or node ids containing `UC-FP3`, `UC-FP7` for grep traceability.
  </action>
  <acceptance_criteria>
    - `grep -r "UC-FP7\|UC-FP3" backend_api_python/tests/test_ibkr_client.py` finds docstrings or test names
    - At least one test chains position (or portfolio) → pnl_single → get_positions for Forex mocks
    - `grep -n "CASH" backend_api_python/tests/test_ibkr_client.py` appears in FP4/FP7-related tests
  </acceptance_criteria>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest tests/test_ibkr_client.py tests/test_ibkr_order_callback.py -k "FP or Forex or pnl_single or get_positions" -x -q --tb=line</automated>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest tests/ -x -q --tb=line</automated>
  </verify>
  <done>
  UC-FP3, UC-FP7, and stock regression assertions pass; full `tests/` suite passes.
  </done>
</task>

</tasks>

<verification>
- All tasks: targeted `pytest` command succeeds; then full `tests/` suite as specified per task.
- Manual spot-check (optional): `ibkr_save_pnl` code path no longer references undefined names (static read of `records.py`).
</verification>

<success_criteria>
1. Forex fills/position paths use stable identifiers: `_conid_to_symbol` and saved `symbol` use `localSymbol or symbol` (e.g. `EUR.USD`).
2. PnL/position API dicts expose `secType`/`exchange`/`currency` from DB for Forex (`CASH` / `IDEALPRO` / quote currency), not equity-only defaults when DB holds values.
3. At least one automated round-trip (UC-FP7) for Forex mocks; UC-FP6 proves `ibkr_save_pnl` runs; stock UC-FP5 prevents regression.
</success_criteria>

<output>
After completion, create `.planning/phases/10-fills-position-pnl-events/10-01-SUMMARY.md` per execute-plan template.
</output>
