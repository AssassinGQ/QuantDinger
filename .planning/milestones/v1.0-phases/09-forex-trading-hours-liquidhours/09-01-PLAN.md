---
phase: 09-forex-trading-hours-liquidhours
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/tests/test_trading_hours.py
  - backend_api_python/app/services/live_trading/ibkr_trading/client.py
  - backend_api_python/tests/test_ibkr_client.py
autonomous: true
requirements:
  - RUNT-01
user_setup: []
must_haves:
  truths:
    - "Forex session open/closed follows ContractDetails.liquidHours + timeZoneId via is_rth_check (not US equity RTH calendars)."
    - "When Forex is_market_open is False, reason includes Forex 24/5 context (weekend/maintenance) distinguishable from equity RTH."
    - "Unit tests UC-FX-L01–L09 and integration UC-FX-I01–I05 pass; Fri–Sun and holiday scenarios use mocked liquidHours."
  artifacts:
    - path: backend_api_python/tests/test_trading_hours.py
      provides: "UC-FX-L01–L09 Forex liquidHours / is_rth_check scenarios"
      contains: "UC-FX-L"
    - path: backend_api_python/app/services/live_trading/ibkr_trading/client.py
      provides: "Forex-specific closed reason when is_rth_check False and market_type==Forex"
      contains: "is_market_open"
    - path: backend_api_python/tests/test_ibkr_client.py
      provides: "TestForexRTHGate UC-FX-I01–I05 with real is_rth_check (autouse override)"
      contains: "TestForexRTHGate"
  key_links:
    - from: "IBKRClient.is_market_open"
      to: "is_rth_check(details, server_time, ...)"
      via: "reqContractDetailsAsync → details.liquidHours"
      pattern: "is_rth_check"
    - from: "tests/test_trading_hours.py"
      to: "trading_hours.is_rth_check"
      via: "_make_details + now=..."
      pattern: "is_rth_check"
---

<objective>
Prove RUNT-01: Forex `is_market_open` uses IBKR `liquidHours` / contract metadata (24/5), not equity-only assumptions. Add Forex-focused unit tests (`UC-FX-L01`–`L09`), a small production change so Forex closed reasons mention 24/5 weekend/maintenance context, and integration tests (`UC-FX-I01`–`I05`) that exercise the full client path with real `is_rth_check` (overriding `test_ibkr_client` autouse).

Purpose: Lock behavior against mock IB schedules (Fri–Sun, maintenance gaps, CLOSED holidays, JST/XAGUSD variants) and operator-visible diagnostics.

Output: Extended `test_trading_hours.py`, Forex branch in `client.py` `is_market_open`, `TestForexRTHGate` in `test_ibkr_client.py`.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-forex-trading-hours-liquidhours/09-CONTEXT.md
@.planning/phases/09-forex-trading-hours-liquidhours/09-RESEARCH.md

## Interfaces (executor reference)

From `backend_api_python/app/services/live_trading/ibkr_trading/trading_hours.py`:
- `parse_liquid_hours(liquid_hours: str, tz: pytz.BaseTzInfo) -> List[Tuple[datetime, datetime]]`
- `is_rth_check(contract_details, server_time_utc, con_id=0, symbol="?", now=None) -> bool` — use `now=` for deterministic tests; boundary is inclusive `start <= now <= end`.

From `backend_api_python/app/services/live_trading/ibkr_trading/client.py`:
- `is_market_open(self, symbol: str, market_type: str = "USStock")` → async path qualifies contract, `reqCurrentTimeAsync`, `reqContractDetailsAsync`, then `is_rth_check`; on failure returns `(False, reason)`.

Existing test helpers: `test_trading_hours._make_details(liquid_hours, tz_id)`, autouse cache clear; `test_ibkr_client._make_mock_ib_insync()`, `_make_client_with_mock_ib()`, and autouse `_always_rth` patching `is_rth_check` — Forex integration tests must override with real `is_rth_check` (`wraps` or `new=`). Clear `IBKRClient._rth_details_cache` in setup/fixture per RESEARCH pitfall #2.

**Execution order within phase:** Task 1 and Task 2 may run in parallel (disjoint primary files). **Task 3 depends on Task 2** (UC-FX-I05 asserts Forex-specific closed message text).
</context>

<tasks>

<task type="auto">
  <name>Task 1: UC-FX-L01–L09 pure logic tests in test_trading_hours.py</name>
  <read_first>
    backend_api_python/app/services/live_trading/ibkr_trading/trading_hours.py (parse_liquid_hours, is_rth_check)
    backend_api_python/tests/test_trading_hours.py (_make_details, TestIsRTHCheck patterns)
  </read_first>
  <files>backend_api_python/tests/test_trading_hours.py</files>
  <action>
Add one or more test classes (e.g. `TestForexLiquidHours` or methods prefixed by UC IDs) covering **all** of the following use cases. Reuse `_make_details`, autouse cache clearing, and `is_rth_check(..., now=tz.localize(...))` with fixed **2026-03** dates and `timeZoneId` consistent with strings (EST → `EST` / US/Eastern via `_resolve_tz`; JST where noted).

**Use case table (must implement):**

| UC ID | Description | Key input | Expected output | SC mapping |
|-------|-------------|-----------|-----------------|------------|
| UC-FX-L01 | Cross-day single segment parse | `liquidHours="20260308:1715-20260309:1700"`, `tz_id=EST`; assert via `parse_liquid_hours` or session shape from `_make_details` | Exactly **1** session; `start.date() != end.date()` | SC-1, SC-3 |
| UC-FX-L02 | Weekday mid-session (Tue afternoon EST) | e.g. `20260310:0100-20260310:2300`, `now` = 2026-03-10 14:00 US/Eastern Tue | `is_rth_check` → **True** | SC-1, SC-2 |
| UC-FX-L03 | Friday after 17:00 close | Last segment ends Fri 2026-03-06 17:00; `now` = Fri 17:01 EST (optional: 17:00:00 → True per inclusive end) | **False** at 17:01 | SC-2 |
| UC-FX-L04 | Saturday closed | `20260307:CLOSED` or segments excluding Sat; `now` = Sat 2026-03-07 12:00 EST | **False** | SC-2, SC-3 |
| UC-FX-L05 | Sunday 17:15 week open | Segment Sun→Mon e.g. `20260308:1715-20260309:1700`; `now` = Sun 17:15 EST → **True**; `now` = Sun 17:14 EST → **False** | True/False as specified | SC-2, SC-3 |
| UC-FX-L06 | Daily maintenance gap 17:00–17:15 | Two segments with gap; `now` = 2026-03-09 17:05 EST | **False** | SC-2 |
| UC-FX-L07 | Holiday CLOSED | e.g. `20260325:CLOSED` mixed with normal; `now` on that calendar day in tz | **False**; CLOSED segments skipped in parse | SC-2 |
| UC-FX-L08 | GBPJPY + JST | `tz_id=JST`, `liquidHours` in Tokyo-local YYYYMMDD:HHMM; one `now` inside session → **True**, one outside → **False** | True / False | SC-1 |
| UC-FX-L09 | XAGUSD-style window | `tz_id=EST`, distinct window vs L02; `now` inside window | **True** | SC-1 |

Do not hand-roll a separate Forex calendar; all assertions flow from `liquidHours` strings + `is_rth_check` / `parse_liquid_hours`.

**Discovery:** Mark the new class or tests with `@pytest.mark.Forex` (or ensure test/node IDs contain the substring `Forex`) so `-k Forex` selects only these additions without pulling unrelated tests.
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest tests/test_trading_hours.py -k Forex -x -q --tb=line</automated>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest tests/ -x -q --tb=line</automated>
  </verify>
  <done>All UC-FX-L01–L09 represented by tests with explicit UC ID in name or docstring; targeted and full suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Forex closed message in client.py (production)</name>
  <read_first>
    backend_api_python/app/services/live_trading/ibkr_trading/client.py (is_market_open, ~995–1060)
  </read_first>
  <files>backend_api_python/app/services/live_trading/ibkr_trading/client.py</files>
  <action>
When `market_type == "Forex"` and `is_rth_check` returns False, extend the returned reason string so operators can distinguish Forex 24/5 closure (weekend/maintenance) from US equity RTH messaging. Keep existing closed semantics; exact English phrasing is implementer discretion but **must** mention Forex 24/5 and weekend/maintenance (per 09-CONTEXT).

**Use case table (behavioral spec):**

| UC ID | Description | Key input | Expected output | SC mapping |
|-------|-------------|-----------|-----------------|------------|
| UC-FX-M01 | Forex closed reason shape | `is_market_open(..., market_type="Forex")`, mock path yields `is_rth_check` False | `(False, reason)` where `reason` contains base closed text **and** Forex 24/5 + weekend/maintenance hint | SC-1, SC-2 |
| UC-FX-M02 | Non-Forex unchanged | `market_type!="Forex"`, `is_rth_check` False | Reason matches existing equity-style message (no new Forex-only suffix) | SC-1 |

Implement UC-FX-M01–M02 in code paths; dedicated test lands in Task 3 (UC-FX-I05) and/or minimal client-level assertion if a small test is added here — **do not** duplicate I05; Task 3 owns integration assertion for the exact string substring.
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest tests/test_ibkr_client.py -k "is_market_open" -x -q --tb=line</automated>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest tests/ -x -q --tb=line</automated>
  </verify>
  <done>Forex branch appended to closed reason; USStock/HShare/others unchanged; full suite green.</done>
</task>

<task type="auto">
  <name>Task 3: UC-FX-I01–I05 integration tests (TestForexRTHGate)</name>
  <read_first>
    backend_api_python/tests/test_ibkr_client.py (_always_rth autouse, TestRTHGate, _make_client_with_mock_ib)
    backend_api_python/app/services/live_trading/ibkr_trading/client.py (is_market_open, _rth_details_cache)
  </read_first>
  <files>backend_api_python/tests/test_ibkr_client.py</files>
  <action>
**Depends on Task 2** for UC-FX-I05 message content.

Add `TestForexRTHGate` (or equivalent) with:
- `setup_method` / fixture: `IBKRClient._rth_details_cache.clear()` (and any instance cache if applicable).
- Override autouse mock: patch `app.services.live_trading.ibkr_trading.trading_hours.is_rth_check` with **`wraps=<real is_rth_check>`** or `new=` real function so integration uses production logic.
- Mock `reqContractDetailsAsync` / `reqCurrentTimeAsync` / qualify so UTC server time aligns with `liquidHours` (see RESEARCH: pick local target time → set UTC for `reqCurrentTimeAsync`).

**Use case table:**

| UC ID | Description | Key input | Expected output | SC mapping |
|-------|-------------|-----------|-----------------|------------|
| UC-FX-I01 | EURUSD + cross-day liquidHours, time inside | `is_market_open("EURUSD","Forex")`, mock details + server UTC in segment | `(True, "")` | SC-1 |
| UC-FX-I02 | Saturday closed | CLOSED or no segment Sat; server time Saturday | `(False, ...)` | SC-2, SC-3 |
| UC-FX-I03 | GBPJPY + JST | `symbol="GBPJPY"`, details `timeZoneId`/liquidHours JST-consistent; server UTC aligned | Open/closed matches real `is_rth_check` | SC-1 |
| UC-FX-I04 | XAGUSD chain | `XAGUSD`, dedicated liquidHours | Result matches mock | SC-1 |
| UC-FX-I05 | Forex closed message | Closed case with `market_type=Forex` | `reason` contains Forex hint from Task 2 (24/5 + weekend/maintenance) | SC-1, SC-2 |

Tag tests with `@pytest.mark.ForexRTH` or name them so `pytest -k ForexRTH` selects this class.
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest tests/test_ibkr_client.py -k "ForexRTH" -x -q --tb=line</automated>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest tests/ -x -q --tb=line</automated>
  </verify>
  <done>UC-FX-I01–I05 covered; autouse override verified; I05 asserts Forex-specific substring; full suite green.</done>
</task>

</tasks>

<verification>
- `RUNT-01` satisfied: Forex path uses `liquidHours` via `is_rth_check` with proof at unit (L01–L09) and integration (I01–I04) layers; I05 validates operator-facing Forex messaging.
- Regression: entire `tests/` passes with `--tb=line` per task.
</verification>

<success_criteria>
1. Forex `is_market_open` reflects IBKR contract `liquidHours` / metadata through `is_rth_check` (tests mock ContractDetails, not US equity calendars).
2. Weekend/holiday/maintenance scenarios match IB-style mock schedules (Fri–Sun, CLOSED).
3. Tests include time-window cases with mocked hours; full-library pytest run is part of every task’s verify block.
</success_criteria>

<output>
After completion, create `.planning/phases/09-forex-trading-hours-liquidhours/09-01-SUMMARY.md`
</output>
