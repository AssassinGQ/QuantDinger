---
phase: 04-market-category-worker-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/app/services/live_trading/ibkr_trading/client.py
  - backend_api_python/tests/test_exchange_engine.py
  - backend_api_python/tests/test_pending_order_worker.py
autonomous: true
requirements: [CONT-04]

must_haves:
  truths:
    - "\"Forex\" ∈ IBKRClient.supported_market_categories"
    - "validate_market_category(\"Forex\") on IBKRClient.__new__(IBKRClient) returns (True, \"\")"
    - "validate_market_category(\"Crypto\") still returns (False, msg) with msg mentioning Crypto"
    - "PendingOrderWorker._execute_live_order with strategy market_category=Forex and ibkr client does not fail at category gate; mark_order_sent path reachable (mocked runner)"
    - "PendingOrderWorker._execute_live_order with market_category=Crypto fails at category gate with ibkr-only-supports message containing Crypto"
    - "Full backend test suite passes (REGR-01)"
  artifacts:
    - path: "backend_api_python/app/services/live_trading/ibkr_trading/client.py"
      provides: "IBKRClient.supported_market_categories includes Forex"
      contains: "supported_market_categories = frozenset({\"USStock\", \"HShare\", \"Forex\"})"
    - path: "backend_api_python/tests/test_exchange_engine.py"
      provides: "UC-1..3, UC-6 assertions on frozenset + validate_market_category"
      contains: "test_ibkr_supported_categories"
    - path: "backend_api_python/tests/test_pending_order_worker.py"
      provides: "UC-4, UC-5 integration tests calling _execute_live_order"
      contains: "PendingOrderWorker"
  key_links:
    - from: "pending_order_worker.py::_execute_live_order"
      to: "BaseStatefulClient.validate_market_category"
      via: "create_client then isinstance(client, BaseStatefulClient) branch"
      pattern: "validate_market_category\\(market_category\\)"
    - from: "IBKRClient.supported_market_categories"
      to: "validate_market_category"
      via: "frozenset membership in base.py"
      pattern: "supported_market_categories"
---

<objective>
Add `"Forex"` to `IBKRClient.supported_market_categories`, update exchange-engine unit tests (flip Forex from reject to accept, update frozenset assertion, keep Crypto rejection), and add `PendingOrderWorker` integration tests that call **`_execute_live_order`** (not `_process_one_live_order` — that symbol does not exist) with mocks for `load_strategy_configs`, `create_client`, `get_runner`, and `records.*`.

Purpose: Satisfy CONT-04 — runner/pending-order pipeline treats Forex as a first-class market category; worker category gate reads the same frozenset as unit tests.

Output: One-line production change in `client.py`, updated `test_exchange_engine.py`, new `test_pending_order_worker.py`.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-market-category-worker-gate/04-CONTEXT.md
@.planning/phases/04-market-category-worker-gate/04-RESEARCH.md
@.planning/phases/04-market-category-worker-gate/04-VALIDATION.md

<interfaces>
From `backend_api_python/app/services/live_trading/base.py`:
```python
@dataclass
class ExecutionResult:
    success: bool
    exchange_id: str = ""
    exchange_order_id: str = ""
    filled: float = 0.0
    avg_price: float = 0.0
    fee: float = 0.0
    fee_ccy: str = ""
    error: str = ""
    note: str = ""
    raw: Dict[str, Any] = field(default_factory=dict)

class BaseStatefulClient(ABC):
    def validate_market_category(self, market_category: str) -> Tuple[bool, str]:
        ...
```

From `backend_api_python/app/services/live_trading/runners/base.py`:
```python
class PreCheckResult:
    def __init__(self, ok: bool = True, reason: str = "", suppress_dedup_clear: bool = False):
        self.ok = ok
        self.reason = reason
        self.suppress_dedup_clear = suppress_dedup_clear
```

From `backend_api_python/app/services/pending_order_worker.py` (live path — **use this method name in tests**):
```python
def _execute_live_order(self, *, order_id: int, order_row: Dict[str, Any], payload: Dict[str, Any]) -> None:
    ...
    cfg = load_strategy_configs(strategy_id)
    ...
    market_category = str(cfg.get("market_category") or "Crypto").strip()
    ...
    client = create_client(exchange_config, market_type=market_type)
    ...
    if isinstance(client, BaseStatefulClient):
        ok, cat_err = client.validate_market_category(market_category)
        if not ok:
            records.mark_order_failed(order_id=order_id, error=cat_err, **_dedup_kw_live)
            return
    runner = get_runner(client)
    ...
```

Executor pattern for unit client tests (existing file):
```python
IBKRClient.__new__(IBKRClient)  # avoids __init__ / TWS
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: client.py frozenset + test_exchange_engine UC-1..UC-3 &amp; UC-6</name>
  <files>backend_api_python/app/services/live_trading/ibkr_trading/client.py, backend_api_python/tests/test_exchange_engine.py</files>
  <read_first>
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py (class IBKRClient, line ~102)
    - backend_api_python/tests/test_exchange_engine.py (TestExchangeEngineBasics::test_ibkr_supported_categories, TestValidateMarketCategory)
    - backend_api_python/app/services/live_trading/base.py (validate_market_category implementation)
    - .planning/phases/04-market-category-worker-gate/04-CONTEXT.md (locked: single-line frozenset change; flip forex test; no PendingOrderWorker edits)
  </read_first>
  <action>
    **Implements: UC-1, UC-2, UC-3, UC-6** (per 04-CONTEXT.md).

    1. In `IBKRClient` (`client.py`), set **exactly**:
       `supported_market_categories = frozenset({"USStock", "HShare", "Forex"})`
       — only this line in production code; do **not** modify `PendingOrderWorker` (user lock).

    2. In `test_exchange_engine.py`:
       - **UC-6 / UC-1:** `test_ibkr_supported_categories`: assert
         `IBKRClient.supported_market_categories == frozenset({"USStock", "HShare", "Forex"})`
         and that `"Forex" in IBKRClient.supported_market_categories`.
       - **UC-2:** Rename `test_ibkr_forex_rejected` → `test_ibkr_forex_ok`; assert `ok` is True (tuple unpack `(ok, msg)` allowed but drop unused `msg` on success path per CONTEXT).
       - **UC-3:** Keep `test_ibkr_crypto_rejected` behavior: `assert not ok` and `"Crypto" in msg`.

    Do not change MT5 tests or unrelated classes.
  </action>
  <acceptance_criteria>
    - [UC-1] `grep -n 'supported_market_categories = frozenset' backend_api_python/app/services/live_trading/ibkr_trading/client.py` shows `Forex` inside the set.
    - [UC-2] `TestValidateMarketCategory::test_ibkr_forex_ok` exists and asserts acceptance (`ok` is True).
    - [UC-3] `test_ibkr_crypto_rejected` still asserts rejection with message containing `Crypto`.
    - [UC-6] `test_ibkr_supported_categories` asserts equality to `frozenset({"USStock", "HShare", "Forex"})`.
    - [REGR-01] `cd backend_api_python &amp;&amp; python -m pytest tests/ -x -q --tb=line` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend_api_python &amp;&amp; python -m pytest tests/test_exchange_engine.py::TestBaseStatefulClientABC::test_ibkr_supported_categories tests/test_exchange_engine.py::TestValidateMarketCategory::test_ibkr_forex_ok tests/test_exchange_engine.py::TestValidateMarketCategory::test_ibkr_crypto_rejected -x -q --tb=line</automated>
    <automated>cd backend_api_python &amp;&amp; python -m pytest tests/ -x -q --tb=line</automated>
  </verify>
  <done>
    UC-1..UC-3 and UC-6 green; REGR-01 full suite green after Task 1 only.
  </done>
</task>

<task type="auto">
  <name>Task 2: test_pending_order_worker.py — UC-4 &amp; UC-5 via _execute_live_order</name>
  <files>backend_api_python/tests/test_pending_order_worker.py</files>
  <read_first>
    - backend_api_python/app/services/pending_order_worker.py (`_execute_live_order` full body, especially `load_strategy_configs`, `create_client`, `validate_market_category`, `get_runner`, `records.mark_order_failed` / `mark_order_sent`)
    - backend_api_python/tests/test_dedup_retry_on_failure.py (mock/patch style for worker)
    - .planning/phases/04-market-category-worker-gate/04-RESEARCH.md (recommended patches; **method name `_execute_live_order`**)
    - backend_api_python/app/services/live_trading/factory.py (only if needed to confirm `create_client` import path for patching — patch string `app.services.pending_order_worker.create_client`)
  </read_first>
  <action>
    **Implements: UC-4, UC-5** (per 04-CONTEXT.md). Reference **`_execute_live_order`** only.

    Create `backend_api_python/tests/test_pending_order_worker.py` with two tests (class name e.g. `TestPendingOrderWorkerCategoryGate`):

    **UC-4 — Forex passes category gate:**
    - `patch` at least: `app.services.pending_order_worker.load_strategy_configs`, `app.services.pending_order_worker.create_client`, `app.services.pending_order_worker.get_runner`, `app.services.pending_order_worker.records.mark_order_failed`, `app.services.pending_order_worker.records.mark_order_sent`, and `app.services.pending_order_worker.PendingOrderWorker._notify_live_best_effort` (return None / no-op) to avoid DB/notifications noise.
    - `load_strategy_configs` return dict including: `"market_category": "Forex"`, `"exchange_config": {"exchange_id": "ibkr-paper"}` (or equivalent keys worker reads), and `"market_type": "forex"` if required by `resolve_exchange_config` / path — align with real `load_strategy_configs` shape from `exchange_execution.load_strategy_configs` if unsure (read `exchange_execution.py` briefly).
    - `create_client` → `IBKRClient.__new__(IBKRClient)` (no full `IBKRClient()`).
    - `get_runner` → `MagicMock()` with `pre_check` returning `PreCheckResult(ok=True)` from `app.services.live_trading.runners.base`, and `execute` returning `ExecutionResult(success=True, exchange_id="ibkr", exchange_order_id="test-1")` from `app.services.live_trading.base`.
    - Build minimal `order_row` and `payload` so `_execute_live_order` passes early guards: `strategy_id &gt; 0`, non-empty `symbol` and `signal_type` (e.g. `close_long` avoids real RTH if pre_check were ever hit — but pre_check is mocked OK).
    - **Assert:** `records.mark_order_failed` is **not** called with a category error: either zero calls to `mark_order_failed`, or every call’s `error` kw must **not** match `ibkr only supports` / **not** be the `cat_err` pattern from `BaseStatefulClient.validate_market_category`. Prefer asserting `mark_order_sent` called once and `mark_order_failed` not called (success path).
    - **Assert:** no failure path that uses only `"Crypto"` as the category rejection for this test (Forex must succeed).

    **UC-5 — Crypto still rejected at category gate:**
    - Same patch set; `load_strategy_configs` returns `"market_category": "Crypto"` (and same `exchange_id` ibkr-paper so `create_client` still yields IBKR).
    - `get_runner` may not be invoked — do not require it.
    - **Assert:** `records.mark_order_failed` called with `error` containing **`Crypto`** and the **`ibkr only supports`** wording (base.py message format).

    Import `PendingOrderWorker` from `app.services.pending_order_worker`. Instantiate `PendingOrderWorker()` and call `w._execute_live_order(order_id=..., order_row=..., payload=...)`.

    Do **not** add MT5-specific tests (CONTEXT: MT5 covered by existing suite).
  </action>
  <acceptance_criteria>
    - [UC-4] Test function (named per pytest discovery, e.g. `test_live_order_forex_passes_category_gate`) calls `PendingOrderWorker._execute_live_order` and proves Forex is not rejected at `validate_market_category` (no category `mark_order_failed` / success path as specified in task).
    - [UC-5] Test function (e.g. `test_live_order_crypto_rejected_at_category_gate`) asserts `mark_order_failed` receives error containing `Crypto` and ibkr unsupported-category pattern.
    - [REGR-01] `cd backend_api_python &amp;&amp; python -m pytest tests/ -x -q --tb=line` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend_api_python &amp;&amp; python -m pytest tests/test_pending_order_worker.py -x -q --tb=line</automated>
    <automated>cd backend_api_python &amp;&amp; python -m pytest tests/ -x -q --tb=line</automated>
  </verify>
  <done>
    UC-4 and UC-5 implemented; full suite green; CONT-04 worker path covered with `_execute_live_order`.
  </done>
</task>

</tasks>

<verification>
After both tasks: `IBKRClient.supported_market_categories` includes Forex; unit tests match CONTEXT; worker tests exercise `_execute_live_order` with mocks; full `pytest tests/` passes.
</verification>

<success_criteria>
- CONT-04 satisfied: Forex in supported set; pending-order live path does not reject Forex solely for category when strategy config says Forex; Crypto still rejected for IBKR.
- REGR-01: entire `tests/` suite passes with `-x`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-market-category-worker-gate/04-01-SUMMARY.md` per execute-plan template.
</output>
