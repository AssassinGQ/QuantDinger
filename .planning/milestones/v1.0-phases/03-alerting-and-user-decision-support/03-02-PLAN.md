---
phase: 03-alerting-and-user-decision-support
plan: 02
type: execute
wave: 2
revision: 1
revision_notes: "Replanned from 03-REVIEWS.md: reason_code dedup isolation + empty-channel tests; align with Wave1 sentinel migration and IBKR_INSUFFICIENT_DATA_ALERT_SIGNAL_TYPE."
depends_on:
  - .planning/phases/03-alerting-and-user-decision-support/03-01-PLAN.md
files_modified:
  - backend_api_python/tests/test_ibkr_insufficient_user_alert.py
  - backend_api_python/tests/test_signal_executor.py
  - backend_api_python/tests/test_data_sufficiency_logging.py
autonomous: true
requirements:
  - N4
  - R4
  - R5
must_haves:
  truths:
    - "Automated tests prove dedup/cooldown (including reason_code dimension), flat vs positioned copy, empty-channel skip, and executor wiring."
    - "Phase 2 logging-module sentinel is superseded in Wave 1 Task 2; Wave 2 adds behavioral tests for N3 payload + executor hook."
    - "Every task verify includes passing the full `backend_api_python/tests` suite."
  artifacts:
    - path: "backend_api_python/tests/test_ibkr_insufficient_user_alert.py"
      provides: "Dedup + payload + dispatch mocks"
    - path: "backend_api_python/tests/test_signal_executor.py"
      provides: "Executor integration for insufficient alert hook"
    - path: "backend_api_python/tests/test_data_sufficiency_logging.py"
      provides: "`ibkr_insufficient_data_alert_sent` payload contract tests"
---

<objective>
Add pytest coverage for Phase 3 insufficient-data user alerts: dedup matrix, notifier invocation, N3 payload keys, and executor hook behavior (R4/R5/N3/N4).
</objective>

<context>
@.planning/phases/03-alerting-and-user-decision-support/03-REVIEWS.md
@.planning/phases/03-alerting-and-user-decision-support/03-CONTEXT.md
@.planning/phases/03-alerting-and-user-decision-support/03-01-PLAN.md
@backend_api_python/tests/test_ibkr_open_guard_execution.py
@backend_api_python/tests/test_data_sufficiency_logging.py
@backend_api_python/app/services/signal_executor.py
@backend_api_python/app/services/ibkr_insufficient_user_alert.py
@backend_api_python/app/services/data_sufficiency_logging.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: `test_ibkr_insufficient_user_alert.py` — dedup + copy + notifier</name>
  <files>backend_api_python/tests/test_ibkr_insufficient_user_alert.py</files>
  <read_first>
    backend_api_python/app/services/ibkr_insufficient_user_alert.py
    backend_api_python/app/services/signal_notifier.py
    backend_api_python/tests/conftest.py
    .planning/phases/03-alerting-and-user-decision-support/03-CONTEXT.md
  </read_first>
  <action>
    Create `backend_api_python/tests/test_ibkr_insufficient_user_alert.py` with deterministic tests:
    - Call `reset_insufficient_user_alert_dedup_state()` in `setup_method` or autouse fixture.
    - Patch `time.monotonic` to simulate cooldown boundary per UC below.
    - Mock `SignalNotifier.notify_signal` capturing kwargs; assert `notification_config` passed through; assert `signal_type` equals `IBKR_INSUFFICIENT_DATA_ALERT_SIGNAL_TYPE` imported from `app.services.ibkr_insufficient_user_alert`.
    - Assert flat copy: body/title from rendered path does NOT contain `有持仓` and does NOT contain forbidden close/hold pairing (use exact substrings from CONTEXT: no `平仓`+`持有` instruction combo — pick one concrete negative assertion documented in test docstring).
    - Assert positioned copy: synthetic `current_positions` with non-zero size for symbol → title or plain message contains `有持仓` and contains user-facing close/hold decision phrase (match implementation string exactly in assert).
    - Dedup: two rapid dispatches with identical key → `notify_signal` called once; after monotonic jump by **301** seconds → second call allowed.
    - Different `exchange_id` → second call not suppressed when first used `ibkr-paper` and second `ibkr-live`.
    - **reason_code isolation:** same `strategy_id`/`symbol`/`exchange_id`, first block with `reason_code=missing_bars`, second with `stale_prev_close` (or any two distinct `.value` strings) within cooldown window → `notify_signal` **twice** (keys must not merge across reason).
    - **Empty channels:** `notification_config={"channels": [], "targets": {}}` → `notify_signal` **not** called; no uncaught exception (patch logger if asserting warnings).
  </action>
  <use_case_spec>
    UC-03-10-01 单调时钟边界：第一次发送后 monotonic 增加 150 秒，第二次同键应被抑制；增加总量 ≥301 秒后应放行。
    UC-03-10-02 交易所维度：`exchange_id` 从 `ibkr-paper` 换到 `ibkr-live` 时冷却状态不共享。
    UC-03-10-03 空仓文案：positions 为空列表时，合并后的用户可见字符串不得包含「有持仓」。
    UC-03-10-04 有仓文案：positions 含当前 symbol 持仓时，必须含「有持仓」且含实现中用于 R5 的明确决策提示句。
    UC-03-10-05 reason 维度：仅 `reason_code` 不同、其余键相同且处于冷却窗内，应产生两次独立用户通知（不被错误 dedup）。
    UC-03-10-06 空渠道：`channels` 为空列表时不调用 `notify_signal`。
  </use_case_spec>
  <use_cases>
    - `monotonic_cooldown_boundary`: UC-03-10-01
    - `exchange_dimension_isolation`: UC-03-10-02
    - `flat_copy_constraints`: UC-03-10-03
    - `positioned_copy_constraints`: UC-03-10-04
    - `reason_code_isolation`: UC-03-10-05
    - `empty_channels_skip_notify`: UC-03-10-06
  </use_cases>
  <verify>
    <automated>python3 -m pytest backend_api_python/tests/test_ibkr_insufficient_user_alert.py -q</automated>
    <automated>python3 -m pytest backend_api_python/tests -q</automated>
    <note>通过全量用例：第二条必须 exit code 0。</note>
  </verify>
  <acceptance_criteria>
    - `backend_api_python/tests/test_ibkr_insufficient_user_alert.py` exists
    - file contains exact function names: `def test_dedup_suppresses_second_send_within_cooldown():`, `def test_dedup_allows_after_cooldown_elapsed():`, `def test_dedup_key_isolates_exchange_id():`, `def test_dedup_key_isolates_reason_code():`, `def test_empty_channels_skips_notify_signal():`, `def test_flat_alert_copy_has_no_position_prompt():`, `def test_positioned_alert_copy_requires_hold_close_prompt():`
    - `python3 -m pytest backend_api_python/tests/test_ibkr_insufficient_user_alert.py -q` exits 0
    - `python3 -m pytest backend_api_python/tests -q` exits 0
  </acceptance_criteria>
  <done>Dedup and user-visible copy behaviors are regression-locked.</done>
</task>

<task type="auto">
  <name>Task 2: Logging tests for `ibkr_insufficient_data_alert_sent`</name>
  <files>backend_api_python/tests/test_data_sufficiency_logging.py</files>
  <read_first>
    backend_api_python/app/services/data_sufficiency_logging.py
    backend_api_python/tests/test_data_sufficiency_logging.py
  </read_first>
  <action>
    Append tests to `backend_api_python/tests/test_data_sufficiency_logging.py` (or colocated new file if file size exceeds ~700 LOC — prefer append here) validating:
    - `build_ibkr_insufficient_data_alert_sent_payload` returns `event` == `ibkr_insufficient_data_alert_sent` and includes keys `strategy_id`, `symbol`, `exchange_id`, `_execution_mode`.
    - `emit_ibkr_insufficient_data_alert_sent` invokes logger with message containing `ibkr_insufficient_data_alert_sent` (use `caplog` or `unittest.mock.Mock` logger).
    Note: `test_logging_module_defines_ibkr_insufficient_data_alert_sent_helpers` is owned by **03-01 Task 2**; this task adds **behavioral** tests only (do not reintroduce the old negative sentinel).
  </action>
  <use_case_spec>
    UC-03-11-01 构建器纯函数：`build_ibkr_insufficient_data_alert_sent_payload` 不修改入参 dict，且输出含固定 `event`。
    UC-03-11-02 发射器副作用：调用 `emit_ibkr_insufficient_data_alert_sent` 后，mock logger 的 `info` 收到关键字参数 `extra` 且 `extra["event"]` 与构建器一致。
  </use_case_spec>
  <use_cases>
    - `payload_builder_pure`: UC-03-11-01
    - `emitter_logs_structured_event`: UC-03-11-02
  </use_cases>
  <verify>
    <automated>python3 -m pytest backend_api_python/tests/test_data_sufficiency_logging.py -q</automated>
    <automated>python3 -m pytest backend_api_python/tests -q</automated>
    <note>通过全量用例：第二条必须 exit code 0。</note>
  </verify>
  <acceptance_criteria>
    - `test_data_sufficiency_logging.py` contains `def test_insufficient_data_alert_sent_payload_shape():` and `def test_emit_insufficient_data_alert_sent_smoke():`
    - `python3 -m pytest backend_api_python/tests/test_data_sufficiency_logging.py -q` exits 0
    - `python3 -m pytest backend_api_python/tests -q` exits 0
  </acceptance_criteria>
  <done>N3 user-alert logging contract is test-locked.</done>
</task>

<task type="auto">
  <name>Task 3: Executor integration — `notify_signal` on insufficient block</name>
  <files>backend_api_python/tests/test_signal_executor.py</files>
  <read_first>
    backend_api_python/tests/test_signal_executor.py
    backend_api_python/tests/test_ibkr_open_guard_execution.py
    backend_api_python/app/services/signal_executor.py
  </read_first>
  <action>
    Extend `test_signal_executor.py` (reuse fixtures/mocks from `test_ibkr_open_guard_execution.py` where possible) to assert that when IBKR sufficiency gate returns insufficient:
    - `SignalNotifier.notify_signal` is invoked **once** per blocked attempt when dedup allows (reset dedup helper between tests).
    - `load_notification_config` is called when `_notification_config` absent or empty in `strategy_ctx`.
    - Guard still returns `False` and does not enqueue orders (existing assertions may already cover return False — add explicit assert on mock enqueuer if present).
  </action>
  <use_case_spec>
    UC-03-12-01 与 Phase 2 同次阻断：`evaluate_ibkr_open_data_sufficiency` 返回 insufficient 时，除既有结构化日志外，`notify_signal` 被调用（在 dedup 允许前提下）。
    UC-03-12-02 配置加载：`_notification_config` 缺失时 `load_notification_config` 至少调用一次。
  </use_case_spec>
  <use_cases>
    - `executor_notifies_on_block`: UC-03-12-01
    - `executor_loads_notification_config_fallback`: UC-03-12-02
  </use_cases>
  <verify>
    <automated>python3 -m pytest backend_api_python/tests/test_signal_executor.py -q</automated>
    <automated>python3 -m pytest backend_api_python/tests -q</automated>
    <note>通过全量用例：第二条必须 exit code 0。</note>
  </verify>
  <acceptance_criteria>
    - `test_signal_executor.py` defines `def test_ibkr_insufficient_block_triggers_user_notify():` and `def test_ibkr_insufficient_block_loads_notification_config_when_missing():`
    - `python3 -m pytest backend_api_python/tests/test_signal_executor.py -q` exits 0
    - `python3 -m pytest backend_api_python/tests -q` exits 0
  </acceptance_criteria>
  <done>Execution path delivers user alerts at the guard block without changing allow/block matrix.</done>
</task>

<task type="auto">
  <name>Task 4: ROADMAP carryover — operator copy notes for `stale_prev_close` / large `missing_window`</name>
  <files>backend_api_python/app/services/ibkr_insufficient_user_alert.py</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/phases/03-alerting-and-user-decision-support/03-CONTEXT.md
    backend_api_python/app/services/ibkr_insufficient_user_alert.py
  </read_first>
  <action>
    Extend the **module-level docstring** of `ibkr_insufficient_user_alert.py` with an operator-facing subsection (Markdown bullets inside the docstring is fine) that documents:
    - How `reason_code == stale_prev_close` should be phrased to users (reference `FreshnessMetadata` / Phase 1 schema at high level without duplicating full spec).
    - How large `missing_window` together with `market_closed_gap` represents wall-clock shortfall across sessions, not only “missing last bar.”
    These are **copy/runbook hints** for implementers and support; no new user-facing UI. Keep under ~40 lines added.
  </action>
  <use_case_spec>
    UC-03-13-01 文档可追溯：运维阅读模块 docstring 即可理解 `stale_prev_close` 与 `missing_window` 在用户文案中的推荐解释口径（与 ROADMAP Phase 3 carryover 对齐）。
  </use_case_spec>
  <use_cases>
    - `carryover_docstring_traceable`: UC-03-13-01
  </use_cases>
  <verify>
    <automated>python3 -m pytest backend_api_python/tests/test_ibkr_insufficient_user_alert.py -q</automated>
    <automated>python3 -m pytest backend_api_python/tests -q</automated>
    <note>通过全量用例：第二条必须 exit code 0。</note>
  </verify>
  <acceptance_criteria>
    - `grep -n "stale_prev_close" backend_api_python/app/services/ibkr_insufficient_user_alert.py` matches at least one line inside the module docstring (triple-quoted string before first `def` or `import` after docstring — grep file head is acceptable)
    - `grep -n "missing_window" backend_api_python/app/services/ibkr_insufficient_user_alert.py` matches at least one line in the same docstring region
    - `grep -n "market_closed_gap" backend_api_python/app/services/ibkr_insufficient_user_alert.py` matches at least one line in docstring
    - `python3 -m pytest backend_api_python/tests -q` exits 0
  </acceptance_criteria>
  <done>ROADMAP Phase 3 carryover bullets for operator copy are anchored in code next to the alert builder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner | Tests must not require live IBKR or outbound webhooks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | T | Test doubles | mitigate | Only mocks/stubs; no real webhook posts |
</threat_model>

<verification>
- Scoped: `python3 -m pytest backend_api_python/tests/test_ibkr_insufficient_user_alert.py backend_api_python/tests/test_data_sufficiency_logging.py backend_api_python/tests/test_signal_executor.py -q`
- Full suite (全量): `python3 -m pytest backend_api_python/tests -q`
</verification>

<success_criteria>
- New focused tests exist and full backend test suite stays green.
- Phase 2 boundary `persist_notification` absent from `data_sufficiency_logging.py` remains asserted by migrated module-source test.
</success_criteria>
