---
phase: 01-ibkr-schedule-sufficiency-domain-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/app/services/data_sufficiency_types.py
  - backend_api_python/app/services/live_trading/ibkr_trading/ibkr_schedule_provider.py
  - backend_api_python/tests/test_data_sufficiency_types.py
  - backend_api_python/tests/test_ibkr_schedule_provider.py
  - backend_api_python/tests/test_data_sufficiency_integration.py
autonomous: true
requirements:
  - R1
  - R2
  - N2
must_haves:
  truths:
    - "Phase 1 exposes a typed, serializable sufficiency contract with stable top-level fields for downstream consumers."
    - "IBKR schedule parsing distinguishes known closed sessions from unknown or malformed schedule states."
    - "Schedule failure is represented explicitly so later open/add guards can fail safe without re-parsing broker payloads."
  artifacts:
    - path: "backend_api_python/app/services/data_sufficiency_types.py"
      provides: "DataSufficiencyResult contract, reason taxonomy, schedule-status taxonomy"
    - path: "backend_api_python/app/services/live_trading/ibkr_trading/ibkr_schedule_provider.py"
      provides: "IBKR adapter over trading_hours parsing and session truth"
    - path: "backend_api_python/tests/test_data_sufficiency_types.py"
      provides: "Typed-contract and reason-code coverage"
    - path: "backend_api_python/tests/test_ibkr_schedule_provider.py"
      provides: "Adapter classification coverage for known/unknown schedule states"
    - path: "backend_api_python/tests/test_data_sufficiency_integration.py"
      provides: "Plan 01 adapter contract smoke + ground-truth cross-checks"
  key_links:
    - from: "backend_api_python/app/services/live_trading/ibkr_trading/ibkr_schedule_provider.py"
      to: "backend_api_python/app/services/live_trading/ibkr_trading/trading_hours.py"
      via: "parse_liquid_hours + is_rth_check reuse (no private underscore imports)"
      pattern: "from app\\.services\\.live_trading\\.ibkr_trading\\.trading_hours import"
    - from: "backend_api_python/app/services/live_trading/ibkr_trading/ibkr_schedule_provider.py"
      to: "backend_api_python/app/services/data_sufficiency_types.py"
      via: "typed adapter output"
      pattern: "DataSufficiency.*Schedule"
---

<objective>
Lock the Phase 1 contract boundary before any validator logic exists: define the canonical sufficiency types and build an IBKR schedule/session adapter that reuses `trading_hours.py` without replacing it.

Purpose: satisfy D-01 through D-05 and D-09 through D-11 while giving later plans a stable, fail-safe input boundary for sufficiency classification.
Output: typed result/reason enums, adapter-facing schedule state, and unit tests that distinguish `unknown_schedule` from known closed-session states.
</objective>

<review_carryover source=".planning/phases/01-ibkr-schedule-sufficiency-domain-model/01-REVIEWS.md RE-REVIEW 2026-04-17">
These items were raised in the second cross-AI review and are folded into this plan (Phase 1). Items explicitly deferred appear in `.planning/ROADMAP.md` under Phase 2/3/4.

**Acceptance / test naming governance:** Acceptance criteria use exact `def test_*():` names as minimum scenario anchors. Renaming or splitting for clarity is allowed when the same scenario remains covered and the plan’s acceptance list is updated in the same diff; deleting scenario coverage without updating the plan is not allowed.

**Timeframe→seconds (versioned):** Introduce a single documented `TIMEFRAME_SECONDS_MAP` (or equivalent) listing supported timeframe keys → bar duration in seconds. `effective_lookback = required_bars * duration_seconds` with no D-07 tolerance band on the contract. In tests, compare floats with a small epsilon (for example `abs(a - b) < 1e-6`).

**Diagnostics correlation:** Extend `DataSufficiencyDiagnostics` with optional `con_id: int | None` when the adapter path has `con_id`, to correlate sufficiency logs with `trading_hours` fuse state.

**Timezone fallback operator signal:** When `timezone_resolution="fallback_utc"`, emit `logger.warning` from the adapter or orchestration layer (not from the pure validator) once per evaluation path, without logging raw `liquidHours`.

**Fallback + schedule_known_* semantics:** Document in adapter module docstring: invalid `timeZoneId` may still yield `schedule_known_open` or `schedule_known_closed` if sessions parse under UTC fallback; in that case `timezone_resolution="fallback_utc"` and `schedule_failure_reason="timezone_id_unresolved"` together mean “parsed sessions are not timezone-trusted until fixed.” Optional typed `timezone_trusted: bool` is deferred to `.planning/ROADMAP.md` Phase 4 hardening unless implemented in Phase 1 with an explicit plan amendment.
</review_carryover>

<review_resolution source="01-REVIEWS.md cross-AI 2026-04-18">
**闭项 / 对齐说明**

- **`test_adapter_does_not_mutate_trading_hours_cache`：** 已在 Task 2 `<acceptance_criteria>` 中列为具名 `def test_*`（与审查中「验收列表未显式枚举」的旧结论对齐；实施时不得删名或挪作他用）。
- **`market_category`：** 由编排/调用方传入适配器与结果类型；计划不要求从 `ContractDetails` 推断交易所类别；与 `kline_fetcher.get_kline(market, ...)` 的对齐由调用方保证（见 `data_sufficiency_types.py` 与 `data_sufficiency_service.py` 模块文档）。
- **`FreshnessMetadata`：** 以仓库 `data_sufficiency_types.py` 中的定义为准；审查中「Plan 01 未写类型」的缺口已由实现与模块文档闭合，Plan 02 仍负责阈值策略与 `stale_prev_close` 行为验收。
</review_resolution>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-ibkr-schedule-sufficiency-domain-model/01-CONTEXT.md
@.planning/phases/01-ibkr-schedule-sufficiency-domain-model/01-RESEARCH.md
@.planning/phases/01-ibkr-schedule-sufficiency-domain-model/01-VALIDATION.md
@backend_api_python/app/services/live_trading/ibkr_trading/trading_hours.py
@backend_api_python/tests/test_trading_hours.py
@backend_api_python/tests/test_signal_executor.py

<interfaces>
From `backend_api_python/app/services/live_trading/ibkr_trading/trading_hours.py`:

```python
def parse_liquid_hours(liquid_hours: str, tz) -> List[Tuple[datetime.datetime, datetime.datetime]]:
    ...

def is_rth_check(contract_details, server_time_utc: datetime.datetime, con_id: int = 0, symbol: str = "?", now=None) -> bool:
    ...

def clear_cache():
    ...
```

From `backend_api_python/tests/test_trading_hours.py` pattern:

```python
details = MagicMock()
details.liquidHours = liquid_hours
details.timeZoneId = tz_id
assert is_rth_check(details, server_utc, con_id=12345) is True
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define typed sufficiency contract and stable reason taxonomy</name>
  <files>backend_api_python/app/services/data_sufficiency_types.py, backend_api_python/tests/test_data_sufficiency_types.py</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/01-ibkr-schedule-sufficiency-domain-model/01-CONTEXT.md
    .planning/phases/01-ibkr-schedule-sufficiency-domain-model/01-RESEARCH.md
    backend_api_python/app/services/live_trading/ibkr_trading/trading_hours.py
    backend_api_python/tests/test_trading_hours.py
  </read_first>
  <action>
    Create `backend_api_python/app/services/data_sufficiency_types.py` with concrete typed definitions that preserve D-01, D-02, D-03, D-05, D-06, and D-07. Use Python `Enum` + `dataclass` (or an equally strong typed construct) for:
    - `DataSufficiencyReasonCode` with exact machine values: `sufficient`, `missing_bars`, `stale_prev_close`, `market_closed_gap`, `unknown_schedule`
    - `IBKRScheduleStatus` with exact values that separate `schedule_known_open`, `schedule_known_closed`, and `schedule_unknown`
    - `DataSufficiencyResult` with stable top-level fields: `sufficient`, `reason_code`, `required_bars`, `available_bars`, `effective_lookback`, `missing_window`, `schedule_status`, `symbol`, `timeframe`, `market_category`, `diagnostics`
    - `DataSufficiencyDiagnostics` as a bounded `dataclass` with explicit optional fields only (no free-form dict). Minimum required keys: `parsed_session_count: int | None`, `schedule_failure_reason: str | None`, `timezone_id: str | None`, `timezone_resolution: Literal["explicit","fallback_utc"] | None`, `prev_close_stale_since: datetime | None`, `con_id: int | None` (optional; populated when adapter knows `con_id` for fuse/schedule correlation)
    - `IBKRScheduleSnapshot` as the adapter-facing typed object returned by `ibkr_schedule_provider.py` with explicit fields: `symbol`, `timeframe`, `market_category`, `server_time_utc`, `schedule_status`, `session_open: bool`, `next_session_open_utc: datetime | None`, `parsed_session_count: int`, `timezone_id: str`, `timezone_resolution: Literal["explicit","fallback_utc"]`, `schedule_failure_reason: str | None`
    - `effective_lookback` MUST be expressed in **seconds** as a `float` representing the required lookback window duration derived from `{timeframe, required_bars}` using a single in-repo `TIMEFRAME_SECONDS_MAP` (or equivalent named constant) documented beside the type definitions (no tolerance window per D-07).
    - `missing_window` MUST be expressed in **seconds** as a `float` representing how much of the required window is missing when insufficient; when sufficient, `missing_window` MUST be `0.0`.
    Add `backend_api_python/tests/test_data_sufficiency_types.py` to assert exact enum values, dataclass field names, and serialization-ready stable top-level keys.
  </action>
  <use_cases>
    - `equal_boundary`: input `required_bars=100`, `available_bars=100` -> expected `sufficient=True`, `reason_code="sufficient"`, `missing_window == 0.0`; validate in `backend_api_python/tests/test_data_sufficiency_types.py`
    - `missing_bars_contract`: input `required_bars=100`, `available_bars=99` -> expected contract accepts `reason_code="missing_bars"` and `missing_window > 0.0` seconds; validate in `backend_api_python/tests/test_data_sufficiency_types.py`
    - `unknown_schedule_contract`: input `schedule_status="schedule_unknown"` -> expected `reason_code="unknown_schedule"` is representable at top level; validate in `backend_api_python/tests/test_data_sufficiency_types.py`
    - `closed_session_contract`: input `schedule_status="schedule_known_closed"` -> expected contract can carry `reason_code="market_closed_gap"` separately from `unknown_schedule`; validate in `backend_api_python/tests/test_data_sufficiency_types.py`
  </use_cases>
  <verify>
    <automated>python3 -m pytest backend_api_python/tests/test_data_sufficiency_types.py -q</automated>
    <automated>python3 -m pytest backend_api_python/tests/test_data_sufficiency_types.py backend_api_python/tests/test_trading_hours.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend_api_python/app/services/data_sufficiency_types.py` contains exact strings `class DataSufficiencyReasonCode`, `class IBKRScheduleStatus`, and `class DataSufficiencyResult`
    - the file contains exact strings `class DataSufficiencyDiagnostics` and `class IBKRScheduleSnapshot`
    - the file contains exact reason-code literals `missing_bars`, `stale_prev_close`, `market_closed_gap`, and `unknown_schedule`
    - the file contains exact field names `required_bars`, `available_bars`, `effective_lookback`, `missing_window`, and `schedule_status`
    - `DataSufficiencyDiagnostics` includes the exact field name `con_id` (optional `int | None`)
    - the file contains a documented `TIMEFRAME_SECONDS_MAP` (or equivalently named constant mapping) listing supported timeframe keys to per-bar duration seconds used for `effective_lookback` computation
    - `DataSufficiencyReasonCode` enum members MUST use UPPER_SNAKE_CASE names and MUST have string values exactly equal to the literals above; `backend_api_python/tests/test_data_sufficiency_types.py` MUST include `def test_reason_enum_values_match_requirement_literals()` asserting `DataSufficiencyReasonCode.MISSING_BARS.value == "missing_bars"` and the same pattern for all reason codes
    - `backend_api_python/tests/test_data_sufficiency_types.py` MUST include these exact test function names: `def test_equal_boundary():`, `def test_missing_bars_contract():`, `def test_unknown_schedule_contract():`, `def test_closed_session_contract():`, `def test_types_import_smoke():`
    - `test_types_import_smoke()` MUST successfully import the module using the same import style as other backend tests (prefer `importlib.import_module(\"app.services.data_sufficiency_types\")` or an equivalent `from app.services...` import) and assert importing does not raise
  </acceptance_criteria>
  <done>The repository has a single typed source of truth for sufficiency results and downstream phases no longer need to invent ad hoc reason fields; this plan does not claim Phase 2/3 execution-blocking or alert-dispatch coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Build IBKR schedule adapter over existing trading_hours seam</name>
  <files>backend_api_python/app/services/live_trading/ibkr_trading/ibkr_schedule_provider.py, backend_api_python/tests/test_ibkr_schedule_provider.py</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/01-ibkr-schedule-sufficiency-domain-model/01-CONTEXT.md
    .planning/phases/01-ibkr-schedule-sufficiency-domain-model/01-RESEARCH.md
    backend_api_python/app/services/data_sufficiency_types.py
    backend_api_python/app/services/live_trading/ibkr_trading/trading_hours.py
    backend_api_python/tests/test_trading_hours.py
  </read_first>
  <action>
    Create `backend_api_python/app/services/live_trading/ibkr_trading/ibkr_schedule_provider.py` as a thin adapter that wraps the existing `trading_hours` behavior instead of replacing it, per D-09 through D-11. Reuse **only public** symbols from `trading_hours.py` (at minimum `parse_liquid_hours` and `is_rth_check`) plus local timezone normalization that mirrors the repo’s current “invalid timezone id falls back to UTC with warning” behavior without importing any `trading_hours` name that begins with `_`.
    Return an `IBKRScheduleSnapshot` that preserves:
    - timezone used for normalization
    - parsed session count
    - whether schedule is known or unknown
    - whether the current moment is inside a known session
    - next session open when discoverable
    - explicit failure metadata for malformed/empty schedule input so later guards can fail safe per D-04/D-05 and N2
    Do not add execution-path blocking, and do not mutate `signal_executor.py` in this phase.
    Add `backend_api_python/tests/test_ibkr_schedule_provider.py` with mock `ContractDetails` cases that distinguish malformed schedule from known closed session and cross-day session handling.
    Adapter MUST NOT import or call any `trading_hours.py` names that start with `_` (no private API coupling). If next-session computation is needed, implement it using public outputs from `parse_liquid_hours(...)` sessions plus explicit comparisons against `server_time_utc`.
    Adapter MUST surface timezone resolution explicitly: when timezone normalization falls back to UTC for an unknown/invalid `timeZoneId`, set `timezone_resolution="fallback_utc"` on `IBKRScheduleSnapshot` and set `schedule_failure_reason="timezone_id_unresolved"` while still allowing `schedule_status` to be `schedule_known_open/closed` if sessions parse. Call `logger.warning` once on this path (adapter or a thin caller; not the future pure validator) without emitting raw broker blobs.
    When building any `DataSufficiencyDiagnostics` crossing from adapter output, copy `con_id` through when available.
    Adapter MUST accept an explicit `server_time_utc: datetime` parameter on its public entrypoint(s) so tests can inject `now` deterministically (do not rely on implicit wall-clock time inside the adapter).
    Add fuse regression coverage by asserting adapter outputs across two injected times around fuse boundaries using `unittest.mock.patch` on fuse-related helpers/constants only if needed, but prefer driving behavior via `now=` injection and controlled `con_id` inputs consistent with `test_trading_hours.py`.
  </action>
  <use_cases>
    - `unknown_schedule`: input `liquidHours=""` or unparsable segment -> expected `schedule_status="schedule_unknown"`, `reason candidate="unknown_schedule"`, next session absent; validate in `backend_api_python/tests/test_ibkr_schedule_provider.py`
    - `market_closed_gap`: known EST/HKT schedule with current time outside all parsed windows -> expected `schedule_status="schedule_known_closed"` and next session open populated when available; validate in `backend_api_python/tests/test_ibkr_schedule_provider.py`
    - `session_known_open`: current time inside parsed session -> expected `schedule_status="schedule_known_open"` and `session_open=True`; validate in `backend_api_python/tests/test_ibkr_schedule_provider.py`
    - `cross_day_session`: Forex-style `20260308:1715-20260309:1700` -> expected parsed cross-day window remains known and open/closed evaluation is correct; validate in `backend_api_python/tests/test_ibkr_schedule_provider.py`
    - `cross_timezone_server_utc_market_hkt`: `server_time_utc` is UTC but `timeZoneId` is `Asia/Hong_Kong` with a known HK session -> expected `timezone_id="Asia/Hong_Kong"` and `schedule_status` matches ground truth computed from `parse_liquid_hours` for the injected `server_time_utc`; validate in `backend_api_python/tests/test_ibkr_schedule_provider.py`
    - `timezone_fallback_metadata`: invalid `timeZoneId` triggers timezone normalization fallback to UTC -> expected `timezone_resolution="fallback_utc"` and `schedule_failure_reason="timezone_id_unresolved"` while still returning a coherent `schedule_status` based on parsed sessions; validate in `backend_api_python/tests/test_ibkr_schedule_provider.py`
    - `fuse_transition_open_after_expiry`: reproduce a fuse-related closed-session suppression window using the same con_id across two injected `server_time_utc` timestamps (before/after boundary) and assert `schedule_status` transitions from `schedule_known_closed` to `schedule_known_open` when the underlying `trading_hours` fuse window would expire; validate in `backend_api_python/tests/test_ibkr_schedule_provider.py`
    - `next_session_open_utc_populated`: simple unambiguous equity-like schedule where `next_session_open_utc` must match an independent expectation derived from `parse_liquid_hours` output (not only indirectly via fuse transition); validate in `backend_api_python/tests/test_ibkr_schedule_provider.py`
    - `no_trading_hours_private_imports`: `ibkr_schedule_provider.py` MUST NOT contain the substring `import _` nor any dotted access of the form `trading_hours._` (static string checks in tests); validate in `backend_api_python/tests/test_ibkr_schedule_provider.py`
  </use_cases>
  <verify>
    <automated>python3 -m pytest backend_api_python/tests/test_ibkr_schedule_provider.py -q</automated>
    <automated>python3 -m pytest backend_api_python/tests/test_ibkr_schedule_provider.py backend_api_python/tests/test_trading_hours.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend_api_python/app/services/live_trading/ibkr_trading/ibkr_schedule_provider.py` imports from `trading_hours.py` instead of reimplementing `liquidHours` parsing from scratch
    - the adapter file contains exact strings `schedule_known_open`, `schedule_known_closed`, and `schedule_unknown`
    - the adapter file contains explicit failure-path handling for empty or malformed schedule input
    - `backend_api_python/tests/test_ibkr_schedule_provider.py` MUST include these exact test function names: `def test_unknown_schedule():`, `def test_market_closed_gap():`, `def test_session_known_open():`, `def test_cross_day_session():`, `def test_cross_timezone_server_utc_market_hkt():`, `def test_timezone_fallback_metadata():`, `def test_fuse_transition_open_after_expiry():`, `def test_next_session_open_utc_populated():`, `def test_no_trading_hours_private_imports():`, `def test_adapter_does_not_mutate_trading_hours_cache():`
    - `test_adapter_does_not_mutate_trading_hours_cache()` MUST call `trading_hours.clear_cache()` before/after adapter calls and assert stable behavior (no exception + deterministic outputs) consistent with existing cache semantics in `test_trading_hours.py`
  </acceptance_criteria>
  <done>Phase 1 has a reusable IBKR schedule/session seam that preserves fail-safe schedule ambiguity instead of collapsing everything to a bare boolean.</done>
</task>

<task type="auto">
  <name>Task 3: Add Plan 01 integration tests + run blocking phase regression gate after contract and adapter land</name>
  <files>backend_api_python/tests/test_data_sufficiency_integration.py, .planning/phases/01-ibkr-schedule-sufficiency-domain-model/01-01-PLAN.md</files>
  <read_first>
    .planning/phases/01-ibkr-schedule-sufficiency-domain-model/01-VALIDATION.md
    backend_api_python/tests/test_data_sufficiency_types.py
    backend_api_python/tests/test_ibkr_schedule_provider.py
    backend_api_python/tests/test_trading_hours.py
  </read_first>
  <action>
    Add `backend_api_python/tests/test_data_sufficiency_integration.py` with at least one end-to-end test that constructs a mock `ContractDetails`, calls the public adapter entrypoint with explicit `server_time_utc`, and asserts the resulting `IBKRScheduleSnapshot.schedule_status` matches an independent ground-truth expectation computed from `parse_liquid_hours` for the same inputs.
    Execute the full relevant Phase 1 suites for contract and adapter coverage, then run the required blocking phase-wide backend regression gate. Treat any failure in type taxonomy, schedule-known vs schedule-unknown separation, or existing `trading_hours` behavior as a hard stop before moving to Plan 02.
  </action>
  <use_cases>
    - `phase1_contract_suite`: contract and adapter suites both green before Plan 02 starts; validate with the commands in `<verify>`
    - `phase_regression_gate`: entire `backend_api_python/tests` suite remains green after adding the new files; validate with the commands in `<verify>`
  </use_cases>
  <verify>
    <automated>python3 -m pytest backend_api_python/tests/test_data_sufficiency_types.py backend_api_python/tests/test_ibkr_schedule_provider.py backend_api_python/tests/test_data_sufficiency_integration.py backend_api_python/tests/test_trading_hours.py -q</automated>
    <automated>python3 -m pytest backend_api_python/tests -q</automated>
  </verify>
  <acceptance_criteria>
    - command output includes all new Phase 1 test files without failures
    - `python3 -m pytest backend_api_python/tests -q` exits with code 0
    - no task in this plan requires modifying `backend_api_python/app/services/signal_executor.py`
    - `backend_api_python/tests/test_data_sufficiency_integration.py` MUST include `def test_contract_details_to_ibkr_schedule_snapshot():`
  </acceptance_criteria>
  <done>Plan 01 is only considered complete when the new contract/adapter suites and the full backend suite both pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IBKR contract details -> schedule adapter | Untrusted or partial broker schedule strings cross into typed normalization logic |
| schedule adapter -> later validator/guard | Ambiguous session state would cascade into wrong sufficiency and blocking decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `ibkr_schedule_provider.py` | mitigate | Preserve explicit `schedule_unknown` state for malformed/empty `liquidHours`; add tests for empty and unparsable schedule segments |
| T-01-02 | R | `data_sufficiency_types.py` | mitigate | Lock stable enum values and top-level fields so downstream guard/alert code can rely on exact reason codes instead of free-form text; string literals like `timezone_resolution` remain Phase-1-documented with Phase-4 typed-constant hardening tracked in ROADMAP |
| T-01-03 | I | `ibkr_schedule_provider.py` | mitigate | Adapter output and tests must avoid logging or storing raw broker `liquidHours` blobs as a downstream contract field |
</threat_model>

<verification>
- `python3 -m pytest backend_api_python/tests/test_data_sufficiency_types.py backend_api_python/tests/test_ibkr_schedule_provider.py backend_api_python/tests/test_data_sufficiency_integration.py backend_api_python/tests/test_trading_hours.py -q`
- `python3 -m pytest backend_api_python/tests -q`
</verification>

<success_criteria>
- Plan 01 introduces typed sufficiency and schedule-status contracts with exact, machine-consumable top-level fields.
- Plan 01 proves `unknown_schedule` is distinguishable from known closed-market cases.
- Plan 01 does not implement execution-path blocking or mutate `signal_executor.py`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ibkr-schedule-sufficiency-domain-model/01-01-SUMMARY.md`
</output>
