---
phase: 01-ibkr-schedule-sufficiency-domain-model
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend_api_python/app/services/data_sufficiency_validator.py
  - backend_api_python/app/services/data_sufficiency_logging.py
  - backend_api_python/app/services/data_sufficiency_service.py
  - backend_api_python/tests/test_data_sufficiency_validator.py
  - backend_api_python/tests/test_data_sufficiency_logging.py
  - backend_api_python/tests/test_data_sufficiency_service.py
  - backend_api_python/tests/test_data_sufficiency_integration.py
autonomous: true
requirements:
  - R1
  - N1
  - N3
must_haves:
  truths:
    - "Given the same required bars, available bars, and IBKR session context, the sufficiency result is deterministic."
    - "Boundary cases such as `available_bars == required_bars` classify as sufficient, while hard-short cases classify as insufficient."
    - "Structured sufficiency logs emit stable machine-readable fields without dumping raw broker schedule payloads."
  artifacts:
    - path: "backend_api_python/app/services/data_sufficiency_validator.py"
      provides: "Pure lookback normalization and sufficiency classification"
    - path: "backend_api_python/app/services/data_sufficiency_logging.py"
      provides: "Structured log payload builder for sufficiency decisions"
    - path: "backend_api_python/app/services/data_sufficiency_service.py"
      provides: "Phase-1 orchestration: classify then emit `ibkr_data_sufficiency_check` without polluting pure validator"
    - path: "backend_api_python/tests/test_data_sufficiency_validator.py"
      provides: "Reason-code and boundary-case suite"
    - path: "backend_api_python/tests/test_data_sufficiency_logging.py"
      provides: "Structured logging coverage"
    - path: "backend_api_python/tests/test_data_sufficiency_service.py"
      provides: "Orchestration unit tests (imports, call order, single emission)"
    - path: "backend_api_python/tests/test_data_sufficiency_integration.py"
      provides: "Adapter→validator→logging orchestration coverage"
  key_links:
    - from: "backend_api_python/app/services/data_sufficiency_validator.py"
      to: "backend_api_python/app/services/data_sufficiency_types.py"
      via: "DataSufficiencyResult construction"
      pattern: "DataSufficiencyResult"
    - from: "backend_api_python/app/services/data_sufficiency_validator.py"
      to: "backend_api_python/app/services/data_sufficiency_types.py"
      via: "consumes `IBKRScheduleSnapshot.schedule_status` + `DataSufficiencyResult` typing"
      pattern: "IBKRScheduleSnapshot|schedule_status"
    - from: "backend_api_python/app/services/data_sufficiency_logging.py"
      to: "backend_api_python/app/services/data_sufficiency_types.py"
      via: "payload derived from typed `DataSufficiencyResult` fields"
      pattern: "DataSufficiencyResult"
    - from: "backend_api_python/app/services/data_sufficiency_service.py"
      to: "backend_api_python/app/services/data_sufficiency_validator.py"
      via: "orchestrator calls pure classifier"
      pattern: "evaluate_.*sufficiency|classify_.*sufficiency"
    - from: "backend_api_python/app/services/data_sufficiency_service.py"
      to: "backend_api_python/app/services/data_sufficiency_logging.py"
      via: "orchestrator emits structured event after classification"
      pattern: "emit_ibkr_data_sufficiency_check|ibkr_data_sufficiency_check"
    - from: "backend_api_python/app/services/data_sufficiency_logging.py"
      to: "backend_api_python/app/utils/logger.py"
      via: "runtime log emission for ibkr_data_sufficiency_check"
      pattern: "logger\\.info|get_logger"
---

<objective>
Implement the pure sufficiency classifier, structured logging helpers, and a thin orchestration service after Plan 01 has fixed the typed inputs and reason taxonomy.

Purpose: fulfill the actual Phase 1 outcome of deterministic classification plus observability, while keeping execution blocking for Phase 2 and alert fan-out for Phase 3.
Output: pure validator logic, structured logging helper, a thin orchestration service that performs log emission, and explicit use-case suites for boundary, precedence, kline-fetcher-aligned bar counting, and orchestration wiring.
</objective>

<review_carryover source=".planning/phases/01-ibkr-schedule-sufficiency-domain-model/01-REVIEWS.md RE-REVIEW 2026-04-17">
Folded into this plan for Phase 1. Cross-phase follow-ups are listed under `.planning/ROADMAP.md` Phase 2/3/4.

**Acceptance / test naming governance:** Same rule as Plan 01 — exact `def test_*():` names anchor scenarios; rename/split allowed when coverage is preserved and acceptance lists are updated in the same diff.

**Pure validator import boundary:** `data_sufficiency_validator.py` MUST depend only on the standard library plus `data_sufficiency_types` (and typing helpers). It MUST NOT import `logging`, `data_sufficiency_logging`, `kline_fetcher`, DB/HTTP clients, or other I/O modules. Bar counting calls a `get_kline_callable` injected at runtime; tests mock it.

**Freshness input for `stale_prev_close`:** Define a small explicit optional input (for example `FreshnessMetadata` as a `dataclass` or `TypedDict` in `data_sufficiency_types.py` or colocated in the validator module) with at minimum `prev_close_timestamp_utc: datetime | None` and `prev_close_age_seconds: float | None`. When metadata is absent or fields are `None`, the validator MUST NOT emit `stale_prev_close` from missing data alone (document default). Threshold semantics for “how stale is too stale” remain Phase 3 alerting policy — see ROADMAP Phase 3.

**`get_kline` failures:** Document and implement one behavior for `compute_available_bars_from_kline_fetcher` when the callable raises: propagate the exception to the orchestration layer (or re-raise) so insufficient-data is not silently masked as zero bars; orchestration may map to a sufficiency outcome in a later phase. Phase 1 tests MUST cover the chosen behavior with mocks.

**Aggregation mock documentation:** In `test_aggregation_1h_from_5m_mocked_get_kline`, add a module-level or test docstring stating which `LOWER_LEVELS` assumptions the mock encodes and that it is not a substitute for production `get_kline` integration drift checks (Phase 2).

**Float assertions:** Use epsilon comparisons for `effective_lookback` and `missing_window` in tests.

**Coverage gaps vs storage:** Phase 1 helper returns bar count from mocked/injected `get_kline` results only; distinguishing “fewer bars returned because of storage gaps vs calendar coverage” for `missing_window` is documented in validator docstring and tightened under Phase 2 real-path tests.
</review_carryover>

<review_resolution source="01-REVIEWS.md cross-AI 2026-04-18">
**闭项 / 对齐说明**

- **`get_kline_callable` 异常与编排：** Phase 1 编排层**不捕获** `compute_available_bars_from_kline_fetcher` / 适配器抛出的异常，也**不**将失败静默映射为零根 K 线；行为以 `data_sufficiency_service.py` 模块与 `evaluate_ibkr_data_sufficiency_and_log` 文档为准。Phase 2 在接入 `signal_executor` 时必须定义「阻断 / 独立 reason / 继续抛出」的最终契约（见 `.planning/ROADMAP.md` Phase 2 carryover）。
- **集成测试命名：** 唯一验收名为 `def test_adapter_to_service_emits_ibkr_data_sufficiency_check():`；`<action>` 叙述中的简称须与此一致。
- **`con_id` 可观测性：** 结构化日志侧已有 `con_id` 字段与 `test_sufficient_log_payload` 断言；跨适配器→结果的端到端覆盖由 `test_data_sufficiency_integration.py` 的 Plan 02 任务继续承担，不强制新增具名用例以免与现有验收列表冲突。
</review_resolution>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-ibkr-schedule-sufficiency-domain-model/01-CONTEXT.md
@.planning/phases/01-ibkr-schedule-sufficiency-domain-model/01-RESEARCH.md
@.planning/phases/01-ibkr-schedule-sufficiency-domain-model/01-VALIDATION.md
@.planning/phases/01-ibkr-schedule-sufficiency-domain-model/01-01-PLAN.md
@backend_api_python/app/services/kline_fetcher.py
@backend_api_python/app/services/live_trading/ibkr_trading/ibkr_schedule_provider.py
@backend_api_python/app/services/data_sufficiency_types.py
@backend_api_python/tests/test_signal_executor.py

<interfaces>
Expected from Plan 01:

```python
class DataSufficiencyReasonCode(Enum):
    SUFFICIENT = "sufficient"
    MISSING_BARS = "missing_bars"
    STALE_PREV_CLOSE = "stale_prev_close"
    MARKET_CLOSED_GAP = "market_closed_gap"
    UNKNOWN_SCHEDULE = "unknown_schedule"

class DataSufficiencyResult:
    sufficient: bool
    reason_code: DataSufficiencyReasonCode
    required_bars: int
    available_bars: int
    effective_lookback: float
    missing_window: float
    schedule_status: IBKRScheduleStatus
```

Relevant existing data seam from `backend_api_python/app/services/kline_fetcher.py`:

```python
LOWER_LEVELS = {
    "1D": ["4H", "1H", "5m", "1m"],
    "1H": ["5m", "1m"],
}

def get_kline(market: str, symbol: str, timeframe: str, limit: int = 1000, before_time=None):
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement pure lookback normalization and deterministic sufficiency classification</name>
  <files>backend_api_python/app/services/data_sufficiency_validator.py, backend_api_python/tests/test_data_sufficiency_validator.py</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/01-ibkr-schedule-sufficiency-domain-model/01-CONTEXT.md
    .planning/phases/01-ibkr-schedule-sufficiency-domain-model/01-RESEARCH.md
    .planning/phases/01-ibkr-schedule-sufficiency-domain-model/01-01-PLAN.md
    backend_api_python/app/services/data_sufficiency_types.py
    backend_api_python/app/services/live_trading/ibkr_trading/ibkr_schedule_provider.py
    backend_api_python/app/services/kline_fetcher.py
    backend_api_python/tests/test_signal_executor.py
  </read_first>
  <action>
    Create `backend_api_python/app/services/data_sufficiency_validator.py` as a pure module that consumes normalized inputs from Plan 01 and bars-first strategy requirements from D-06. The module must:
    - accept exact inputs `symbol`, `timeframe`, `market_category`, `required_bars`, `available_bars`, `schedule_status`, and optional `FreshnessMetadata` (or equivalent) for prior-close staleness per `<review_carryover>` defaults
    - compute `effective_lookback_seconds` and `missing_window_seconds` from the bars/timeframe contract without introducing tolerance windows, per D-07, then copy them into `DataSufficiencyResult.effective_lookback` and `DataSufficiencyResult.missing_window` (both floats, units seconds) exactly as defined in Plan 01 Task 1
    - implement `compute_available_bars_from_kline_fetcher(market, symbol, timeframe, required_bars, before_time_utc, get_kline_callable)` as a **pure** helper that invokes the injected `get_kline_callable` (tests mock production shape) plus in-memory bar counting only (no DB writes, no network). The helper MUST mirror `LOWER_LEVELS` aggregation intent from `kline_fetcher.py` by mocking `get_kline` return values in tests; do not fork `kline_fetcher` internals beyond the callable boundary and counting returned bars. Document exception propagation vs masking in the module docstring.
    - classify by deterministic precedence that does not depend on execution mode: `unknown_schedule` first for schedule ambiguity, `stale_prev_close` for stale prior-close freshness failure, `market_closed_gap` for schedule-known closed-gap insufficiency, `missing_bars` for hard count shortfall, `sufficient` only when none apply
    - keep `signal_executor.py` untouched and avoid any decision about open/add blocking in this phase
    - MUST remain side-effect free: no imports from `logging`, no `logger.*` calls, no imports from `data_sufficiency_logging.py`, and no imports from `kline_fetcher` or other I/O modules (see `<review_carryover>`)
    Add `backend_api_python/tests/test_data_sufficiency_validator.py` with explicit scenario coverage and full-file suites. Reuse repo pytest + `unittest.mock` patterns only.
  </action>
  <use_cases>
    - `available_bars_equals_required_bars`: input `required_bars=100`, `available_bars=100`, `schedule_status="schedule_known_open"` -> expected `sufficient=True`, `reason_code="sufficient"`; validate in `backend_api_python/tests/test_data_sufficiency_validator.py`
    - `missing_bars`: input `required_bars=100`, `available_bars=80`, `schedule_status="schedule_known_open"` -> expected `sufficient=False`, `reason_code="missing_bars"`, `missing_window_seconds > 0.0` and copied `DataSufficiencyResult.missing_window == missing_window_seconds`; validate in `backend_api_python/tests/test_data_sufficiency_validator.py`
    - `unknown_schedule`: input `schedule_status="schedule_unknown"` regardless of bar count -> expected `sufficient=False`, `reason_code="unknown_schedule"`; validate in `backend_api_python/tests/test_data_sufficiency_validator.py`
    - `market_closed_gap`: input `schedule_status="schedule_known_closed"` with session-known-but-closed gap -> expected `sufficient=False`, `reason_code="market_closed_gap"`; validate in `backend_api_python/tests/test_data_sufficiency_validator.py`
    - `stale_prev_close`: input freshness metadata marks previous close stale -> expected `sufficient=False`, `reason_code="stale_prev_close"`; validate in `backend_api_python/tests/test_data_sufficiency_validator.py`
    - `cross_day_session`: input cross-day known session metadata and sufficient bars -> expected deterministic non-`unknown_schedule` classification; validate in `backend_api_python/tests/test_data_sufficiency_validator.py`
    - `precedence_unknown_schedule_over_missing_bars`: input `schedule_status="schedule_unknown"` with `required_bars=100` and `available_bars=50` -> expected `reason_code="unknown_schedule"` (NOT `missing_bars`); validate in `backend_api_python/tests/test_data_sufficiency_validator.py`
    - `aggregation_1h_from_5m_mocked_get_kline`: mock `get_kline` to return 600 5m bars and verify `compute_available_bars_from_kline_fetcher(..., timeframe="1H", required_bars=10, ...)` returns a deterministic integer bar count consistent with the mocked series length; validate in `backend_api_python/tests/test_data_sufficiency_validator.py` with a docstring documenting assumed `LOWER_LEVELS` behavior and the limitation vs real `get_kline`
    - `missing_window_zero_when_sufficient`: when `sufficient=True`, assert `missing_window == 0.0` with float epsilon; validate in `backend_api_python/tests/test_data_sufficiency_validator.py`
    - `effective_lookback_seconds_boundary`: assert `effective_lookback` matches `TIMEFRAME_SECONDS_MAP` (from Plan 01) × `required_bars` for at least one timeframe with epsilon; validate in `backend_api_python/tests/test_data_sufficiency_validator.py`
    - `get_kline_raises_documented_behavior`: mock `get_kline_callable` to raise; assert the documented propagation/surface behavior (no silent zero-bar fallback); validate in `backend_api_python/tests/test_data_sufficiency_validator.py`
  </use_cases>
  <verify>
    <automated>python3 -m pytest backend_api_python/tests/test_data_sufficiency_validator.py -q</automated>
    <automated>python3 -m pytest backend_api_python/tests/test_data_sufficiency_validator.py backend_api_python/tests/test_ibkr_schedule_provider.py backend_api_python/tests/test_data_sufficiency_types.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend_api_python/app/services/data_sufficiency_validator.py` contains exact strings `effective_lookback`, `missing_window`, and `DataSufficiencyResult`
    - the validator file contains exact strings `compute_available_bars_from_kline_fetcher` and `effective_lookback_seconds` and `missing_window_seconds`
    - the validator file contains exact reason-code literals or enum members for `unknown_schedule`, `market_closed_gap`, `stale_prev_close`, `missing_bars`, and `sufficient`
    - `backend_api_python/tests/test_data_sufficiency_validator.py` MUST include these exact test function names: `def test_available_bars_equals_required_bars():`, `def test_missing_bars():`, `def test_unknown_schedule():`, `def test_market_closed_gap():`, `def test_stale_prev_close():`, `def test_precedence_unknown_schedule_over_missing_bars():`, `def test_aggregation_1h_from_5m_mocked_get_kline():`, `def test_missing_window_zero_when_sufficient():`, `def test_effective_lookback_seconds_boundary():`, `def test_get_kline_raises_documented_behavior():`
    - `backend_api_python/app/services/data_sufficiency_validator.py` MUST NOT import `logging` and MUST NOT contain the substring `ibkr_data_sufficiency_check`
    - no file in this plan modifies `backend_api_python/app/services/signal_executor.py`
  </acceptance_criteria>
  <done>Phase 1 has a pure validator that can deterministically classify sufficiency without embedding enforcement logic or broker-fetching behavior; execution-path allow/block and alert dispatch remain explicitly deferred to later phases.</done>
</task>

<task type="auto">
  <name>Task 2: Add structured sufficiency logging with stable payload fields</name>
  <files>backend_api_python/app/services/data_sufficiency_logging.py, backend_api_python/tests/test_data_sufficiency_logging.py</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/01-ibkr-schedule-sufficiency-domain-model/01-CONTEXT.md
    .planning/phases/01-ibkr-schedule-sufficiency-domain-model/01-RESEARCH.md
    backend_api_python/app/services/data_sufficiency_types.py
    backend_api_python/app/utils/logger.py
  </read_first>
  <action>
    Create `backend_api_python/app/services/data_sufficiency_logging.py` to assemble and emit structured `logging` payloads for the event name `ibkr_data_sufficiency_check` required by N3. Keep the helper Phase-1-scoped:
    - it should emit exact fields `event`, `symbol`, `timeframe`, `market_category`, `sufficient`, `reason_code`, `required_bars`, `available_bars`, `effective_lookback`, `missing_window`, and `schedule_status`; when `DataSufficiencyDiagnostics.con_id` is present on the `DataSufficiencyResult`, also emit optional bounded field `con_id` at the top level of the payload for operator correlation
    - it must not emit raw `liquidHours`, raw broker `ContractDetails`, or oversized diagnostics dumps
    - it may include a sanitized diagnostics subset only if the keys are stable and bounded
    Implement `build_ibkr_data_sufficiency_check_payload(result: DataSufficiencyResult) -> dict[str, object]` (pure) and `emit_ibkr_data_sufficiency_check(payload: dict[str, object], logger=None) -> None` (side effect) where `emit_*` performs the actual `logger.info(..., extra=payload)` call using `backend_api_python/app/utils/logger.py` conventions.
    Add `backend_api_python/tests/test_data_sufficiency_logging.py` to assert exact event name, exact reason code propagation, explicit absence of raw schedule payload leakage, and that `emit_ibkr_data_sufficiency_check` uses the logger exactly once per call (use `unittest.mock` on the logger object).
  </action>
  <use_cases>
    - `sufficient_log_payload`: input result with `reason_code="sufficient"` -> expected event `ibkr_data_sufficiency_check` and exact top-level counts in payload; validate in `backend_api_python/tests/test_data_sufficiency_logging.py`
    - `unknown_schedule_log_payload`: input result with `reason_code="unknown_schedule"` -> expected payload keeps exact reason code and `schedule_status="schedule_unknown"`; validate in `backend_api_python/tests/test_data_sufficiency_logging.py`
    - `no_raw_broker_payload`: input diagnostics include mock `liquidHours` or broker object -> expected log payload excludes those raw fields; validate in `backend_api_python/tests/test_data_sufficiency_logging.py`
    - `market_closed_gap_log_payload`: known closed session result -> expected payload includes `reason_code="market_closed_gap"` without human-only prose substitution; validate in `backend_api_python/tests/test_data_sufficiency_logging.py`
    - `emit_once_per_call`: calling `emit_ibkr_data_sufficiency_check` with a built payload triggers exactly one `info`/`log` call on the mocked logger; validate in `backend_api_python/tests/test_data_sufficiency_logging.py`
  </use_cases>
  <verify>
    <automated>python3 -m pytest backend_api_python/tests/test_data_sufficiency_logging.py -q</automated>
    <automated>python3 -m pytest backend_api_python/tests/test_data_sufficiency_logging.py backend_api_python/tests/test_data_sufficiency_types.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend_api_python/app/services/data_sufficiency_logging.py` contains exact string `ibkr_data_sufficiency_check`
    - the logging helper contains exact fields `reason_code`, `required_bars`, `available_bars`, `effective_lookback`, `missing_window`, and `schedule_status`
    - the logging helper does not contain raw-field output names `liquidHours` or `contract_details` in the emitted payload contract
    - `backend_api_python/tests/test_data_sufficiency_logging.py` MUST include these exact test function names: `def test_sufficient_log_payload():`, `def test_unknown_schedule_log_payload():`, `def test_no_raw_broker_payload():`, `def test_market_closed_gap_log_payload():`, `def test_emit_once_per_call():`
  </acceptance_criteria>
  <done>Phase 1 exposes a stable observability payload for sufficiency decisions without weakening downstream automation through ambiguous or noisy logs.</done>
</task>

<task type="auto">
  <name>Task 3: Add orchestration service + expand integration tests + run blocking phase regression gate</name>
  <files>backend_api_python/app/services/data_sufficiency_service.py, backend_api_python/tests/test_data_sufficiency_service.py, backend_api_python/tests/test_data_sufficiency_integration.py, .planning/phases/01-ibkr-schedule-sufficiency-domain-model/01-02-PLAN.md</files>
  <read_first>
    .planning/phases/01-ibkr-schedule-sufficiency-domain-model/01-VALIDATION.md
    backend_api_python/app/services/data_sufficiency_validator.py
    backend_api_python/app/services/data_sufficiency_logging.py
    backend_api_python/tests/test_data_sufficiency_validator.py
    backend_api_python/tests/test_data_sufficiency_logging.py
    backend_api_python/tests/test_data_sufficiency_service.py
    backend_api_python/tests/test_ibkr_schedule_provider.py
    backend_api_python/tests/test_data_sufficiency_types.py
  </read_first>
  <action>
    Create `backend_api_python/app/services/data_sufficiency_service.py` with a public function such as `evaluate_ibkr_data_sufficiency_and_log(...)` that:
    - builds/accepts an `IBKRScheduleSnapshot` (from Plan 01 adapter) plus `required_bars` and mocked `get_kline_callable`
    - calls `compute_available_bars_from_kline_fetcher(...)` then the pure classifier in `data_sufficiency_validator.py`
    - calls `build_ibkr_data_sufficiency_check_payload(...)` then `emit_ibkr_data_sufficiency_check(...)` so N3 emission happens in orchestration, not inside the validator
    - documents in the module / entrypoint docstring that **Phase 1** leaves exceptions from the adapter or `get_kline_callable` **uncaught** (no silent zero-bar fallback); if bar counting raises, **no** `ibkr_data_sufficiency_check` log is emitted for that invocation because classification is not reached
    Add `backend_api_python/tests/test_data_sufficiency_service.py` with focused unit tests that mock `get_kline`, the adapter function, and the logger to assert import-time wiring and single-emission invariants without requiring a full IBKR contract string matrix.
    Expand `backend_api_python/tests/test_data_sufficiency_integration.py` with an end-to-end test that exercises: `ContractDetails` → adapter → service → mocked logger emission, asserting the emitted payload includes `event="ibkr_data_sufficiency_check"` and the expected `reason_code`. The `test_adapter_to_service_emits_ibkr_data_sufficiency_check` path MUST use a `get_kline` mock whose return shape and bar list plausibly reflect `LOWER_LEVELS` aggregation (not only a flat arbitrary list), and the test file docstring MUST call out that production drift against real `kline_fetcher.get_kline` is additionally gated in Phase 2 (see ROADMAP).
    Run the full Phase 1 use-case suites for validator, logging, adapter, types, integration, and orchestration together, then execute the required blocking backend regression command `python3 -m pytest backend_api_python/tests -q`. If any failure appears in reason precedence, boundary equality, kline-fetcher alignment, or structured payload shape, stop instead of partially advancing the phase.
  </action>
  <use_cases>
    - `phase1_reason_matrix`: validator + logging + adapter + type suites all green together; validate with the combined suite command in `<verify>`
    - `phase_regression_gate`: entire backend test suite remains green after all Phase 1 files exist; validate with the full-suite command in `<verify>`
  </use_cases>
  <verify>
    <automated>python3 -m pytest backend_api_python/tests/test_data_sufficiency_validator.py backend_api_python/tests/test_data_sufficiency_logging.py backend_api_python/tests/test_data_sufficiency_service.py backend_api_python/tests/test_ibkr_schedule_provider.py backend_api_python/tests/test_data_sufficiency_types.py backend_api_python/tests/test_data_sufficiency_integration.py -q</automated>
    <automated>python3 -m pytest backend_api_python/tests -q</automated>
  </verify>
  <acceptance_criteria>
    - combined Phase 1 suites exit with code 0
    - `python3 -m pytest backend_api_python/tests -q` exits with code 0
    - `backend_api_python/tests/test_data_sufficiency_integration.py` MUST include `def test_adapter_to_service_emits_ibkr_data_sufficiency_check():`
    - `backend_api_python/tests/test_data_sufficiency_service.py` MUST include `def test_evaluate_entrypoint_calls_emit_once():`
    - `backend_api_python/app/services/data_sufficiency_service.py` MUST import both `data_sufficiency_validator` and `data_sufficiency_logging` and MUST call `emit_ibkr_data_sufficiency_check` exactly once per public evaluation entrypoint under normal success paths (asserted in integration test via mocks)
  </acceptance_criteria>
  <done>Plan 02 is complete only when deterministic classification, structured logging, and the full backend suite all pass together.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| normalized schedule state -> validator | Incorrect precedence here could cause wrong sufficiency output for later risk guards |
| validator result -> structured log payload | Ambiguous or noisy payloads would weaken downstream alerts, audits, and operator trust |
| validator (pure) -> orchestration service | Side effects must not leak into the validator; orchestration owns emission and call ordering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | T | `data_sufficiency_validator.py` | mitigate | Encode deterministic precedence with explicit tests for `unknown_schedule`, `market_closed_gap`, `stale_prev_close`, and `missing_bars` |
| T-01-05 | R | `data_sufficiency_logging.py` | mitigate | Emit exact event name and exact reason-code field so downstream automation can correlate decisions reliably |
| T-01-06 | I | `data_sufficiency_logging.py` | mitigate | Assert logs exclude raw broker schedule blobs and only include bounded sanitized metadata |
| T-01-07 | E | `data_sufficiency_service.py` | mitigate | Centralize log emission so pure validator remains testable without logger mocks; integration tests assert single emission per evaluation |
</threat_model>

<verification>
- `python3 -m pytest backend_api_python/tests/test_data_sufficiency_validator.py backend_api_python/tests/test_data_sufficiency_logging.py backend_api_python/tests/test_data_sufficiency_service.py backend_api_python/tests/test_ibkr_schedule_provider.py backend_api_python/tests/test_data_sufficiency_types.py backend_api_python/tests/test_data_sufficiency_integration.py -q`
- `python3 -m pytest backend_api_python/tests -q`
</verification>

<success_criteria>
- Plan 02 produces deterministic, typed sufficiency results for the required Phase 1 reason codes.
- Plan 02 preserves bars-first threshold semantics with no tolerance window.
- Plan 02 emits `ibkr_data_sufficiency_check` logs with stable fields and no raw broker payload leakage.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ibkr-schedule-sufficiency-domain-model/01-02-SUMMARY.md`
</output>
