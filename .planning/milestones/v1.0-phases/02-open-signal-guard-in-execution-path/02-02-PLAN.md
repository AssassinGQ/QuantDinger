---
phase: 02-open-signal-guard-in-execution-path
plan: 02
type: execute
wave: 2
revision: 2
revision_notes: "Replanned from 02-REVIEWS.md + 02-RESEARCH.md (façade vs Phase 1 raises, execute_batch exchange, joint gate pitfalls); expanded per-task test_spec; verify second line = full backend suite."
depends_on:
  - .planning/phases/02-open-signal-guard-in-execution-path/02-01-PLAN.md
files_modified:
  - backend_api_python/app/services/data_sufficiency_guard.py
  - backend_api_python/app/services/kline_fetcher.py
  - backend_api_python/app/services/signal_executor.py
  - backend_api_python/app/strategies/runners/cross_sectional_runner.py
  - backend_api_python/tests/test_signal_executor.py
  - backend_api_python/tests/test_ibkr_open_guard_execution.py
autonomous: true
requirements:
  - R3
  - N2
  - N3
  - N4
must_haves:
  truths:
    - "IBKR live open/add signals are blocked when sufficiency is false or cannot be evaluated safely; close/reduce never blocked by this gate."
    - "Guard runs in `SignalExecutor.execute` only for `_execution_mode==live`, `exchange_id in {ibkr-paper, ibkr-live}`, and risk-increasing open/add intent (including `target_weight` reclassification per R-05)."
    - "Ordering is state machine → IBKR sufficiency gate (when applicable) → `_check_ai_filter` → sizing/normalizer → enqueue (D-07 / user 2B)."
    - "At least one execution-path test binds the guard’s kline seam to stubbed `get_kline` behavior including `LOWER_LEVELS` fallback (D-12 / R-08)."
  artifacts:
    - path: "backend_api_python/app/services/data_sufficiency_guard.py"
      provides: "Façade: evaluate + exception→synthetic insufficient + bounded diagnostics; optional contract resolution hooks"
    - path: "backend_api_python/app/services/signal_executor.py"
      provides: "Single choke-point guard + intent classification"
    - path: "backend_api_python/tests/test_ibkr_open_guard_execution.py"
      provides: "Integration tests: allow/block matrix, reduce-path negative, get_kline alignment"
  key_links:
    - from: "backend_api_python/app/services/signal_executor.py"
      to: "backend_api_python/app/services/data_sufficiency_guard.py"
      via: "imports guard façade only (thin coupling)"
      pattern: "evaluate.*open.*guard|data_sufficiency_guard"
---

<objective>
Implement the Phase 2 IBKR open/add data-sufficiency gate at the single integration point `SignalExecutor.execute`, with fail-safe behavior on evaluation exceptions (D-01–D-04), dual structured logs per D-09, explicit `target_weight`/intent handling (R-05), and execution-path tests — including threading `exchange` into `execute_batch` so multi-symbol runners can supply contract context (ROADMAP carryover / R-06).
Depends on Plan `02-01` for types + blocked-open payload builders.
</objective>

<review_carryover source="02-REVIEWS.md consensus + 02-CONTEXT.md R-01–R-09">
- **R-01:** Execution-path mapping follows **02-CONTEXT D-01–D-04**; Phase 1 `data_sufficiency_service.py` remains library-level propagation — façade in `data_sufficiency_guard.py` catches.
- **R-02:** Tests MUST NOT assert `ibkr_insufficient_data_alert_sent` in Phase 2 (Phase 3).
- **R-03:** Blocked-open payload uses stable keys from Plan 02-01 builder; executor passes strategy/symbol/exchange_id/effective_intent.
- **R-04:** Bounded diagnostics already in types; façade must not put stack traces in structured extra.
- **R-05:** `target_weight` effective intent mirrors `_calculate_target_weight_amount` branches (document helper name in implementation).
- **R-06:** **Both** `_execution_mode == live` **and** `exchange_id in {ibkr-paper, ibkr-live}` — tests cover non-live + IBKR id (skip guard) and live + non-IBKR (skip guard).
- **R-07:** Reduce path never invokes sufficiency evaluator (call-count / patch).
- **R-08 / D-12:** LOWER_LEVELS kline seam test is blocking exit work.
- **R-09:** Document in module comment only: per-signal duplicate fetch under `execute_batch` acceptable for Phase 2 (no cross-signal cache).
</review_carryover>

<research_carryover source=".planning/phases/02-open-signal-guard-in-execution-path/02-RESEARCH.md">
- **Pattern:** Thin `data_sufficiency_guard.py` wraps `evaluate_ibkr_data_sufficiency_and_log`; executor never uses Phase 1 “raise through” behavior for open/add policy.
- **Pattern:** Reorder `execute()` so sufficiency runs **before** `_check_ai_filter` (skip AI when already insufficient).
- **Pattern:** Thread `exchange` into `execute_batch` + `CrossSectionalRunner` so contract resolution is possible for multi-symbol IBKR.
- **Don’t hand-roll:** Delegate bar counting to existing `get_kline` + `compute_available_bars_from_kline_fetcher` via wrapper only.
- **Pitfalls:** `exchange=None` batch path must be covered by forwarding tests; distinguish `exchange_id` from account paper/live naming.
</research_carryover>

<context>
@.planning/phases/02-open-signal-guard-in-execution-path/02-CONTEXT.md
@.planning/phases/02-open-signal-guard-in-execution-path/02-REVIEWS.md
@.planning/phases/02-open-signal-guard-in-execution-path/02-RESEARCH.md
@backend_api_python/app/services/data_sufficiency_service.py
@backend_api_python/app/services/data_sufficiency_guard.py
@backend_api_python/app/services/signal_executor.py
@backend_api_python/app/services/kline_fetcher.py
@backend_api_python/app/strategies/runners/cross_sectional_runner.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard façade — evaluate, exception translation, kline seam</name>
  <files>backend_api_python/app/services/data_sufficiency_guard.py, backend_api_python/app/services/kline_fetcher.py (imports only if needed)</files>
  <read_first>
    .planning/phases/02-open-signal-guard-in-execution-path/02-CONTEXT.md
    backend_api_python/app/services/data_sufficiency_service.py
    backend_api_python/app/services/data_sufficiency_types.py
    backend_api_python/app/services/data_sufficiency_logging.py
    backend_api_python/app/services/kline_fetcher.py
    backend_api_python/tests/test_data_sufficiency_integration.py
  </read_first>
  <action>
    Create `backend_api_python/app/services/data_sufficiency_guard.py` exporting at minimum:
    - `class EvaluationPathError(Exception): ...` optional thin wrapper if useful for categorization.
    - `evaluate_ibkr_open_data_sufficiency(...)` — **sync** API used by `SignalExecutor`:
      - Inputs: `contract_details`, `server_time_utc`, `symbol`, `timeframe`, `market_category`, `required_bars`, `before_time_utc`, `con_id`, optional `logger`.
      - Builds `get_kline_callable` as a **thin wrapper** around `kline_fetcher.get_kline` with signature compatible with `compute_available_bars_from_kline_fetcher` (delegate to the real `get_kline` — no business-logic fork).
      - Calls `evaluate_ibkr_data_sufficiency_and_log(...)`.
      - On **any** exception from the orchestration path: catch, log **once** at translation site with `logger.exception` or `exc_info=True` **only** here (not in structured payload), build synthetic `DataSufficiencyResult` with `sufficient=False`, `reason_code=DataSufficiencyReasonCode.DATA_EVALUATION_FAILED`, populate `evaluation_error_summary` via `truncate_evaluation_error_summary(str(exc), max_chars=200)` and `evaluation_error_category` inferred (`schedule` vs `kline` vs `unknown`) from exception type/message heuristics documented in docstring.
      - Return `DataSufficiencyResult` in both paths (never leak raw exception to caller).
    - `contract_details_missing_fail_closed(...)` helper returning a minimal synthetic `DataSufficiencyResult` when `exchange is None` or contract cannot be resolved — **must** fail closed for open/add (insufficient), with `reason_code=DATA_EVALUATION_FAILED` (or document single alternate stable code if team chooses — default per research: same as evaluation failure for audit consistency).
    Document explicitly in module docstring: **Execution path overrides Phase 1 library propagation** per `02-CONTEXT.md`.
  </action>
  <test_spec>
    | Case ID | Scenario | Expected |
    |---------|----------|----------|
    | TS-20 | Mock `evaluate_ibkr_data_sufficiency_and_log` raises `RuntimeError("kline boom")` | Returned result has `reason_code==data_evaluation_failed`, summary bounded, category `kline` or `unknown` |
    | TS-21 | Successful evaluation returns insufficient | Pass-through `DataSufficiencyResult`; `ibkr_data_sufficiency_check` emitted inside service |
    | TS-22 | Wrapper forwards to real `get_kline` name | Patch `kline_fetcher.get_kline` and assert called with `(market_category, symbol, timeframe, ...)` consistent with integration test pattern |
    | TS-23 | `contract_details_missing_fail_closed(None)` / unresolved contract | `sufficient is False`; suitable for blocking open/add; documented reason code |
    | TS-24 | Exception translation logging | Only **one** translation site uses `exc_info` / `logger.exception` — structured payload keys do not contain traceback text |
  </test_spec>
  <verify>
    <automated>python3 -m pytest backend_api_python/tests/test_ibkr_open_guard_execution.py -q</automated>
    <automated>python3 -m pytest backend_api_python/tests -q</automated>
    <note>第二条为全量回归（整个 `backend_api_python/tests`）；每个任务提交前必须 exit code 0。</note>
  </verify>
  <acceptance_criteria>
    - File `backend_api_python/app/services/data_sufficiency_guard.py` exists and imports `evaluate_ibkr_data_sufficiency_and_log`
    - Docstring contains literal phrase `02-CONTEXT.md` or `D-01`
    - `evaluate_ibkr_open_data_sufficiency` catches exceptions from `evaluate_ibkr_data_sufficiency_and_log` and returns synthetic insufficient result with `DATA_EVALUATION_FAILED`
    - Unit tests live under `backend_api_python/tests/test_ibkr_open_guard_execution.py` with `def test_guard_façade_maps_exception_to_data_evaluation_failed():` and `def test_contract_missing_fail_closed_is_insufficient():`
    - `python3 -m pytest backend_api_python/tests -q` exits 0
  </acceptance_criteria>
  <done>Guard façade isolates Phase 2 exception policy from Phase 1 library semantics.</done>
</task>

<task type="auto">
  <name>Task 2: SignalExecutor — intent classification, gate placement, logs</name>
  <files>backend_api_python/app/services/signal_executor.py, backend_api_python/tests/test_signal_executor.py</files>
  <read_first>
    .planning/phases/02-open-signal-guard-in-execution-path/02-CONTEXT.md
    backend_api_python/app/services/signal_executor.py
    backend_api_python/app/services/data_sufficiency_guard.py
    backend_api_python/app/services/data_sufficiency_logging.py
    backend_api_python/tests/test_signal_executor.py
  </read_first>
  <action>
    Refactor `execute()` ordering to match **D-07 / user 2B**:
    1. Existing state machine checks (`is_signal_allowed`, spot short rejection) remain first.
    2. **Before** `_check_ai_filter`, insert IBKR sufficiency gate when **all** hold:
       - `str(strategy_ctx.get("_execution_mode") or "").strip().lower() == "live"`
       - `exchange_id = (strategy_ctx.get("exchange_config") or {}).get("exchange_id","")` satisfies `exchange_id in ("ibkr-paper","ibkr-live")` (R-06 — must not use account naming fields).
       - Intent is risk-increasing open/add per **R-05**:
         - If `signal.get("target_weight") is None`: treat as open/add when normalized `signal["type"]` contains `open_` or `add_` (case-insensitive) and **not** `reduce_` / `close`.
         - If `target_weight` is present: mirror the **branch structure** of `_calculate_target_weight_amount` (same comparisons of `target_amount` vs `old_size`) to classify effective intent as `open_*`, `add_*`, `reduce_*`, `close_*`, or hold — gate runs only for `open_*` and `add_*`.
    3. Implement sufficiency using `evaluate_ibkr_open_data_sufficiency` with:
       - `contract_details` resolved from `exchange` + `symbol` via a dedicated helper (extract to private method `_resolve_ibkr_contract_details_for_symbol(exchange, symbol)`; return `None` if unavailable — then use fail-closed synthetic result from guard helpers).
       - `server_time_utc`: `datetime.datetime.now(datetime.timezone.utc)` unless tests inject clock — prefer optional strategy hook only if needed; **tests must patch datetime**.
       - `timeframe`: `(strategy_ctx.get("trading_config") or {}).get("timeframe","1H")`.
       - `market_category`: `strategy_ctx.get("_market_category","Crypto")`.
       - `required_bars`: `int((strategy_ctx.get("trading_config") or {}).get("ibkr_data_required_bars", (strategy_ctx.get("trading_config") or {}).get("required_bars", 100)))` — document keys in docstring (defaults OK).
       - `before_time_utc`: derive from signal timestamp ms if present else `None`.
       - `con_id`: integer from resolved contract if available else `0`.
    When result `sufficient` is False:
      - Call `emit_ibkr_open_blocked_insufficient_data(build_ibkr_open_blocked_insufficient_data_payload(...))` with `synthetic_evaluation_failure=True` iff `reason_code == DataSufficiencyReasonCode.DATA_EVALUATION_FAILED` (including `contract_details` missing / fail-closed path when modeled as that reason — otherwise document explicit reason for missing-contract case).
      - Return False **before** AI filter and before enqueue.
    When result `sufficient` is True: proceed to `_check_ai_filter` then existing sizing path unchanged.
    **Bypass** gate entirely for reduce/close/scale-down signal paths (D-08).
    Do not add `persist_notification` for sufficiency.
  </action>
  <test_spec>
    | Case ID | Scenario | Expected |
    |---------|----------|----------|
    | TS-30 | `ibkr-paper`, live, `open_long`, sufficient=True | `execute_exchange_order` called |
    | TS-31 | same, sufficient=False | blocked log + no `execute_exchange_order` |
    | TS-32 | `reduce_long`, insufficient | Order path still called (mock returns success) |
    | TS-33 | `target_weight` leads to `add_long` with insufficient data | Blocked (guard uses effective intent) |
    | TS-34 | `exchange_id=binance` | No sufficiency evaluator invocation (patch guard to detect) |
    | TS-35 | Exception → synthetic fail | `ibkr_open_blocked` emitted; no unhandled exception |
    | TS-36 | `_execution_mode=signal` (non-live), `exchange_id=ibkr-paper`, `open_long` | Guard **skipped** — sufficiency evaluator not invoked (R-06 joint gate) |
    | TS-37 | `live` + `exchange_id=binance` | Guard skipped (already TS-34; keep both naming styles in tests) |
    | TS-38 | `ibkr-paper` vs `ibkr-live` | At least one test each OR parameterized — both ids must hit same guard path when live |
    | TS-39 | Blocked insufficient (non-exception) | `synthetic_evaluation_failure=False`; `reason_code` from classifier (e.g. `missing_bars`) |
  </test_spec>
  <verify>
    <automated>python3 -m pytest backend_api_python/tests/test_signal_executor.py -q</automated>
    <automated>python3 -m pytest backend_api_python/tests -q</automated>
    <note>第二条为全量回归（整个 `backend_api_python/tests`）；每个任务提交前必须 exit code 0。</note>
  </verify>
  <acceptance_criteria>
    - `signal_executor.py` calls `_check_ai_filter` **after** the new sufficiency block (grep order: sufficiency-related symbol appears above `_check_ai_filter` call in `execute`)
    - New tests include at minimum: `def test_ibkr_live_open_blocked_when_insufficient():`, `def test_reduce_not_blocked_by_sufficiency():`, `def test_non_ibkr_exchange_skips_sufficiency_guard():`, `def test_joint_gate_skips_guard_when_not_live():` in `test_signal_executor.py` and/or `test_ibkr_open_guard_execution.py`
    - `python3 -m pytest backend_api_python/tests -q` exits 0
  </acceptance_criteria>
  <done>Single choke-point gate matches locked ordering and IBKR routing invariants.</done>
</task>

<task type="auto">
  <name>Task 3: Thread IBKR `exchange` through `execute_batch` + cross-sectional runner</name>
  <files>backend_api_python/app/services/signal_executor.py, backend_api_python/app/strategies/runners/cross_sectional_runner.py</files>
  <read_first>
    backend_api_python/app/services/signal_executor.py
    backend_api_python/app/strategies/runners/cross_sectional_runner.py
    backend_api_python/app/strategies/runners/base_runner.py
  </read_first>
  <action>
    Add optional parameter `exchange: Any = None` to `SignalExecutor.execute_batch`.
    Pass `exchange=exchange` into each inner `self.execute(..., exchange=exchange)` call (replace hard-coded `None`).
    Update `CrossSectionalRunner._dispatch_signals` to pass `exchange` through to `execute_batch` — store `exchange` on the runner as `self._live_exchange = exchange` set at start of `run()`, or pass via `_dispatch_signals` signature from `run` — prefer explicit parameter threading from `run()` → `_run_single_tick` → `_dispatch_signals` → `execute_batch` without fragile hidden globals.
  </action>
  <test_spec>
    | Case ID | Scenario | Expected |
    |---------|----------|----------|
    | TS-40 | `execute_batch(..., exchange=mock_ex)` | Inner `execute` receives same mock on `exchange` kw |
    | TS-41 | Cross-sectional dispatch | Runner forwards exchange object |
    | TS-42 | Regression vs single-symbol | `SingleSymbolRunner` path unchanged except shared `execute_batch` signature compatibility — existing callers still work with default `exchange=None` |
  </test_spec>
  <verify>
    <automated>python3 -m pytest backend_api_python/tests/test_signal_executor.py backend_api_python/tests/test_ibkr_open_guard_execution.py -q</automated>
    <automated>python3 -m pytest backend_api_python/tests -q</automated>
    <note>第二条为全量回归（整个 `backend_api_python/tests`）；每个任务提交前必须 exit code 0。</note>
  </verify>
  <acceptance_criteria>
    - `execute_batch` signature includes `exchange` default `None`
    - `cross_sectional_runner.py` passes exchange into `execute_batch`
    - Test `def test_execute_batch_forwards_exchange_to_execute():` exists proving forward (can mock `SignalExecutor.execute` via patch)
    - `python3 -m pytest backend_api_python/tests -q` exits 0
  </acceptance_criteria>
  <done>Batch IBKR strategies can resolve contracts for sufficiency same as single-symbol path.</done>
</task>

<task type="auto">
  <name>Task 4: Execution-path kline alignment + guard invocation matrix (D-12 / R-07 / R-08)</name>
  <files>backend_api_python/tests/test_ibkr_open_guard_execution.py</files>
  <read_first>
    .planning/ROADMAP.md
    backend_api_python/tests/test_data_sufficiency_integration.py
    backend_api_python/app/services/data_sufficiency_validator.py
    backend_api_python/app/services/kline_fetcher.py
  </read_first>
  <action>
    Extend `backend_api_python/tests/test_ibkr_open_guard_execution.py` with:
    1. **`test_execution_path_get_kline_lower_levels_alignment`**: Patch `kline_fetcher.get_kline` with a stub whose `LOWER_LEVELS` behavior matches Phase 1 integration test (`1H` empty → `5m` seed bars). Drive **one** call path that hits `compute_available_bars_from_kline_fetcher` through `evaluate_ibkr_open_data_sufficiency` (not bypassing kline).
    2. **`test_reduce_path_does_not_invoke_sufficiency_evaluator`**: Patch `evaluate_ibkr_data_sufficiency_and_log` or guard entry so that **call count == 0** when executing a reduce signal under ibkr-live (R-07).
    3. Document in file header comment that Phase 3 owns `ibkr_insufficient_data_alert_sent`.
    Ensure tests use IBKR strategy contexts consistent with production keys (`_execution_mode`, `exchange_config.exchange_id`, `_market_category`).
  </action>
  <test_spec>
    | Case ID | Scenario | Expected |
    |---------|----------|----------|
    | TS-50 | LOWER_LEVELS stub | `get_kline` invoked for fallback timeframe at least once |
    | TS-51 | reduce live IBKR | Evaluator mock call_count == 0 |
    | TS-52 | Assertions grep | Phase 2 test modules do not reference `ibkr_insufficient_data_alert_sent` (optional grep guard per R-02) |
  </test_spec>
  <verify>
    <automated>python3 -m pytest backend_api_python/tests/test_ibkr_open_guard_execution.py -q</automated>
    <automated>python3 -m pytest backend_api_python/tests -q</automated>
    <note>第二条为全量回归（整个 `backend_api_python/tests`）；每个任务提交前必须 exit code 0。</note>
  </verify>
  <acceptance_criteria>
    - File contains `def test_execution_path_get_kline_lower_levels_alignment():` and `def test_reduce_path_does_not_invoke_sufficiency_evaluator():`
    - Full suite green: `python3 -m pytest backend_api_python/tests -q` exit code 0
  </acceptance_criteria>
  <done>Exit criteria D-12 + review R-07/R-08 satisfied with automated proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Strategy config (`exchange_id`, `_execution_mode`) | Untrusted input — gate must use exact equality on allowlist |
| External IBKR / kline services | May raise; guard must fail closed for opens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | E | `SignalExecutor.execute` | mitigate | IBKR allowlist + live mode; block open/add only |
| T-02-11 | R | Synthetic diagnostics | mitigate | Truncate + category; no stack traces in structured payloads (D-03) |
</threat_model>

<verification>
- Scoped: `python3 -m pytest backend_api_python/tests/test_signal_executor.py backend_api_python/tests/test_ibkr_open_guard_execution.py -q`
- Full suite (全量): `python3 -m pytest backend_api_python/tests -q`
</verification>

<success_criteria>
- Open/add blocked on insufficiency; close/reduce unaffected; two structured log events only (D-09); no `ibkr_insufficient_data_alert_sent` in Phase 2 tests.
- Ordering: sufficiency before `_check_ai_filter`.
- ≥1 execution-path `get_kline` seam test with LOWER_LEVELS behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-open-signal-guard-in-execution-path/02-02-SUMMARY.md`
</output>
