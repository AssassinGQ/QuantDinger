---
phase: 08-quantity-normalization-ib-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/app/services/live_trading/order_normalizer/forex.py
  - backend_api_python/tests/test_order_normalizer.py
autonomous: true
requirements:
  - EXEC-04
user_setup: []
must_haves:
  truths:
    - "ForexNormalizer.normalize returns raw quantity unchanged (no math.floor)."
    - "ForexNormalizer.check still rejects non-positive qty and accepts small positive qty."
    - "Unit tests UC-N1–UC-N6 and legacy test_normalize expect 1000.7 passthrough."
  artifacts:
    - path: backend_api_python/app/services/live_trading/order_normalizer/forex.py
      provides: "normalize passthrough; signature -> float"
      contains: "def normalize"
    - path: backend_api_python/tests/test_order_normalizer.py
      provides: "TestForexNormalizer covers UC-N1–UC-N6"
      contains: "TestForexNormalizer"
  key_links:
    - from: "tests/test_order_normalizer.py"
      to: "ForexNormalizer.normalize"
      via: "direct calls on ForexNormalizer()"
      pattern: "normalize\\("
---

<objective>
Change `ForexNormalizer.normalize()` to pass through `raw_qty`, fix the return type annotation to `float`, remove unused `math` import, and lock behavior with UC-N1–UC-N6 tests plus update `test_normalize` from floor (1000→1000.7) to passthrough.

Purpose: Align EXEC-04 with IB-driven sizing (`_align_qty_to_contract`); avoid silent floor on fractional Forex sizes.
Output: Updated `forex.py` and extended `TestForexNormalizer`.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-quantity-normalization-ib-alignment/08-CONTEXT.md
@.planning/phases/08-quantity-normalization-ib-alignment/08-RESEARCH.md

<interfaces>
From `backend_api_python/app/services/live_trading/order_normalizer/__init__.py`:
```python
class OrderNormalizer(ABC):
    @abstractmethod
    def normalize(self, raw_qty: float, symbol: str) -> float: ...
    @abstractmethod
    def check(self, qty: float, symbol: str) -> Tuple[bool, str]: ...
```

Current `backend_api_python/app/services/live_trading/order_normalizer/forex.py` uses `math.floor` and `-> int`; Phase 8 replaces with `return raw_qty` and `-> float`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ForexNormalizer passthrough + UC-N1–UC-N6 tests</name>
  <read_first>
    - backend_api_python/app/services/live_trading/order_normalizer/forex.py
    - backend_api_python/app/services/live_trading/order_normalizer/__init__.py
    - backend_api_python/tests/test_order_normalizer.py (class TestForexNormalizer)
  </read_first>
  <files>backend_api_python/app/services/live_trading/order_normalizer/forex.py, backend_api_python/tests/test_order_normalizer.py</files>
  <action>
    **Production (minimal):** In `forex.py`, implement `normalize(self, raw_qty: float, symbol: str) -> float` as `return raw_qty`. Remove `import math` if no longer used. Do not add `normalize()` calls in `place_market_order` or elsewhere.

    **Update existing test:** `TestForexNormalizer.test_normalize`: change assertion from `1000.7 → 1000` to `1000.7 → 1000.7` (passthrough).

    **Add or extend tests** so each use case below is explicitly covered (one test per UC or parametrized table — executor choice):

    - **UC-N1** — Input: `normalize(20000.99, "EURUSD")`. Expected output: `20000.99`. Assertions: `out == 20000.99`. Also: `normalize(20000.99, "GBPJPY")` → `20000.99` (same numeric, second symbol per RESEARCH supplement).
    - **UC-N2** — Input: `normalize(0.5, "EURUSD")`. Expected: `0.5`. Assertions: `out == 0.5`.
    - **UC-N3** — Input: `normalize(0.001, "EURUSD")`. Expected: `0.001`. Assertions: `out == 0.001`.
    - **UC-N4** — Input: `normalize(1e9, "EURUSD")`. Expected: `1e9`. Assertions: `out == 1e9`.
    - **UC-N5** — Input: `check(-5, "EURUSD")`. Expected: `(False, msg)` with rejection. Assertions: `ok is False`; `"positive" in msg.lower()`; `"-5" in msg or "-5.0" in msg`.
    - **UC-N6** — Input: `check(0.001, "EURUSD")`. Expected: `(True, "")`. Assertions: `ok is True`; reason `== ""`.
  </action>
  <acceptance_criteria>
    - `grep -n "return raw_qty" backend_api_python/app/services/live_trading/order_normalizer/forex.py` returns a match inside `normalize`.
    - `grep "math\." backend_api_python/app/services/live_trading/order_normalizer/forex.py` has no matches (no floor).
    - `grep -n "def normalize" backend_api_python/app/services/live_trading/order_normalizer/forex.py` shows `-> float` in the same signature block.
    - `grep -n "1000.7" backend_api_python/tests/test_order_normalizer.py` shows an assertion expecting `1000.7` (not `1000`) for Forex normalize.
    - `grep -E "20000\.99|UC-N1|normalize\(20000" backend_api_python/tests/test_order_normalizer.py` finds coverage for UC-N1-scale behavior.
  </acceptance_criteria>
  <verify>
    <automated>
      cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest tests/test_order_normalizer.py -k "ForexNormalizer" -x -q --tb=line
    </automated>
    <automated>
      cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest tests/ -x -q --tb=line
    </automated>
  </verify>
  <done>ForexNormalizer passthrough and UC-N1–UC-N6 are implemented; targeted and full pytest suites pass (REGR-01).</done>
</task>

</tasks>

<verification>
- `pytest tests/test_order_normalizer.py -k "ForexNormalizer"` passes.
- Full `pytest tests/` passes (REGR-01).
</verification>

<success_criteria>
- `ForexNormalizer.normalize` is identity on float input; type annotation is `float`.
- All UC-N1–UC-N6 behaviors are asserted in tests; legacy floor expectation for `1000.7` is removed.
</success_criteria>

<output>
After completion, create `.planning/phases/08-quantity-normalization-ib-alignment/08-01-SUMMARY.md`
</output>
