---
phase: 12-frontend-ibkr-exchanges-for-forex
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - quantdinger_vue/src/views/trading-assistant/index.vue
  - quantdinger_vue/src/locales/lang/en-US.js
  - quantdinger_vue/src/locales/lang/zh-CN.js
  - quantdinger_vue/tests/unit/frnt-01-forex-ibkr-options.spec.js
autonomous: true
requirements:
  - FRNT-01
user_setup: []
must_haves:
  truths:
    - "When market_category is Forex, the live-trading broker select lists mt5, ibkr-paper, and ibkr-live"
    - "Forex + IBKR submits exchange_config as { exchange_id: 'ibkr-paper' | 'ibkr-live' } only (no mt5_* keys)"
    - "Forex + IBKR uses POST /api/ibkr/connect for Test connection; Forex + MT5 uses POST /api/mt5/connect"
    - "Edit load maps exchange_id mt5 vs ibkr-paper/ibkr-live onto forex_broker_id without forcing mt5"
  artifacts:
    - path: quantdinger_vue/src/views/trading-assistant/index.vue
      provides: "isForexMarket, isForexMT5, isForexIBKR; FOREX_BROKER_OPTIONS with three brokers; submit + test + backfill"
      contains: "isForexMarket"
    - path: quantdinger_vue/tests/unit/frnt-01-forex-ibkr-options.spec.js
      provides: "FRNT-01 regression guard on source shape"
      min_lines: 15
  key_links:
    - from: quantdinger_vue/src/views/trading-assistant/index.vue
      to: "/api/ibkr/connect"
      via: "handleTestConnection"
      pattern: "/api/ibkr/connect"
    - from: quantdinger_vue/src/views/trading-assistant/index.vue
      to: "exchange_config"
      via: "basePayload exchange_config branch"
      pattern: "exchange_config:"
---

<objective>
Extend the strategy create/edit wizard so that when `market_category` is **Forex**, the Forex broker dropdown offers **MetaTrader 5**, **IBKR Paper**, and **IBKR Live**; persisting **Forex + IBKR** uses the same `{ exchange_id: 'ibkr-paper' | 'ibkr-live' }` shape as stock IBKR; **Test connection** calls `POST /api/ibkr/connect` for Forex+IBKR and MT5 path only for Forex+MT5; remove automatic default to MT5; rename `isMT5Market` → `isForexMarket` with `isForexMT5` / `isForexIBKR` sub-checks per locked CONTEXT.

Purpose: Satisfy **FRNT-01** and mirror stock IBKR UX for Forex without backend changes (Phase 11 already validates pairs).

Output: Updated `index.vue`, `trading-assistant.brokerNames` keys for `ibkr-paper` / `ibkr-live`, one Jest guard spec, green `npm run test:unit` (pattern) and full `pytest tests/` backend regression.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-frontend-ibkr-exchanges-for-forex/12-CONTEXT.md
@.planning/phases/12-frontend-ibkr-exchanges-for-forex/12-RESEARCH.md
@.planning/REQUIREMENTS.md
@quantdinger_vue/src/api/strategy.js

<interfaces>
From `quantdinger_vue/src/views/trading-assistant/index.vue` (stock IBKR — reuse labels/values for Forex IBKR rows):

```javascript
const BROKER_OPTIONS = [
  { value: 'ibkr-paper', labelKey: 'ibkr-paper', name: 'IBKR Paper' },
  { value: 'ibkr-live', labelKey: 'ibkr-live', name: 'IBKR Live' }
]
```

Current Forex constant (extend, do not remove `mt5`):

```javascript
const FOREX_BROKER_OPTIONS = [
  { value: 'mt5', labelKey: 'mt5', name: 'MetaTrader 5' }
]
```

Locked payload shape for **Forex + IBKR** (identical to stock IBKR branch):

```javascript
{ exchange_id: 'ibkr-paper' } // or 'ibkr-live'; no other keys
```

IBKR test connection request body (existing stock path):

```javascript
await this.$http.post('/api/ibkr/connect', {
  broker_id: this.currentBrokerId || 'ibkr-paper'
})
```

`isIBKRBroker` (unchanged): `return this.currentBrokerId && this.currentBrokerId.startsWith('ibkr-')`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: trading-assistant index.vue — Forex IBKR behavior</name>
  <files>quantdinger_vue/src/views/trading-assistant/index.vue</files>
  <read_first>
    - quantdinger_vue/src/views/trading-assistant/index.vue (template ~1335–1545; constants ~1708–1725; computed ~1742–1908; handleWatchlistSymbolChange ~2519–2568; handleMultiSymbolChange ~2570–2610; loadStrategyDataToForm exchange block ~2945–2995; handleForexBrokerSelectChange ~3846–3850; handleTestConnection ~3942–4034; submit exchange_config ~4270–4305; save_credential gate ~4423–4437)
    - .planning/phases/12-frontend-ibkr-exchanges-for-forex/12-CONTEXT.md (locked decisions)
  </read_first>
  <action>
    1. **`FOREX_BROKER_OPTIONS`:** Set to exactly three entries in this order:
       - `{ value: 'mt5', labelKey: 'mt5', name: 'MetaTrader 5' }`
       - `{ value: 'ibkr-paper', labelKey: 'ibkr-paper', name: 'IBKR Paper' }`
       - `{ value: 'ibkr-live', labelKey: 'ibkr-live', name: 'IBKR Live' }`

    2. **Rename computed `isMT5Market` → `isForexMarket`:** body remains `return this.selectedMarketCategory === 'Forex'`. Replace every template/JS reference to `isMT5Market` with `isForexMarket` (template `v-else-if`, `isBrokerMarket`, `handleTestConnection`, submit ternary, save_credential condition).

    3. **Add computed properties:**
       - `isForexMT5 () { return this.selectedMarketCategory === 'Forex' && this.currentBrokerId === 'mt5' }`
       - `isForexIBKR () { return this.selectedMarketCategory === 'Forex' && this.currentBrokerId && this.currentBrokerId.startsWith('ibkr-') }`

    4. **`isBrokerMarket`:** `return this.isIBKRMarket || this.isForexMarket` (replace inner `isMT5Market`).

    5. **`isLiveTradingAvailable` — Forex branch (cat === 'Forex'):** Return true iff `this.currentBrokerId === 'mt5' || (this.currentBrokerId && String(this.currentBrokerId).startsWith('ibkr-'))` (so IBKR Forex is not treated as unavailable).

    6. **`forex_broker_id` decorator:** Remove `initialValue: 'mt5'`; keep `rules: [{ required: true, ... }]`. User must select a broker (CONTEXT: no default).

    7. **Template (Forex block):** Keep `v-else-if="isForexMarket"`. Change MT5-only section from `v-if="currentBrokerId === 'mt5'"` to `v-if="isForexMT5"`. After the Forex broker `<a-select>`, add a block `v-if="isForexIBKR"` that duplicates the stock IBKR info alert pair from lines ~1359–1367 (`a-alert` with `trading-assistant.form.ibkrGatewayManagedTitle` / `ibkrGatewayManagedHint` inside `template v-if="isIBKRBroker"` under `isIBKRMarket`) so Forex+IBKR shows the same gateway-managed messaging as US/HK IBKR.

    8. **`handleForexBrokerSelectChange`:** Set `this.currentBrokerId = value` with **no** `|| 'mt5'` fallback.

    9. **`handleWatchlistSymbolChange` / `handleMultiSymbolChange`:** Delete the blocks that set `this.currentBrokerId = 'mt5'` and `setFieldsValue({ forex_broker_id: 'mt5' })` when `selectedMarketCategory === 'Forex'`. Optionally set `this.currentBrokerId = ''` for Forex when switching to Forex if needed for consistent gating; do **not** auto-fill `forex_broker_id`.

    10. **`loadStrategyDataToForm` (exchange_config):** When `isForexMarket` and live:
        - If `exchangeId` is `ibkr-paper` or `ibkr-live`: `this.currentBrokerId = exchangeId`; `this.form.setFieldsValue({ forex_broker_id: exchangeId })`; do **not** apply `exchangeId || 'mt5'`; clear or omit MT5 credential fields for this path.
        - If `exchangeId === 'mt5'` (or legacy MT5-only saved shape): keep existing MT5 `setFieldsValue` including `mt5_server`, `mt5_login`, etc.
        - In the trailing `if (isBrokerMarket || isForexMarket)` block that sets `this.currentBrokerId = exchangeId || (isForexMarket ? 'mt5' : 'ibkr')`: for Forex use `this.currentBrokerId = exchangeId || ''` (empty string when missing), **never** default saved IBKR strategies to `mt5`.

    11. **`handleTestConnection`:** Before the MT5 branch, handle IBKR for Forex:
        - First condition for IBKR connect: `if (this.isIBKRMarket || (this.isForexMarket && this.isIBKRBroker))` — reuse the same `POST /api/ibkr/connect` body `{ broker_id: this.currentBrokerId || 'ibkr-paper' }` and same success/error handling as the existing `isIBKRMarket` block.
        - MT5 path: run only when `this.isForexMarket && this.isForexMT5` (replace bare `this.isForexMarket` MT5 block so Forex+IBKR never hits `/api/mt5/connect`).

    12. **Submit `exchange_config` (live):** Replace the single `this.isForexMarket ? { ... MT5 ... }` branch with:
        - If `this.isIBKRMarket`: unchanged `{ exchange_id: values.broker_id || this.currentBrokerId || 'ibkr' }`.
        - Else if `this.isForexMarket && (values.forex_broker_id || this.currentBrokerId || '').toString().startsWith('ibkr-')`: `{ exchange_id: values.forex_broker_id || this.currentBrokerId || 'ibkr-paper' }` — **only** `exchange_id`, zero `mt5_*` keys.
        - Else if `this.isForexMarket`: MT5-shaped object as today: `exchange_id: values.forex_broker_id || this.currentBrokerId || 'mt5'`, plus `mt5_server`, `mt5_login`, `mt5_password`, `mt5_terminal_path` from `values`.
        - Else: crypto branch unchanged.

    13. **Vault save after create:** Change `if (isLive && values.save_credential && !this.isIBKRMarket && !this.isMT5Market)` to `if (isLive && values.save_credential && this.isCryptoMarket)` so Forex (MT5 or IBKR) never triggers `createExchangeCredential`.

    14. **Comments:** Update any comment that says “MT5/Forex” where it now should say “Forex” if it becomes misleading; keep edits minimal.
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger && ! grep -q "isMT5Market" quantdinger_vue/src/views/trading-assistant/index.vue && grep -q "isForexMarket" quantdinger_vue/src/views/trading-assistant/index.vue && grep -q "ibkr-paper" quantdinger_vue/src/views/trading-assistant/index.vue && grep -q "save_credential && this.isCryptoMarket" quantdinger_vue/src/views/trading-assistant/index.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "isMT5Market" quantdinger_vue/src/views/trading-assistant/index.vue` returns no matches
    - `grep -n "isForexMarket" quantdinger_vue/src/views/trading-assistant/index.vue` matches at least 5 lines
    - `grep -n "FOREX_BROKER_OPTIONS" -A6 quantdinger_vue/src/views/trading-assistant/index.vue` shows lines containing `ibkr-paper` and `ibkr-live` immediately after the array opener
    - `grep -n "initialValue: 'mt5'" quantdinger_vue/src/views/trading-assistant/index.vue` returns no matches inside `forex_broker_id` decorator
    - `grep -n "'/api/ibkr/connect'" quantdinger_vue/src/views/trading-assistant/index.vue` appears in a condition that includes `isForexMarket` and `isIBKRBroker` OR documents same control flow (file contains `isForexMarket && this.isIBKRBroker` near `ibkr/connect`)
    - `grep -n "save_credential && this.isCryptoMarket" quantdinger_vue/src/views/trading-assistant/index.vue` returns at least one match
  </acceptance_criteria>
  <done>index.vue implements FRNT-01 wiring; Task 1 grep-based verify command exits 0</done>
</task>

<task type="auto">
  <name>Task 2: i18n brokerNames for ibkr-paper and ibkr-live</name>
  <files>quantdinger_vue/src/locales/lang/en-US.js, quantdinger_vue/src/locales/lang/zh-CN.js</files>
  <read_first>
    - quantdinger_vue/src/locales/lang/en-US.js (object `trading-assistant.brokerNames` ~1448–1456)
    - quantdinger_vue/src/locales/lang/zh-CN.js (object `trading-assistant.brokerNames` ~1325–1333)
  </read_first>
  <action>
    Inside **`'trading-assistant.brokerNames'`** in both files, add two keys (comma placement valid JS):
    - `'ibkr-paper': 'IBKR Paper'` (en-US) / `'ibkr-paper': 'IBKR 模拟'` or equivalent clear Chinese label for paper (zh-CN)
    - `'ibkr-live': 'IBKR Live'` (en-US) / `'ibkr-live': 'IBKR 实盘'` or equivalent for live (zh-CN)

    Strings must match the **display intent** of stock `BROKER_OPTIONS` names (`IBKR Paper` / `IBKR Live` in English).
  </action>
  <verify>
    <automated>grep -q "'ibkr-paper':" quantdinger_vue/src/locales/lang/en-US.js && grep -q "'ibkr-live':" quantdinger_vue/src/locales/lang/en-US.js && grep -q "'ibkr-paper':" quantdinger_vue/src/locales/lang/zh-CN.js && grep -q "'ibkr-live':" quantdinger_vue/src/locales/lang/zh-CN.js</automated>
  </verify>
  <acceptance_criteria>
    - `en-US.js` contains the substring `'ibkr-paper':` and `'ibkr-live':` inside brokerNames
    - `zh-CN.js` contains the substring `'ibkr-paper':` and `'ibkr-live':` inside brokerNames
    - `node --check quantdinger_vue/src/locales/lang/en-US.js` exits 0; `node --check quantdinger_vue/src/locales/lang/zh-CN.js` exits 0
  </acceptance_criteria>
  <done>Both locale files define the two keys; node syntax check passes</done>
</task>

<task type="auto">
  <name>Task 3: Jest guard + regression commands</name>
  <files>quantdinger_vue/tests/unit/frnt-01-forex-ibkr-options.spec.js</files>
  <read_first>
    - quantdinger_vue/jest.config.js
    - quantdinger_vue/src/views/trading-assistant/index.vue (FOREX_BROKER_OPTIONS block only, ~1719–1726)
  </read_first>
  <action>
    Create **`quantdinger_vue/tests/unit/frnt-01-forex-ibkr-options.spec.js`** that:
    - Reads `src/views/trading-assistant/index.vue` with `fs.readFileSync` + `path.join(__dirname, ...)` relative to the test file so cwd-independent
    - Asserts file text includes `value: 'ibkr-paper'` and `value: 'ibkr-live'` in the `FOREX_BROKER_OPTIONS` area (use `expect(content).toMatch(/value:\s*'ibkr-paper'/)` and same for `ibkr-live`)
    - Asserts `isForexMarket` appears and `isMT5Market` does not (regex or string `not.toContain('isMT5Market')`)

    Do not mount the full 6k-line component for this phase.
  </action>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/quantdinger_vue && npm run test:unit -- --testPathPattern=frnt-01-forex-ibkr-options --no-cache 2>&1 && cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest tests/ -q --tb=line</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:unit -- --testPathPattern=frnt-01-forex-ibkr-options` exits 0
    - Test file path exists: `quantdinger_vue/tests/unit/frnt-01-forex-ibkr-options.spec.js`
    - `cd backend_api_python && python -m pytest tests/ -q` exits 0 (full backend regression; includes `test_strategy_exchange_validation.py`)
  </acceptance_criteria>
  <done>Jest guard passes; full backend pytest suite passes; FRNT-01 source shape locked by test</done>
</task>

</tasks>

<verification>
- Task 3 `<automated>`: frnt-01 Jest pattern + full `cd backend_api_python && python -m pytest tests/ -q` (matches ROADMAP success criterion 3: frontend cases + full backend regression)
- Optional extra confidence: `cd quantdinger_vue && npm run test:unit` without pattern (full Vue unit suite) if time allows
</verification>

<success_criteria>
- FRNT-01 satisfied: Forex broker UI lists `ibkr-paper` and `ibkr-live`; persistence uses IBKR-only `exchange_config` for those IDs; test connection uses `/api/ibkr/connect` for Forex+IBKR
- All task acceptance_criteria grep/commands pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-ibkr-exchanges-for-forex/12-01-SUMMARY.md`
</output>
