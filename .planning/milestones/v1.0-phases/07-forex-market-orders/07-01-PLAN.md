---
phase: 07-forex-market-orders
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/app/services/live_trading/ibkr_trading/client.py
  - backend_api_python/tests/test_ibkr_client.py
autonomous: true
requirements:
  - EXEC-01
user_setup: []
must_haves:
  truths:
    - "Forex place_market_order happy paths (EURUSD, GBPJPY, XAUUSD) submit MarketOrder with IOC, CASH contract, base-currency totalQuantity"
    - "UC-E1 qualify failure returns Invalid Forex contract message including Forex and symbol"
    - "UC-E2 aligned qty<=0 for Forex returns failure message containing base alignment text and Forex minimum-size hint substring"
    - "UC-E3 qty<=0 from normalizer does not call placeOrder"
    - "UC-R1 USStock place_market_order still uses tif DAY for open_long; UC-R2 HShare still uses tif DAY for open_long"
    - "cd backend_api_python && python -m pytest tests/ -x -q --tb=line exits 0 (REGR-01)"
  artifacts:
    - path: backend_api_python/app/services/live_trading/ibkr_trading/client.py
      provides: "Forex-specific qty<=0 message branch in place_market_order and place_limit_order"
      contains: "For Forex (IDEALPRO)"
    - path: backend_api_python/tests/test_ibkr_client.py
      provides: "TestPlaceMarketOrderForex covering UC-M1–M3, UC-E1–E3, UC-R1–R2"
      contains: "TestPlaceMarketOrderForex"
  key_links:
    - from: "tests/test_ibkr_client.py TestPlaceMarketOrderForex"
      to: "IBKRClient.place_market_order"
      via: "@patch ib_insync + _make_client_with_mock_ib"
      pattern: "place_market_order\\(.*Forex"
    - from: "client.py place_market_order"
      to: "_align_qty_to_contract"
      via: "qty = await self._align_qty_to_contract"
      pattern: "qty <= 0"
---

<objective>
Lock EXEC-01 Forex market-order behavior with integration tests (mock IB, same granularity as TestTifForexPolicy) and improve user-visible error text when lot alignment yields zero quantity for Forex only.

Purpose: Production path for Forex market orders already exists; this phase encodes UC-M/UC-E/UC-R/REGR-01 and the qty=0 Forex hint without inventing new trading features.
Output: Updated `client.py` (Forex qty=0 message only) and `TestPlaceMarketOrderForex` in `test_ibkr_client.py`.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-forex-market-orders/07-CONTEXT.md
@.planning/phases/07-forex-market-orders/07-RESEARCH.md

<interfaces>
From `backend_api_python/app/services/live_trading/ibkr_trading/client.py` (executor must preserve signatures):

```python
def place_market_order(
    self, symbol: str, side: str, quantity: float,
    market_type: str = "USStock", **kwargs,
) -> LiveOrderResult:
    ...

def place_limit_order(
    self, symbol: str, side: str, quantity: float, price: float,
    market_type: str = "USStock", **kwargs,
) -> LiveOrderResult:
    ...
```

Existing helpers in `tests/test_ibkr_client.py`: `_make_mock_ib_insync()`, `_make_client_with_mock_ib()`, `_make_trade_mock()`.

MockForex enforces `len(pair) == 6` (see `_make_mock_ib_insync`).

`KNOWN_FOREX_PAIRS` and `normalize_symbol` require Forex symbols to be 6 letters after cleaning (see `symbols.py`); use e.g. `ABCDEF` for UC-E1 (unknown pair) with `qualifyContractsAsync` returning `[]`, not the literal string `INVALID`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Forex qty&lt;=0 message after alignment (place_market_order + place_limit_order)</name>
  <read_first>
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py (read `place_market_order` ~1063–1125 and `place_limit_order` ~1133–1195, especially `if qty &lt;= 0` blocks)
    - backend_api_python/app/services/live_trading/ibkr_trading/symbols.py (`normalize_symbol` Forex rules)
    - .planning/phases/07-forex-market-orders/07-CONTEXT.md (&lt;specifics&gt; error / qty=0)
    - .planning/phases/07-forex-market-orders/07-RESEARCH.md (## Test Use Cases — UC-E2)
  </read_first>
  <files>backend_api_python/app/services/live_trading/ibkr_trading/client.py</files>
  <action>
    **Scope:** Only change the `qty &lt;= 0` branch after `_align_qty_to_contract` when `market_type == "Forex"` in **both** `place_market_order` and `place_limit_order`. Do not change `_align_qty_to_contract` logic, normalizers, or non-Forex messages.

    **Target message (exact strings for implementation):**
    - Non-Forex (unchanged): `f"Quantity {quantity} rounds to 0 after lot-size alignment for {symbol}"`
    - Forex (new): same prefix sentence, then append exactly: ` For Forex (IDEALPRO), the amount may be below the minimum tradable size for this pair.`

    **No separate task for UC proof here** — Task 2 tests will assert the substring `For Forex (IDEALPRO)` and `minimum tradable size`.

    **Use cases covered by this code (reference only for reviewers):**
    - **UC-E2:** Inputs: `market_type="Forex"`, `quantity=10000.0`, mock `reqContractDetailsAsync` returns one detail object with numeric `sizeIncrement=25000.0` so `floor(10000/25000)*25000 == 0`. Expected: `success=False`, `message` contains `Quantity 10000.0 rounds to 0 after lot-size alignment for` (symbol as passed, e.g. EURUSD), and contains `For Forex (IDEALPRO), the amount may be below the minimum tradable size for this pair.` Assertion conditions: `result.success is False`, `Forex` hint substring present, `placeOrder` not called or call_count 0 (order must not submit when qty&lt;=0 — already true before this message change).
  </action>
  <acceptance_criteria>
    - `rg -n "For Forex \\(IDEALPRO\\)" backend_api_python/app/services/live_trading/ibkr_trading/client.py` prints at least 2 lines (market + limit branches) or 1 line if factored into shared helper — minimum 1 occurrence of the exact substring `For Forex (IDEALPRO), the amount may be below the minimum tradable size for this pair.`
    - `rg -n "Quantity .* rounds to 0 after lot-size alignment" backend_api_python/app/services/live_trading/ibkr_trading/client.py` still matches the non-Forex branch unchanged (USStock/HShare).
    - `rg -n "place_market_order|place_limit_order" backend_api_python/app/services/live_trading/ibkr_trading/client.py` — both functions contain a `qty &lt;= 0` check after alignment with Forex-specific message path.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python &amp;&amp; python -m pytest tests/test_ibkr_client.py -x -q --tb=line</automated>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python &amp;&amp; python -m pytest tests/ -x -q --tb=line</automated>
  </verify>
  <done>Forex and non-Forex qty-zero-after-alignment messages are distinct; full test suite passes (Task 2 may still add tests — if new tests fail, complete Task 2 in same session).</done>
</task>

<task type="auto">
  <name>Task 2: TestPlaceMarketOrderForex — UC-M1–M3, UC-E1–E3, UC-R1–R2, REGR-01</name>
  <read_first>
    - backend_api_python/tests/test_ibkr_client.py (helpers `_make_mock_ib_insync`, `_make_client_with_mock_ib`, `_make_trade_mock`; classes `TestTifForexPolicy`, `TestPlaceMarketOrder` / fire-and-forget tests; avoid duplicating `test_error_message_includes_market_type` unless extending)
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py (`place_market_order`, `_align_qty_to_contract` interaction)
    - backend_api_python/app/services/live_trading/ibkr_trading/symbols.py (`KNOWN_FOREX_PAIRS`, `normalize_symbol`)
    - .planning/phases/07-forex-market-orders/07-CONTEXT.md (&lt;specifics&gt; use cases)
    - .planning/phases/07-forex-market-orders/07-RESEARCH.md (## Test Use Cases &amp; Specifications)
  </read_first>
  <files>backend_api_python/tests/test_ibkr_client.py</files>
  <action>
    Add a new class **`TestPlaceMarketOrderForex`** (name exact for `-k PlaceMarketOrderForex`) after `TestTifForexPolicy` (or adjacent Forex execution section). Use **`@patch("app.services.live_trading.ibkr_trading.client.ib_insync", _make_mock_ib_insync())`** on each test method that calls `place_market_order` / `place_limit_order`. Use **`_make_client_with_mock_ib()`**. Do **not** patch `MarketOrder`. Do **not** mock `place_market_order` itself.

    **Shared setup notes:**
    - Default `client._ib.placeOrder.return_value = _make_trade_mock(status="Submitted", filled=0, avg_price=0, order_id=&lt;unique int per test&gt;)`.
    - For tests that need qualify to fail, replace `client._ib.qualifyContractsAsync` with an async def returning `[]` (same pattern as `test_error_message_includes_market_type`).
    - For **UC-E2**, before `place_market_order`: `IBKRClient._lot_size_cache.clear()` (class-level cache) to avoid cross-test pollution; patch `client._ib.reqContractDetailsAsync` with an async function returning a list of one object: `types.SimpleNamespace(sizeIncrement=25000.0, minSize=0)` (or equivalent with real `float` fields). Input: `symbol="EURUSD"`, `side="buy"`, `quantity=10000.0`, `market_type="Forex"`, `signal_type="open_long"`. Expected: `success is False`, message contains `For Forex (IDEALPRO), the amount may be below the minimum tradable size for this pair.` and contains `Quantity 10000.0 rounds to 0`; `client._ib.placeOrder.call_count == 0`.

    **Per use case — implement one dedicated test method each (IDs in docstring):**

    | UC ID | Inputs | Expected outputs / assertions |
    |-------|--------|------------------------------|
    | **UC-M1** | `place_market_order("EURUSD", "buy", 20000.0, "Forex", signal_type="open_long")` | `success True`, `status "Submitted"`; `placeOrder` call_args: contract `secType == "CASH"`, `symbol == "EUR"`, `currency == "USD"`; order `totalQuantity == 20000`, `tif == "IOC"` |
    | **UC-M2** | `place_market_order("GBPJPY", "sell", 50000.0, "Forex", signal_type="open_short")` | Same pattern; `symbol == "GBP"`, `currency == "JPY"`; `action == "SELL"`; `totalQuantity == 50000`; `tif == "IOC"` |
    | **UC-M3** | `place_market_order("XAUUSD", "buy", 10.0, "Forex", signal_type="open_long")` | `symbol == "XAU"`, `currency == "USD"`; `totalQuantity == 10.0` (or `10` if float compares); `tif == "IOC"` |
    | **UC-E1** | `symbol="ABCDEF"`, `side="buy"`, `quantity=20000.0`, `market_type="Forex"`, qualify async returns `[]` | `success False`; `message` contains `Invalid`, `Forex`, and `ABCDEF` |
    | **UC-E2** | See Shared setup notes above | As specified |
    | **UC-E3** | `place_market_order("EURUSD", "buy", 0.0, "Forex")` | `success False`; `message` contains `Quantity must be positive`; `client._ib.placeOrder.call_count == 0` |
    | **UC-R1** | `place_market_order("AAPL", "buy", 100.0, "USStock", signal_type="open_long")` | `placed_order.tif == "DAY"` from `placeOrder.call_args[0][1]` |
    | **UC-R2** | `place_market_order("00005", "buy", 400.0, "HShare", signal_type="open_long")` | `placed_order.tif == "DAY"` |

    **REGR-01:** Full suite command is listed in `verify` (not a separate test function).
  </action>
  <acceptance_criteria>
    - `rg -n "class TestPlaceMarketOrderForex" backend_api_python/tests/test_ibkr_client.py` returns exactly one class definition line.
    - `rg -n "UC-M1|UC-M2|UC-M3|UC-E1|UC-E2|UC-E3|UC-R1|UC-R2" backend_api_python/tests/test_ibkr_client.py` matches at least 8 lines (docstrings or comments marking each UC).
    - `cd backend_api_python &amp;&amp; python -m pytest tests/test_ibkr_client.py -k PlaceMarketOrderForex -x -q --tb=line` exits 0.
    - `cd backend_api_python &amp;&amp; python -m pytest tests/ -x -q --tb=line` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python &amp;&amp; python -m pytest tests/test_ibkr_client.py -k PlaceMarketOrderForex -x -q --tb=line</automated>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python &amp;&amp; python -m pytest tests/ -x -q --tb=line</automated>
  </verify>
  <done>All listed UCs have automated assertions; REGR-01 (full suite green) satisfied.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_ibkr_client.py -k PlaceMarketOrderForex -x -q --tb=line` passes.
- `python -m pytest tests/ -x -q --tb=line` passes.
- Manual spot-check: Forex qty=0 message contains IDEALPRO hint substring (optional; covered by UC-E2).
</verification>

<success_criteria>
- EXEC-01: Forex market orders covered by mock-IB integration tests with contract/order field assertions; Forex-specific zero-qty message after alignment ships in both market and limit entrypoints; repository test suite stays green.
</success_criteria>

<output>
After completion, create `.planning/phases/07-forex-market-orders/07-01-SUMMARY.md` per execute-plan template.
</output>
