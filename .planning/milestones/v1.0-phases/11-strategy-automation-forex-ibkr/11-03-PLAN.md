---
phase: 11-strategy-automation-forex-ibkr
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend_api_python/tests/test_ibkr_forex_paper_smoke.py
autonomous: true
requirements:
  - RUNT-03
user_setup: []
must_haves:
  truths:
    - "test_ibkr_forex_paper_smoke.py runs three pairs (EURUSD, GBPJPY, XAGUSD) each with open+close cycle without live IBKR"
    - "Each test fires a plausible callback sequence after mocked placeOrder (orderStatus / execDetails / position / pnl as registered in IBKRClient)"
  artifacts:
    - path: backend_api_python/tests/test_ibkr_forex_paper_smoke.py
      provides: UC-SA-SMK-01–03
      contains: "EURUSD"
      contains: "GBPJPY"
      contains: "XAGUSD"
  key_links:
    - from: backend_api_python/tests/test_ibkr_forex_paper_smoke.py
      to: app.services.live_trading.ibkr_trading.client.IBKRClient
      via: _make_client_with_mock_ib or extended mock from test_ibkr_client.py
---

<objective>
Add a dedicated mock–IBKR Paper smoke module that simulates qualify → placeOrder → fill → position → PnL callback ordering for three Forex pairs (EURUSD, GBPJPY, XAGUSD), each completing an open then close cycle. No real TWS connection.

Purpose: RUNT-03 success criterion (2) supplementary proof and UC-SA-SMK-01–03 from `11-RESEARCH.md`.

Output: `test_ibkr_forex_paper_smoke.py` green under pytest.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-strategy-automation-forex-ibkr/11-RESEARCH.md
@backend_api_python/tests/test_ibkr_client.py
@backend_api_python/app/services/live_trading/ibkr_trading/client.py

<interfaces>
Reuse helpers from `test_ibkr_client.py`: `_make_mock_ib_insync`, `_make_client_with_mock_ib` (or equivalent module-level fixtures). If imports are private, **duplicate minimal mock setup inside the smoke file** with a comment pointing to the source pattern — tests must run standalone.

Forex pairs and conIds (from `11-CONTEXT.md` specifics):
- EURUSD → localSymbol `EUR.USD`, conId **12087792** (use in mock qualify mutation).
- GBPJPY → `GBP.JPY` (pick a stable fake conId e.g. **12345678** for tests).
- XAGUSD → `XAGUSD` or broker-local symbol per `IBKRClient` normalize path (grep `XAG` in client/tests); use conId **87654321** if no canonical value.

Callback registration in `IBKRClient` — executor must read `client.py` for: `orderStatusEvent`, `execDetailsEvent`, `positionEvent`, `pnlSingleEvent` (or actual names used) and invoke mocks in an order that does not contradict handler guards.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: test_ibkr_forex_paper_smoke.py — three pairs, open+close, mocked IB</name>
  <files>backend_api_python/tests/test_ibkr_forex_paper_smoke.py</files>
  <read_first>
    - backend_api_python/tests/test_ibkr_client.py (search for _make_mock_ib_insync, qualifyContractsAsync, placeOrder)
    - backend_api_python/app/services/live_trading/ibkr_trading/client.py (event handler registration, place_market_order, Forex branches)
  </read_first>
  <action>
    Create `backend_api_python/tests/test_ibkr_forex_paper_smoke.py`.

    1. For each symbol in `("EURUSD", "GBPJPY", "XAGUSD")`, implement a test function (or parametrized test) tagged in docstring with **UC-SA-SMK-01**, **UC-SA-SMK-02**, **UC-SA-SMK-03** respectively.

    2. Build `IBKRClient` with mocked `ib_insync` (reuse project helpers **or** inline AsyncMock) such that:
       - `qualifyContractsAsync` mutates the `Forex` contract to set `conId` &gt; 0 and `localSymbol` consistent with the pair (EURUSD → `EUR.USD`, GBPJPY → `GBP.JPY`, XAGUSD per normalize_symbol output).
       - `placeMarketOrder` / `placeOrder` returns an object with `.order`, `.orderStatus`, `.contract` attributes sufficient for the client’s post-submit logic.

    3. For **open** step: call `place_market_order(symbol, side, quantity, market_type="forex", market_category="Forex")` with side from `map_signal_to_side("open_long", market_category="Forex")` (`buy`) and quantity **10000.0** (or project minimum that passes alignment in mock — adjust if client rejects; use same quantity as `test_ibkr_client` Forex examples if any).

    4. Simulate callbacks in **plausible** order (document order in comment): e.g. orderStatus Filled → execDetails → positionEvent → pnlSingleEvent. Use concrete MagicMock callables attached to the mock IB object the client registers against.

    5. For **close** step: call `place_market_order` with `close_long` mapped side (`sell`) or equivalent to flatten; fire callbacks again; assert position spy or handler side effects (e.g. mock that records `localSymbol` / `conId` on each handler invocation) show **non-zero then zero** position path OR assert `place_market_order` returned `success=True` twice — **concrete minimum:** assert `LiveOrderResult.success is True` for both calls and assert mock `placeOrder` or `place_market_order` internal path was reached **2** times per test (use `@patch` spy on `IB.placeOrder` or `ib_insync.IB.placeOrder` matching the client’s call site).

    6. **No network:** patch `IB.connectAsync` / `connect` to no-op success if the client constructor triggers connection.

    Prefix: module docstring must state "Mock IBKR Paper — no live connection."
  </action>
  <acceptance_criteria>
    - `rg -n "UC-SA-SMK-0" backend_api_python/tests/test_ibkr_forex_paper_smoke.py` returns three matches (SMK-01, 02, 03).
    - `rg -n "EURUSD|GBPJPY|XAGUSD" backend_api_python/tests/test_ibkr_forex_paper_smoke.py` each appear at least once.
    - `rg -n "12087792|EUR\.USD" backend_api_python/tests/test_ibkr_forex_paper_smoke.py` matches for EURUSD mock qualify (accept either conId line or localSymbol in mock).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest tests/test_ibkr_forex_paper_smoke.py -q --tb=short</automated>
  </verify>
  <done>Smoke module passes; three pair tests exercise open+close with mocked IB layer.</done>
</task>

</tasks>

<verification>
`cd backend_api_python && python -m pytest tests/test_ibkr_forex_paper_smoke.py -q`
After merge with other plans: `python -m pytest tests/ -q`
</verification>

<success_criteria>
- UC-SA-SMK-01–03 automated with mocked ib_insync.
- No live IBKR host/port required for pytest.
</success_criteria>

<output>
After completion, create `.planning/phases/11-strategy-automation-forex-ibkr/11-03-SUMMARY.md`
</output>
