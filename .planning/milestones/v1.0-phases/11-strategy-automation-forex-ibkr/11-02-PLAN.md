---
phase: 11-strategy-automation-forex-ibkr
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
  - "03"
files_modified:
  - backend_api_python/tests/test_forex_ibkr_e2e.py
  - .planning/phases/11-strategy-automation-forex-ibkr/11-PAPER-RUNBOOK.md
autonomous: true
requirements:
  - RUNT-03
user_setup: []
must_haves:
  truths:
    - "test_forex_ibkr_e2e.py proves HTTP create (or equivalent) plus worker path reaches runner.execute with Forex+ibkr-paper"
    - "Four Forex signal types and one USStock regression case are asserted against mocked runner/execute or place_market_order"
    - "11-PAPER-RUNBOOK.md documents EURUSD paper steps and full pytest verify command"
  artifacts:
    - path: backend_api_python/tests/test_forex_ibkr_e2e.py
      provides: UC-SA-E2E-F1–F4 + UC-SA-E2E-REGR
      contains: "test_forex_ibkr_e2e"
    - path: .planning/phases/11-strategy-automation-forex-ibkr/11-PAPER-RUNBOOK.md
      provides: Operator paper checklist
      contains: "EURUSD"
      contains: "pytest"
  key_links:
    - from: backend_api_python/tests/test_forex_ibkr_e2e.py
      to: app.services.pending_order_worker.PendingOrderWorker._execute_live_order
      via: direct call after POST /strategies/create with patches on worker deps
---

<objective>
Add Flask `test_client`–based integration tests that prove signal → pending worker → `create_client` → `StatefulClientRunner.execute` → IBKR Forex path (mocked), covering four Forex signals plus one USStock regression. Add `11-PAPER-RUNBOOK.md` for manual IBKR Paper EURUSD open/close and reconciliation, with the canonical full-suite verify command.

Purpose: RUNT-03 success criteria (1) and (2): end-to-end proof and documented paper verification.

Output: `test_forex_ibkr_e2e.py`, phase runbook markdown.
</objective>

<execution_context>
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/workflows/execute-plan.md
@/home/hgq/workspace/aicoder/ws4/QuantDinger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-strategy-automation-forex-ibkr/11-CONTEXT.md
@.planning/phases/11-strategy-automation-forex-ibkr/11-RESEARCH.md
@backend_api_python/tests/test_strategy_force_rebalance.py
@backend_api_python/tests/test_pending_order_worker.py
@backend_api_python/app/routes/strategy.py
@backend_api_python/app/services/pending_order_worker.py

<interfaces>
`PendingOrderWorker._execute_live_order(self, order_id, order_row, payload)` — existing live path.

Patch targets (import paths must match **where the name is looked up**):
- `app.services.pending_order_worker.load_strategy_configs`
- `app.services.pending_order_worker.create_client`
- `app.services.pending_order_worker.get_runner`
- `app.services.pending_order_worker.records.mark_order_sent` / `mark_order_failed`

Flask minimal app pattern from `test_strategy_force_rebalance.py`: `Flask(__name__)`, `register_blueprint(strategy_bp, url_prefix='/api/strategy')`, `before_request` sets `g.user_id = 1`. Route is `POST /api/strategy/strategies/create`.

Forex load_strategy_configs stub must include:
```python
{
  "market_category": "Forex",
  "exchange_config": {"exchange_id": "ibkr-paper"},
  "market_type": "forex",
}
```
USStock regression: `"market_category": "USStock"`, same `ibkr-paper`, symbol e.g. `AAPL`, `market_type` appropriate for stock path (e.g. from existing worker tests).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: test_forex_ibkr_e2e.py — API + worker chain (UC-SA-E2E-F1–F4, REGR)</name>
  <files>backend_api_python/tests/test_forex_ibkr_e2e.py</files>
  <read_first>
    - backend_api_python/tests/test_strategy_force_rebalance.py
    - backend_api_python/tests/test_pending_order_worker.py
    - backend_api_python/tests/test_strategy_display_group.py
    - backend_api_python/app/routes/strategy.py (create_strategy route ~64–78)
    - backend_api_python/app/services/pending_order_worker.py (_execute_live_order)
  </read_first>
  <action>
    Create `backend_api_python/tests/test_forex_ibkr_e2e.py`.

    **Fixture:** Minimal Flask app registering `strategy_bp` at `/api/strategy`, `before_request` sets `g.user_id = 1`. If `@login_required` blocks tests, patch `app.routes.strategy.login_required` to a no-op decorator (same pattern as other route tests if present) OR patch `app.utils.auth.login_required` where `strategy` imports it — grep the project for existing `login_required` test bypass.

    **Section A — API contract:** One test that `POST /api/strategy/strategies/create` with JSON body including `strategy_name`, `market_category="Forex"`, `exchange_config={"exchange_id":"ibkr-paper"}`, `trading_config` containing `"symbol":"EURUSD"`, `"market_type":"forex"` returns HTTP 200 and JSON `code==1` and `data.id` present. Relies on Plan 11-01 validation (depends_on 01). Use unique `strategy_name` per test run.

    **Section B — Worker chain (parametrize signal_type):** For each `signal_type` in `open_long`, `close_long`, `open_short`, `close_short`:
    - Patch `load_strategy_configs` to return Forex+ibkr-paper config (see interfaces).
    - Patch `create_client` to return `IBKRClient.__new__(IBKRClient)` (no `__init__`).
    - Patch `get_runner` to return a `MagicMock` with `pre_check` → `PreCheckResult(ok=True)`, `execute` → `ExecutionResult(success=True, exchange_id="ibkr", exchange_order_id="e2e-test", note="live_order_submitted")`.
    - Instantiate `PendingOrderWorker()`, call `_execute_live_order(order_id=100+hash(signal_type)%900, order_row={...}, payload={...})` with `symbol="EURUSD"`, `amount=10000.0`, matching `signal_type`.
    - Assert `mark_order_sent` called once, `mark_order_failed` not called.
    - **Runner invocation (required):** After each Section B/C `_execute_live_order` call, assert the mocked runner’s execution path was hit — e.g. `assert mock_runner.execute.call_count >= 1` (or `== 1` per test) **or** assert `mock_runner.execute.assert_called()` / `assert_called_once()` with the same `MagicMock` returned by `get_runner`. This proves the chain reaches `StatefulClientRunner.execute`, not only `mark_order_sent`.

    **Section C — USStock regression:** Same patch pattern with `load_strategy_configs` returning `market_category="USStock"`, `exchange_config={"exchange_id":"ibkr-paper"}`, `market_type` as used in equity tests (e.g. from `test_pending_order_worker` or `"stock"` if that is what `load_strategy_configs` uses — **concrete:** use `market_type` string `"usstock"` or `"stock"` only if `create_client` is invoked with that value; match existing Phase 4 tests). Symbol `AAPL`, `signal_type=open_long`. Assert `mark_order_sent` once **and** the same `mock_runner.execute` call assertion as Section B.

    Import `PreCheckResult` from `app.services.live_trading.runners.base`, `ExecutionResult` from `app.services.live_trading.base`.

    Prefix test function names or docstrings with `UC-SA-E2E-F1` … `F4` and `UC-SA-E2E-REGR` for grep.
  </action>
  <acceptance_criteria>
    - `rg -n "UC-SA-E2E" backend_api_python/tests/test_forex_ibkr_e2e.py` returns at least 5 lines.
    - `rg -n "strategies/create|test_client" backend_api_python/tests/test_forex_ibkr_e2e.py` confirms Flask test_client usage.
    - `rg -n "open_long|close_long|open_short|close_short" backend_api_python/tests/test_forex_ibkr_e2e.py` lists all four signals.
    - `rg -n "USStock|AAPL" backend_api_python/tests/test_forex_ibkr_e2e.py` confirms regression case.
    - `rg -n "execute\.(call_count|assert_called)" backend_api_python/tests/test_forex_ibkr_e2e.py` confirms mocked runner `execute` is asserted (not only `mark_order_sent`).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger/backend_api_python && python -m pytest tests/test_forex_ibkr_e2e.py -q --tb=short</automated>
  </verify>
  <done>All E2E tests pass; worker chain asserts success path for Forex four signals + USStock one case.</done>
</task>

<task type="auto">
  <name>Task 2: 11-PAPER-RUNBOOK.md + full suite gate command</name>
  <files>.planning/phases/11-strategy-automation-forex-ibkr/11-PAPER-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/11-strategy-automation-forex-ibkr/11-VALIDATION.md
    - .planning/ROADMAP.md (Phase 11 success criteria)
  </read_first>
  <action>
    Create `.planning/phases/11-strategy-automation-forex-ibkr/11-PAPER-RUNBOOK.md` with:
    1. **Prerequisites:** IBKR TWS or Gateway in **Paper** mode; API port (default paper 7497) noted; QuantDinger backend running per project norm (`docker-compose` or local).
    2. **Strategy config:** Explicit key-value bullets — `market_category=Forex`, `exchange_id` in `{ibkr-paper, ibkr-live}` (paper section uses `ibkr-paper`), symbol `EURUSD`, `market_type`/`trading_config.market_type` set to the value the backend expects for Forex (e.g. `forex` — **grep `exchange_execution.load_strategy_configs` or worker for actual string** and paste exact value into runbook).
    3. **Operator steps:** numbered list: start connector → create/enable strategy → trigger or wait for open signal → confirm fill/position in logs or DB → close position → confirm reconciled position size ~0 and PnL row if applicable.
    4. **Verify (automated):** Exact shell command on its own line:
       `cd backend_api_python && python -m pytest tests/ -q`
    5. **Phase 11 test subset:** Line listing:
       `python -m pytest tests/test_strategy_exchange_validation.py tests/test_forex_ibkr_e2e.py tests/test_ibkr_forex_paper_smoke.py -q`
       (requires plan 11-03 complete so `test_ibkr_forex_paper_smoke.py` exists).

    No placeholder text like "TBD"; use concrete defaults from repo (7497, EURUSD).
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/11-strategy-automation-forex-ibkr/11-PAPER-RUNBOOK.md` exits 0.
    - `rg -n "EURUSD|7497|python -m pytest tests/" .planning/phases/11-strategy-automation-forex-ibkr/11-PAPER-RUNBOOK.md` returns matches.
    - File contains literal substring `ibkr-paper`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/hgq/workspace/aicoder/ws4/QuantDinger && test -f .planning/phases/11-strategy-automation-forex-ibkr/11-PAPER-RUNBOOK.md && rg -q "python -m pytest tests/" .planning/phases/11-strategy-automation-forex-ibkr/11-PAPER-RUNBOOK.md</automated>
  </verify>
  <done>Runbook exists and is grep-complete for EURUSD, paper port, and pytest commands.</done>
</task>

</tasks>

<verification>
After Task 1: `pytest tests/test_forex_ibkr_e2e.py -q`
Phase gate: `cd backend_api_python && python -m pytest tests/ -q`
</verification>

<success_criteria>
- E2E file demonstrates Flask create + worker execution path with mocks.
- Runbook documents manual EURUSD paper flow and full test suite command.
</success_criteria>

<output>
After completion, create `.planning/phases/11-strategy-automation-forex-ibkr/11-02-SUMMARY.md`
</output>
